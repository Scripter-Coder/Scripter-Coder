--skid if u want idc just dont forget about credits or smt
--Webhook
local WebhookScripterHub = loadstring(game:HttpGet("https://raw.githubusercontent.com/Scripter-Coder/Scripter-Coder/refs/heads/main/Universal/Webhook"))()

--Library
local OrionLib = loadstring(game:HttpGet("https://raw.githubusercontent.com/Scripter-Coder/Scripter-Coder/refs/heads/main/Universal/Orion%20Hub%20Library"))()

--Startup Script Notification
OrionLib:MakeNotification({Name = "From Scripter",Content = "Script Loads (Can Take 1-5 Seconds)",Image = "rbxassetid://7734059095",Time = 1})

--If Slap Battles Then
if game.PlaceId == 6403373529 or game.PlaceId == 9015014224 or game.PlaceId == 11520107397 or game.PlaceId == 124596094333302 or game.PlaceId == 7234087065 or game.PlaceId == 9020359053 or game.PlaceId == 9068206286 or game.PlaceId == 9426795465 or game.PlaceId == 9431156611 or game.PlaceId == 11359765900 or game.PlaceId == 11367956190 or game.PlaceId == 11828384869 or game.PlaceId == 12432152490 or game.PlaceId == 12712288037 or game.PlaceId == 12845859004 or game.PlaceId == 13833961666 or game.PlaceId == 15228348051 or game.PlaceId == 15507333474 or game.PlaceId == 16034567693 or game.PlaceId == 17290438723 or game.PlaceId == 18550498098 or game.PlaceId == 18698003301 or game.PlaceId == 74169485398268 or game.PlaceId == 89837553336708 or game.PlaceId == 102908326578005 or game.PlaceId == 103505724406848 or game.PlaceId == 14422118326 or game.PlaceId == 92516899071319 or game.PlaceId == 115782629143468 or game.PlaceId == 95706409702231 or game.PlaceId == 118650724506449 or game.PlaceId == 97220865182663 or game.PlaceId == 98726100529621 or game.PlaceId == 136005148166028 or game.PlaceId == 117232463555132 or game.PlaceId == 129665246576996 or game.PlaceId == 132277598079047 or game.PlaceId == 136690395520488 or game.PlaceId == 86045914443715 or game.PlaceId == 75803343514634 or game.PlaceId == 80420091630966 or game.PlaceId == 98871361390239 or game.PlaceId == 90553746175288 or game.PlaceId == 106620300132058 or game.PlaceId == 94481828238038 or game.PlaceId == 122901288403496 or game.PlaceId == 79885102123162 or game.PlaceId == 86643839793301 or game.PlaceId == 113228834069218 or game.PlaceId == 95356852680586 then

local Window = OrionLib:MakeWindow({Name = "Masteries Hub (Scripter Hub)",HidePremium = true,IntroEnabled = true,IntroText = "Slap Battles - Mastery Hub",IntroIcon = "rbxassetid://15315284749",ToggleIcon = "assetid://7734091286"})

OrionLib:MakeNotification({Name = "From Scripter",Content = "GoodLuck!",Image = "rbxassetid://7734059095",Time = 3})

Tab = Window:MakeTab({
    Name = "Welcome",
    Icon = "rbxassetid://7734059095",
    PremiumOnly = false
})

local player = game.Players.LocalPlayer
Tab:AddLabel("Hello " .. player.Name .. "!")

Section = Tab:AddSection({
    Name = "Before u will start using mine script would u like to bypass anticheat?"
})

Tab:AddButton({
	Name = "Bypass All Anticheat",
	Callback = function()
loadstring(game:HttpGet("https://raw.githubusercontent.com/RobScripter/Slap-Battles/main/BypassAntiCheat.lua"))()
task.wait(1.5)
OrionLib:MakeNotification({Name = "Success",Content = "Bypassed All Anticheat",Image = "rbxassetid://7733715400",Time = 3})
end
})

local Tab = Window:MakeTab({
    Name = "Info",
    Icon = "rbxassetid://7734068321",
    PremiumOnly = false
})

-- ===== MASTERY PROGRESS TRACKER (ScreenGui + Paragraph) =====

local Players = game:GetService("Players")
local player = Players.LocalPlayer
local playerGui = player:WaitForChild("PlayerGui")
local RunService = game:GetService("RunService")

-- Remove existing GUI if it exists
if playerGui:FindFirstChild("MasteryProgress") then
    playerGui.MasteryProgress:Destroy()
end

-- ===== CREATE SCREENGUI =====
local hudGui = Instance.new("ScreenGui")
hudGui.Name = "MasteryProgress"
hudGui.ResetOnSpawn = false
hudGui.Parent = playerGui

local frame = Instance.new("Frame")
frame.Name = "TrackerOverlay"
frame.Size = UDim2.fromOffset(340, 36)
frame.Position = UDim2.fromOffset(40, 120)
frame.BackgroundColor3 = Color3.fromRGB(25, 35, 35)
frame.BackgroundTransparency = 0.35
frame.BorderSizePixel = 0
frame.Visible = true
frame.Parent = hudGui

local uiCorner = Instance.new("UICorner")
uiCorner.CornerRadius = UDim.new(0, 6)
uiCorner.Parent = frame

local bar = Instance.new("Frame")
bar.Name = "TopBar"
bar.Size = UDim2.new(1, 0, 0, 32)
bar.BackgroundTransparency = 1
bar.Parent = frame

local titleLabel = Instance.new("TextLabel")
titleLabel.Name = "Title"
titleLabel.Size = UDim2.new(1, 0, 1, 0)
titleLabel.BackgroundTransparency = 1
titleLabel.Text = "Mastery Auto-Tracker"
titleLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
titleLabel.TextSize = 16
titleLabel.Font = Enum.Font.GothamBold
titleLabel.Parent = bar

local contentFrame = Instance.new("Frame")
contentFrame.Name = "Content"
contentFrame.Size = UDim2.new(1, 0, 1, -32)
contentFrame.Position = UDim2.new(0, 0, 0, 32)
contentFrame.BackgroundTransparency = 1
contentFrame.Parent = frame

local closeBtn = Instance.new("TextButton")
closeBtn.Size = UDim2.new(0, 24, 0, 24)
closeBtn.Position = UDim2.new(1, -28, 0, 4)
closeBtn.BackgroundTransparency = 1
closeBtn.Text = "X"
closeBtn.TextColor3 = Color3.fromRGB(255, 100, 100)
closeBtn.TextSize = 18
closeBtn.Font = Enum.Font.GothamBold
closeBtn.Parent = bar

local isTrackerActive = true

closeBtn.MouseButton1Click:Connect(function()
    isTrackerActive = false
    hudGui:Destroy()
    pcall(function()
        if masteryToggle then
            masteryToggle:Set(false)
        end
    end)
end)

-- ===== PARAGRAPH REFERENCE =====
local masteryParagraph = nil
local masteryToggle = nil

-- Function to update paragraph
local function updateParagraph(text)
    if masteryParagraph then
        pcall(function()
            masteryParagraph:Set(text)
        end)
    end
end

-- ===== GLOVE MASTERY CLIENT =====
local GloveMasteryClient
task.spawn(function()
    local deps = game:GetService("ReplicatedFirst"):FindFirstChild("Dependencies")
    if deps then
        local mod = deps:FindFirstChild("GloveMasteryClient")
        if mod then
            pcall(function() 
                GloveMasteryClient = require(mod) 
                print("✅ GloveMasteryClient loaded!")
            end)
        end
    end
end)

local GLOVE_ALIASES = {
    ["Moai"] = "🗿",
    ["moai"] = "🗿",
    ["Moyai"] = "🗿"
}

local function getActiveGlove()
    local target = nil
    
    local ls = player:FindFirstChild("leaderstats")
    if ls and ls:FindFirstChild("Glove") then
        local glove = ls.Glove.Value
        if glove and glove ~= "" and glove ~= "None" then
            target = glove
        end
    end
    
    if not target and player.Character then
        local tool = player.Character:FindFirstChildOfClass("Tool")
        if tool and tool.Name ~= "HumanoidRootPart" then
            target = tool.Name
        end
    end
    
    if not target then
        local backpack = player:FindFirstChild("Backpack")
        if backpack then
            local tool = backpack:FindFirstChildOfClass("Tool")
            if tool then
                target = tool.Name
            end
        end
    end

    if target and GLOVE_ALIASES[target] then
        return GLOVE_ALIASES[target], target
    end
    
    return target, target
end

-- Function to check if quest is completed
local function isQuestCompleted(taskInfo)
    if taskInfo.Completed ~= nil then
        return taskInfo.Completed == true
    end
    
    local currentValue = tonumber(taskInfo.ProgressCurrent) or 0
    local maxValue = tonumber(taskInfo.ProgressMax) or 1
    
    -- If current value equals or exceeds max value, it's completed
    return currentValue >= maxValue
end

local lastRenderedKey = ""

-- Function to render overlay AND update paragraph
local function renderOverlay(rows, currentGloveDisplay)
    local keyParts = { tostring(currentGloveDisplay or "Mastery") }
    for _, row in ipairs(rows) do
        table.insert(keyParts, tostring(row.quest) .. "|" .. tostring(row.progress) .. "|" .. tostring(row.completed))
    end

    local newKey = table.concat(keyParts, ";")
    if newKey == lastRenderedKey then return end
    lastRenderedKey = newKey

    -- Update ScreenGui
    for _, child in ipairs(contentFrame:GetChildren()) do
        if child:IsA("TextLabel") then
            child:Destroy()
        end
    end

    local displayName = tostring(currentGloveDisplay or "Mastery")
    titleLabel.Text = displayName

    local count = #rows
    if count == 0 then
        frame.Size = UDim2.fromOffset(340, 32)
        updateParagraph("Glove Mastery: " .. displayName .. "\nNo active quests found.")
        return
    end

    -- Build paragraph text
    local paragraphLines = {}
    table.insert(paragraphLines, "Glove Mastery: " .. displayName)
    
    for index, row in ipairs(rows) do
        local label = Instance.new("TextLabel")
        label.Name = "QuestRow"
        label.Size = UDim2.new(1, 0, 0, 44)
        label.Position = UDim2.new(0, 0, 0, (index - 1) * 44)
        label.BackgroundTransparency = 1
        
        local progressText = row.progress or ""
        local completedText = row.completed and " ✅" or ""
        label.Text = tostring(row.quest or "") .. "\n" .. progressText .. completedText
        
        label.TextColor3 = row.completed and Color3.fromRGB(0, 255, 100) or Color3.fromRGB(255, 255, 255)
        label.TextSize = 14
        label.Font = Enum.Font.Gotham
        label.TextWrapped = true
        label.Parent = contentFrame
        
        -- Add to paragraph
        table.insert(paragraphLines, "")
        table.insert(paragraphLines, "                    QUEST " .. index)
        table.insert(paragraphLines, "")
        table.insert(paragraphLines, "Description: " .. tostring(row.quest or ""))
        table.insert(paragraphLines, "Progress: " .. progressText .. (row.completed and " ✅" or ""))
    end

    frame.Size = UDim2.fromOffset(340, math.clamp(32 + count * 44, 60, 400))
    
    -- Update paragraph
    updateParagraph(table.concat(paragraphLines, "\n"))
end

local function renderStatus(title, message)
    if message == "" then
        renderOverlay({}, title or "Mastery")
        updateParagraph("Glove Mastery: " .. tostring(title or "Mastery") .. "\nNo data available.")
    else
        renderOverlay({ { quest = tostring(message or ""), progress = "", completed = false } }, title or "Mastery")
        updateParagraph("Glove Mastery: " .. tostring(title or "Mastery") .. "\n" .. tostring(message))
    end
end

local getthreadidentity = getthreadidentity or getthreadcontext or getidentity or function() return nil end
local setthreadidentity = setthreadidentity or setthreadcontext or setidentity or function() end

-- ===== MAIN LOOP =====
task.spawn(function()
    while isTrackerActive do
        task.wait()
        
        local internalGloveName, displayGloveName = getActiveGlove()
        
        if not internalGloveName then
            renderStatus("Mastery", "")
            continue
        end

        if not GloveMasteryClient then
            renderStatus("Mastery", "Loading data...")
            continue
        end

        local originalId = getthreadidentity()
        
        local s, parsedData = pcall(function()
            return GloveMasteryClient.GetMasteryChallengeProgresses(GloveMasteryClient, internalGloveName)
        end)
        
        if not s then
            s, parsedData = pcall(function()
                return GloveMasteryClient.GetMasteryChallengeProgresses(internalGloveName)
            end)
        end
        
        if not s and displayGloveName ~= internalGloveName then
            s, parsedData = pcall(function()
                return GloveMasteryClient.GetMasteryChallengeProgresses(displayGloveName)
            end)
        end

        if originalId then pcall(function() setthreadidentity(originalId) end) end

        if not s or type(parsedData) ~= "table" then
            renderOverlay({}, displayGloveName)
            continue
        end

        local rows = {}
        for _, taskInfo in ipairs(parsedData) do
            local taskName = taskInfo.Name or "Unknown Quest"
            local currentValue = tonumber(taskInfo.ProgressCurrent) or 0
            local maxValue = tonumber(taskInfo.ProgressMax) or 1
            local completed = isQuestCompleted(taskInfo)
            local percent = math.floor((currentValue / maxValue) * 100)
            
            local progressText = tostring(currentValue) .. " / " .. tostring(maxValue) .. " (" .. tostring(percent) .. "%)"
            
            table.insert(rows, {
                quest = taskName,
                progress = progressText,
                completed = completed
            })
        end

        renderOverlay(rows, displayGloveName)
    end
end)

-- Create the paragraph
masteryParagraph = Tab:AddParagraph("Glove Mastery Progress:", "Loading...")

-- Initial state
hudGui.Enabled = false
frame.Visible = false

local Tab = Window:MakeTab({
    Name = "Other Stuff",
    Icon = "rbxassetid://7734068321",
    PremiumOnly = false
})

Section = Tab:AddSection({
    Name = "Give FE/Visual Free Mastery"
})

-- Create dropdown for Mastery selection
local masteryNames = {
    "spin", "Stick", "Brick", "Flash", "Spring", "Dice", "Thanos", "Fort", "Space", "Hive", "Mace", "Killstreak", "Gummy",
    "Engineer", "Phantom", "Wormhole", "Stalker", "Cherry", "Booster", "BONK", "Cloud", "Soul", 
    "[REDACTED]", "bus", "Phase", "Bomb", "Bubble", "Shard", "Moon", "Rage", "Chain", "Tycoon", 
    "🗿", "Obby", "Voodoo", "rob", "Tinkerer", "Glovel", "Run", "Frostbite", "Plank", "Hexa", "Eggler", "Car Keys", 
    "Ultra Instinct", "Acrobat"
}

-- Variable to store selected mastery
local selectedMastery = masteryNames[1]

-- Check if hookfunction is supported
local hookfunctionSupported = pcall(function()
    return hookfunction
end)

-- Add dropdown for mastery selection
Tab:AddDropdown({
    Name = "Select Glove for Mastery",
    Options = masteryNames,
    Default = selectedMastery,
    Callback = function(Value)
        selectedMastery = Value
        if hookfunctionSupported then

        end
    end
})

-- Add button to apply mastery
Tab:AddButton({
    Name = "Apply Mastery",
    Callback = function()
        if not hookfunctionSupported then
            OrionLib:MakeNotification({
                Name = "Error",
                Content = "Your executor doesn't support hookfunction",
                Image = "rbxassetid://7743878857",
                Time = 5
            })
            return
        end
        
        local success, err = pcall(function()
            local mod = require(game.ReplicatedFirst.Dependencies.GloveMasteryClient)
            local old = mod.IsMasteredVersionEnabled
            hookfunction(mod.IsMasteredVersionEnabled, function(self, g)
                return g == selectedMastery or old(self, g)
            end)
        end)
        
        if success then
            OrionLib:MakeNotification({
                Name = "Success",
                Content = selectedMastery .. " mastery applied!",
                Image = "rbxassetid://7734059095",
                Time = 2
            })
        else
            OrionLib:MakeNotification({
                Name = "Error",
                Content = "Failed to apply mastery: " .. tostring(err),
                Image = "rbxassetid://7743878857",
                Time = 3
            })
	        OrionLib:MakeNotification({
        Name = "Warning",
        Content = "Your executor doesn't support hookfunction",
        Image = "rbxassetid://7743878857",
        Time = 5
    })
        end
    end
})

-- ===== SERVICES =====
local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local VirtualUser = game:GetService("VirtualUser")
local LocalPlayer = Players.LocalPlayer

-- ===== SECTION: MAIN STUFF =====
Tab:AddSection({
    Name = "Main Stuff"
})

-- Anchor Yourself
local anchorEnabled = false
Tab:AddToggle({
    Name = "Anchor Yourself",
    Default = false,
    Callback = function(Value)
        anchorEnabled = Value
        if Value then
            local char = LocalPlayer.Character
            if char then
                local head = char:FindFirstChild("Head")
                if head then
                    head.Anchored = true
                end
            end
        else
            local char = LocalPlayer.Character
            if char then
                local head = char:FindFirstChild("Head")
                if head then
                    head.Anchored = false
                end
            end
        end
    end
})

-- Auto Die On Ragdoll
local autoDieOnRagdoll = false
Tab:AddToggle({
    Name = "Auto Die On Ragdoll",
    Default = false,
    Callback = function(Value)
        autoDieOnRagdoll = Value
        if Value then
            task.spawn(function()
                while autoDieOnRagdoll do
                    pcall(function()
                        local char = LocalPlayer.Character
                        if char then
                            local ragdolled = char:FindFirstChild("Ragdolled")
                            if ragdolled and ragdolled.Value == true then
                                local humanoid = char:FindFirstChildOfClass("Humanoid")
                                if humanoid then
                                    humanoid.Health = 0
                                end
                            end
                        end
                    end)
                    task.wait(0.1)
                end
            end)
        end
    end
})

-- Teleport To Safespot
Tab:AddButton({
    Name = "Teleport To Safespot",
    Callback = function()
        local safespot = workspace:FindFirstChild("Safespot")
        if safespot then
            local base = safespot:FindFirstChild("Base")
            if base then
                local char = LocalPlayer.Character
                if char then
                    local hrp = char:FindFirstChild("HumanoidRootPart")
                    if hrp then
                        hrp.CFrame = base.CFrame + Vector3.new(0, 10, 0)
                    end
                end
            end
        else
            OrionLib:MakeNotification({
                Name = "Error",
                Content = "Create Safespot first!",
                Image = "rbxassetid://4483345998",
                Time = 3
            })
        end
    end
})

-- Auto Teleport To Safespot
local autoSafespot = false
local safespotLoop = nil
Tab:AddToggle({
    Name = "Auto Teleport To Safespot",
    Default = false,
    Callback = function(Value)
        autoSafespot = Value
        if Value then
            if safespotLoop then
                task.cancel(safespotLoop)
                safespotLoop = nil
            end
            safespotLoop = task.spawn(function()
                while autoSafespot do
                    pcall(function()
                        local safespot = workspace:FindFirstChild("Safespot")
                        if safespot then
                            local base = safespot:FindFirstChild("Base")
                            if base then
                                local char = LocalPlayer.Character
                                if char then
                                    local hrp = char:FindFirstChild("HumanoidRootPart")
                                    if hrp then
                                        hrp.CFrame = base.CFrame + Vector3.new(0, 10, 0)
                                    end
                                end
                            end
                        end
                    end)
                    task.wait(0.5)
                end
            end)
        else
            if safespotLoop then
                task.cancel(safespotLoop)
                safespotLoop = nil
            end
        end
    end
})

-- Create Safespot
Tab:AddButton({
    Name = "Create Safespot",
    Callback = function()
        if workspace:FindFirstChild("Safespot") == nil then
            local Safespot = Instance.new("Model", workspace)
            Safespot.Name = "Safespot"

            local base = Instance.new("Part")
            base.Name = "Base"
            base.Size = Vector3.new(500, 10, 500)
            base.Position = Vector3.new(10000, -50, 10000)
            base.Anchored = true
            base.CanCollide = true
            base.Transparency = 0.5
            base.Parent = Safespot

            local partsData = {
                {"DefendPart",  Vector3.new(500, 117, 5),   Vector3.new(10000.2, 13, 9752.45)},
                {"DefendPart1", Vector3.new(5, 117, 496),   Vector3.new(10248.2, 13, 10002.4)},
                {"DefendPart2", Vector3.new(497, 117, 6),   Vector3.new(9998.13, 13, 10247.2)},
                {"DefendPart3", Vector3.new(7, 117, 490),   Vector3.new(9752.71, 13, 9999.28)},
                {"DefendPart4", Vector3.new(491, 10, 491),  Vector3.new(10001.1, 76, 9999.66)},
            }

            for _, partInfo in pairs(partsData) do
                local part = Instance.new("Part")
                part.Name = partInfo[1]
                part.Size = partInfo[2]
                part.Position = partInfo[3]
                part.Anchored = true
                part.CanCollide = true
                part.Transparency = 0.5
                part.Parent = Safespot
            end
            
            OrionLib:MakeNotification({
                Name = "Success",
                Content = "Safespot Created Successfully!",
                Image = "rbxassetid://7733919427",
                Time = 3
            })
        else
            OrionLib:MakeNotification({
                Name = "Error",
                Content = "Safespot already exists!",
                Image = "rbxassetid://4483345998",
                Time = 3
            })
        end
    end
})

-- Destroy/Delete Safespot
Tab:AddButton({
    Name = "Destroy/Delete Safespot",
    Callback = function()
        local safespot = workspace:FindFirstChild("Safespot")
        if safespot then
            safespot:Destroy()
            OrionLib:MakeNotification({
                Name = "Done",
                Content = "Safespot Destroyed/Deleted Successfully!",
                Image = "rbxassetid://7733919427",
                Time = 3
            })
        else
            OrionLib:MakeNotification({
                Name = "Error",
                Content = "Safespot Not Found",
                Image = "rbxassetid://4483345998",
                Time = 3
            })
        end
    end
})

-- Teleport To Normal Arena Portal
Tab:AddButton({
    Name = "Teleport To Normal Arena Portal",
    Callback = function()
        local char = LocalPlayer.Character
        if char then
            local hrp = char:FindFirstChild("HumanoidRootPart")
            if hrp then
                hrp.CFrame = CFrame.new(-1209, 328, 3)
            end
        end
    end
})

-- Auto Teleport To Normal Arena Portal
local autoPortal = false
local portalLoop = nil
Tab:AddToggle({
    Name = "Auto Teleport To Normal Arena Portal",
    Default = false,
    Callback = function(Value)
        autoPortal = Value
        if Value then
            if portalLoop then
                task.cancel(portalLoop)
                portalLoop = nil
            end
            portalLoop = task.spawn(function()
                while autoPortal do
                    pcall(function()
                        local char = LocalPlayer.Character
                        if char then
                            local hrp = char:FindFirstChild("HumanoidRootPart")
                            if hrp then
                                hrp.CFrame = CFrame.new(-1209, 328, 3)
                            end
                        end
                    end)
                    task.wait(0.5)
                end
            end)
        else
            if portalLoop then
                task.cancel(portalLoop)
                portalLoop = nil
            end
        end
    end
})

-- ===== SECTION: MISC STUFF =====
Tab:AddSection({
    Name = "Misc Stuff"
})

-- Anti AFK
local antiAfkEnabled = false
Tab:AddToggle({
    Name = "Anti AFK",
    Default = false,
    Callback = function(Value)
        antiAfkEnabled = Value
        if Value then
            LocalPlayer.Idled:Connect(function()
                VirtualUser:CaptureController()
                VirtualUser:ClickButton2(Vector2.new())
            end)
        end
    end
})

-- Anti Ragdoll
_G.AntiRagdoll = false
AntiRagdoll = Tab:AddToggle({
    Name = "Anti Ragdoll",
    Default = false,
    Callback = function(Value)
        _G.AntiRagdoll = Value
        
        if _G.AntiRagdoll then
            task.spawn(function()
                local lastCFrame = nil
                local lastPosition = nil
                local character = game.Players.LocalPlayer.Character
                local humanoidRootPart = nil
                
                while _G.AntiRagdoll do
                    pcall(function()
                        character = game.Players.LocalPlayer.Character
                        if not character then 
                            task.wait(0.1)
                            return
                        end
                        
                        humanoidRootPart = character:FindFirstChild("HumanoidRootPart")
                        local ragdolledValue = character:FindFirstChild("Ragdolled")
                        
                        if humanoidRootPart and (not ragdolledValue or ragdolledValue.Value == false) then
                            lastCFrame = humanoidRootPart.CFrame
                            lastPosition = humanoidRootPart.Position
                        end
                        
                        if ragdolledValue and ragdolledValue.Value == true and lastCFrame and humanoidRootPart then
                            humanoidRootPart.CFrame = lastCFrame
                            humanoidRootPart.AssemblyLinearVelocity = Vector3.new(0, 0, 0)
                            humanoidRootPart.AssemblyAngularVelocity = Vector3.new(0, 0, 0)
                            
                            for _, part in ipairs(character:GetDescendants()) do
                                if part:IsA("BasePart") and part ~= humanoidRootPart then
                                    part.AssemblyLinearVelocity = Vector3.new(0, 0, 0)
                                    part.AssemblyAngularVelocity = Vector3.new(0, 0, 0)
                                end
                            end
                            
                            local humanoid = character:FindFirstChild("Humanoid")
                            if humanoid then
                                humanoid.PlatformStand = false
                                humanoid.Sit = false
                            end
                            
                            task.wait()
                            if character and character:FindFirstChild("Ragdolled") then
                                character.Ragdolled.Value = false
                            end
                        end
                    end)
                    task.wait()
                end
            end)
            
            OrionLib:MakeNotification({
                Name = "Anti Ragdoll",
                Content = "Enabled! Instant ragdoll prevention.",
                Image = "rbxassetid://7734059095",
                Time = 2
            })
        end
    end    
})

local Tab = Window:MakeTab({
    Name = "spin",
    Icon = "rbxassetid://7734068321",
    PremiumOnly = false
})

Section = Tab:AddSection({
    Name = "1 Quest"
})

Tab:AddButton({
    Name = "Teleport To Safespot",
    Callback = function()
        local player = game.Players.LocalPlayer
        local character = player.Character
        
        -- Check conditions
        local hasSpinGlove = player.leaderstats and player.leaderstats:FindFirstChild("Glove") and player.leaderstats.Glove.Value == "spin"
        local hasEntered = character and character:FindFirstChild("entered")
        local hasSafespot = workspace:FindFirstChild("Safespot")
        
        if not hasSpinGlove or not hasEntered or not hasSafespot then
            local errorMsg = ""
            if not hasSpinGlove then
                errorMsg = "Equip Spin Glove"
            elseif not hasEntered then
                errorMsg = "Enter Arena First"
            elseif not hasSafespot then
                errorMsg = "Create Safespot First"
            end
            
            OrionLib:MakeNotification({
                Name = "Error",
                Content = errorMsg,
                Image = "rbxassetid://4483345998",
                Time = 3
            })
            return
        end
        
        -- Teleport to Safespot
        local safespot = workspace:FindFirstChild("Safespot")
        if safespot then
            local base = safespot:FindFirstChild("Base")
            if base then
                local hrp = character:FindFirstChild("HumanoidRootPart")
                if hrp then
                    hrp.CFrame = base.CFrame + Vector3.new(0, 10, 0)
                    
                    OrionLib:MakeNotification({
                        Name = "Teleported",
                        Content = "Teleported to Safespot!",
                        Image = "rbxassetid://7743878857",
                        Time = 2
                    })
                end
            end
        end
    end
})

local Tab = Window:MakeTab({
    Name = "Stick",
    Icon = "rbxassetid://7734068321",
    PremiumOnly = false
})

Section = Tab:AddSection({
    Name = "1 Quest"
})

Section = Tab:AddSection({
    Name = "Receiver"
})

-- Auto Teleport To Safespot + Slap Aura
local autoSafespot = false
local safespotLoop = nil
local slapAuraConnection = nil

Tab:AddToggle({
    Name = "Auto Teleport To Safespot + Slap Aura",
    Default = false,
    Callback = function(Value)
        autoSafespot = Value
        
        if Value then
            -- Start Safespot teleport loop
            if safespotLoop then
                task.cancel(safespotLoop)
                safespotLoop = nil
            end
            
            safespotLoop = task.spawn(function()
                while autoSafespot do
                    pcall(function()
                        local safespot = workspace:FindFirstChild("Safespot")
                        if safespot then
                            local base = safespot:FindFirstChild("Base")
                            if base then
                                local char = game.Players.LocalPlayer.Character
                                if char then
                                    local hrp = char:FindFirstChild("HumanoidRootPart")
                                    if hrp then
                                        hrp.CFrame = base.CFrame + Vector3.new(0, 10, 0)
                                    end
                                end
                            end
                        end
                    end)
                    task.wait(0.5)
                end
            end)
            
            -- Start Slap Aura (fires GeneralHit on all players every 0.5 seconds)
            if slapAuraConnection then
                slapAuraConnection:Disconnect()
                slapAuraConnection = nil
            end
            
            slapAuraConnection = game:GetService("RunService").Heartbeat:Connect(function()
                if not autoSafespot then return end
                
                pcall(function()
                    local generalHit = game:GetService("ReplicatedStorage"):FindFirstChild("GeneralHit")
                    if not generalHit then return end
                    
                    -- Fire GeneralHit on all players
                    for _, player in pairs(game:GetService("Players"):GetPlayers()) do
                        if player.Character then
                            -- Get a body part (Right Arm, Left Arm, Head, etc.)
                            local targetPart = player.Character:FindFirstChild("Right Arm") or 
                                              player.Character:FindFirstChild("Left Arm") or
                                              player.Character:FindFirstChild("Head") or 
                                              player.Character:FindFirstChild("Torso") or 
                                              player.Character:FindFirstChild("HumanoidRootPart")
                            if targetPart then
                                generalHit:FireServer(targetPart)
                            end
                        end
                    end
                end)
            end)
            
            OrionLib:MakeNotification({
                Name = "Auto Safespot + Slap Aura",
                Content = "Enabled! Teleporting to Safespot & slapping all players.",
                Image = "rbxassetid://7743878857",
                Time = 3
            })
            
        else
            -- Stop Safespot teleport loop
            if safespotLoop then
                task.cancel(safespotLoop)
                safespotLoop = nil
            end
            
            -- Stop Slap Aura
            if slapAuraConnection then
                slapAuraConnection:Disconnect()
                slapAuraConnection = nil
            end
            
            OrionLib:MakeNotification({
                Name = "Auto Safespot + Slap Aura",
                Content = "Disabled!",
                Image = "rbxassetid://7743878857",
                Time = 2
            })
        end
    end
})

Section = Tab:AddSection({
    Name = "Helper"
})

-- Auto Teleport To Safespot (Receiver)
local autoReceiverSafespot = false
local receiverSafespotLoop = nil

Tab:AddToggle({
    Name = "Auto Teleport To Safespot (Receiver)",
    Default = false,
    Callback = function(Value)
        autoReceiverSafespot = Value
        
        if Value then
            -- Check if Safespot exists
            local safespot = workspace:FindFirstChild("Safespot")
            if not safespot then
                OrionLib:MakeNotification({
                    Name = "Error",
                    Content = "Safespot not found! Create Safespot first.",
                    Image = "rbxassetid://4483345998",
                    Time = 3
                })
                -- Turn off the toggle
                -- Note: You'll need to store the toggle reference to set it back
                autoReceiverSafespot = false
                return
            end
            
            if receiverSafespotLoop then
                task.cancel(receiverSafespotLoop)
                receiverSafespotLoop = nil
            end
            
            receiverSafespotLoop = task.spawn(function()
                while autoReceiverSafespot do
                    pcall(function()
                        local char = game.Players.LocalPlayer.Character
                        if char then
                            local hrp = char:FindFirstChild("HumanoidRootPart")
                            if hrp then
                                hrp.CFrame = CFrame.new(10000, -42, 9996)
                            end
                        end
                    end)
                    task.wait(0.5)
                end
            end)
            
            OrionLib:MakeNotification({
                Name = "Auto Teleport",
                Content = "Auto teleport to Safespot (Receiver) enabled!",
                Image = "rbxassetid://7743878857",
                Time = 2
            })
            
        else
            if receiverSafespotLoop then
                task.cancel(receiverSafespotLoop)
                receiverSafespotLoop = nil
            end
            
            OrionLib:MakeNotification({
                Name = "Auto Teleport",
                Content = "Auto teleport to Safespot (Receiver) disabled!",
                Image = "rbxassetid://7743878857",
                Time = 2
            })
        end
    end
})

local Tab = Window:MakeTab({
    Name = "Brick",
    Icon = "rbxassetid://7734068321",
    PremiumOnly = false
})

Section = Tab:AddSection({
    Name = "1 Quest"
})

local brickMasterRunning = false
local brickMasterConnection = nil
local brickMasterLoop = nil

Tab:AddToggle({
    Name = "Auto Obtain Brick Master Badge",
    Default = false,
    Callback = function(Value)
        if Value then
            local player = game.Players.LocalPlayer
            
            -- Check conditions
            local hasBrickGlove = player.leaderstats and player.leaderstats:FindFirstChild("Glove") and player.leaderstats.Glove.Value == "Brick"
            local hasEntered = player.Character and player.Character:FindFirstChild("entered")
            local hasSafespot = workspace:FindFirstChild("Safespot")
            
            if not hasBrickGlove or not hasEntered or not hasSafespot then
                local errorMsg = ""
                if not hasBrickGlove then
                    errorMsg = "Equip Brick Glove"
                elseif not hasEntered then
                    errorMsg = "Enter Arena First"
                elseif not hasSafespot then
                    errorMsg = "Create Safespot First"
                end
                
                OrionLib:MakeNotification({
                    Name = "Error",
                    Content = errorMsg,
                    Image = "rbxassetid://4483345998",
                    Time = 3
                })
                return false
            end
            
            -- Teleport to Safespot first
            local safespot = workspace:FindFirstChild("Safespot")
            if safespot then
                local base = safespot:FindFirstChild("Base")
                if base then
                    local hrp = player.Character:FindFirstChild("HumanoidRootPart")
                    if hrp then
                        hrp.CFrame = base.CFrame + Vector3.new(0, 10, 0)
                    end
                end
            end
            
            task.wait(0.5)
            
            -- Start the loop
            getgenv().BrickMasterLoop = true
            brickMasterLoop = task.spawn(function()
                while getgenv().BrickMasterLoop do
                    pcall(function()
                        -- Check if still in Safespot, if not teleport back
                        local safespot = workspace:FindFirstChild("Safespot")
                        if safespot then
                            local base = safespot:FindFirstChild("Base")
                            if base then
                                local char = game.Players.LocalPlayer.Character
                                if char then
                                    local hrp = char:FindFirstChild("HumanoidRootPart")
                                    if hrp then
                                        -- Keep teleporting to Safespot to stay safe
                                        hrp.CFrame = base.CFrame + Vector3.new(0, 10, 0)
                                    end
                                end
                            end
                        end
                        
                        -- Fire lbrick remote
                        local lbrick = game:GetService("ReplicatedStorage"):FindFirstChild("lbrick")
                        if lbrick then
                            lbrick:FireServer()
                        end
                        
                        -- Update GUI brick count
                        pcall(function()
                            local gui = game.Players.LocalPlayer.PlayerGui:FindFirstChild("BRICKCOUNT")
                            if gui then
                                local imageLabel = gui:FindFirstChild("ImageLabel")
                                if imageLabel then
                                    local textLabel = imageLabel:FindFirstChild("TextLabel")
                                    if textLabel then
                                        local currentCount = tonumber(textLabel.Text) or 0
                                        textLabel.Text = tostring(currentCount + 1)
                                    end
                                end
                            end
                        end)
                    end)
                    task.wait(1.5)
                end
            end)
            
            OrionLib:MakeNotification({
                Name = "Brick Master",
                Content = "Auto Obtain Brick Master Badge Started!",
                Image = "rbxassetid://7743878857",
                Time = 3
            })
            
        else
            -- STOP
            getgenv().BrickMasterLoop = false
            if brickMasterLoop then
                task.cancel(brickMasterLoop)
                brickMasterLoop = nil
            end
            
            OrionLib:MakeNotification({
                Name = "Brick Master",
                Content = "Auto Obtain Brick Master Badge Stopped!",
                Image = "rbxassetid://7743878857",
                Time = 2
            })
        end
    end    
})

Section = Tab:AddSection({
    Name = "|||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||"
})

Section = Tab:AddSection({
    Name = "2-4 Quest"
})

Section = Tab:AddSection({
    Name = "Receiver"
})

-- Auto Teleport To Safespot + Auto Spawn Bricks
local autoSafespot = false
local safespotLoop = nil

Tab:AddToggle({
    Name = "Auto Teleport To Safespot + Auto Spawn Bricks",
    Default = false,
    Callback = function(Value)
        autoSafespot = Value
        
        if Value then
            -- Check if Safespot exists
            local safespot = workspace:FindFirstChild("Safespot")
            if not safespot then
                OrionLib:MakeNotification({
                    Name = "Error",
                    Content = "Safespot not found! Create Safespot first.",
                    Image = "rbxassetid://4483345998",
                    Time = 3
                })
                autoSafespot = false
                return
            end
            
            -- Start Safespot teleport + Brick spawn loop
            if safespotLoop then
                task.cancel(safespotLoop)
                safespotLoop = nil
            end
            
            safespotLoop = task.spawn(function()
                while autoSafespot do
                    pcall(function()
                        local safespot = workspace:FindFirstChild("Safespot")
                        if safespot then
                            local base = safespot:FindFirstChild("Base")
                            if base then
                                local char = game.Players.LocalPlayer.Character
                                if char then
                                    local hrp = char:FindFirstChild("HumanoidRootPart")
                                    if hrp then
                                        hrp.CFrame = base.CFrame + Vector3.new(0, 10, 0)
                                    end
                                end
                            end
                        end
                        
                        -- Fire lbrick remote
                        local lbrick = game:GetService("ReplicatedStorage"):FindFirstChild("lbrick")
                        if lbrick then
                            lbrick:FireServer()
                        end
                        
                        -- Update GUI brick count
                        pcall(function()
                            local gui = game.Players.LocalPlayer.PlayerGui:FindFirstChild("BRICKCOUNT")
                            if gui then
                                local imageLabel = gui:FindFirstChild("ImageLabel")
                                if imageLabel then
                                    local textLabel = imageLabel:FindFirstChild("TextLabel")
                                    if textLabel then
                                        local currentCount = tonumber(textLabel.Text) or 0
                                        textLabel.Text = tostring(currentCount + 1)
                                    end
                                end
                            end
                        end)
                    end)
                    task.wait(0.5)
                end
            end)
            
            OrionLib:MakeNotification({
                Name = "Auto Safespot + Auto Spawn Bricks",
                Content = "Enabled! Teleporting to Safespot & spawning bricks.",
                Image = "rbxassetid://7743878857",
                Time = 3
            })
            
        else
            -- Stop Safespot teleport + brick spawn loop
            if safespotLoop then
                task.cancel(safespotLoop)
                safespotLoop = nil
            end
            
            OrionLib:MakeNotification({
                Name = "Auto Safespot + Auto Spawn Bricks",
                Content = "Disabled!",
                Image = "rbxassetid://7743878857",
                Time = 2
            })
        end
    end
})

Section = Tab:AddSection({
    Name = "Helper"
})

-- Auto Teleport To Safespot + Auto Teleport To Bricks (Union Check)
local autoReceiverSafespot = false
local receiverSafespotLoop = nil

-- The specific size to check for
local TARGET_UNION_SIZE = Vector3.new(1.3293739557266235, 0.4905083179473877, 0.8358331918716431)

Tab:AddToggle({
    Name = "Auto Teleport To Safespot + Auto Teleport To Bricks",
    Default = false,
    Callback = function(Value)
        autoReceiverSafespot = Value
        
        if Value then
            -- Check if Safespot exists
            local safespot = workspace:FindFirstChild("Safespot")
            if not safespot then
                OrionLib:MakeNotification({
                    Name = "Error",
                    Content = "Safespot not found! Create Safespot first.",
                    Image = "rbxassetid://4483345998",
                    Time = 3
                })
                autoReceiverSafespot = false
                return
            end
            
            if receiverSafespotLoop then
                task.cancel(receiverSafespotLoop)
                receiverSafespotLoop = nil
            end
            
            receiverSafespotLoop = task.spawn(function()
                while autoReceiverSafespot do
                    pcall(function()
                        local char = game.Players.LocalPlayer.Character
                        if char then
                            local hrp = char:FindFirstChild("HumanoidRootPart")
                            if hrp then
                                -- Check for Union parts with specific size
                                local targetPart = nil
                                for _, part in pairs(workspace:GetDescendants()) do
                                    if part:IsA("Part") and part.Name == "Union" then
                                        -- Check if size matches exactly
                                        if part.Size.X == TARGET_UNION_SIZE.X and 
                                           part.Size.Y == TARGET_UNION_SIZE.Y and 
                                           part.Size.Z == TARGET_UNION_SIZE.Z then
                                            targetPart = part
                                            break
                                        end
                                    end
                                end
                                
                                -- Teleport to Safespot or Union
                                if targetPart then
                                    -- Teleport to Union position
                                    hrp.CFrame = targetPart.CFrame + Vector3.new(0, 3, 0)
                                    print("✅ Teleported to Union")
                                else
                                    -- Teleport to Safespot
                                    hrp.CFrame = CFrame.new(10000, -42, 9996)
                                    print("✅ Teleported to Safespot")
                                end
                            end
                        end
                    end)
                    task.wait(0.5)
                end
            end)
            
            OrionLib:MakeNotification({
                Name = "Auto Teleport",
                Content = "Auto teleport to Safespot/Bricks enabled!",
                Image = "rbxassetid://7743878857",
                Time = 2
            })
            
        else
            if receiverSafespotLoop then
                task.cancel(receiverSafespotLoop)
                receiverSafespotLoop = nil
            end
            
            OrionLib:MakeNotification({
                Name = "Auto Teleport",
                Content = "Auto teleport to Safespot/Bricks disabled!",
                Image = "rbxassetid://7743878857",
                Time = 2
            })
        end
    end
})

Section = Tab:AddSection({
    Name = "|||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||"
})

Section = Tab:AddSection({
    Name = "3 Quest"
})

Tab:AddToggle({
    Name = "Auto Fling Bricks",
    Default = false,
    Callback = function(Value)
        if Value then
            -- START
            local player = game.Players.LocalPlayer
            
            if player.leaderstats.Glove.Value ~= "Brick" then
                OrionLib:MakeNotification({
                    Name = "Error",
                    Content = "Equip Brick Glove first!",
                    Image = "rbxassetid://4483345998",
                    Time = 3
                })
                return false
            end
            
            local TARGET_UNION_SIZE = Vector3.new(1.3293739557266235, 0.4905083179473877, 0.8358331918716431)
            
            getgenv().UnionsFlingLoop = true
            
            task.spawn(function()
                local RunService = game:GetService("RunService")
                local speaker = game.Players.LocalPlayer
                local flinging = true
                
                -- Noclip for flinging
                local noclipConnection
                noclipConnection = RunService.Stepped:Connect(function()
                    if speaker.Character then
                        for _, part in pairs(speaker.Character:GetDescendants()) do
                            if part:IsA("BasePart") and part.CanCollide then
                                part.CanCollide = false
                            end
                        end
                    end
                end)
                
                -- Death handler
                local function onCharacterAdded(char)
                    local humanoid = char:FindFirstChildWhichIsA("Humanoid")
                    if humanoid then
                        humanoid.Died:Connect(function()
                            flinging = false
                            if noclipConnection then
                                noclipConnection:Disconnect()
                            end
                            getgenv().UnionsFlingLoop = false
                        end)
                    end
                end
                
                -- Handle initial character
                local humanoid = speaker.Character and speaker.Character:FindFirstChildWhichIsA("Humanoid")
                if humanoid then
                    humanoid.Died:Connect(function()
                        flinging = false
                        if noclipConnection then
                            noclipConnection:Disconnect()
                        end
                        getgenv().UnionsFlingLoop = false
                    end)
                end
                
                -- Handle respawns
                speaker.CharacterAdded:Connect(onCharacterAdded)
                
                while getgenv().UnionsFlingLoop and flinging do
                    RunService.Heartbeat:Wait()
                    local character = speaker.Character
                    local root = character and character:FindFirstChild("HumanoidRootPart")
                    local vel, movel = nil, 0.1

                    while not (character and character.Parent and root and root.Parent) do
                        RunService.Heartbeat:Wait()
                        character = speaker.Character
                        root = character and character:FindFirstChild("HumanoidRootPart")
                    end

                    -- Fling velocity
                    vel = root.Velocity
                    root.Velocity = vel * 10000 + Vector3.new(0, 10000, 0)

                    RunService.RenderStepped:Wait()
                    if character and character.Parent and root and root.Parent then
                        root.Velocity = vel
                    end

                    RunService.Stepped:Wait()
                    if character and character.Parent and root and root.Parent then
                        root.Velocity = vel + Vector3.new(0, movel, 0)
                        movel = movel * -1
                    end
                    
                    -- Bring Unions to player (only specific size)
                    for _, obj in pairs(workspace:GetDescendants()) do
                        if obj:IsA("BasePart") and obj.Name == "Union" then
                            -- Check if size matches target
                            if obj.Size.X == TARGET_UNION_SIZE.X and 
                               obj.Size.Y == TARGET_UNION_SIZE.Y and 
                               obj.Size.Z == TARGET_UNION_SIZE.Z then
                                pcall(function()
                                    -- Save original properties
                                    local originalCFrame = obj.CFrame
                                    local originalAnchored = obj.Anchored
                                    
                                    -- Unanchor to move
                                    obj.Anchored = false
                                    
                                    -- Bring Union directly to player's root
                                    obj.CFrame = root.CFrame
                                    task.wait(0.01)
                                    
                                    -- Return Union to original position
                                    obj.CFrame = originalCFrame
                                    obj.Anchored = originalAnchored
                                end)
                            end
                        end
                    end
                    
                    task.wait(0.05)
                end
                
                -- Cleanup connections when loop ends
                if noclipConnection then
                    noclipConnection:Disconnect()
                end
            end)
            
            OrionLib:MakeNotification({
                Name = "Auto Fling Bricks",
                Content = "Enabled! Flinging and bringing Unions.",
                Image = "rbxassetid://7743878857",
                Time = 3
            })
            
        else
            -- STOP
            getgenv().UnionsFlingLoop = false
        end
    end    
})

local Tab = Window:MakeTab({
    Name = "Flash",
    Icon = "rbxassetid://7734068321",
    PremiumOnly = false
})

Section = Tab:AddSection({
    Name = "1-3 Quest"
})

Tab:AddButton({
    Name = "Auto Use Ability (Instant But Kicks)",
    Callback = function()
        if game.Players.LocalPlayer.leaderstats.Glove.Value == "Flash" then
            for i = 1, 1500 do
                game:GetService("ReplicatedStorage"):WaitForChild("FlashTeleport"):FireServer()
                task.wait()
            end
        else
            OrionLib:MakeNotification({
                Name = "Error",
                Content = "Equip Flash Glove",
                Image = "rbxassetid://7743878857",
                Time = 3
            })
        end
    end
})

Section = Tab:AddSection({
    Name = "|||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||"
})

Section = Tab:AddSection({
    Name = "2 Quest"
})

Section = Tab:AddSection({
    Name = "Receiver"
})

-- Auto Teleport To Safespot + Auto Use Ability + Slap Aura (Helper)
local autoHelperSafespot = false
local helperSafespotLoop = nil

Tab:AddToggle({
    Name = "Auto Teleport To Safespot + Auto Use Ability + Slap Aura",
    Default = false,
    Callback = function(Value)
        autoHelperSafespot = Value
        
        if Value then
            -- Check if Safespot exists
            local safespot = workspace:FindFirstChild("Safespot")
            if not safespot then
                OrionLib:MakeNotification({
                    Name = "Error",
                    Content = "Safespot not found! Create Safespot first.",
                    Image = "rbxassetid://4483345998",
                    Time = 3
                })
                autoHelperSafespot = false
                return
            end
            
            if helperSafespotLoop then
                task.cancel(helperSafespotLoop)
                helperSafespotLoop = nil
            end
            
            helperSafespotLoop = task.spawn(function()
                while autoHelperSafespot do
                    pcall(function()
                        -- Teleport to Safespot
                        local safespot = workspace:FindFirstChild("Safespot")
                        if safespot then
                            local base = safespot:FindFirstChild("Base")
                            if base then
                                local char = game.Players.LocalPlayer.Character
                                if char then
                                    local hrp = char:FindFirstChild("HumanoidRootPart")
                                    if hrp then
                                        hrp.CFrame = base.CFrame + Vector3.new(0, 10, 0)
                                    end
                                end
                            end
                        end
                        
                        -- FlashTeleport ability (every 3 seconds)
                        local flashTeleport = game:GetService("ReplicatedStorage"):FindFirstChild("FlashTeleport")
                        if flashTeleport then
                            flashTeleport:FireServer()
                        end
                        
                        task.wait(3) -- Wait 3 seconds for ability
                        
                        -- FlashHit Slap Aura (hit all players)
                        local flashHit = game:GetService("ReplicatedStorage"):FindFirstChild("FlashHit")
                        if flashHit then
                            local localPlayer = game.Players.LocalPlayer
                            for _, player in pairs(game:GetService("Players"):GetPlayers()) do
                                if player ~= localPlayer and player.Character then
                                    local targetPart = player.Character:FindFirstChild("Head") or 
                                                      player.Character:FindFirstChild("Torso") or 
                                                      player.Character:FindFirstChild("HumanoidRootPart")
                                    if targetPart then
                                        flashHit:FireServer(targetPart)
                                    end
                                end
                            end
                        end
                        
                        task.wait(0.5) -- Wait 0.5 seconds before next loop
                    end)
                end
            end)
            
            OrionLib:MakeNotification({
                Name = "Auto Helper",
                Content = "Auto Teleport + Ability + Slap Aura Enabled!",
                Image = "rbxassetid://7743878857",
                Time = 3
            })
            
        else
            if helperSafespotLoop then
                task.cancel(helperSafespotLoop)
                helperSafespotLoop = nil
            end
            
            OrionLib:MakeNotification({
                Name = "Auto Helper",
                Content = "Disabled!",
                Image = "rbxassetid://7743878857",
                Time = 2
            })
        end
    end
})

Section = Tab:AddSection({
    Name = "Helper"
})

-- Auto Teleport To Receiver (Helper)
local autoReceiverTeleport = false
local receiverTeleportLoop = nil

Tab:AddToggle({
    Name = "Auto Teleport To Receiver",
    Default = false,
    Callback = function(Value)
        autoReceiverTeleport = Value
        
        if Value then
            -- Check if Safespot exists
            local safespot = workspace:FindFirstChild("Safespot")
            if not safespot then
                OrionLib:MakeNotification({
                    Name = "Error",
                    Content = "Safespot not found! Create Safespot first.",
                    Image = "rbxassetid://4483345998",
                    Time = 3
                })
                autoReceiverTeleport = false
                return
            end
            
            if receiverTeleportLoop then
                task.cancel(receiverTeleportLoop)
                receiverTeleportLoop = nil
            end
            
            receiverTeleportLoop = task.spawn(function()
                while autoReceiverTeleport do
                    pcall(function()
                        local char = game.Players.LocalPlayer.Character
                        if char then
                            local hrp = char:FindFirstChild("HumanoidRootPart")
                            if hrp then
                                hrp.CFrame = CFrame.new(10000, -42, 9996)
                            end
                        end
                    end)
                    task.wait(0.5)
                end
            end)
            
            OrionLib:MakeNotification({
                Name = "Auto Receiver Teleport",
                Content = "Auto teleport to Receiver position enabled!",
                Image = "rbxassetid://7743878857",
                Time = 2
            })
            
        else
            if receiverTeleportLoop then
                task.cancel(receiverTeleportLoop)
                receiverTeleportLoop = nil
            end
            
            OrionLib:MakeNotification({
                Name = "Auto Receiver Teleport",
                Content = "Auto teleport to Receiver position disabled!",
                Image = "rbxassetid://7743878857",
                Time = 2
            })
        end
    end
})

local Tab = Window:MakeTab({
    Name = "Spring",
    Icon = "rbxassetid://7734068321",
    PremiumOnly = false
})

Section = Tab:AddSection({
    Name = "1-2-3 Quest"
})

Section = Tab:AddSection({
    Name = "Receiver"
})

-- Auto Teleport To Safespot + Auto Jump + Slap Aura (Receiver)
local autoReceiverSafespot = false
local receiverSafespotLoop = nil

Tab:AddToggle({
    Name = "Auto Teleport To Safespot + Auto Jump + Slap Aura",
    Default = false,
    Callback = function(Value)
        autoReceiverSafespot = Value
        
        if Value then
            -- Check if Safespot exists
            local safespot = workspace:FindFirstChild("Safespot")
            if not safespot then
                OrionLib:MakeNotification({
                    Name = "Error",
                    Content = "Safespot not found! Create Safespot first.",
                    Image = "rbxassetid://4483345998",
                    Time = 3
                })
                autoReceiverSafespot = false
                return
            end
            
            if receiverSafespotLoop then
                task.cancel(receiverSafespotLoop)
                receiverSafespotLoop = nil
            end
            
            receiverSafespotLoop = task.spawn(function()
                while autoReceiverSafespot do
                    pcall(function()
                        local char = game.Players.LocalPlayer.Character
                        if char then
                            local hrp = char:FindFirstChild("HumanoidRootPart")
                            local humanoid = char:FindFirstChildOfClass("Humanoid")
                            
                            if hrp and humanoid then
                                -- Teleport to first position
                                hrp.CFrame = CFrame.new(10000, -42, 9996)
                                task.wait(0.05)
                                
                                -- Teleport to second position
                                hrp.CFrame = CFrame.new(9999, -42, 9991)
                                task.wait(0.05)
                                
                                -- Set JumpPower to 1
                                humanoid.JumpPower = 1
                                task.wait(0.05)
                                
                                -- Jump
                                humanoid.Jump = true
                                task.wait(0.1) -- Jump every 0.1 seconds
                                
                                -- Springhit Slap Aura (every 0.5 seconds)
                                local springhit = game:GetService("ReplicatedStorage"):FindFirstChild("springhit")
                                if springhit then
                                    local localPlayer = game.Players.LocalPlayer
                                    for _, player in pairs(game:GetService("Players"):GetPlayers()) do
                                        if player ~= localPlayer and player.Character then
                                            local targetPart = player.Character:FindFirstChild("Left Arm") or 
                                                              player.Character:FindFirstChild("Right Arm") or
                                                              player.Character:FindFirstChild("Head") or 
                                                              player.Character:FindFirstChild("Torso") or 
                                                              player.Character:FindFirstChild("HumanoidRootPart")
                                            if targetPart then
                                                springhit:FireServer(targetPart)
                                            end
                                        end
                                    end
                                end
                            end
                        end
                    end)
                    task.wait(0.5)
                end
            end)
            
            OrionLib:MakeNotification({
                Name = "Auto Receiver",
                Content = "Auto Teleport + Jump + Slap Aura Enabled!",
                Image = "rbxassetid://7743878857",
                Time = 3
            })
            
        else
            if receiverSafespotLoop then
                task.cancel(receiverSafespotLoop)
                receiverSafespotLoop = nil
            end
            
            -- Reset JumpPower back to normal
            pcall(function()
                local char = game.Players.LocalPlayer.Character
                if char then
                    local humanoid = char:FindFirstChildOfClass("Humanoid")
                    if humanoid then
                        humanoid.JumpPower = 50
                    end
                end
            end)
            
            OrionLib:MakeNotification({
                Name = "Auto Receiver",
                Content = "Disabled!",
                Image = "rbxassetid://7743878857",
                Time = 2
            })
        end
    end
})

Section = Tab:AddSection({
    Name = "Helper"
})

-- Auto Teleport To Receiver (Helper)
local autoHelperReceiver = false
local helperReceiverLoop = nil

Tab:AddToggle({
    Name = "Auto Teleport To Receiver",
    Default = false,
    Callback = function(Value)
        autoHelperReceiver = Value
        
        if Value then
            -- Check if Safespot exists
            local safespot = workspace:FindFirstChild("Safespot")
            if not safespot then
                OrionLib:MakeNotification({
                    Name = "Error",
                    Content = "Safespot not found! Create Safespot first.",
                    Image = "rbxassetid://4483345998",
                    Time = 3
                })
                autoHelperReceiver = false
                return
            end
            
            if helperReceiverLoop then
                task.cancel(helperReceiverLoop)
                helperReceiverLoop = nil
            end
            
            helperReceiverLoop = task.spawn(function()
                while autoHelperReceiver do
                    pcall(function()
                        local char = game.Players.LocalPlayer.Character
                        if char then
                            local hrp = char:FindFirstChild("HumanoidRootPart")
                            if hrp then
                                hrp.CFrame = CFrame.new(10000, -42, 9996)
                            end
                        end
                    end)
                    task.wait(0.5)
                end
            end)
            
            OrionLib:MakeNotification({
                Name = "Auto Helper Receiver",
                Content = "Auto teleport to Receiver position enabled!",
                Image = "rbxassetid://7743878857",
                Time = 2
            })
            
        else
            if helperReceiverLoop then
                task.cancel(helperReceiverLoop)
                helperReceiverLoop = nil
            end
            
            OrionLib:MakeNotification({
                Name = "Auto Helper Receiver",
                Content = "Auto teleport to Receiver position disabled!",
                Image = "rbxassetid://7743878857",
                Time = 2
            })
        end
    end
})

local Tab = Window:MakeTab({
    Name = "Dice",
    Icon = "rbxassetid://7734068321",
    PremiumOnly = false
})

Section = Tab:AddSection({
    Name = "1-2-3 Quest"
})

Section = Tab:AddSection({
    Name = "Receiver"
})

-- Auto Teleport To Safespot + Slap Aura (Receiver)
local autoReceiverSlap = false
local receiverSlapLoop = nil

Tab:AddToggle({
    Name = "Auto Teleport To Safespot + Slap Aura",
    Default = false,
    Callback = function(Value)
        autoReceiverSlap = Value
        
        if Value then
            -- Check if Safespot exists
            local safespot = workspace:FindFirstChild("Safespot")
            if not safespot then
                OrionLib:MakeNotification({
                    Name = "Error",
                    Content = "Safespot not found! Create Safespot first.",
                    Image = "rbxassetid://4483345998",
                    Time = 3
                })
                autoReceiverSlap = false
                return
            end
            
            if receiverSlapLoop then
                task.cancel(receiverSlapLoop)
                receiverSlapLoop = nil
            end
            
            receiverSlapLoop = task.spawn(function()
                while autoReceiverSlap do
                    pcall(function()
                        local char = game.Players.LocalPlayer.Character
                        if char then
                            local hrp = char:FindFirstChild("HumanoidRootPart")
                            if hrp then
                                -- Teleport to Safespot
                                local safespot = workspace:FindFirstChild("Safespot")
                                if safespot then
                                    local base = safespot:FindFirstChild("Base")
                                    if base then
                                        hrp.CFrame = base.CFrame + Vector3.new(0, 10, 0)
                                    end
                                end
                            end
                        end
                        
                        -- DiceHit Slap Aura (every 0.5 seconds)
                        local diceHit = game:GetService("ReplicatedStorage"):FindFirstChild("DiceHit")
                        if diceHit then
                            local localPlayer = game.Players.LocalPlayer
                            for _, player in pairs(game:GetService("Players"):GetPlayers()) do
                                if player ~= localPlayer and player.Character then
                                    local targetPart = player.Character:FindFirstChild("Head") or 
                                                      player.Character:FindFirstChild("Torso") or 
                                                      player.Character:FindFirstChild("HumanoidRootPart")
                                    if targetPart then
                                        diceHit:FireServer(targetPart)
                                    end
                                end
                            end
                        end
                    end)
                    task.wait(0.5)
                end
            end)
            
            OrionLib:MakeNotification({
                Name = "Auto Receiver",
                Content = "Auto Teleport + Slap Aura Enabled!",
                Image = "rbxassetid://7743878857",
                Time = 3
            })
            
        else
            if receiverSlapLoop then
                task.cancel(receiverSlapLoop)
                receiverSlapLoop = nil
            end
            
            OrionLib:MakeNotification({
                Name = "Auto Receiver",
                Content = "Disabled!",
                Image = "rbxassetid://7743878857",
                Time = 2
            })
        end
    end
})

Section = Tab:AddSection({
    Name = "Helper"
})

-- Auto Enter Arena + Auto Teleport To Receiver + Auto Die On Ragdoll (Helper)
local autoHelperFull = false
local helperFullLoop = nil

Tab:AddToggle({
    Name = "Auto Enter Arena + Auto Teleport To Receiver + Auto Die On Ragdoll",
    Default = false,
    Callback = function(Value)
        autoHelperFull = Value
        
        if Value then
            -- Check if Safespot exists
            local safespot = workspace:FindFirstChild("Safespot")
            if not safespot then
                OrionLib:MakeNotification({
                    Name = "Error",
                    Content = "Safespot not found! Create Safespot first.",
                    Image = "rbxassetid://4483345998",
                    Time = 3
                })
                autoHelperFull = false
                return
            end
            
            if helperFullLoop then
                task.cancel(helperFullLoop)
                helperFullLoop = nil
            end
            
            helperFullLoop = task.spawn(function()
                while autoHelperFull do
                    pcall(function()
                        local player = game.Players.LocalPlayer
                        local char = player.Character
                        
                        if not char then
                            task.wait(0.5)
                            return
                        end
                        
                        local hrp = char:FindFirstChild("HumanoidRootPart")
                        local humanoid = char:FindFirstChildOfClass("Humanoid")
                        
                        if not hrp or not humanoid then
                            task.wait(0.5)
                            return
                        end
                        
                        -- Check if player has entered
                        local hasEntered = char:FindFirstChild("entered")
                        
                        -- Auto Enter Arena
                        if not hasEntered then
                            hrp.CFrame = CFrame.new(-1209, 328, 3)
                            task.wait(0.5)
                        end
                        
                        -- Teleport to Receiver position (Helper's Safespot)
                        local safespot = workspace:FindFirstChild("Safespot")
                        if safespot then
                            local base = safespot:FindFirstChild("Base")
                            if base then
                                hrp.CFrame = base.CFrame + Vector3.new(0, 10, 0)
                            end
                        else
                            hrp.CFrame = CFrame.new(10000, -42, 9996)
                        end
                        
                        task.wait(0.5)
                        
                        -- Check Ragdolled and DicePart
                        local ragdolled = char:FindFirstChild("Ragdolled")
                        local dicePart = workspace:FindFirstChild("DicePart")
                        
                        -- Auto Die On Ragdoll
                        if ragdolled and ragdolled.Value == true then
                            humanoid.Health = 0
                            print("💀 Died due to ragdoll!")
                        end
                        
                        -- Check DicePart progress
                        if dicePart then
                            local billboard = dicePart:FindFirstChild("BillboardGui")
                            if billboard then
                                local imageLabel = billboard:FindFirstChild("ImageLabel")
                                if imageLabel then
                                    local textLabel = imageLabel:FindFirstChild("TextLabel")
                                    if textLabel then
                                        local text = textLabel.Text
                                        local number = tonumber(text)
                                        if number and number >= 60 then
                                            print("✅ DicePart progress is " .. number .. "%!")
                                        end
                                    end
                                end
                            end
                        end
                    end)
                    task.wait(0.5)
                end
            end)
            
            OrionLib:MakeNotification({
                Name = "Auto Helper",
                Content = "Auto Enter Arena + Teleport + Die On Ragdoll Enabled!",
                Image = "rbxassetid://7743878857",
                Time = 3
            })
            
        else
            if helperFullLoop then
                task.cancel(helperFullLoop)
                helperFullLoop = nil
            end
            
            OrionLib:MakeNotification({
                Name = "Auto Helper",
                Content = "Disabled!",
                Image = "rbxassetid://7743878857",
                Time = 2
            })
        end
    end
})

local Tab = Window:MakeTab({
    Name = "Thanos",
    Icon = "rbxassetid://7734068321",
    PremiumOnly = false
})

Section = Tab:AddSection({
    Name = "1-2-3 Quest"
})

Section = Tab:AddSection({
    Name = "Receiver"
})

-- Auto Teleport To Safespot + Slap Aura + Auto Use Ability (Receiver)
local autoReceiverFull = false
local receiverFullLoop = nil

Tab:AddToggle({
    Name = "Auto Teleport To Safespot + Slap Aura + Auto Use Ability",
    Default = false,
    Callback = function(Value)
        autoReceiverFull = Value
        
        if Value then
            -- Check if Safespot exists
            local safespot = workspace:FindFirstChild("Safespot")
            if not safespot then
                OrionLib:MakeNotification({
                    Name = "Error",
                    Content = "Safespot not found! Create Safespot first.",
                    Image = "rbxassetid://4483345998",
                    Time = 3
                })
                autoReceiverFull = false
                return
            end
            
            if receiverFullLoop then
                task.cancel(receiverFullLoop)
                receiverFullLoop = nil
            end
            
            receiverFullLoop = task.spawn(function()
                local abilityTimer = 0
                
                while autoReceiverFull do
                    pcall(function()
                        local char = game.Players.LocalPlayer.Character
                        if char then
                            local hrp = char:FindFirstChild("HumanoidRootPart")
                            if hrp then
                                -- Teleport to Safespot
                                local safespot = workspace:FindFirstChild("Safespot")
                                if safespot then
                                    local base = safespot:FindFirstChild("Base")
                                    if base then
                                        hrp.CFrame = base.CFrame + Vector3.new(0, 10, 0)
                                    end
                                end
                            end
                        end
                        
                        -- GeneralAbility (every 3 seconds)
                        abilityTimer = abilityTimer + 0.5
                        if abilityTimer >= 3 then
                            abilityTimer = 0
                            local generalAbility = game:GetService("ReplicatedStorage"):FindFirstChild("GeneralAbility")
                            if generalAbility then
                                generalAbility:FireServer(2)
                            end
                        end
                        
                        -- GeneralHit Slap Aura (every 0.5 seconds)
                        local generalHit = game:GetService("ReplicatedStorage"):FindFirstChild("GeneralHit")
                        if generalHit then
                            local localPlayer = game.Players.LocalPlayer
                            for _, player in pairs(game:GetService("Players"):GetPlayers()) do
                                if player ~= localPlayer and player.Character then
                                    local targetPart = player.Character:FindFirstChild("Right Arm") or 
                                                      player.Character:FindFirstChild("Left Arm") or
                                                      player.Character:FindFirstChild("Head") or 
                                                      player.Character:FindFirstChild("Torso") or 
                                                      player.Character:FindFirstChild("HumanoidRootPart")
                                    if targetPart then
                                        generalHit:FireServer(targetPart)
                                    end
                                end
                            end
                        end
                    end)
                    task.wait(0.5)
                end
            end)
            
            OrionLib:MakeNotification({
                Name = "Auto Receiver",
                Content = "Auto Teleport + Slap Aura + Ability Enabled!",
                Image = "rbxassetid://7743878857",
                Time = 3
            })
            
        else
            if receiverFullLoop then
                task.cancel(receiverFullLoop)
                receiverFullLoop = nil
            end
            
            OrionLib:MakeNotification({
                Name = "Auto Receiver",
                Content = "Disabled!",
                Image = "rbxassetid://7743878857",
                Time = 2
            })
        end
    end
})

Section = Tab:AddSection({
    Name = "Helper"
})

-- Auto Teleport To Receiver + Slap Aura (Helper)
local autoHelperSlap = false
local helperSlapLoop = nil

Tab:AddToggle({
    Name = "Auto Teleport To Receiver + Slap Aura",
    Default = false,
    Callback = function(Value)
        autoHelperSlap = Value
        
        if Value then
            -- Check if Default glove is equipped
            local player = game.Players.LocalPlayer
            local glove = player.leaderstats and player.leaderstats:FindFirstChild("Glove")
            if not glove or glove.Value ~= "Default" then
                OrionLib:MakeNotification({
                    Name = "Error",
                    Content = "Equip Default Glove first!",
                    Image = "rbxassetid://4483345998",
                    Time = 3
                })
                autoHelperSlap = false
                return
            end
            
            -- Check if Safespot exists
            local safespot = workspace:FindFirstChild("Safespot")
            if not safespot then
                OrionLib:MakeNotification({
                    Name = "Error",
                    Content = "Safespot not found! Create Safespot first.",
                    Image = "rbxassetid://4483345998",
                    Time = 3
                })
                autoHelperSlap = false
                return
            end
            
            if helperSlapLoop then
                task.cancel(helperSlapLoop)
                helperSlapLoop = nil
            end
            
            helperSlapLoop = task.spawn(function()
                while autoHelperSlap do
                    pcall(function()
                        local char = game.Players.LocalPlayer.Character
                        if char then
                            local hrp = char:FindFirstChild("HumanoidRootPart")
                            if hrp then
                                -- Teleport to Receiver position
                                local safespot = workspace:FindFirstChild("Safespot")
                                if safespot then
                                    local base = safespot:FindFirstChild("Base")
                                    if base then
                                        hrp.CFrame = base.CFrame + Vector3.new(0, 10, 0)
                                    end
                                else
                                    hrp.CFrame = CFrame.new(10000, -42, 9996)
                                end
                            end
                        end
                        
                        -- "b" remote Slap Aura (every 0.5 seconds)
                        local bRemote = game:GetService("ReplicatedStorage"):FindFirstChild("b")
                        if bRemote then
                            local localPlayer = game.Players.LocalPlayer
                            for _, player in pairs(game:GetService("Players"):GetPlayers()) do
                                if player ~= localPlayer and player.Character then
                                    local targetPart = player.Character:FindFirstChild("Right Arm") or 
                                                      player.Character:FindFirstChild("Left Arm") or
                                                      player.Character:FindFirstChild("Head") or 
                                                      player.Character:FindFirstChild("Torso") or 
                                                      player.Character:FindFirstChild("HumanoidRootPart")
                                    if targetPart then
                                        bRemote:FireServer(targetPart)
                                    end
                                end
                            end
                        end
                    end)
                    task.wait(0.5)
                end
            end)
            
            OrionLib:MakeNotification({
                Name = "Auto Helper",
                Content = "Auto Teleport + Slap Aura Enabled!",
                Image = "rbxassetid://7743878857",
                Time = 3
            })
            
        else
            if helperSlapLoop then
                task.cancel(helperSlapLoop)
                helperSlapLoop = nil
            end
            
            OrionLib:MakeNotification({
                Name = "Auto Helper",
                Content = "Disabled!",
                Image = "rbxassetid://7743878857",
                Time = 2
            })
        end
    end
})

local Tab = Window:MakeTab({
    Name = "Fort",
    Icon = "rbxassetid://7734068321",
    PremiumOnly = false
})

Section = Tab:AddSection({
    Name = "1 Quest"
})

Tab:AddButton({
    Name = "Get Badge + Plank Glove",
    Callback = function()
        local player = game.Players.LocalPlayer
        if player.leaderstats.Glove.Value == "Fort" and game.Players.LocalPlayer.Character:FindFirstChild("entered") then
            local hrp = player.Character.HumanoidRootPart
            local originalPos = hrp.CFrame

            hrp.CFrame = CFrame.new(8, 97, 4)
            wait(0.2)
            hrp.Anchored = true
            wait(0.3)
            game:GetService("ReplicatedStorage").Fortlol:FireServer()
            wait(3.5)
            hrp.Anchored = false
            wait(0.1)
            hrp.CFrame = CFrame.new(8, 106, -6)
            wait(0.5)
            hrp.CFrame = originalPos
        else
            OrionLib:MakeNotification({ Name = "Error", Content = "Equip Fort Glove/Enter Arena", Image = "rbxassetid://7743878857", Time = 3 })
        end
    end
})

Section = Tab:AddSection({
    Name = "|||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||"
})

Section = Tab:AddSection({
    Name = "2-3 Quest"
})

-- Auto Teleport To Safespot + Auto Use Ability + Auto Destroy Fort Walls
local autoFortDestroy = false
local fortDestroyLoop = nil

Tab:AddToggle({
    Name = "Auto Teleport To Safespot + Auto Use Ability + Auto Destroy Fort Walls",
    Default = false,
    Callback = function(Value)
        autoFortDestroy = Value
        
        if Value then
            -- Check if Safespot exists
            local safespot = workspace:FindFirstChild("Safespot")
            if not safespot then
                OrionLib:MakeNotification({
                    Name = "Error",
                    Content = "Safespot not found! Create Safespot first.",
                    Image = "rbxassetid://4483345998",
                    Time = 3
                })
                autoFortDestroy = false
                return
            end
            
            if fortDestroyLoop then
                task.cancel(fortDestroyLoop)
                fortDestroyLoop = nil
            end
            
            fortDestroyLoop = task.spawn(function()
                while autoFortDestroy do
                    pcall(function()
                        local player = game.Players.LocalPlayer
                        local char = player.Character
                        
                        if not char then
                            task.wait(0.5)
                            return
                        end
                        
                        local hrp = char:FindFirstChild("HumanoidRootPart")
                        if not hrp then
                            task.wait(0.5)
                            return
                        end
                        
                        -- Teleport to Safespot
                        local safespot = workspace:FindFirstChild("Safespot")
                        if safespot then
                            local base = safespot:FindFirstChild("Base")
                            if base then
                                hrp.CFrame = base.CFrame + Vector3.new(0, 10, 0)
                            end
                        end
                        
                        task.wait(0.5)
                        
                        -- Use Fortlol ability
                        local fortlol = game:GetService("ReplicatedStorage"):FindFirstChild("Fortlol")
                        if fortlol then
                            fortlol:FireServer()
                            print("🏰 Fortlol ability used!")
                        end
                        
                        task.wait(0.5)
                        
                        -- Find BasePart named "Part" with color (172, 73, 73)
                        local targetPart = nil
                        local targetColor = Color3.fromRGB(172, 73, 73)
                        
                        for _, part in pairs(workspace:GetDescendants()) do
                            if part:IsA("BasePart") and part.Name == "Part" then
                                -- Check if color matches
                                local partColor = part.Color
                                if partColor.R == targetColor.R and 
                                   partColor.G == targetColor.G and 
                                   partColor.B == targetColor.B then
                                    targetPart = part
                                    break
                                end
                            end
                        end
                        
                        if targetPart then
                            -- Teleport to the target part
                            hrp.CFrame = targetPart.CFrame + Vector3.new(0, 3, 0)
                            print("🎯 Teleported to Fort Wall at: " .. tostring(targetPart.Position))
                            
                            task.wait(0.3)
                            
                            -- Use SelfKnockback remote
                            local selfKnockback = game:GetService("ReplicatedStorage"):FindFirstChild("SelfKnockback")
                            if selfKnockback then
                                selfKnockback:FireServer({ 
                                    Force = 0, 
                                    Direction = Vector3.new(0, 0.01, 0) 
                                })
                                print("💥 SelfKnockback used on Fort Wall!")
                            end
                        else
                            print("⚠️ No Fort Wall found with color (172, 73, 73)")
                        end
                    end)
                    
                    -- Wait 4 seconds before repeating
                    task.wait(4)
                end
            end)
            
            OrionLib:MakeNotification({
                Name = "Auto Fort Destroy",
                Content = "Auto Teleport + Ability + Destroy Fort Walls Enabled!",
                Image = "rbxassetid://7743878857",
                Time = 3
            })
            
        else
            if fortDestroyLoop then
                task.cancel(fortDestroyLoop)
                fortDestroyLoop = nil
            end
            
            OrionLib:MakeNotification({
                Name = "Auto Fort Destroy",
                Content = "Disabled!",
                Image = "rbxassetid://7743878857",
                Time = 2
            })
        end
    end
})

local Tab = Window:MakeTab({
    Name = "Space",
    Icon = "rbxassetid://7734068321",
    PremiumOnly = false
})

Section = Tab:AddSection({
    Name = "1 Quest"
})

Tab:AddToggle({
    Name = "Auto Use Ability",
    Default = false,
    Callback = function(Value)
        if Value then
            -- START
            if game.Players.LocalPlayer.leaderstats.Glove.Value == "Space" and game.Players.LocalPlayer.Character:FindFirstChild("entered") then
                
                getgenv().SpaceAbilityLoop = true
                task.spawn(function()
                    while getgenv().SpaceAbilityLoop do
                        game:GetService("ReplicatedStorage").ZeroGSound:FireServer()
                        task.wait()
                    end
                end)
            else
                OrionLib:MakeNotification({ 
                    Name = "Error", 
                    Content = "Equip Space Glove", 
                    Image = "rbxassetid://7743878857", 
                    Time = 3 
                })
                return false -- Turn toggle off
            end
        else
            -- STOP
            getgenv().SpaceAbilityLoop = false
            
        end
    end
})

Section = Tab:AddSection({
    Name = "|||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||"
})

Section = Tab:AddSection({
    Name = "2 Quest"
})

Tab:AddButton({
    Name = "Someone Using bus?",
    Callback = function()
        local found = false

        for _, player in ipairs(game.Players:GetPlayers()) do
            local leaderstats = player:FindFirstChild("leaderstats")
            if leaderstats then
                local glove = leaderstats:FindFirstChild("Glove")
                if glove and glove.Value == "bus" then
                    found = true
                    OrionLib:MakeNotification({
                        Name = "Detected!",
                        Content = player.Name .. " has equipped the bus glove.",
                        Image = "rbxassetid://7733715400",
                        Time = 3
                    })
                end
            end
        end

        if not found then
            OrionLib:MakeNotification({
                Name = "Error",
                Content = "No one has equipped the bus glove",
                Image = "rbxassetid://7743878857",
                Time = 3
            })
        end
    end
})

Tab:AddButton({
    Name = "Teleport To bus Player",
    Callback = function()
        local localPlayer = game.Players.LocalPlayer
        local foundPlayer = nil

        for _, player in pairs(game.Players:GetPlayers()) do
            if player ~= localPlayer then
                local stats = player:FindFirstChild("leaderstats")
                if stats and stats:FindFirstChild("Glove") and stats.Glove.Value == "bus" then
                    foundPlayer = player
                    break
                end
            end
        end

        if foundPlayer and foundPlayer.Character and foundPlayer.Character:FindFirstChild("HumanoidRootPart") then
            local hrp = foundPlayer.Character.HumanoidRootPart
            localPlayer.Character:PivotTo(hrp.CFrame + Vector3.new(0, 5, 0)) -- Teleport above

            OrionLib:MakeNotification({
                Name = "Success!",
                Content = "Teleported To: " .. foundPlayer.Name,
                Image = "rbxassetid://7733715400",
                Time = 3
            })
        else
            OrionLib:MakeNotification({
                Name = "Error",
                Content = "bus Player Not Found",
                Image = "rbxassetid://7743878857",
                Time = 3
            })
        end
    end
})

Tab:AddToggle({
    Name = "Auto Bring Bus",
    Default = false,
    Callback = function(Value) 
        Giftfarm = Value
        
        task.spawn(function()
            while Giftfarm do
                local player = game.Players.LocalPlayer
                if not player then 
                    task.wait(0.1) 
                    continue 
                end
                
                local character = player.Character
                if not character or not character.Parent then 
                    task.wait(0.01) 
                    continue 
                end
                
                local hrp = character:FindFirstChild("HumanoidRootPart")
                if not hrp then 
                    task.wait(0.01) 
                    continue 
                end
                
                -- Find BusModel (MeshPart) in workspace
                for _, v in pairs(workspace:GetDescendants()) do
                    if v.Name == "BusModel" and v:IsA("MeshPart") then
                        pcall(function()
                            -- Store original properties
                            local originalCFrame = v.CFrame
                            local originalAnchored = v.Anchored
                            
                            -- Remove TouchInterest if it exists
                            for _, child in pairs(v:GetChildren()) do
                                if child:IsA("TouchInterest") then
                                    child:Destroy()
                                end
                            end
                            
                            -- Bring bus to player
                            v.Anchored = false
                            v.CFrame = hrp.CFrame + Vector3.new(0, 0, 5) -- Slightly in front of player
                            task.wait(0.05)
                            
                            -- Restore original position (optional - comment out if you want bus to stay)
                            -- v.CFrame = originalCFrame
                            -- v.Anchored = originalAnchored
                        end)
                    end
                end
                
                task.wait(0.05) -- Fast loop for better collection
            end
        end)
    end
})

Section = Tab:AddSection({
    Name = "|||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||"
})

Section = Tab:AddSection({
    Name = "3 Quest"
})

Tab:AddButton({
    Name = "Teleport To Cannon Island",
    Callback = function()
        if game.Players.LocalPlayer.leaderstats.Glove.Value == "Space" then
            game.Players.LocalPlayer.Character.HumanoidRootPart.CFrame = CFrame.new(229, 47, 170)
        else
            OrionLib:MakeNotification({ Name = "Error", Content = "Equip Space Glove", Image = "rbxassetid://7743878857", Time = 3 })
        end
    end
})

Tab:AddButton({
    Name = "Teleport To Slapple Island",
    Callback = function()
        if game.Players.LocalPlayer.leaderstats.Glove.Value == "Space" then
            game.Players.LocalPlayer.Character.HumanoidRootPart.CFrame = CFrame.new(-389, 51, -23)
        else
            OrionLib:MakeNotification({ Name = "Error", Content = "Equip Space Glove", Image = "rbxassetid://7743878857", Time = 3 })
        end
    end
})

Section = Tab:AddSection({
    Name = "|||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||"
})

Section = Tab:AddSection({
    Name = "1-4 Quest"
})

Section = Tab:AddSection({
    Name = "Receiver"
})

-- Auto Teleport To Safespot + Auto Use Ability + Slap Aura (Receiver)
local autoReceiverAbilitySlap = false
local receiverAbilitySlapLoop = nil

Tab:AddToggle({
    Name = "Auto Teleport To Safespot + Auto Use Ability + Slap Aura",
    Default = false,
    Callback = function(Value)
        autoReceiverAbilitySlap = Value
        
        if Value then
            -- Check if Safespot exists
            local safespot = workspace:FindFirstChild("Safespot")
            if not safespot then
                OrionLib:MakeNotification({
                    Name = "Error",
                    Content = "Safespot not found! Create Safespot first.",
                    Image = "rbxassetid://4483345998",
                    Time = 3
                })
                autoReceiverAbilitySlap = false
                return
            end
            
            if receiverAbilitySlapLoop then
                task.cancel(receiverAbilitySlapLoop)
                receiverAbilitySlapLoop = nil
            end
            
            receiverAbilitySlapLoop = task.spawn(function()
                while autoReceiverAbilitySlap do
                    pcall(function()
                        local char = game.Players.LocalPlayer.Character
                        if char then
                            local hrp = char:FindFirstChild("HumanoidRootPart")
                            if hrp then
                                -- Teleport to Safespot
                                local safespot = workspace:FindFirstChild("Safespot")
                                if safespot then
                                    local base = safespot:FindFirstChild("Base")
                                    if base then
                                        hrp.CFrame = base.CFrame + Vector3.new(0, 10, 0)
                                    end
                                end
                            end
                        end
                        
                        -- ZeroGSound ability (every 0.1 seconds)
                        local zeroGSound = game:GetService("ReplicatedStorage"):FindFirstChild("ZeroGSound")
                        if zeroGSound then
                            zeroGSound:FireServer()
                        end
                        
                        task.wait(0.1)
                        
                        -- HtSpace Slap Aura (every 0.5 seconds)
                        local htSpace = game:GetService("ReplicatedStorage"):FindFirstChild("HtSpace")
                        if htSpace then
                            local localPlayer = game.Players.LocalPlayer
                            for _, player in pairs(game:GetService("Players"):GetPlayers()) do
                                if player ~= localPlayer and player.Character then
                                    local targetPart = player.Character:FindFirstChild("Right Arm") or 
                                                      player.Character:FindFirstChild("Left Arm") or
                                                      player.Character:FindFirstChild("Head") or 
                                                      player.Character:FindFirstChild("Torso") or 
                                                      player.Character:FindFirstChild("HumanoidRootPart")
                                    if targetPart then
                                        htSpace:FireServer(targetPart)
                                    end
                                end
                            end
                        end
                    end)
                    task.wait(0.5)
                end
            end)
            
            OrionLib:MakeNotification({
                Name = "Auto Receiver",
                Content = "Auto Teleport + Ability + Slap Aura Enabled!",
                Image = "rbxassetid://7743878857",
                Time = 3
            })
            
        else
            if receiverAbilitySlapLoop then
                task.cancel(receiverAbilitySlapLoop)
                receiverAbilitySlapLoop = nil
            end
            
            OrionLib:MakeNotification({
                Name = "Auto Receiver",
                Content = "Disabled!",
                Image = "rbxassetid://7743878857",
                Time = 2
            })
        end
    end
})

Section = Tab:AddSection({
    Name = "Helper"
})

-- Auto Teleport To Receiver (Helper)
local autoHelperReceiverSimple = false
local helperReceiverSimpleLoop = nil

Tab:AddToggle({
    Name = "Auto Teleport To Receiver",
    Default = false,
    Callback = function(Value)
        autoHelperReceiverSimple = Value
        
        if Value then
            -- Check if Safespot exists
            local safespot = workspace:FindFirstChild("Safespot")
            if not safespot then
                OrionLib:MakeNotification({
                    Name = "Error",
                    Content = "Safespot not found! Create Safespot first.",
                    Image = "rbxassetid://4483345998",
                    Time = 3
                })
                autoHelperReceiverSimple = false
                return
            end
            
            if helperReceiverSimpleLoop then
                task.cancel(helperReceiverSimpleLoop)
                helperReceiverSimpleLoop = nil
            end
            
            helperReceiverSimpleLoop = task.spawn(function()
                while autoHelperReceiverSimple do
                    pcall(function()
                        local char = game.Players.LocalPlayer.Character
                        if char then
                            local hrp = char:FindFirstChild("HumanoidRootPart")
                            if hrp then
                                -- Teleport to Safespot position
                                local safespot = workspace:FindFirstChild("Safespot")
                                if safespot then
                                    local base = safespot:FindFirstChild("Base")
                                    if base then
                                        hrp.CFrame = base.CFrame + Vector3.new(0, 10, 0)
                                    end
                                else
                                    hrp.CFrame = CFrame.new(10000, -42, 9996)
                                end
                            end
                        end
                    end)
                    task.wait(0.5)
                end
            end)
            
            OrionLib:MakeNotification({
                Name = "Auto Helper",
                Content = "Auto teleport to Receiver position enabled!",
                Image = "rbxassetid://7743878857",
                Time = 2
            })
            
        else
            if helperReceiverSimpleLoop then
                task.cancel(helperReceiverSimpleLoop)
                helperReceiverSimpleLoop = nil
            end
            
            OrionLib:MakeNotification({
                Name = "Auto Helper",
                Content = "Auto teleport to Receiver position disabled!",
                Image = "rbxassetid://7743878857",
                Time = 2
            })
        end
    end
})

local Tab = Window:MakeTab({
    Name = "Hive",
    Icon = "rbxassetid://7734068321",
    PremiumOnly = false
})

Section = Tab:AddSection({
    Name = "1-2-3-4 Quest"
})

Section = Tab:AddSection({
    Name = "Receiver"
})

-- Auto Teleport + Auto Use Ability + Slap Aura (Receiver)
local autoReceiverGeneral = false
local receiverGeneralLoop = nil

Tab:AddToggle({
    Name = "Auto Teleport + Auto Use Ability + Slap Aura",
    Default = false,
    Callback = function(Value)
        autoReceiverGeneral = Value
        
        if Value then
            -- Check if Safespot exists
            local safespot = workspace:FindFirstChild("Safespot")
            if not safespot then
                OrionLib:MakeNotification({
                    Name = "Error",
                    Content = "Safespot not found! Create Safespot first.",
                    Image = "rbxassetid://4483345998",
                    Time = 3
                })
                autoReceiverGeneral = false
                return
            end
            
            if receiverGeneralLoop then
                task.cancel(receiverGeneralLoop)
                receiverGeneralLoop = nil
            end
            
            receiverGeneralLoop = task.spawn(function()
                while autoReceiverGeneral do
                    pcall(function()
                        local char = game.Players.LocalPlayer.Character
                        if char then
                            local hrp = char:FindFirstChild("HumanoidRootPart")
                            if hrp then
                                -- Teleport to Safespot
                                local safespot = workspace:FindFirstChild("Safespot")
                                if safespot then
                                    local base = safespot:FindFirstChild("Base")
                                    if base then
                                        hrp.CFrame = base.CFrame + Vector3.new(0, 10, 0)
                                    end
                                end
                            end
                        end
                        
                        -- GeneralAbility (every 0.1 seconds)
                        local generalAbility = game:GetService("ReplicatedStorage"):FindFirstChild("GeneralAbility")
                        if generalAbility then
                            generalAbility:FireServer()
                        end
                        
                        task.wait(0.1)
                        
                        -- GeneralHit Slap Aura (every 0.5 seconds)
                        local generalHit = game:GetService("ReplicatedStorage"):FindFirstChild("GeneralHit")
                        if generalHit then
                            local localPlayer = game.Players.LocalPlayer
                            for _, player in pairs(game:GetService("Players"):GetPlayers()) do
                                if player ~= localPlayer and player.Character then
                                    local targetPart = player.Character:FindFirstChild("Torso") or 
                                                      player.Character:FindFirstChild("Head") or
                                                      player.Character:FindFirstChild("Right Arm") or
                                                      player.Character:FindFirstChild("Left Arm") or
                                                      player.Character:FindFirstChild("HumanoidRootPart")
                                    if targetPart then
                                        generalHit:FireServer(targetPart)
                                    end
                                end
                            end
                        end
                    end)
                    task.wait(0.5)
                end
            end)
            
            OrionLib:MakeNotification({
                Name = "Auto Receiver",
                Content = "Auto Teleport + Ability + Slap Aura Enabled!",
                Image = "rbxassetid://7743878857",
                Time = 3
            })
            
        else
            if receiverGeneralLoop then
                task.cancel(receiverGeneralLoop)
                receiverGeneralLoop = nil
            end
            
            OrionLib:MakeNotification({
                Name = "Auto Receiver",
                Content = "Disabled!",
                Image = "rbxassetid://7743878857",
                Time = 2
            })
        end
    end
})

Section = Tab:AddSection({
    Name = "Helper"
})

-- Auto Teleport To Receiver (Helper)
local autoHelperReceiverTeleport = false
local helperReceiverTeleportLoop = nil

Tab:AddToggle({
    Name = "Auto Teleport To Receiver",
    Default = false,
    Callback = function(Value)
        autoHelperReceiverTeleport = Value
        
        if Value then
            -- Check if Safespot exists
            local safespot = workspace:FindFirstChild("Safespot")
            if not safespot then
                OrionLib:MakeNotification({
                    Name = "Error",
                    Content = "Safespot not found! Create Safespot first.",
                    Image = "rbxassetid://4483345998",
                    Time = 3
                })
                autoHelperReceiverTeleport = false
                return
            end
            
            if helperReceiverTeleportLoop then
                task.cancel(helperReceiverTeleportLoop)
                helperReceiverTeleportLoop = nil
            end
            
            helperReceiverTeleportLoop = task.spawn(function()
                while autoHelperReceiverTeleport do
                    pcall(function()
                        local char = game.Players.LocalPlayer.Character
                        if char then
                            local hrp = char:FindFirstChild("HumanoidRootPart")
                            if hrp then
                                -- Teleport to Safespot position
                                local safespot = workspace:FindFirstChild("Safespot")
                                if safespot then
                                    local base = safespot:FindFirstChild("Base")
                                    if base then
                                        hrp.CFrame = base.CFrame + Vector3.new(0, 10, 0)
                                    end
                                else
                                    hrp.CFrame = CFrame.new(10000, -42, 9996)
                                end
                            end
                        end
                    end)
                    task.wait(0.5)
                end
            end)
            
            OrionLib:MakeNotification({
                Name = "Auto Helper",
                Content = "Auto teleport to Receiver position enabled!",
                Image = "rbxassetid://7743878857",
                Time = 2
            })
            
        else
            if helperReceiverTeleportLoop then
                task.cancel(helperReceiverTeleportLoop)
                helperReceiverTeleportLoop = nil
            end
            
            OrionLib:MakeNotification({
                Name = "Auto Helper",
                Content = "Auto teleport to Receiver position disabled!",
                Image = "rbxassetid://7743878857",
                Time = 2
            })
        end
    end
})

local Tab = Window:MakeTab({
    Name = "Defense",
    Icon = "rbxassetid://7734068321",
    PremiumOnly = false
})

Section = Tab:AddSection({
    Name = "1-2-3-4 Quest"
})

Section = Tab:AddSection({
    Name = "Receiver"
})

-- Auto Teleport To Safespot + Auto Use Ability + Slap Aura (Receiver)
local autoReceiverBarrier = false
local receiverBarrierLoop = nil

Tab:AddToggle({
    Name = "Auto Teleport To Safespot + Auto Use Ability + Slap Aura",
    Default = false,
    Callback = function(Value)
        autoReceiverBarrier = Value
        
        if Value then
            -- Check if Safespot exists
            local safespot = workspace:FindFirstChild("Safespot")
            if not safespot then
                OrionLib:MakeNotification({
                    Name = "Error",
                    Content = "Safespot not found! Create Safespot first.",
                    Image = "rbxassetid://4483345998",
                    Time = 3
                })
                autoReceiverBarrier = false
                return
            end
            
            if receiverBarrierLoop then
                task.cancel(receiverBarrierLoop)
                receiverBarrierLoop = nil
            end
            
            receiverBarrierLoop = task.spawn(function()
                while autoReceiverBarrier do
                    pcall(function()
                        local char = game.Players.LocalPlayer.Character
                        if char then
                            local hrp = char:FindFirstChild("HumanoidRootPart")
                            if hrp then
                                -- Teleport to Safespot
                                local safespot = workspace:FindFirstChild("Safespot")
                                if safespot then
                                    local base = safespot:FindFirstChild("Base")
                                    if base then
                                        hrp.CFrame = base.CFrame + Vector3.new(0, 10, 0)
                                    end
                                end
                            end
                        end
                        
                        -- Barrier ability (every 0.01 seconds)
                        local barrier = game:GetService("ReplicatedStorage"):FindFirstChild("Barrier")
                        if barrier then
                            barrier:FireServer(1)
                        end
                        
                        task.wait(0.01)
                        
                        -- DefenseHit Slap Aura (every 0.5 seconds)
                        local defenseHit = game:GetService("ReplicatedStorage"):FindFirstChild("DefenseHit")
                        if defenseHit then
                            local localPlayer = game.Players.LocalPlayer
                            for _, player in pairs(game:GetService("Players"):GetPlayers()) do
                                if player ~= localPlayer and player.Character then
                                    local targetPart = player.Character:FindFirstChild("Left Arm") or 
                                                      player.Character:FindFirstChild("Right Arm") or
                                                      player.Character:FindFirstChild("Head") or 
                                                      player.Character:FindFirstChild("Torso") or 
                                                      player.Character:FindFirstChild("HumanoidRootPart")
                                    if targetPart then
                                        defenseHit:FireServer(targetPart)
                                    end
                                end
                            end
                        end
                    end)
                    task.wait(0.5)
                end
            end)
            
            OrionLib:MakeNotification({
                Name = "Auto Receiver",
                Content = "Auto Teleport + Barrier + Slap Aura Enabled!",
                Image = "rbxassetid://7743878857",
                Time = 3
            })
            
        else
            if receiverBarrierLoop then
                task.cancel(receiverBarrierLoop)
                receiverBarrierLoop = nil
            end
            
            OrionLib:MakeNotification({
                Name = "Auto Receiver",
                Content = "Disabled!",
                Image = "rbxassetid://7743878857",
                Time = 2
            })
        end
    end
})

Section = Tab:AddSection({
    Name = "Helper"
})

-- Auto Teleport To Receiver (Helper)
local autoHelperReceiverTeleport2 = false
local helperReceiverTeleportLoop2 = nil

Tab:AddToggle({
    Name = "Auto Teleport To Receiver",
    Default = false,
    Callback = function(Value)
        autoHelperReceiverTeleport2 = Value
        
        if Value then
            -- Check if Safespot exists
            local safespot = workspace:FindFirstChild("Safespot")
            if not safespot then
                OrionLib:MakeNotification({
                    Name = "Error",
                    Content = "Safespot not found! Create Safespot first.",
                    Image = "rbxassetid://4483345998",
                    Time = 3
                })
                autoHelperReceiverTeleport2 = false
                return
            end
            
            if helperReceiverTeleportLoop2 then
                task.cancel(helperReceiverTeleportLoop2)
                helperReceiverTeleportLoop2 = nil
            end
            
            helperReceiverTeleportLoop2 = task.spawn(function()
                while autoHelperReceiverTeleport2 do
                    pcall(function()
                        local char = game.Players.LocalPlayer.Character
                        if char then
                            local hrp = char:FindFirstChild("HumanoidRootPart")
                            if hrp then
                                -- Teleport to Safespot position
                                local safespot = workspace:FindFirstChild("Safespot")
                                if safespot then
                                    local base = safespot:FindFirstChild("Base")
                                    if base then
                                        hrp.CFrame = base.CFrame + Vector3.new(0, 10, 0)
                                    end
                                else
                                    hrp.CFrame = CFrame.new(10000, -42, 9996)
                                end
                            end
                        end
                    end)
                    task.wait(0.5)
                end
            end)
            
            OrionLib:MakeNotification({
                Name = "Auto Helper",
                Content = "Auto teleport to Receiver position enabled!",
                Image = "rbxassetid://7743878857",
                Time = 2
            })
            
        else
            if helperReceiverTeleportLoop2 then
                task.cancel(helperReceiverTeleportLoop2)
                helperReceiverTeleportLoop2 = nil
            end
        end
    end
})

local Tab = Window:MakeTab({
    Name = "Mace",
    Icon = "rbxassetid://7734068321",
    PremiumOnly = false
})

Section = Tab:AddSection({
    Name = "1-2-3-4 Quest"
})

Section = Tab:AddSection({
    Name = "Receiver"
})

-- Auto Teleport To Safespot + Slap Aura (Receiver)
local autoReceiverSlapAura = false
local receiverSlapAuraLoop = nil

Tab:AddToggle({
    Name = "Auto Teleport To Safespot + Slap Aura",
    Default = false,
    Callback = function(Value)
        autoReceiverSlapAura = Value
        
        if Value then
            -- Check if Safespot exists
            local safespot = workspace:FindFirstChild("Safespot")
            if not safespot then
                OrionLib:MakeNotification({
                    Name = "Error",
                    Content = "Safespot not found! Create Safespot first.",
                    Image = "rbxassetid://4483345998",
                    Time = 3
                })
                autoReceiverSlapAura = false
                return
            end
            
            if receiverSlapAuraLoop then
                task.cancel(receiverSlapAuraLoop)
                receiverSlapAuraLoop = nil
            end
            
            receiverSlapAuraLoop = task.spawn(function()
                while autoReceiverSlapAura do
                    pcall(function()
                        local char = game.Players.LocalPlayer.Character
                        if char then
                            local hrp = char:FindFirstChild("HumanoidRootPart")
                            if hrp then
                                -- Teleport to Safespot
                                local safespot = workspace:FindFirstChild("Safespot")
                                if safespot then
                                    local base = safespot:FindFirstChild("Base")
                                    if base then
                                        hrp.CFrame = base.CFrame + Vector3.new(0, 10, 0)
                                    end
                                end
                            end
                        end
                        
                        -- GeneralHit Slap Aura (every 0.5 seconds)
                        local generalHit = game:GetService("ReplicatedStorage"):FindFirstChild("GeneralHit")
                        if generalHit then
                            local localPlayer = game.Players.LocalPlayer
                            for _, player in pairs(game:GetService("Players"):GetPlayers()) do
                                if player ~= localPlayer and player.Character then
                                    local targetPart = player.Character:FindFirstChild("Left Arm") or 
                                                      player.Character:FindFirstChild("Right Arm") or
                                                      player.Character:FindFirstChild("Head") or 
                                                      player.Character:FindFirstChild("Torso") or 
                                                      player.Character:FindFirstChild("HumanoidRootPart")
                                    if targetPart then
                                        generalHit:FireServer(targetPart, 93.05674743652344)
                                    end
                                end
                            end
                        end
                    end)
                    task.wait(0.5)
                end
            end)
            
            OrionLib:MakeNotification({
                Name = "Auto Receiver",
                Content = "Auto Teleport + Slap Aura Enabled!",
                Image = "rbxassetid://7743878857",
                Time = 3
            })
            
        else
            if receiverSlapAuraLoop then
                task.cancel(receiverSlapAuraLoop)
                receiverSlapAuraLoop = nil
            end
            
            OrionLib:MakeNotification({
                Name = "Auto Receiver",
                Content = "Disabled!",
                Image = "rbxassetid://7743878857",
                Time = 2
            })
        end
    end
})

Section = Tab:AddSection({
    Name = "Helper"
})

-- Auto Teleport To Receiver + Auto Die On Ragdoll (Helper)
local autoHelperRagdoll = false
local helperRagdollLoop = nil

Tab:AddToggle({
    Name = "Auto Teleport To Receiver + Auto Die On Ragdoll",
    Default = false,
    Callback = function(Value)
        autoHelperRagdoll = Value
        
        if Value then
            -- Check if Safespot exists
            local safespot = workspace:FindFirstChild("Safespot")
            if not safespot then
                OrionLib:MakeNotification({
                    Name = "Error",
                    Content = "Safespot not found! Create Safespot first.",
                    Image = "rbxassetid://4483345998",
                    Time = 3
                })
                autoHelperRagdoll = false
                return
            end
            
            if helperRagdollLoop then
                task.cancel(helperRagdollLoop)
                helperRagdollLoop = nil
            end
            
            helperRagdollLoop = task.spawn(function()
                while autoHelperRagdoll do
                    pcall(function()
                        local player = game.Players.LocalPlayer
                        local char = player.Character
                        
                        -- Wait for character if dead
                        if not char or not char.Parent then
                            print("⏳ Waiting for respawn...")
                            char = player.CharacterAdded:Wait()
                            task.wait(1)
                        end
                        
                        if not char then
                            task.wait(0.5)
                            return
                        end
                        
                        local hrp = char:FindFirstChild("HumanoidRootPart")
                        local humanoid = char:FindFirstChildOfClass("Humanoid")
                        
                        if not hrp or not humanoid then
                            task.wait(0.5)
                            return
                        end
                        
                        -- Check if player has entered
                        local hasEntered = char:FindFirstChild("entered")
                        
                        -- Auto Enter Arena
                        if not hasEntered then
                            print("🚶 Entering arena...")
                            hrp.CFrame = CFrame.new(-1209, 328, 3)
                            task.wait(0.5)
                        else
                            print("✅ Already in arena")
                        end
                        
                        -- Teleport to Receiver position
                        local safespot = workspace:FindFirstChild("Safespot")
                        if safespot then
                            local base = safespot:FindFirstChild("Base")
                            if base then
                                hrp.CFrame = base.CFrame + Vector3.new(0, 10, 0)
                            end
                        else
                            hrp.CFrame = CFrame.new(10000, -42, 9996)
                        end
                        
                        task.wait(0.5)
                        
                        -- Check Ragdolled
                        local ragdolled = char:FindFirstChild("Ragdolled")
                        if ragdolled and ragdolled.Value == true then
                            humanoid.Health = 0
                            print("💀 Died due to ragdoll!")
                            task.wait(0.5)
                        end
                    end)
                    task.wait(0.5)
                end
            end)
            
            OrionLib:MakeNotification({
                Name = "Auto Helper",
                Content = "Auto Teleport + Auto Die On Ragdoll Enabled!",
                Image = "rbxassetid://7743878857",
                Time = 3
            })
            
        else
            if helperRagdollLoop then
                task.cancel(helperRagdollLoop)
                helperRagdollLoop = nil
            end
            
            OrionLib:MakeNotification({
                Name = "Auto Helper",
                Content = "Disabled!",
                Image = "rbxassetid://7743878857",
                Time = 2
            })
        end
    end
})

local Tab = Window:MakeTab({
    Name = "Killstreak",
    Icon = "rbxassetid://7734068321",
    PremiumOnly = false
})

Section = Tab:AddSection({
    Name = "1-2-3 Quest"
})

Section = Tab:AddSection({
    Name = "Receiver"
})

-- Auto Teleport To Safespot + Slap Aura (Receiver)
local autoReceiverSlapAura = false
local receiverSlapAuraLoop = nil

Tab:AddToggle({
    Name = "Auto Teleport To Safespot + Slap Aura",
    Default = false,
    Callback = function(Value)
        autoReceiverSlapAura = Value
        
        if Value then
            -- Check if Safespot exists
            local safespot = workspace:FindFirstChild("Safespot")
            if not safespot then
                OrionLib:MakeNotification({
                    Name = "Error",
                    Content = "Safespot not found! Create Safespot first.",
                    Image = "rbxassetid://4483345998",
                    Time = 3
                })
                autoReceiverSlapAura = false
                return
            end
            
            if receiverSlapAuraLoop then
                task.cancel(receiverSlapAuraLoop)
                receiverSlapAuraLoop = nil
            end
            
            receiverSlapAuraLoop = task.spawn(function()
                while autoReceiverSlapAura do
                    pcall(function()
                        local char = game.Players.LocalPlayer.Character
                        if char then
                            local hrp = char:FindFirstChild("HumanoidRootPart")
                            if hrp then
                                -- Teleport to Safespot
                                local safespot = workspace:FindFirstChild("Safespot")
                                if safespot then
                                    local base = safespot:FindFirstChild("Base")
                                    if base then
                                        hrp.CFrame = base.CFrame + Vector3.new(0, 10, 0)
                                    end
                                end
                            end
                        end
                        
                        -- KSHit Slap Aura (every 0.5 seconds)
                        local ksHit = game:GetService("ReplicatedStorage"):FindFirstChild("KSHit")
                        if ksHit then
                            local localPlayer = game.Players.LocalPlayer
                            for _, player in pairs(game:GetService("Players"):GetPlayers()) do
                                if player ~= localPlayer and player.Character then
                                    local targetPart = player.Character:FindFirstChild("Head") or 
                                                      player.Character:FindFirstChild("Torso") or
                                                      player.Character:FindFirstChild("Right Arm") or
                                                      player.Character:FindFirstChild("Left Arm") or
                                                      player.Character:FindFirstChild("HumanoidRootPart")
                                    if targetPart then
                                        ksHit:FireServer(targetPart)
                                    end
                                end
                            end
                        end
                    end)
                    task.wait(0.5)
                end
            end)
            
            OrionLib:MakeNotification({
                Name = "Auto Receiver",
                Content = "Auto Teleport + Slap Aura Enabled!",
                Image = "rbxassetid://7743878857",
                Time = 3
            })
            
        else
            if receiverSlapAuraLoop then
                task.cancel(receiverSlapAuraLoop)
                receiverSlapAuraLoop = nil
            end
            
            OrionLib:MakeNotification({
                Name = "Auto Receiver",
                Content = "Disabled!",
                Image = "rbxassetid://7743878857",
                Time = 2
            })
        end
    end
})

Section = Tab:AddSection({
    Name = "Helper"
})

-- Auto Teleport To Receiver + Auto Die On Ragdoll (Helper)
local autoHelperRagdoll = false
local helperRagdollLoop = nil

Tab:AddToggle({
    Name = "Auto Teleport To Receiver + Auto Die On Ragdoll",
    Default = false,
    Callback = function(Value)
        autoHelperRagdoll = Value
        
        if Value then
            -- Check if Safespot exists
            local safespot = workspace:FindFirstChild("Safespot")
            if not safespot then
                OrionLib:MakeNotification({
                    Name = "Error",
                    Content = "Safespot not found! Create Safespot first.",
                    Image = "rbxassetid://4483345998",
                    Time = 3
                })
                autoHelperRagdoll = false
                return
            end
            
            if helperRagdollLoop then
                task.cancel(helperRagdollLoop)
                helperRagdollLoop = nil
            end
            
            helperRagdollLoop = task.spawn(function()
                while autoHelperRagdoll do
                    pcall(function()
                        local player = game.Players.LocalPlayer
                        local char = player.Character
                        
                        -- Wait for character if dead
                        if not char or not char.Parent then
                            print("⏳ Waiting for respawn...")
                            char = player.CharacterAdded:Wait()
                            task.wait(1)
                        end
                        
                        if not char then
                            task.wait(0.5)
                            return
                        end
                        
                        local hrp = char:FindFirstChild("HumanoidRootPart")
                        local humanoid = char:FindFirstChildOfClass("Humanoid")
                        
                        if not hrp or not humanoid then
                            task.wait(0.5)
                            return
                        end
                        
                        -- Check if player has entered
                        local hasEntered = char:FindFirstChild("entered")
                        
                        -- Auto Enter Arena
                        if not hasEntered then
                            print("🚶 Entering arena...")
                            hrp.CFrame = CFrame.new(-1209, 328, 3)
                            task.wait(0.5)
                        else
                            print("✅ Already in arena")
                        end
                        
                        -- Teleport to Receiver position
                        local safespot = workspace:FindFirstChild("Safespot")
                        if safespot then
                            local base = safespot:FindFirstChild("Base")
                            if base then
                                hrp.CFrame = base.CFrame + Vector3.new(0, 10, 0)
                            end
                        else
                            hrp.CFrame = CFrame.new(10000, -42, 9996)
                        end
                        
                        task.wait(0.5)
                        
                        -- Check Ragdolled
                        local ragdolled = char:FindFirstChild("Ragdolled")
                        if ragdolled and ragdolled.Value == true then
                            humanoid.Health = 0
                            print("💀 Died due to ragdoll!")
                            task.wait(0.5)
                        end
                    end)
                    task.wait(0.5)
                end
            end)
            
            OrionLib:MakeNotification({
                Name = "Auto Helper",
                Content = "Auto Teleport + Auto Die On Ragdoll Enabled!",
                Image = "rbxassetid://7743878857",
                Time = 3
            })
            
        else
            if helperRagdollLoop then
                task.cancel(helperRagdollLoop)
                helperRagdollLoop = nil
            end
            
            OrionLib:MakeNotification({
                Name = "Auto Helper",
                Content = "Disabled!",
                Image = "rbxassetid://7743878857",
                Time = 2
            })
        end
    end
})

local Tab = Window:MakeTab({
    Name = "Gummy",
    Icon = "rbxassetid://7734068321",
    PremiumOnly = false
})

Section = Tab:AddSection({
    Name = "1-2-3 Quest"
})

Section = Tab:AddSection({
    Name = "Receiver"
})

-- Auto Teleport To Safespot + Slap Aura (Receiver)
local autoReceiverSlapAura = false
local receiverSlapAuraLoop = nil

Tab:AddToggle({
    Name = "Auto Teleport To Safespot + Slap Aura",
    Default = false,
    Callback = function(Value)
        autoReceiverSlapAura = Value
        
        if Value then
            -- Check if Safespot exists
            local safespot = workspace:FindFirstChild("Safespot")
            if not safespot then
                OrionLib:MakeNotification({
                    Name = "Error",
                    Content = "Safespot not found! Create Safespot first.",
                    Image = "rbxassetid://4483345998",
                    Time = 3
                })
                autoReceiverSlapAura = false
                return
            end
            
            if receiverSlapAuraLoop then
                task.cancel(receiverSlapAuraLoop)
                receiverSlapAuraLoop = nil
            end
            
            receiverSlapAuraLoop = task.spawn(function()
                while autoReceiverSlapAura do
                    pcall(function()
                        local char = game.Players.LocalPlayer.Character
                        if char then
                            local hrp = char:FindFirstChild("HumanoidRootPart")
                            if hrp then
                                -- Teleport to Safespot
                                local safespot = workspace:FindFirstChild("Safespot")
                                if safespot then
                                    local base = safespot:FindFirstChild("Base")
                                    if base then
                                        hrp.CFrame = base.CFrame + Vector3.new(0, 10, 0)
                                    end
                                end
                            end
                        end
                        
                        -- GeneralHit Slap Aura (every 0.5 seconds) with Left Leg
                        local generalHit = game:GetService("ReplicatedStorage"):FindFirstChild("GeneralHit")
                        if generalHit then
                            local localPlayer = game.Players.LocalPlayer
                            for _, player in pairs(game:GetService("Players"):GetPlayers()) do
                                if player ~= localPlayer and player.Character then
                                    local targetPart = player.Character:FindFirstChild("Left Leg") or 
                                                      player.Character:FindFirstChild("Right Leg") or
                                                      player.Character:FindFirstChild("Left Arm") or
                                                      player.Character:FindFirstChild("Right Arm") or
                                                      player.Character:FindFirstChild("Head") or 
                                                      player.Character:FindFirstChild("Torso") or 
                                                      player.Character:FindFirstChild("HumanoidRootPart")
                                    if targetPart then
                                        generalHit:FireServer(targetPart)
                                    end
                                end
                            end
                        end
                    end)
                    task.wait(0.5)
                end
            end)
            
            OrionLib:MakeNotification({
                Name = "Auto Receiver",
                Content = "Auto Teleport + Slap Aura Enabled!",
                Image = "rbxassetid://7743878857",
                Time = 3
            })
            
        else
            if receiverSlapAuraLoop then
                task.cancel(receiverSlapAuraLoop)
                receiverSlapAuraLoop = nil
            end
            
            OrionLib:MakeNotification({
                Name = "Auto Receiver",
                Content = "Disabled!",
                Image = "rbxassetid://7743878857",
                Time = 2
            })
        end
    end
})

Section = Tab:AddSection({
    Name = "Helper"
})

-- Auto Teleport To Receiver + Auto Die On Ragdoll (Helper)
local autoHelperRagdoll = false
local helperRagdollLoop = nil

Tab:AddToggle({
    Name = "Auto Teleport To Receiver + Auto Die On Ragdoll",
    Default = false,
    Callback = function(Value)
        autoHelperRagdoll = Value
        
        if Value then
            -- Check if Safespot exists
            local safespot = workspace:FindFirstChild("Safespot")
            if not safespot then
                OrionLib:MakeNotification({
                    Name = "Error",
                    Content = "Safespot not found! Create Safespot first.",
                    Image = "rbxassetid://4483345998",
                    Time = 3
                })
                autoHelperRagdoll = false
                return
            end
            
            if helperRagdollLoop then
                task.cancel(helperRagdollLoop)
                helperRagdollLoop = nil
            end
            
            helperRagdollLoop = task.spawn(function()
                while autoHelperRagdoll do
                    pcall(function()
                        local player = game.Players.LocalPlayer
                        local char = player.Character
                        
                        -- Wait for character if dead
                        if not char or not char.Parent then
                            print("⏳ Waiting for respawn...")
                            char = player.CharacterAdded:Wait()
                            task.wait(1)
                        end
                        
                        if not char then
                            task.wait(0.5)
                            return
                        end
                        
                        local hrp = char:FindFirstChild("HumanoidRootPart")
                        local humanoid = char:FindFirstChildOfClass("Humanoid")
                        
                        if not hrp or not humanoid then
                            task.wait(0.5)
                            return
                        end
                        
                        -- Check if player has entered
                        local hasEntered = char:FindFirstChild("entered")
                        
                        -- Auto Enter Arena
                        if not hasEntered then
                            print("🚶 Entering arena...")
                            hrp.CFrame = CFrame.new(-1209, 328, 3)
                            task.wait(0.5)
                        else
                            print("✅ Already in arena")
                        end
                        
                        -- Teleport to Receiver position
                        local safespot = workspace:FindFirstChild("Safespot")
                        if safespot then
                            local base = safespot:FindFirstChild("Base")
                            if base then
                                hrp.CFrame = base.CFrame + Vector3.new(0, 10, 0)
                            end
                        else
                            hrp.CFrame = CFrame.new(10000, -42, 9996)
                        end
                        
                        task.wait(0.5)
                        
                        -- Check Ragdolled
                        local ragdolled = char:FindFirstChild("Ragdolled")
                        if ragdolled and ragdolled.Value == true then
                            humanoid.Health = 0
                            print("💀 Died due to ragdoll!")
                            task.wait(0.5)
                        end
                    end)
                    task.wait(0.5)
                end
            end)
            
            OrionLib:MakeNotification({
                Name = "Auto Helper",
                Content = "Auto Teleport + Auto Die On Ragdoll Enabled!",
                Image = "rbxassetid://7743878857",
                Time = 3
            })
            
        else
            if helperRagdollLoop then
                task.cancel(helperRagdollLoop)
                helperRagdollLoop = nil
            end
            
            OrionLib:MakeNotification({
                Name = "Auto Helper",
                Content = "Disabled!",
                Image = "rbxassetid://7743878857",
                Time = 2
            })
        end
    end
})

local Tab = Window:MakeTab({
    Name = "Engineer",
    Icon = "rbxassetid://7734068321",
    PremiumOnly = false
})

Section = Tab:AddSection({
    Name = "1-2-3 Quest"
})

Section = Tab:AddSection({
    Name = "Receiver"
})

-- Auto Teleport To Safespot + Auto Use Ability (Receiver)
local autoReceiverSentry = false
local receiverSentryLoop = nil

Tab:AddToggle({
    Name = "Auto Teleport To Safespot + Auto Use Ability",
    Default = false,
    Callback = function(Value)
        autoReceiverSentry = Value
        
        if Value then
            -- Check if Safespot exists
            local safespot = workspace:FindFirstChild("Safespot")
            if not safespot then
                OrionLib:MakeNotification({
                    Name = "Error",
                    Content = "Safespot not found! Create Safespot first.",
                    Image = "rbxassetid://4483345998",
                    Time = 3
                })
                autoReceiverSentry = false
                return
            end
            
            if receiverSentryLoop then
                task.cancel(receiverSentryLoop)
                receiverSentryLoop = nil
            end
            
            receiverSentryLoop = task.spawn(function()
                local abilityTimer = 0
                
                while autoReceiverSentry do
                    pcall(function()
                        local char = game.Players.LocalPlayer.Character
                        if char then
                            local hrp = char:FindFirstChild("HumanoidRootPart")
                            if hrp then
                                -- Teleport to Safespot
                                local safespot = workspace:FindFirstChild("Safespot")
                                if safespot then
                                    local base = safespot:FindFirstChild("Base")
                                    if base then
                                        hrp.CFrame = base.CFrame + Vector3.new(0, 10, 0)
                                    end
                                end
                            end
                        end
                        
                        -- Sentry ability (every 6 seconds)
                        abilityTimer = abilityTimer + 0.5
                        if abilityTimer >= 6 then
                            abilityTimer = 0
                            local sentry = game:GetService("ReplicatedStorage"):FindFirstChild("Sentry")
                            if sentry then
                                sentry:FireServer()
                                print("🔫 Sentry ability used!")
                            end
                        end
                    end)
                    task.wait(0.5)
                end
            end)
            
            OrionLib:MakeNotification({
                Name = "Auto Receiver",
                Content = "Auto Teleport + Sentry Ability Enabled!",
                Image = "rbxassetid://7743878857",
                Time = 3
            })
            
        else
            if receiverSentryLoop then
                task.cancel(receiverSentryLoop)
                receiverSentryLoop = nil
            end
            
            OrionLib:MakeNotification({
                Name = "Auto Receiver",
                Content = "Disabled!",
                Image = "rbxassetid://7743878857",
                Time = 2
            })
        end
    end
})

Section = Tab:AddSection({
    Name = "Helper"
})

-- Auto Teleport To Receiver/Sentry's Glove + Auto Die On Ragdoll (Helper)
local autoHelperSentry = false
local helperSentryLoop = nil

Tab:AddToggle({
    Name = "Auto Teleport To Receiver/Sentry's Glove + Auto Die On Ragdoll",
    Default = false,
    Callback = function(Value)
        autoHelperSentry = Value
        
        if Value then
            -- Check if Safespot exists
            local safespot = workspace:FindFirstChild("Safespot")
            if not safespot then
                OrionLib:MakeNotification({
                    Name = "Error",
                    Content = "Safespot not found! Create Safespot first.",
                    Image = "rbxassetid://4483345998",
                    Time = 3
                })
                autoHelperSentry = false
                return
            end
            
            if helperSentryLoop then
                task.cancel(helperSentryLoop)
                helperSentryLoop = nil
            end
            
            helperSentryLoop = task.spawn(function()
                while autoHelperSentry do
                    pcall(function()
                        local player = game.Players.LocalPlayer
                        local char = player.Character
                        
                        -- Wait for character if dead
                        if not char or not char.Parent then
                            print("⏳ Waiting for respawn...")
                            char = player.CharacterAdded:Wait()
                            task.wait(1)
                        end
                        
                        if not char then
                            task.wait(0.5)
                            return
                        end
                        
                        local hrp = char:FindFirstChild("HumanoidRootPart")
                        local humanoid = char:FindFirstChildOfClass("Humanoid")
                        
                        if not hrp or not humanoid then
                            task.wait(0.5)
                            return
                        end
                        
                        -- Check if player has entered
                        local hasEntered = char:FindFirstChild("entered")
                        
                        -- Auto Enter Arena
                        if not hasEntered then
                            print("🚶 Entering arena...")
                            hrp.CFrame = CFrame.new(-1209, 328, 3)
                            task.wait(0.5)
                        else
                            print("✅ Already in arena")
                        end
                        
                        -- Teleport to Receiver position (Safespot)
                        local safespot = workspace:FindFirstChild("Safespot")
                        if safespot then
                            local base = safespot:FindFirstChild("Base")
                            if base then
                                hrp.CFrame = base.CFrame + Vector3.new(0, 10, 0)
                            end
                        else
                            hrp.CFrame = CFrame.new(10000, -42, 9996)
                        end
                        
                        task.wait(0.5)
                        
                        -- Find Turret and teleport to Glove
                        local turret = workspace:FindFirstChild("Turret")
                        if turret then
                            local hingePart = turret:FindFirstChild("HingePart")
                            if hingePart then
                                local glove = hingePart:FindFirstChild("Glove")
                                if glove then
                                    hrp.CFrame = glove.CFrame + Vector3.new(0, 3, 0)
                                    print("🎯 Teleported to Sentry's Glove!")
                                else
                                    print("⚠️ Glove not found in Turret.HingePart")
                                end
                            else
                                print("⚠️ HingePart not found in Turret")
                            end
                        else
                            print("⚠️ Turret not found in workspace")
                        end
                        
                        task.wait(0.5)
                        
                        -- Check Ragdolled
                        local ragdolled = char:FindFirstChild("Ragdolled")
                        if ragdolled and ragdolled.Value == true then
                            humanoid.Health = 0
                            print("💀 Died due to ragdoll!")
                            task.wait(0.5)
                        end
                    end)
                    task.wait(0.5)
                end
            end)
            
            OrionLib:MakeNotification({
                Name = "Auto Helper",
                Content = "Auto Teleport + Auto Die On Ragdoll Enabled!",
                Image = "rbxassetid://7743878857",
                Time = 3
            })
            
        else
            if helperSentryLoop then
                task.cancel(helperSentryLoop)
                helperSentryLoop = nil
            end
            
            OrionLib:MakeNotification({
                Name = "Auto Helper",
                Content = "Disabled!",
                Image = "rbxassetid://7743878857",
                Time = 2
            })
        end
    end
})

local Tab = Window:MakeTab({
    Name = "Phantom",
    Icon = "rbxassetid://7734068321",
    PremiumOnly = false
})

Section = Tab:AddSection({
    Name = "1-2-3 Quest"
})

Section = Tab:AddSection({
    Name = "Receiver"
})

-- Auto Teleport To Safespot + Auto Use Ability + Slap Aura (Receiver)
local autoReceiverPhantom = false
local receiverPhantomLoop = nil

Tab:AddToggle({
    Name = "Auto Teleport To Safespot + Auto Use Ability + Slap Aura",
    Default = false,
    Callback = function(Value)
        autoReceiverPhantom = Value
        
        if Value then
            -- Check if Safespot exists
            local safespot = workspace:FindFirstChild("Safespot")
            if not safespot then
                OrionLib:MakeNotification({
                    Name = "Error",
                    Content = "Safespot not found! Create Safespot first.",
                    Image = "rbxassetid://4483345998",
                    Time = 3
                })
                autoReceiverPhantom = false
                return
            end
            
            if receiverPhantomLoop then
                task.cancel(receiverPhantomLoop)
                receiverPhantomLoop = nil
            end
            
            receiverPhantomLoop = task.spawn(function()
                while autoReceiverPhantom do
                    pcall(function()
                        local char = game.Players.LocalPlayer.Character
                        if char then
                            local hrp = char:FindFirstChild("HumanoidRootPart")
                            if hrp then
                                -- Teleport to Safespot
                                local safespot = workspace:FindFirstChild("Safespot")
                                if safespot then
                                    local base = safespot:FindFirstChild("Base")
                                    if base then
                                        hrp.CFrame = base.CFrame + Vector3.new(0, 10, 0)
                                    end
                                end
                            end
                        end
                        
                        -- PhantomDash ability (every 0.01 seconds)
                        local phantomDash = game:GetService("ReplicatedStorage"):FindFirstChild("PhantomDash")
                        if phantomDash then
                            local phantom = char and char:FindFirstChild("Phantom")
                            if phantom then
                                phantomDash:InvokeServer(phantom)
                            end
                        end
                        
                        task.wait(0.01)
                        
                        -- PhantomHit Slap Aura (every 0.5 seconds)
                        local phantomHit = game:GetService("ReplicatedStorage"):FindFirstChild("PhantomHit")
                        if phantomHit then
                            local localPlayer = game.Players.LocalPlayer
                            for _, player in pairs(game:GetService("Players"):GetPlayers()) do
                                if player ~= localPlayer and player.Character then
                                    local targetPart = player.Character:FindFirstChild("Torso") or 
                                                      player.Character:FindFirstChild("Head") or
                                                      player.Character:FindFirstChild("Right Arm") or
                                                      player.Character:FindFirstChild("Left Arm") or
                                                      player.Character:FindFirstChild("HumanoidRootPart")
                                    if targetPart then
                                        phantomHit:FireServer(targetPart)
                                    end
                                end
                            end
                        end
                    end)
                    task.wait(0.5)
                end
            end)
            
            OrionLib:MakeNotification({
                Name = "Auto Receiver",
                Content = "Auto Teleport + Phantom Ability + Slap Aura Enabled!",
                Image = "rbxassetid://7743878857",
                Time = 3
            })
            
        else
            if receiverPhantomLoop then
                task.cancel(receiverPhantomLoop)
                receiverPhantomLoop = nil
            end
            
            OrionLib:MakeNotification({
                Name = "Auto Receiver",
                Content = "Disabled!",
                Image = "rbxassetid://7743878857",
                Time = 2
            })
        end
    end
})

Section = Tab:AddSection({
    Name = "Helper"
})

-- Auto Teleport To Receiver (Helper)
local autoHelperReceiverTeleport = false
local helperReceiverTeleportLoop = nil

Tab:AddToggle({
    Name = "Auto Teleport To Receiver",
    Default = false,
    Callback = function(Value)
        autoHelperReceiverTeleport = Value
        
        if Value then
            -- Check if Safespot exists
            local safespot = workspace:FindFirstChild("Safespot")
            if not safespot then
                OrionLib:MakeNotification({
                    Name = "Error",
                    Content = "Safespot not found! Create Safespot first.",
                    Image = "rbxassetid://4483345998",
                    Time = 3
                })
                autoHelperReceiverTeleport = false
                return
            end
            
            if helperReceiverTeleportLoop then
                task.cancel(helperReceiverTeleportLoop)
                helperReceiverTeleportLoop = nil
            end
            
            helperReceiverTeleportLoop = task.spawn(function()
                while autoHelperReceiverTeleport do
                    pcall(function()
                        local char = game.Players.LocalPlayer.Character
                        if char then
                            local hrp = char:FindFirstChild("HumanoidRootPart")
                            if hrp then
                                -- Teleport to Safespot position
                                local safespot = workspace:FindFirstChild("Safespot")
                                if safespot then
                                    local base = safespot:FindFirstChild("Base")
                                    if base then
                                        hrp.CFrame = base.CFrame + Vector3.new(0, 10, 0)
                                    end
                                else
                                    hrp.CFrame = CFrame.new(10000, -42, 9996)
                                end
                            end
                        end
                    end)
                    task.wait(0.5)
                end
            end)
            
            OrionLib:MakeNotification({
                Name = "Auto Helper",
                Content = "Auto teleport to Receiver position enabled!",
                Image = "rbxassetid://7743878857",
                Time = 2
            })
            
        else
            if helperReceiverTeleportLoop then
                task.cancel(helperReceiverTeleportLoop)
                helperReceiverTeleportLoop = nil
            end
            
            OrionLib:MakeNotification({
                Name = "Auto Helper",
                Content = "Auto teleport to Receiver position disabled!",
                Image = "rbxassetid://7743878857",
                Time = 2
            })
        end
    end
})

local Tab = Window:MakeTab({
    Name = "Wormhole",
    Icon = "rbxassetid://7734068321",
    PremiumOnly = false
})

Section = Tab:AddSection({
    Name = "1-2-3 Quest"
})

Section = Tab:AddSection({
    Name = "Receiver"
})

-- Auto Teleport To Safespot + Auto Use Ability + Slap Aura + Auto Teleport To Circle Portals (Receiver)
local autoReceiverWormhole = false
local receiverWormholeLoop = nil

Tab:AddToggle({
    Name = "Auto Teleport To Safespot + Auto Use Ability + Slap Aura + Auto Teleport To Circle Portals",
    Default = false,
    Callback = function(Value)
        autoReceiverWormhole = Value
        
        if Value then
            -- Check if Safespot exists
            local safespot = workspace:FindFirstChild("Safespot")
            if not safespot then
                OrionLib:MakeNotification({
                    Name = "Error",
                    Content = "Safespot not found! Create Safespot first.",
                    Image = "rbxassetid://4483345998",
                    Time = 3
                })
                autoReceiverWormhole = false
                return
            end
            
            if receiverWormholeLoop then
                task.cancel(receiverWormholeLoop)
                receiverWormholeLoop = nil
            end
            
            receiverWormholeLoop = task.spawn(function()
                local portalTimer = 0
                
                while autoReceiverWormhole do
                    pcall(function()
                        local char = game.Players.LocalPlayer.Character
                        if char then
                            local hrp = char:FindFirstChild("HumanoidRootPart")
                            if hrp then
                                -- Teleport to Safespot
                                local safespot = workspace:FindFirstChild("Safespot")
                                if safespot then
                                    local base = safespot:FindFirstChild("Base")
                                    if base then
                                        hrp.CFrame = base.CFrame + Vector3.new(0, 10, 0)
                                    end
                                end
                            end
                        end
                        
                        -- WormholePlace ability (every 0.1 seconds)
                        local wormholePlace = game:GetService("ReplicatedStorage"):FindFirstChild("WormholePlace")
                        if wormholePlace then
                            wormholePlace:FireServer()
                        end
                        
                        task.wait(0.1)
                        
                        -- WormHit Slap Aura (every 0.5 seconds)
                        local wormHit = game:GetService("ReplicatedStorage"):FindFirstChild("WormHit")
                        if wormHit then
                            local localPlayer = game.Players.LocalPlayer
                            for _, player in pairs(game:GetService("Players"):GetPlayers()) do
                                if player ~= localPlayer and player.Character then
                                    local targetPart = player.Character:FindFirstChild("Torso") or 
                                                      player.Character:FindFirstChild("Head") or
                                                      player.Character:FindFirstChild("Right Arm") or
                                                      player.Character:FindFirstChild("Left Arm") or
                                                      player.Character:FindFirstChild("HumanoidRootPart")
                                    if targetPart then
                                        wormHit:FireServer(targetPart)
                                    end
                                end
                            end
                        end
                        
                        -- Auto Teleport to Circle Portals (every 2 seconds)
                        portalTimer = portalTimer + 0.5
                        if portalTimer >= 2 then
                            portalTimer = 0
                            
                            local wormholes = workspace:FindFirstChild("Wormholes")
                            if wormholes then
                                local one = wormholes:FindFirstChild("One")
                                local two = wormholes:FindFirstChild("Two")
                                
                                if one and two and hrp then
                                    -- Alternate between One and Two
                                    if portalTimer == 0 then
                                        hrp.CFrame = one.CFrame + Vector3.new(0, 3, 0)
                                        print("🌀 Teleported to Wormhole One")
                                    else
                                        hrp.CFrame = two.CFrame + Vector3.new(0, 3, 0)
                                        print("🌀 Teleported to Wormhole Two")
                                    end
                                else
                                    print("⚠️ Wormholes folder or parts not found!")
                                end
                            else
                                print("⚠️ Wormholes folder not found!")
                            end
                        end
                    end)
                    task.wait(0.5)
                end
            end)
            
            OrionLib:MakeNotification({
                Name = "Auto Receiver",
                Content = "Auto Teleport + Wormhole Ability + Slap Aura + Portal Teleport Enabled!",
                Image = "rbxassetid://7743878857",
                Time = 3
            })
            
        else
            if receiverWormholeLoop then
                task.cancel(receiverWormholeLoop)
                receiverWormholeLoop = nil
            end
            
            OrionLib:MakeNotification({
                Name = "Auto Receiver",
                Content = "Disabled!",
                Image = "rbxassetid://7743878857",
                Time = 2
            })
        end
    end
})

Section = Tab:AddSection({
    Name = "Helper"
})

-- Auto Teleport To Receiver (Helper)
local autoHelperReceiverWormhole = false
local helperReceiverWormholeLoop = nil

Tab:AddToggle({
    Name = "Auto Teleport To Receiver",
    Default = false,
    Callback = function(Value)
        autoHelperReceiverWormhole = Value
        
        if Value then
            -- Check if Safespot exists
            local safespot = workspace:FindFirstChild("Safespot")
            if not safespot then
                OrionLib:MakeNotification({
                    Name = "Error",
                    Content = "Safespot not found! Create Safespot first.",
                    Image = "rbxassetid://4483345998",
                    Time = 3
                })
                autoHelperReceiverWormhole = false
                return
            end
            
            if helperReceiverWormholeLoop then
                task.cancel(helperReceiverWormholeLoop)
                helperReceiverWormholeLoop = nil
            end
            
            helperReceiverWormholeLoop = task.spawn(function()
                while autoHelperReceiverWormhole do
                    pcall(function()
                        local char = game.Players.LocalPlayer.Character
                        if char then
                            local hrp = char:FindFirstChild("HumanoidRootPart")
                            if hrp then
                                -- Teleport to Safespot position
                                local safespot = workspace:FindFirstChild("Safespot")
                                if safespot then
                                    local base = safespot:FindFirstChild("Base")
                                    if base then
                                        hrp.CFrame = base.CFrame + Vector3.new(0, 10, 0)
                                    end
                                else
                                    hrp.CFrame = CFrame.new(10000, -42, 9996)
                                end
                            end
                        end
                    end)
                    task.wait(0.5)
                end
            end)
            
            OrionLib:MakeNotification({
                Name = "Auto Helper",
                Content = "Auto teleport to Receiver position enabled!",
                Image = "rbxassetid://7743878857",
                Time = 2
            })
            
        else
            if helperReceiverWormholeLoop then
                task.cancel(helperReceiverWormholeLoop)
                helperReceiverWormholeLoop = nil
            end
            
            OrionLib:MakeNotification({
                Name = "Auto Helper",
                Content = "Auto teleport to Receiver position disabled!",
                Image = "rbxassetid://7743878857",
                Time = 2
            })
        end
    end
})

local Tab = Window:MakeTab({
    Name = "Stalker",
    Icon = "rbxassetid://7734068321",
    PremiumOnly = false
})

Section = Tab:AddSection({
    Name = "1-2 Quest"
})

Section = Tab:AddSection({
    Name = "Receiver"
})

-- Teleport To Safespot
Tab:AddButton({
    Name = "Teleport To Safespot",
    Callback = function()
        local safespot = workspace:FindFirstChild("Safespot")
        if safespot then
            local base = safespot:FindFirstChild("Base")
            if base then
                local char = LocalPlayer.Character
                if char then
                    local hrp = char:FindFirstChild("HumanoidRootPart")
                    if hrp then
                        hrp.CFrame = base.CFrame + Vector3.new(0, 10, 0)
                    end
                end
            end
        else
            OrionLib:MakeNotification({
                Name = "Error",
                Content = "Create Safespot first!",
                Image = "rbxassetid://4483345998",
                Time = 3
            })
        end
    end
})

-- Use Slap Aura (Receiver)
local autoReceiverSlapAuraOnly = false
local receiverSlapAuraOnlyLoop = nil

Tab:AddToggle({
    Name = "Use Slap Aura",
    Default = false,
    Callback = function(Value)
        autoReceiverSlapAuraOnly = Value
        
        if Value then
            if receiverSlapAuraOnlyLoop then
                task.cancel(receiverSlapAuraOnlyLoop)
                receiverSlapAuraOnlyLoop = nil
            end
            
            receiverSlapAuraOnlyLoop = task.spawn(function()
                while autoReceiverSlapAuraOnly do
                    pcall(function()
                        -- GeneralHit Slap Aura (every 0.5 seconds)
                        local generalHit = game:GetService("ReplicatedStorage"):FindFirstChild("GeneralHit")
                        if generalHit then
                            local localPlayer = game.Players.LocalPlayer
                            for _, player in pairs(game:GetService("Players"):GetPlayers()) do
                                if player ~= localPlayer and player.Character then
                                    local targetPart = player.Character:FindFirstChild("Right Arm") or 
                                                      player.Character:FindFirstChild("Left Arm") or
                                                      player.Character:FindFirstChild("Head") or 
                                                      player.Character:FindFirstChild("Torso") or 
                                                      player.Character:FindFirstChild("HumanoidRootPart")
                                    if targetPart then
                                        generalHit:FireServer(targetPart, false, 20)
                                    end
                                end
                            end
                        end
                    end)
                    task.wait(0.5)
                end
            end)
            
            OrionLib:MakeNotification({
                Name = "Auto Receiver",
                Content = "Slap Aura Enabled!",
                Image = "rbxassetid://7743878857",
                Time = 2
            })
            
        else
            if receiverSlapAuraOnlyLoop then
                task.cancel(receiverSlapAuraOnlyLoop)
                receiverSlapAuraOnlyLoop = nil
            end
            
            OrionLib:MakeNotification({
                Name = "Auto Receiver",
                Content = "Slap Aura Disabled!",
                Image = "rbxassetid://7743878857",
                Time = 2
            })
        end
    end
})

Section = Tab:AddSection({
    Name = "Helper"
})

-- Auto Teleport To Receiver (Helper)
local autoHelperReceiverTeleportSimple = false
local helperReceiverTeleportSimpleLoop = nil

Tab:AddToggle({
    Name = "Auto Teleport To Receiver",
    Default = false,
    Callback = function(Value)
        autoHelperReceiverTeleportSimple = Value
        
        if Value then
            -- Check if Safespot exists
            local safespot = workspace:FindFirstChild("Safespot")
            if not safespot then
                OrionLib:MakeNotification({
                    Name = "Error",
                    Content = "Safespot not found! Create Safespot first.",
                    Image = "rbxassetid://4483345998",
                    Time = 3
                })
                autoHelperReceiverTeleportSimple = false
                return
            end
            
            if helperReceiverTeleportSimpleLoop then
                task.cancel(helperReceiverTeleportSimpleLoop)
                helperReceiverTeleportSimpleLoop = nil
            end
            
            helperReceiverTeleportSimpleLoop = task.spawn(function()
                while autoHelperReceiverTeleportSimple do
                    pcall(function()
                        local char = game.Players.LocalPlayer.Character
                        if char then
                            local hrp = char:FindFirstChild("HumanoidRootPart")
                            if hrp then
                                -- Teleport to Safespot position
                                local safespot = workspace:FindFirstChild("Safespot")
                                if safespot then
                                    local base = safespot:FindFirstChild("Base")
                                    if base then
                                        hrp.CFrame = base.CFrame + Vector3.new(0, 10, 0)
                                    end
                                else
                                    hrp.CFrame = CFrame.new(10000, -42, 9996)
                                end
                            end
                        end
                    end)
                    task.wait(0.5)
                end
            end)
            
            OrionLib:MakeNotification({
                Name = "Auto Helper",
                Content = "Auto teleport to Receiver position enabled!",
                Image = "rbxassetid://7743878857",
                Time = 2
            })
            
        else
            if helperReceiverTeleportSimpleLoop then
                task.cancel(helperReceiverTeleportSimpleLoop)
                helperReceiverTeleportSimpleLoop = nil
            end
            
            OrionLib:MakeNotification({
                Name = "Auto Helper",
                Content = "Auto teleport to Receiver position disabled!",
                Image = "rbxassetid://7743878857",
                Time = 2
            })
        end
    end
})

local Tab = Window:MakeTab({
    Name = "Cherry",
    Icon = "rbxassetid://7734068321",
    PremiumOnly = false
})

Section = Tab:AddSection({
    Name = "1-2-3-4 Quest"
})

Section = Tab:AddSection({
    Name = "Receiver"
})

-- Auto Teleport To Safespot + Slap Aura + Auto Use Ability + Auto Use Cherry's Clone Glove (Receiver)
local autoReceiverCherry = false
local receiverCherryLoop = nil

Tab:AddToggle({
    Name = "Auto Teleport To Safespot + Slap Aura + Auto Use Ability + Auto Use Cherry's Clone Glove",
    Default = false,
    Callback = function(Value)
        autoReceiverCherry = Value
        
        if Value then
            -- Check if Safespot exists
            local safespot = workspace:FindFirstChild("Safespot")
            if not safespot then
                OrionLib:MakeNotification({
                    Name = "Error",
                    Content = "Safespot not found! Create Safespot first.",
                    Image = "rbxassetid://4483345998",
                    Time = 3
                })
                autoReceiverCherry = false
                return
            end
            
            if receiverCherryLoop then
                task.cancel(receiverCherryLoop)
                receiverCherryLoop = nil
            end
            
            receiverCherryLoop = task.spawn(function()
                local cloneTimer = 0
                
                while autoReceiverCherry do
                    pcall(function()
                        local char = game.Players.LocalPlayer.Character
                        if char then
                            local hrp = char:FindFirstChild("HumanoidRootPart")
                            if hrp then
                                -- Teleport to Safespot
                                local safespot = workspace:FindFirstChild("Safespot")
                                if safespot then
                                    local base = safespot:FindFirstChild("Base")
                                    if base then
                                        hrp.CFrame = base.CFrame + Vector3.new(0, 10, 0)
                                    end
                                end
                            end
                        end
                        
                        -- GeneralAbility (every 0.1 seconds)
                        local generalAbility = game:GetService("ReplicatedStorage"):FindFirstChild("GeneralAbility")
                        if generalAbility then
                            generalAbility:FireServer()
                        end
                        
                        task.wait(0.1)
                        
                        -- GeneralHit Slap Aura (every 0.5 seconds)
                        local generalHit = game:GetService("ReplicatedStorage"):FindFirstChild("GeneralHit")
                        if generalHit then
                            local localPlayer = game.Players.LocalPlayer
                            for _, player in pairs(game:GetService("Players"):GetPlayers()) do
                                if player ~= localPlayer and player.Character then
                                    local targetPart = player.Character:FindFirstChild("Left Arm") or 
                                                      player.Character:FindFirstChild("Right Arm") or
                                                      player.Character:FindFirstChild("Head") or 
                                                      player.Character:FindFirstChild("Torso") or 
                                                      player.Character:FindFirstChild("HumanoidRootPart")
                                    if targetPart then
                                        generalHit:FireServer(targetPart)
                                    end
                                end
                            end
                        end
                        
                        -- Auto Use Cherry's Clone Glove (every 0.5 seconds)
                        cloneTimer = cloneTimer + 0.5
                        if cloneTimer >= 0.5 then
                            cloneTimer = 0
                            
                            local cherryStorage = workspace:FindFirstChild("cherry_storage")
                            if cherryStorage then
                                for _, model in pairs(cherryStorage:GetChildren()) do
                                    if model:IsA("Model") and model.Name:lower():match("cherry") then
                                        local cloneRemote = model:FindFirstChild("clone_remote")
                                        if cloneRemote then
                                            pcall(function()
                                                cloneRemote:FireServer()
                                                print("🍒 Cherry clone remote fired!")
                                            end)
                                        end
                                    end
                                end
                            end
                        end
                    end)
                    task.wait(0.5)
                end
            end)
            
            OrionLib:MakeNotification({
                Name = "Auto Receiver",
                Content = "Auto Teleport + Slap Aura + Ability + Cherry Clone Enabled!",
                Image = "rbxassetid://7743878857",
                Time = 3
            })
            
        else
            if receiverCherryLoop then
                task.cancel(receiverCherryLoop)
                receiverCherryLoop = nil
            end
            
            OrionLib:MakeNotification({
                Name = "Auto Receiver",
                Content = "Disabled!",
                Image = "rbxassetid://7743878857",
                Time = 2
            })
        end
    end
})

Section = Tab:AddSection({
    Name = "Helper"
})

-- Auto Teleport To Receiver + Auto Teleport To Cherry's Clone Glove + Auto Die On Ragdoll (Helper)
local autoHelperCherry = false
local helperCherryLoop = nil

Tab:AddToggle({
    Name = "Auto Teleport To Receiver + Auto Teleport To Cherry's Clone Glove + Auto Die On Ragdoll",
    Default = false,
    Callback = function(Value)
        autoHelperCherry = Value
        
        if Value then
            -- Check if Safespot exists
            local safespot = workspace:FindFirstChild("Safespot")
            if not safespot then
                OrionLib:MakeNotification({
                    Name = "Error",
                    Content = "Safespot not found! Create Safespot first.",
                    Image = "rbxassetid://4483345998",
                    Time = 3
                })
                autoHelperCherry = false
                return
            end
            
            if helperCherryLoop then
                task.cancel(helperCherryLoop)
                helperCherryLoop = nil
            end
            
            helperCherryLoop = task.spawn(function()
                while autoHelperCherry do
                    pcall(function()
                        local player = game.Players.LocalPlayer
                        local char = player.Character
                        
                        -- Wait for character if dead
                        if not char or not char.Parent then
                            print("⏳ Waiting for respawn...")
                            char = player.CharacterAdded:Wait()
                            task.wait(1)
                        end
                        
                        if not char then
                            task.wait(0.5)
                            return
                        end
                        
                        local hrp = char:FindFirstChild("HumanoidRootPart")
                        local humanoid = char:FindFirstChildOfClass("Humanoid")
                        
                        if not hrp or not humanoid then
                            task.wait(0.5)
                            return
                        end
                        
                        -- Check if player has entered
                        local hasEntered = char:FindFirstChild("entered")
                        
                        -- Auto Enter Arena
                        if not hasEntered then
                            print("🚶 Entering arena...")
                            hrp.CFrame = CFrame.new(-1209, 328, 3)
                            task.wait(0.5)
                        else
                            print("✅ Already in arena")
                        end
                        
                        -- Teleport to Receiver position (Safespot)
                        local safespot = workspace:FindFirstChild("Safespot")
                        if safespot then
                            local base = safespot:FindFirstChild("Base")
                            if base then
                                hrp.CFrame = base.CFrame + Vector3.new(0, 10, 0)
                            end
                        else
                            hrp.CFrame = CFrame.new(10000, -42, 9996)
                        end
                        
                        task.wait(0.5)
                        
                        -- Find Cherry's Clone Glove
                        local cherryStorage = workspace:FindFirstChild("cherry_storage")
                        if cherryStorage then
                            for _, model in pairs(cherryStorage:GetChildren()) do
                                if model:IsA("Model") and model.Name:lower():match("cherry") then
                                    local tool = model:FindFirstChild("Cherry")
                                    if tool then
                                        local glove = tool:FindFirstChild("Glove")
                                        if glove and glove:IsA("MeshPart") then
                                            hrp.CFrame = glove.CFrame + Vector3.new(0, 3, 0)
                                            print("🍒 Teleported to Cherry's Clone Glove!")
                                        end
                                    end
                                end
                            end
                        else
                            print("⚠️ cherry_storage folder not found!")
                        end
                        
                        task.wait(0.5)
                        
                        -- Check Ragdolled
                        local ragdolled = char:FindFirstChild("Ragdolled")
                        if ragdolled and ragdolled.Value == true then
                            humanoid.Health = 0
                            print("💀 Died due to ragdoll!")
                            task.wait(0.5)
                        end
                    end)
                    task.wait(0.5)
                end
            end)
            
            OrionLib:MakeNotification({
                Name = "Auto Helper",
                Content = "Auto Teleport + Cherry Clone Glove + Auto Die On Ragdoll Enabled!",
                Image = "rbxassetid://7743878857",
                Time = 3
            })
            
        else
            if helperCherryLoop then
                task.cancel(helperCherryLoop)
                helperCherryLoop = nil
            end
            
            OrionLib:MakeNotification({
                Name = "Auto Helper",
                Content = "Disabled!",
                Image = "rbxassetid://7743878857",
                Time = 2
            })
        end
    end
})

local Tab = Window:MakeTab({
    Name = "Booster",
    Icon = "rbxassetid://7734068321",
    PremiumOnly = false
})

Section = Tab:AddSection({
    Name = "1 Quest"
})

Section = Tab:AddSection({
    Name = "Receiver"
})

-- Auto Teleport To Safespot + Slap Aura (Receiver)
local autoReceiverSlapAuraSimple = false
local receiverSlapAuraSimpleLoop = nil

Tab:AddToggle({
    Name = "Auto Teleport To Safespot + Slap Aura",
    Default = false,
    Callback = function(Value)
        autoReceiverSlapAuraSimple = Value
        
        if Value then
            -- Check if Safespot exists
            local safespot = workspace:FindFirstChild("Safespot")
            if not safespot then
                OrionLib:MakeNotification({
                    Name = "Error",
                    Content = "Safespot not found! Create Safespot first.",
                    Image = "rbxassetid://4483345998",
                    Time = 3
                })
                autoReceiverSlapAuraSimple = false
                return
            end
            
            if receiverSlapAuraSimpleLoop then
                task.cancel(receiverSlapAuraSimpleLoop)
                receiverSlapAuraSimpleLoop = nil
            end
            
            receiverSlapAuraSimpleLoop = task.spawn(function()
                while autoReceiverSlapAuraSimple do
                    pcall(function()
                        local char = game.Players.LocalPlayer.Character
                        if char then
                            local hrp = char:FindFirstChild("HumanoidRootPart")
                            if hrp then
                                -- Teleport to Safespot
                                local safespot = workspace:FindFirstChild("Safespot")
                                if safespot then
                                    local base = safespot:FindFirstChild("Base")
                                    if base then
                                        hrp.CFrame = base.CFrame + Vector3.new(0, 10, 0)
                                    end
                                end
                            end
                        end
                        
                        -- GeneralHit Slap Aura (every 0.5 seconds)
                        local generalHit = game:GetService("ReplicatedStorage"):FindFirstChild("GeneralHit")
                        if generalHit then
                            local localPlayer = game.Players.LocalPlayer
                            for _, player in pairs(game:GetService("Players"):GetPlayers()) do
                                if player ~= localPlayer and player.Character then
                                    local targetPart = player.Character:FindFirstChild("Head") or 
                                                      player.Character:FindFirstChild("Torso") or
                                                      player.Character:FindFirstChild("Right Arm") or
                                                      player.Character:FindFirstChild("Left Arm") or
                                                      player.Character:FindFirstChild("HumanoidRootPart")
                                    if targetPart then
                                        generalHit:FireServer(targetPart)
                                    end
                                end
                            end
                        end
                    end)
                    task.wait(0.5)
                end
            end)
            
            OrionLib:MakeNotification({
                Name = "Auto Receiver",
                Content = "Auto Teleport + Slap Aura Enabled!",
                Image = "rbxassetid://7743878857",
                Time = 3
            })
            
        else
            if receiverSlapAuraSimpleLoop then
                task.cancel(receiverSlapAuraSimpleLoop)
                receiverSlapAuraSimpleLoop = nil
            end
            
            OrionLib:MakeNotification({
                Name = "Auto Receiver",
                Content = "Disabled!",
                Image = "rbxassetid://7743878857",
                Time = 2
            })
        end
    end
})

Section = Tab:AddSection({
    Name = "Helper"
})

-- Auto Teleport To Receiver (Helper)
local autoHelperReceiverSimple = false
local helperReceiverSimpleLoop = nil

Tab:AddToggle({
    Name = "Auto Teleport To Receiver",
    Default = false,
    Callback = function(Value)
        autoHelperReceiverSimple = Value
        
        if Value then
            -- Check if Safespot exists
            local safespot = workspace:FindFirstChild("Safespot")
            if not safespot then
                OrionLib:MakeNotification({
                    Name = "Error",
                    Content = "Safespot not found! Create Safespot first.",
                    Image = "rbxassetid://4483345998",
                    Time = 3
                })
                autoHelperReceiverSimple = false
                return
            end
            
            if helperReceiverSimpleLoop then
                task.cancel(helperReceiverSimpleLoop)
                helperReceiverSimpleLoop = nil
            end
            
            helperReceiverSimpleLoop = task.spawn(function()
                while autoHelperReceiverSimple do
                    pcall(function()
                        local char = game.Players.LocalPlayer.Character
                        if char then
                            local hrp = char:FindFirstChild("HumanoidRootPart")
                            if hrp then
                                -- Teleport to Safespot position
                                local safespot = workspace:FindFirstChild("Safespot")
                                if safespot then
                                    local base = safespot:FindFirstChild("Base")
                                    if base then
                                        hrp.CFrame = base.CFrame + Vector3.new(0, 10, 0)
                                    end
                                else
                                    hrp.CFrame = CFrame.new(10000, -42, 9996)
                                end
                            end
                        end
                    end)
                    task.wait(0.5)
                end
            end)
            
            OrionLib:MakeNotification({
                Name = "Auto Helper",
                Content = "Auto teleport to Receiver position enabled!",
                Image = "rbxassetid://7743878857",
                Time = 2
            })
            
        else
            if helperReceiverSimpleLoop then
                task.cancel(helperReceiverSimpleLoop)
                helperReceiverSimpleLoop = nil
            end
            
            OrionLib:MakeNotification({
                Name = "Auto Helper",
                Content = "Auto teleport to Receiver position disabled!",
                Image = "rbxassetid://7743878857",
                Time = 2
            })
        end
    end
})

local Tab = Window:MakeTab({
    Name = "BONK",
    Icon = "rbxassetid://7734068321",
    PremiumOnly = false
})

Section = Tab:AddSection({
    Name = "1-2-3 Quest"
})

Section = Tab:AddSection({
    Name = "Receiver"
})

-- Auto Teleport To Safespot + Auto Use Ability + Slap Aura (Receiver)
local autoReceiverBonk = false
local receiverBonkLoop = nil

Tab:AddToggle({
    Name = "Auto Teleport To Safespot + Auto Use Ability + Slap Aura",
    Default = false,
    Callback = function(Value)
        autoReceiverBonk = Value
        
        if Value then
            -- Check if Safespot exists
            local safespot = workspace:FindFirstChild("Safespot")
            if not safespot then
                OrionLib:MakeNotification({
                    Name = "Error",
                    Content = "Safespot not found! Create Safespot first.",
                    Image = "rbxassetid://4483345998",
                    Time = 3
                })
                autoReceiverBonk = false
                return
            end
            
            if receiverBonkLoop then
                task.cancel(receiverBonkLoop)
                receiverBonkLoop = nil
            end
            
            receiverBonkLoop = task.spawn(function()
                while autoReceiverBonk do
                    pcall(function()
                        local char = game.Players.LocalPlayer.Character
                        if char then
                            local hrp = char:FindFirstChild("HumanoidRootPart")
                            if hrp then
                                -- Teleport to Safespot
                                local safespot = workspace:FindFirstChild("Safespot")
                                if safespot then
                                    local base = safespot:FindFirstChild("Base")
                                    if base then
                                        hrp.CFrame = base.CFrame + Vector3.new(0, 10, 0)
                                    end
                                end
                            end
                        end
                        
                        -- BONK ability (every 0.1 seconds)
                        local bonk = game:GetService("ReplicatedStorage"):FindFirstChild("BONK")
                        if bonk then
                            bonk:FireServer(-0.2956634000001941)
                        end
                        
                        task.wait(0.1)
                        
                        -- GeneralHit Slap Aura (every 0.5 seconds)
                        local generalHit = game:GetService("ReplicatedStorage"):FindFirstChild("GeneralHit")
                        if generalHit then
                            local localPlayer = game.Players.LocalPlayer
                            for _, player in pairs(game:GetService("Players"):GetPlayers()) do
                                if player ~= localPlayer and player.Character then
                                    local targetPart = player.Character:FindFirstChild("Left Arm") or 
                                                      player.Character:FindFirstChild("Right Arm") or
                                                      player.Character:FindFirstChild("Head") or 
                                                      player.Character:FindFirstChild("Torso") or 
                                                      player.Character:FindFirstChild("HumanoidRootPart")
                                    if targetPart then
                                        generalHit:FireServer(targetPart)
                                    end
                                end
                            end
                        end
                    end)
                    task.wait(0.5)
                end
            end)
            
            OrionLib:MakeNotification({
                Name = "Auto Receiver",
                Content = "Auto Teleport + BONK Ability + Slap Aura Enabled!",
                Image = "rbxassetid://7743878857",
                Time = 3
            })
            
        else
            if receiverBonkLoop then
                task.cancel(receiverBonkLoop)
                receiverBonkLoop = nil
            end
            
            OrionLib:MakeNotification({
                Name = "Auto Receiver",
                Content = "Disabled!",
                Image = "rbxassetid://7743878857",
                Time = 2
            })
        end
    end
})

Section = Tab:AddSection({
    Name = "Helper"
})

-- Auto Teleport To Receiver + Auto Die On Ragdoll (Helper)
local autoHelperBonk = false
local helperBonkLoop = nil

Tab:AddToggle({
    Name = "Auto Teleport To Receiver + Auto Die On Ragdoll",
    Default = false,
    Callback = function(Value)
        autoHelperBonk = Value
        
        if Value then
            -- Check if Safespot exists
            local safespot = workspace:FindFirstChild("Safespot")
            if not safespot then
                OrionLib:MakeNotification({
                    Name = "Error",
                    Content = "Safespot not found! Create Safespot first.",
                    Image = "rbxassetid://4483345998",
                    Time = 3
                })
                autoHelperBonk = false
                return
            end
            
            if helperBonkLoop then
                task.cancel(helperBonkLoop)
                helperBonkLoop = nil
            end
            
            helperBonkLoop = task.spawn(function()
                while autoHelperBonk do
                    pcall(function()
                        local player = game.Players.LocalPlayer
                        local char = player.Character
                        
                        -- Wait for character if dead
                        if not char or not char.Parent then
                            print("⏳ Waiting for respawn...")
                            char = player.CharacterAdded:Wait()
                            task.wait(1)
                        end
                        
                        if not char then
                            task.wait(0.5)
                            return
                        end
                        
                        local hrp = char:FindFirstChild("HumanoidRootPart")
                        local humanoid = char:FindFirstChildOfClass("Humanoid")
                        
                        if not hrp or not humanoid then
                            task.wait(0.5)
                            return
                        end
                        
                        -- Check if player has entered
                        local hasEntered = char:FindFirstChild("entered")
                        
                        -- Auto Enter Arena
                        if not hasEntered then
                            print("🚶 Entering arena...")
                            hrp.CFrame = CFrame.new(-1209, 328, 3)
                            task.wait(0.5)
                        else
                            print("✅ Already in arena")
                        end
                        
                        -- Teleport to Receiver position (Safespot)
                        local safespot = workspace:FindFirstChild("Safespot")
                        if safespot then
                            local base = safespot:FindFirstChild("Base")
                            if base then
                                hrp.CFrame = base.CFrame + Vector3.new(0, 10, 0)
                            end
                        else
                            hrp.CFrame = CFrame.new(10000, -42, 9996)
                        end
                        
                        task.wait(0.5)
                        
                        -- Check Ragdolled
                        local ragdolled = char:FindFirstChild("Ragdolled")
                        if ragdolled and ragdolled.Value == true then
                            humanoid.Health = 0
                            print("💀 Died due to ragdoll!")
                            task.wait(0.5)
                        end
                    end)
                    task.wait(0.5)
                end
            end)
            
            OrionLib:MakeNotification({
                Name = "Auto Helper",
                Content = "Auto Teleport + Auto Die On Ragdoll Enabled!",
                Image = "rbxassetid://7743878857",
                Time = 3
            })
            
        else
            if helperBonkLoop then
                task.cancel(helperBonkLoop)
                helperBonkLoop = nil
            end
            
            OrionLib:MakeNotification({
                Name = "Auto Helper",
                Content = "Disabled!",
                Image = "rbxassetid://7743878857",
                Time = 2
            })
        end
    end
})

local Tab = Window:MakeTab({
    Name = "Cloud",
    Icon = "rbxassetid://7734068321",
    PremiumOnly = false
})

Section = Tab:AddSection({
    Name = "1-4 Quest (CLOSED TEMPORARY/SOON)"
})

Section = Tab:AddSection({
    Name = "|||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||"
})

Section = Tab:AddSection({
    Name = "2 Quest"
})

Tab:AddButton({
    Name = "Teleport To Safespot",
    Callback = function()
        local player = game.Players.LocalPlayer
        local character = player.Character
        
        -- Check conditions
        local hasSpinGlove = player.leaderstats and player.leaderstats:FindFirstChild("Glove") and player.leaderstats.Glove.Value == "Cloud"
        local hasEntered = character and character:FindFirstChild("entered")
        local hasSafespot = workspace:FindFirstChild("Safespot")
        
        if not hasSpinGlove or not hasEntered or not hasSafespot then
            local errorMsg = ""
            if not hasSpinGlove then
                errorMsg = "Equip Spin Glove"
            elseif not hasEntered then
                errorMsg = "Enter Arena First"
            elseif not hasSafespot then
                errorMsg = "Create Safespot First"
            end
            
            OrionLib:MakeNotification({
                Name = "Error",
                Content = errorMsg,
                Image = "rbxassetid://4483345998",
                Time = 3
            })
            return
        end
        
        -- Teleport to Safespot
        local safespot = workspace:FindFirstChild("Safespot")
        if safespot then
            local base = safespot:FindFirstChild("Base")
            if base then
                local hrp = character:FindFirstChild("HumanoidRootPart")
                if hrp then
                    hrp.CFrame = base.CFrame + Vector3.new(0, 10, 0)
                    
                    OrionLib:MakeNotification({
                        Name = "Teleported",
                        Content = "Teleported to Safespot!",
                        Image = "rbxassetid://7743878857",
                        Time = 2
                    })
                end
            end
        end
    end
})

Section = Tab:AddSection({
    Name = "|||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||"
})

Section = Tab:AddSection({
    Name = "3 Quest"
})

Tab:AddSlider({
    Name = "Set Speed Cloud",
    Min = 0.1,
    Max = 1.2,
    Default = 0.5,
    Color = Color3.fromRGB(255, 255, 255),
    Increment = 0.1,
    ValueName = "Speed",
    Callback = function(Value)
        _G.SetSpeedCloud = Value
    end
})

CloudSpeed = Tab:AddToggle({
    Name = "Auto Set Cloud Speed",
    Default = false,
    Callback = function(Value)
        _G.CloudSpeed = Value

        if game.Players.LocalPlayer.leaderstats.Glove.Value == "Cloud" then
            while _G.CloudSpeed and game.Players.LocalPlayer.leaderstats.Glove.Value == "Cloud" do
                for i, v in pairs(game.Workspace:GetChildren()) do
                    if v.Name:match(game.Players.LocalPlayer.Name) and v:FindFirstChild("BodyVelocity") then
                        v.BodyVelocity.Velocity = v.BodyVelocity.Velocity * _G.SetSpeedCloud
                    end
                end
                task.wait(0.10)
            end
        elseif _G.CloudSpeed == true then
            OrionLib:MakeNotification({
                Name = "Error",
                Content = "Equip Cloud",
                Image = "rbxassetid://7743878857",
                Time = 5
            })
            wait(0.05)
            CloudSpeed:Set(false)
        end
    end
})

local Tab = Window:MakeTab({
    Name = "Soul",
    Icon = "rbxassetid://7734068321",
    PremiumOnly = false
})

Section = Tab:AddSection({
    Name = "1-2-3-4 Quest"
})

Section = Tab:AddSection({
    Name = "Receiver"
})

-- Auto Teleport To Safespot + Auto Use Ability + Slap Aura (Receiver)
local autoReceiverSoul = false
local receiverSoulLoop = nil

Tab:AddToggle({
    Name = "Auto Teleport To Safespot + Auto Use Ability + Slap Aura",
    Default = false,
    Callback = function(Value)
        autoReceiverSoul = Value
        
        if Value then
            -- Check if Safespot exists
            local safespot = workspace:FindFirstChild("Safespot")
            if not safespot then
                OrionLib:MakeNotification({
                    Name = "Error",
                    Content = "Safespot not found! Create Safespot first.",
                    Image = "rbxassetid://4483345998",
                    Time = 3
                })
                autoReceiverSoul = false
                return
            end
            
            if receiverSoulLoop then
                task.cancel(receiverSoulLoop)
                receiverSoulLoop = nil
            end
            
            receiverSoulLoop = task.spawn(function()
                while autoReceiverSoul do
                    pcall(function()
                        local char = game.Players.LocalPlayer.Character
                        if char then
                            local hrp = char:FindFirstChild("HumanoidRootPart")
                            if hrp then
                                -- Teleport to Safespot
                                local safespot = workspace:FindFirstChild("Safespot")
                                if safespot then
                                    local base = safespot:FindFirstChild("Base")
                                    if base then
                                        hrp.CFrame = base.CFrame + Vector3.new(0, 10, 0)
                                    end
                                end
                            end
                        end
                        
                        -- GeneralAbility (every 0.1 seconds)
                        local generalAbility = game:GetService("ReplicatedStorage"):FindFirstChild("GeneralAbility")
                        if generalAbility then
                            generalAbility:FireServer(false)
                        end
                        
                        task.wait(0.1)
                        
                        -- Soul charge remote (every 0.1 seconds)
                        local soulRemote = game:GetService("ReplicatedStorage"):FindFirstChild("Events")
                        if soulRemote then
                            local soul = soulRemote:FindFirstChild("soul")
                            if soul then
                                soul:FireServer("charge")
                            end
                        end
                        
                        task.wait(0.1)
                        
                        -- GeneralHit Slap Aura (every 0.5 seconds)
                        local generalHit = game:GetService("ReplicatedStorage"):FindFirstChild("GeneralHit")
                        if generalHit then
                            local localPlayer = game.Players.LocalPlayer
                            for _, player in pairs(game:GetService("Players"):GetPlayers()) do
                                if player ~= localPlayer and player.Character then
                                    local targetPart = player.Character:FindFirstChild("Left Arm") or 
                                                      player.Character:FindFirstChild("Right Arm") or
                                                      player.Character:FindFirstChild("Head") or 
                                                      player.Character:FindFirstChild("Torso") or 
                                                      player.Character:FindFirstChild("HumanoidRootPart")
                                    if targetPart then
                                        generalHit:FireServer(targetPart)
                                    end
                                end
                            end
                        end
                    end)
                    task.wait(0.5)
                end
            end)
            
            OrionLib:MakeNotification({
                Name = "Auto Receiver",
                Content = "Auto Teleport + Soul Ability + Slap Aura Enabled!",
                Image = "rbxassetid://7743878857",
                Time = 3
            })
            
        else
            if receiverSoulLoop then
                task.cancel(receiverSoulLoop)
                receiverSoulLoop = nil
            end
            
            OrionLib:MakeNotification({
                Name = "Auto Receiver",
                Content = "Disabled!",
                Image = "rbxassetid://7743878857",
                Time = 2
            })
        end
    end
})

Section = Tab:AddSection({
    Name = "Helper"
})

-- Auto Teleport To Receiver + Auto Die On Ragdoll (Helper)
local autoHelperSoul = false
local helperSoulLoop = nil

Tab:AddToggle({
    Name = "Auto Teleport To Receiver + Auto Die On Ragdoll",
    Default = false,
    Callback = function(Value)
        autoHelperSoul = Value
        
        if Value then
            -- Check if Safespot exists
            local safespot = workspace:FindFirstChild("Safespot")
            if not safespot then
                OrionLib:MakeNotification({
                    Name = "Error",
                    Content = "Safespot not found! Create Safespot first.",
                    Image = "rbxassetid://4483345998",
                    Time = 3
                })
                autoHelperSoul = false
                return
            end
            
            if helperSoulLoop then
                task.cancel(helperSoulLoop)
                helperSoulLoop = nil
            end
            
            helperSoulLoop = task.spawn(function()
                while autoHelperSoul do
                    pcall(function()
                        local player = game.Players.LocalPlayer
                        local char = player.Character
                        
                        -- Wait for character if dead
                        if not char or not char.Parent then
                            print("⏳ Waiting for respawn...")
                            char = player.CharacterAdded:Wait()
                            task.wait(1)
                        end
                        
                        if not char then
                            task.wait(0.5)
                            return
                        end
                        
                        local hrp = char:FindFirstChild("HumanoidRootPart")
                        local humanoid = char:FindFirstChildOfClass("Humanoid")
                        
                        if not hrp or not humanoid then
                            task.wait(0.5)
                            return
                        end
                        
                        -- Check if player has entered
                        local hasEntered = char:FindFirstChild("entered")
                        
                        -- Auto Enter Arena
                        if not hasEntered then
                            print("🚶 Entering arena...")
                            hrp.CFrame = CFrame.new(-1209, 328, 3)
                            task.wait(0.5)
                        else
                            print("✅ Already in arena")
                        end
                        
                        -- Teleport to Receiver position (Safespot)
                        local safespot = workspace:FindFirstChild("Safespot")
                        if safespot then
                            local base = safespot:FindFirstChild("Base")
                            if base then
                                hrp.CFrame = base.CFrame + Vector3.new(0, 10, 0)
                            end
                        else
                            hrp.CFrame = CFrame.new(10000, -42, 9996)
                        end
                        
                        task.wait(0.5)
                        
                        -- Check Ragdolled
                        local ragdolled = char:FindFirstChild("Ragdolled")
                        if ragdolled and ragdolled.Value == true then
                            humanoid.Health = 0
                            print("💀 Died due to ragdoll!")
                            task.wait(0.5)
                        end
                    end)
                    task.wait(0.5)
                end
            end)
            
            OrionLib:MakeNotification({
                Name = "Auto Helper",
                Content = "Auto Teleport + Auto Die On Ragdoll Enabled!",
                Image = "rbxassetid://7743878857",
                Time = 3
            })
            
        else
            if helperSoulLoop then
                task.cancel(helperSoulLoop)
                helperSoulLoop = nil
            end
            
            OrionLib:MakeNotification({
                Name = "Auto Helper",
                Content = "Disabled!",
                Image = "rbxassetid://7743878857",
                Time = 2
            })
        end
    end
})

local Tab = Window:MakeTab({
    Name = "[REDACTED]",
    Icon = "rbxassetid://7734068321",
    PremiumOnly = false
})

Section = Tab:AddSection({
    Name = "1-2-3 Quest"
})

Tab:AddButton({
    Name = "Spawn Portal",
    Callback = function()
        if game.Players.LocalPlayer.leaderstats.Glove.Value == "[REDACTED]" then
            game:GetService("ReplicatedStorage").Well:FireServer()
        else
            OrionLib:MakeNotification({
                Name = "Error",
                Content = "Equip Redacted",
                Image = "rbxassetid://7743878857",
                Time = 3
            })
        end
    end
})

Tab:AddToggle({
    Name = "Auto Secure Portal",
    Default = false,
    Callback = function(Value)
        if Value then
            -- START
            if game.workspace:FindFirstChild("WellModel") and workspace.WellModel:FindFirstChild("Portal") then
                
                getgenv().SecurePortalLoop = true
                task.spawn(function()
                    while getgenv().SecurePortalLoop do
                        if game.Players.LocalPlayer.Character and game.Players.LocalPlayer.Character:FindFirstChild("HumanoidRootPart") then
                            game.Players.LocalPlayer.Character.HumanoidRootPart.CFrame = workspace.WellModel.Portal.CFrame
                        end
                        task.wait()
                    end
                end)
            else
                OrionLib:MakeNotification({
                    Name = "Error",
                    Content = "Portal Not Spawned/Founded",
                    Image = "rbxassetid://7743878857",
                    Time = 3
                })
                return false -- Turn toggle off
            end
        else
            -- STOP
            getgenv().SecurePortalLoop = false
            
        end
    end
})

local Tab = Window:MakeTab({
    Name = "bus",
    Icon = "rbxassetid://7734068321",
    PremiumOnly = false
})

Section = Tab:AddSection({
    Name = "1 Quest"
})

Tab:AddButton({
    Name = "bus Slap Aura",
    Callback = function()
if game.Players.LocalPlayer.leaderstats.Glove.Value == "bus" then
-- Get the ReplicatedStorage and Players service
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local Players = game:GetService("Players")

-- Start the infinite loop to fire for all players
while true do
    -- Loop through each player in the game
    for _, player in pairs(Players:GetPlayers()) do
        -- Ensure the player has a character and it contains the "Right Arm"
        if player.Character and player.Character:FindFirstChild("Right Arm") then
            -- Create the arguments table for the FireServer function
            local args = {
                [1] = player.Character["Right Arm"]
            }

            -- Fire the server with the "Right Arm" part as an argument
            ReplicatedStorage.hitbus:FireServer(unpack(args))
        end
    end

    -- Wait 1 second before running the loop again (adjust the wait time as needed)
    task.wait(0.5)
end
else
OrionLib:MakeNotification({Name = "Error",Content = "Equip bus",Image = "rbxassetid://7743878857",Time = 3})
end
end
})

Section = Tab:AddSection({
    Name = "2-3 Quest"
})

Tab:AddButton({
    Name = "Spawn Bus (Normal)",
    Callback = function()
        if game.Players.LocalPlayer.leaderstats.Glove.Value == "Bus" then
            game:GetService("ReplicatedStorage").busmoment:FireServer()
        else
            OrionLib:MakeNotification({
                Name = "Error",
                Content = "Equip Bus",
                Image = "rbxassetid://7743878857",
                Time = 3
            })
        end
    end
})

Tab:AddButton({
    Name = "Spawn Bus (Fast + Kicks)",
    Callback = function()
        while true do
            game:GetService("ReplicatedStorage").busmoment:FireServer()
            wait()
        end
    end
})

local Tab = Window:MakeTab({
    Name = "Phase",
    Icon = "rbxassetid://7734068321",
    PremiumOnly = false
})

Section = Tab:AddSection({
    Name = "1-3 Quest"
})

local AutoAbiltySlapPhaseRunning = false

Tab:AddToggle({
    Name = "Auto Ability + Slap People",
    Default = false,
    Callback = function(Value)
        AutoAbiltySlapPhaseRunning = Value
        local player = game.Players.LocalPlayer

        if AutoAbiltySlapPhaseRunning then
            if player.leaderstats.Glove.Value == "Phase" then
                task.spawn(function()
                    while AutoAbiltySlapPhaseRunning do
                        local players = game:GetService("Players"):GetPlayers()
                        local targetPlayer

                        repeat
                            targetPlayer = players[math.random(1, #players)]
                        until targetPlayer ~= player and targetPlayer.Character and targetPlayer.Character:FindFirstChild("HumanoidRootPart")

                        if targetPlayer and targetPlayer.Character then
                            player.Character.HumanoidRootPart.CFrame = CFrame.new(targetPlayer.Character.HumanoidRootPart.Position)
                        end

                        task.wait(1)
                    end
                end)

                task.spawn(function()
                    while true do
                        game:GetService("ReplicatedStorage").PhaseA:FireServer()
                        task.wait(14)
                    end
                end)
            else
                AutoAbiltySlapPhaseRunning = false
                OrionLib:MakeNotification({
                    Name = "Error",
                    Content = "Equip Phase Glove",
                    Image = "rbxassetid://7743878857",
                    Time = 3
                })
            end
        end
    end
})

Section = Tab:AddSection({
    Name = "2 Quest"
})

Tab:AddButton({
    Name = "Auto Ability + Teleport To All Islands (Takes 112 Seconds)",
    Callback = function()
        local player = game.Players.LocalPlayer

        if player.leaderstats.Glove.Value == "Phase" and player.Character:FindFirstChild("entered") then
            local function phaseTo(position)
                player.Character.HumanoidRootPart.CFrame = CFrame.new(122, 255, 3)
                task.wait(0.1)
                game:GetService("ReplicatedStorage").PhaseA:FireServer()
                task.wait(0.1)
                player.Character.HumanoidRootPart.CFrame = CFrame.new(position)
                task.wait(14)
            end

            phaseTo(Vector3.new(236, -16, -6))
            phaseTo(Vector3.new()) -- likely needs correction
            phaseTo(Vector3.new(4, -5, -223))
            phaseTo(Vector3.new(-216, -5, -6))
            phaseTo(Vector3.new(-401, 51, -16))
            phaseTo(Vector3.new(256, 34, 194))
            phaseTo(Vector3.new(4, -5, 215))
            phaseTo(Vector3.new(-125, -5, 122))
        else
            OrionLib:MakeNotification({
                Name = "Error",
                Content = "Equip Phase Glove/Use In Arena",
                Image = "rbxassetid://7743878857",
                Time = 3
            })
        end
    end
})

local Tab = Window:MakeTab({
    Name = "Bomb",
    Icon = "rbxassetid://7734068321",
    PremiumOnly = false
})

Section = Tab:AddSection({
    Name = "1-2-3 Quest"
})

Tab:AddButton({
    Name = "Throw Bomb/Explode Bomb",
    Callback = function()
        if game.Players.LocalPlayer.leaderstats.Glove.Value == "Bomb" then
            game:GetService("ReplicatedStorage").BombThrow:FireServer("Ebutton")
        else
            OrionLib:MakeNotification({
                Name = "Error",
                Content = "Equip Bomb",
                Image = "rbxassetid://7743878857",
                Time = 3
            })
        end
    end
})

Tab:AddToggle({
    Name = "Auto Do 1-2-3 Tasks",
    Default = false,
    Callback = function(Value)
        if Value then
            -- START
            if game.Players.LocalPlayer.leaderstats.Glove.Value == "Bomb" then
                
                getgenv().AutoBombTasksLoop = true
                task.spawn(function()
                    while getgenv().AutoBombTasksLoop do
                        local player = game.Players.LocalPlayer

                        -- Check if character exists
                        if not player.Character or not player.Character:FindFirstChild("HumanoidRootPart") then
                            task.wait(1)
                            continue
                        end

                        -- Move to safe area
                        player.Character.HumanoidRootPart.CFrame = CFrame.new(-908, 328, 5)
                        task.wait(1)

                        -- Move to platform
                        player.Character.HumanoidRootPart.CFrame = CFrame.new(10019, -42, 9960)
                        task.wait(1)

                        -- Throw Bomb
                        game:GetService("ReplicatedStorage").BombThrow:FireServer("Ebutton")
                        task.wait(1)

                        -- Wait to teleport to bomb
                        local bombFound = false
                        for i = 1, 30 do -- Timeout after 30 seconds
                            if not getgenv().AutoBombTasksLoop then break end
                            
                            for _, v in pairs(game.Workspace:GetChildren()) do
                                if string.find(v.Name, "_bømb") then
                                    player.Character.HumanoidRootPart.CFrame = v.CFrame
                                    bombFound = true
                                    break
                                end
                            end
                            
                            if bombFound then
                                break
                            else
                                task.wait(1)
                            end
                        end

                        task.wait(9)
                        
                        -- Kill player
                        local humanoid = player.Character:FindFirstChild("Humanoid")
                        if humanoid then
                            humanoid.Health = 0
                        end
                        
                        task.wait(23)
                    end
                end)
            else
                OrionLib:MakeNotification({
                    Name = "Error",
                    Content = "Equip Bomb Glove",
                    Image = "rbxassetid://7743878857",
                    Time = 3
                })
                return false -- Turn toggle off
            end
        else
            -- STOP
            getgenv().AutoBombTasksLoop = false
            
        end
    end
})

local Tab = Window:MakeTab({
    Name = "Bubble",
    Icon = "rbxassetid://7734068321",
    PremiumOnly = false
})

Section = Tab:AddSection({
    Name = "1-2-3-4 Quest"
})

Tab:AddButton({
	Name = "Use Bubble's Ability",
	Callback = function()  
game:GetService("ReplicatedStorage").BubbleThrow:FireServer()
end
})

Tab:AddToggle({
    Name = "Auto Use Bubble's Ability",
    Default = false,
    Callback = function(Value)
        if Value then
            -- START
            
            getgenv().AutoBubbleLoop = true
            task.spawn(function()
                while getgenv().AutoBubbleLoop do
                    game:GetService("ReplicatedStorage").BubbleThrow:FireServer()
                    task.wait(10)
                end
            end)
        else
            -- STOP
            getgenv().AutoBubbleLoop = false
            
        end
    end
})

Tab:AddButton({
    Name = "Bubble Slap Aura",
    Callback = function()
if game.Players.LocalPlayer.leaderstats.Glove.Value == "Bubble" then
-- Get the ReplicatedStorage and Players service
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local Players = game:GetService("Players")

-- Start the infinite loop to fire for all players
while true do
    -- Loop through each player in the game
    for _, player in pairs(Players:GetPlayers()) do
        -- Ensure the player has a character and it contains the "Right Arm"
        if player.Character and player.Character:FindFirstChild("Right Arm") then
            -- Create the arguments table for the FireServer function
            local args = {
                [1] = player.Character["Right Arm"]
            }

            -- Fire the server with the "Right Arm" part as an argument
            ReplicatedStorage.BubbleHit:FireServer(unpack(args))
        end
    end

    -- Wait 1 second before running the loop again (adjust the wait time as needed)
    task.wait(0.5)
end
else
OrionLib:MakeNotification({Name = "Error",Content = "Equip Bubble",Image = "rbxassetid://7743878857",Time = 3})
end
end
})

local Tab = Window:MakeTab({
    Name = "Shard",
    Icon = "rbxassetid://7734068321",
    PremiumOnly = false
})

Section = Tab:AddSection({
    Name = "1-2-3-4-5 Quest"
})

local running = false -- declare global toggle state

Tab:AddToggle({
    Name = "Auto Use Shard's Glove Ability",
    Default = false,
    Callback = function(Value)
        if Value then
            -- Start the loop with exact 0.1 second interval
            shardCloneLoop = true
            
            spawn(function()
                while cherryCloneLoop do
                    local player = game.Players.LocalPlayer
                    
                    -- Check if player has Cherry glove
                    if player.leaderstats and player.leaderstats.Glove and player.leaderstats.Glove.Value == "Shard" then
                        -- Check if player has entered
                        if player.Character and player.Character:FindFirstChild("entered") then
                            -- Fire the Cherry remote
                            game:GetService("ReplicatedStorage").Events.Shards:FireServer()
                        else
                            OrionLib:MakeNotification({
                                Name = "Not Entered",
                                Content = "You need to enter the arena first!",
                                Image = "rbxassetid://4483345998",
                                Time = 3
                            })
                            wait(1) -- Wait longer when not entered
                        end
                    else
                        OrionLib:MakeNotification({
                            Name = "Wrong Glove",
                            Content = "You need Shard glove equipped!",
                            Image = "rbxassetid://4483345998",
                            Time = 3
                        })
                        wait(1) -- Wait longer when wrong glove
                    end
                    
                    wait(0.1) -- Normal 0.1 second delay
                end
            end)
        else
            -- Stop the loop
            shardCloneLoop = false
        end
    end
})

Tab:AddButton({
    Name = "Shard Slap Aura",
    Callback = function()
        while true do
            local players = game:GetService("Players")
            local replicatedStorage = game:GetService("ReplicatedStorage")

            for _, player in ipairs(players:GetPlayers()) do
                local character = player.Character
                if character then
                    local leftArm = character:FindFirstChild("Left Arm")
                    if leftArm then
                        replicatedStorage.ShardHIT:FireServer(leftArm)
                    end
                end
            end

            wait(0.5)
        end
    end
})

local Tab = Window:MakeTab({
    Name = "Moon",
    Icon = "rbxassetid://7734068321",
    PremiumOnly = false
})

Section = Tab:AddSection({
    Name = "1-2 Quest"
})

Tab:AddToggle({
    Name = "Auto Ragdoll + Touch Ground",
    Default = false,
    Callback = function(Value)
        if Value then
            -- Start the loop
            autoRagdollLoop = game:GetService("RunService").Heartbeat:Connect(function()
                -- Check if player has entered
                if game.Players.LocalPlayer.Character and game.Players.LocalPlayer.Character:FindFirstChild("entered") then
                    -- Fire knockback remote
                    game.ReplicatedStorage.SelfKnockback:FireServer({
                        ["Force"] = 0,
                        ["Direction"] = Vector3.new(0, 0.01, 0)
                    })
                    
                    -- Teleport to position
                    if game.Players.LocalPlayer.Character:FindFirstChild("HumanoidRootPart") then
                        game.Players.LocalPlayer.Character.HumanoidRootPart.CFrame = CFrame.new(1, -5, 8)
                    end
                end
            end)
        else
            -- Stop the loop
            if autoRagdollLoop then
                autoRagdollLoop:Disconnect()
                autoRagdollLoop = nil
            end
        end
    end
})

Tab:AddToggle({
    Name = "Auto Spam Equip/Unequip Moon Glove",
    Default = false,
    Callback = function(Value)
        if Value then
            -- Start the loop
            moonGloveLoop = game:GetService("RunService").Heartbeat:Connect(function()
                local player = game.Players.LocalPlayer
                local backpack = player:FindFirstChild("Backpack")
                
                if backpack then
                    local moonTool = backpack:FindFirstChild("Moon")
                    
                    if moonTool then
                        -- Check if tool is currently equipped
                        local character = player.Character
                        local currentlyEquipped = character and character:FindFirstChild("Moon")
                        
                        if currentlyEquipped then
                            -- Unequip: move tool back to backpack
                            moonTool.Parent = backpack
                        else
                            -- Equip: move tool to character
                            moonTool.Parent = character
                        end
                    end
                end
            end)
        else
            -- Stop the loop
            if moonGloveLoop then
                moonGloveLoop:Disconnect()
                moonGloveLoop = nil
            end
        end
    end
})

local Tab = Window:MakeTab({
    Name = "Rage",
    Icon = "rbxassetid://7734068321",
    PremiumOnly = false
})

Section = Tab:AddSection({
    Name = "1-4 Quest"
})

Tab:AddButton({
    Name = "Rage Slap Aura",
    Callback = function()
        local ReplicatedStorage = game:GetService("ReplicatedStorage")
        local Players = game:GetService("Players")

        local function fireForAllPlayers()
            for _, player in pairs(Players:GetPlayers()) do
                if player.Character and player.Character:FindFirstChild("Torso") then
                    ReplicatedStorage.GRRRR:FireServer(player.Character.Torso)
                end
            end
        end

        while true do
            fireForAllPlayers()
            wait(0.5)
        end
    end
})

Tab:AddButton({
    Name = "Use Rage Ability",
    Callback = function()
game:GetService("ReplicatedStorage").GRRRRR:FireServer()
    end
})

local Tab = Window:MakeTab({
    Name = "Chain",
    Icon = "rbxassetid://7734068321",
    PremiumOnly = false
})

Section = Tab:AddSection({
    Name = "1-2-3 Quest"
})

Tab:AddToggle({
    Name = "Auto Slap Players",
    Default = false,
    Callback = function(AutoSlapChain)
        running = AutoSlapChain
        if not running then return end

        task.spawn(function()
            local Players = game:GetService("Players")
            local ReplicatedStorage = game:GetService("ReplicatedStorage")
            local LocalPlayer = Players.LocalPlayer

            local function teleportToRandomPlayer()
                local playerList = Players:GetPlayers()
                if #playerList > 1 then
                    local randomPlayer
                    repeat
                        randomPlayer = playerList[math.random(1, #playerList)]
                    until randomPlayer ~= LocalPlayer

                    local targetChar = randomPlayer.Character
                    if targetChar and targetChar:FindFirstChild("HumanoidRootPart") then
                        LocalPlayer.Character.HumanoidRootPart.CFrame = CFrame.new(
                            targetChar.HumanoidRootPart.Position + Vector3.new(0, 3, 0)
                        )
                    end
                end
            end

            local function fireGeneralHitForAllPlayers()
                for _, player in pairs(Players:GetPlayers()) do
                    if player ~= LocalPlayer and player.Character and player.Character:FindFirstChild("HumanoidRootPart") then
                        ReplicatedStorage:WaitForChild("GeneralHit"):FireServer(player.Character.HumanoidRootPart)
                    end
                end
            end

            local count = 0
            while running and count < 750 do
                if LocalPlayer.leaderstats.Glove.Value == "Chain" then
                    teleportToRandomPlayer()
                    fireGeneralHitForAllPlayers()
                    count += 1
                    task.wait(2.1)
                else
                    OrionLib:MakeNotification({
                        Name = "Error",
                        Content = "Equip Chain Glove",
                        Image = "rbxassetid://7743878857",
                        Time = 3
                    })
                    break
                end
            end
        end)
    end
})

local Tab = Window:MakeTab({
    Name = "Tycoon",
    Icon = "rbxassetid://7734068321",
    PremiumOnly = false
})

Section = Tab:AddSection({
    Name = "1-3 Quest"
})

Tab:AddButton({
    Name = "Place Tycoon To Safe Spot",
    Callback = function()
        if game.Players.LocalPlayer.leaderstats.Glove.Value == "Tycoon" then
            local player = game.Players.LocalPlayer

            -- Step 1: Move to spawn
            player.Character.HumanoidRootPart.CFrame = CFrame.new(0, 0, 0)
            task.wait(1)

            -- Step 2: Move to safe tycoon location
            player.Character.HumanoidRootPart.CFrame = CFrame.new(17884, -24, -3539)
            task.wait(1)

            -- Step 3: Activate ability
            local args = {
                [1] = CFrame.new(17893.552734375, -26.257728576660156, -3535.35888671875, 
                0.00862560048699379, 3.3499091500743816e-08, -0.9999628067016602, 
                8.43312697451637e-10, 1, 3.350761090814558e-08, 
                0.9999628067016602, -1.1323045834288337e-09, 0.00862560048699379)
            }

            game:GetService("ReplicatedStorage"):WaitForChild("GeneralAbility"):FireServer(unpack(args))
            task.wait(1)

            -- Step 4: Teleport back to spawn
            player.Character.HumanoidRootPart.CFrame = CFrame.new(0, 0, 0)
        else
            OrionLib:MakeNotification({
                Name = "Error",
                Content = "Equip Tycoon Glove/Get Safespot",
                Image = "rbxassetid://7743878857",
                Time = 3
            })
        end
    end
})

local running = false

Tab:AddToggle({
    Name = "Auto Slap Players",
    Default = false,
    Callback = function(AutoSlapTycoon)
        running = AutoSlapTycoon
        if not running then return end

        task.spawn(function()
            local Players = game:GetService("Players")
            local ReplicatedStorage = game:GetService("ReplicatedStorage")
            local LocalPlayer = Players.LocalPlayer

            local function teleportToRandomPlayer()
                local playerList = Players:GetPlayers()
                if #playerList > 1 then
                    local randomPlayer
                    repeat
                        randomPlayer = playerList[math.random(1, #playerList)]
                    until randomPlayer ~= LocalPlayer

                    local targetChar = randomPlayer.Character
                    if targetChar and targetChar:FindFirstChild("HumanoidRootPart") then
                        LocalPlayer.Character.HumanoidRootPart.CFrame = CFrame.new(
                            targetChar.HumanoidRootPart.Position + Vector3.new(0, 3, 0)
                        )
                    end
                end
            end

            local function fireGeneralHitForAllPlayers()
                for _, player in pairs(Players:GetPlayers()) do
                    if player ~= LocalPlayer and player.Character and player.Character:FindFirstChild("HumanoidRootPart") then
                        ReplicatedStorage:WaitForChild("GeneralHit"):FireServer(player.Character.HumanoidRootPart)
                    end
                end
            end

            local count = 0
            while running and count < 750 do
                if LocalPlayer.leaderstats.Glove.Value == "Tycoon" then
                    teleportToRandomPlayer()
                    fireGeneralHitForAllPlayers()
                    count += 1
                    task.wait(2.1)
                else
                    OrionLib:MakeNotification({
                        Name = "Error",
                        Content = "Equip Tycoon Glove",
                        Image = "rbxassetid://7743878857",
                        Time = 3
                    })
                    break
                end
            end
        end)
    end
})

local Tab = Window:MakeTab({
    Name = "🗿",
    Icon = "rbxassetid://7734068321",
    PremiumOnly = false
})

Section = Tab:AddSection({
    Name = "1 Quest"
})

Tab:AddButton({
    Name = "Spawn 🗿",
    Callback = function()
        if game.Players.LocalPlayer.leaderstats.Glove.Value == "🗿" then
            local args = {
                [1] = CFrame.new(0, 0, 0) * CFrame.Angles(0, 0, 0)
            }
            game:GetService("ReplicatedStorage").GeneralAbility:FireServer(unpack(args))
        else
            OrionLib:MakeNotification({
                Name = "Error",
                Content = "Equip 🗿",
                Image = "rbxassetid://7743878857",
                Time = 3
            })
        end
    end
})

Section = Tab:AddSection({
    Name = "2-3 Quest"
})

Tab:AddButton({
    Name = "🗿 Slap Aura",
    Callback = function()
        local ReplicatedStorage = game:GetService("ReplicatedStorage")
        local Players = game:GetService("Players")

        local function fireForAllPlayers()
            for _, player in pairs(Players:GetPlayers()) do
                if player.Character and player.Character:FindFirstChild("Torso") then
                    ReplicatedStorage.GeneralHit:FireServer(player.Character.Torso)
                end
            end
        end

        while true do
            fireForAllPlayers()
            wait(1)
        end
    end
})

local Tab = Window:MakeTab({
    Name = "Obby",
    Icon = "rbxassetid://7734068321",
    PremiumOnly = false
})

Section = Tab:AddSection({
    Name = "1 Quest"
})

Tab:AddButton({
    Name = "Obby Slap Aura",
    Callback = function()
        while true do
            local players = game:GetService("Players")
            local replicatedStorage = game:GetService("ReplicatedStorage")

            for _, player in ipairs(players:GetPlayers()) do
                local character = player.Character
                if character then
                    local leftArm = character:FindFirstChild("Left Arm")
                    if leftArm then
                        replicatedStorage.GeneralHit:FireServer(leftArm)
                    end
                end
            end

            wait(1)
        end
    end
})

local Tab = Window:MakeTab({
    Name = "Voodoo",
    Icon = "rbxassetid://7734068321",
    PremiumOnly = false
})

Section = Tab:AddSection({
    Name = "1-2 Quest"
})

local running = false -- declare global toggle state

Tab:AddToggle({
    Name = "Auto Slap Players",
    Default = false,
    Callback = function(AutoSlapVoodoo)
        running = AutoSlapVoodoo
        if not running then return end

        task.spawn(function()
            local Players = game:GetService("Players")
            local ReplicatedStorage = game:GetService("ReplicatedStorage")
            local LocalPlayer = Players.LocalPlayer

            local function teleportToRandomPlayer()
                local playerList = Players:GetPlayers()
                if #playerList > 1 then
                    local randomPlayer = nil
                    repeat
                        randomPlayer = playerList[math.random(1, #playerList)]
                    until randomPlayer ~= LocalPlayer

                    local targetChar = randomPlayer.Character
                    if targetChar and targetChar:FindFirstChild("HumanoidRootPart") then
                        LocalPlayer.Character.HumanoidRootPart.CFrame = CFrame.new(targetChar.HumanoidRootPart.Position + Vector3.new(0, 3, 0))
                    end
                end
            end

            local function fireGeneralHitForAllPlayers()
                for _, player in pairs(Players:GetPlayers()) do
                    if player ~= LocalPlayer and player.Character and player.Character:FindFirstChild("HumanoidRootPart") then
                        ReplicatedStorage:WaitForChild("GeneralHit"):FireServer(player.Character.HumanoidRootPart)
                    end
                end
            end

            local count = 0
            while running and count < 750 do
                if LocalPlayer.leaderstats.Glove.Value == "Voodoo" then
                    teleportToRandomPlayer()
                    fireGeneralHitForAllPlayers()
                    count += 1
                    task.wait(1)
                else
                    OrionLib:MakeNotification({
                        Name = "Error",
                        Content = "Equip Voodoo Glove",
                        Image = "rbxassetid://7743878857",
                        Time = 3
                    })
                    break
                end
            end
        end)
    end
})

Section = Tab:AddSection({
    Name = "3 Quest"
})

local voodooToggle = false
local loopThread = nil

Tab:AddToggle({
    Name = "Voodoo Spam Ability Without CD",
    Default = false,
    Callback = function(state)
        voodooToggle = state
        local player = game.Players.LocalPlayer

        if voodooToggle then
            if player.leaderstats and player.leaderstats.Glove.Value == "Voodoo" then
                loopThread = task.spawn(function()
                    while voodooToggle and player.leaderstats.Glove.Value == "Voodoo" do
                        game:GetService("ReplicatedStorage").GeneralAbility:FireServer()
                        task.wait(3.5)
                    end
                end)
            else
                voodooToggle = false
                OrionLib:MakeNotification({
                    Name = "Error",
                    Content = "Equip Voodoo Glove",
                    Image = "rbxassetid://7743878857",
                    Time = 3
                })
            end
        else
            if loopThread then
                print("Voodoo spam stopped")
                loopThread = nil
            end
        end
    end
})

local Tab = Window:MakeTab({
    Name = "rob",
    Icon = "rbxassetid://7734068321",
    PremiumOnly = false
})

Section = Tab:AddSection({
    Name = "1 Quest"
})

Tab:AddButton({
    Name = "Create Null Portal (Need Bob And Rob Gloves)",
    Callback = function()
        game.Players.LocalPlayer.Character.HumanoidRootPart.CFrame = CFrame.new()
        wait(0.5)

        fireclickdetector(game:GetService("Workspace").Lobby.rob.ClickDetector)
        wait(0.5)
        game:GetService("ReplicatedStorage").rob:FireServer()
        wait(0.5)

        fireclickdetector(game:GetService("Workspace").Lobby.bob.ClickDetector)
        wait(0.5)
        game:GetService("ReplicatedStorage").bob:FireServer()

        OrionLib:MakeNotification({
            Name = "Error",
            Content = "Need Bob/Rob Badges To Own",
            Image = "rbxassetid://7743878857",
            Time = 3
        })
    end
})

Section = Tab:AddSection({
    Name = "2 Quest"
})

Tab:AddSlider({
    Name = "WalkSpeed",
    Min = 20,
    Max = 1000,
    Default = 20,
    Color = Color3.fromRGB(255, 255, 255),
    Increment = 1,
    ValueName = "WalkSpeed",
    Callback = function(Value)
        game.Players.LocalPlayer.Character.Humanoid.WalkSpeed = Value
        Walkspeed = Value
    end
})

Tab:AddTextbox({
    Name = "WalkSpeed",
    Default = "UserSpeed",
    TextDisappear = false,
    Callback = function(Value)
        game.Players.LocalPlayer.Character.Humanoid.WalkSpeed = Value
        Walkspeed = Value
    end
})

Tab:AddToggle({
    Name = "Walkspeed Set Auto",
    Default = false,
    Callback = function(Value)
        KeepWalkspeed = Value

        while KeepWalkspeed do
            if game.Players.LocalPlayer.Character:FindFirstChild("Humanoid") and
               game.Players.LocalPlayer.Character.Humanoid.WalkSpeed ~= Walkspeed then
                game.Players.LocalPlayer.Character.Humanoid.WalkSpeed = Walkspeed
            end
            task.wait()
        end
    end
})

Section = Tab:AddSection({
    Name = "3 Quest"
})

local AutoAbsorb = false

Tab:AddToggle({
    Name = "Auto Absorb People",
    Default = false,
    Callback = function(Value)
        AutoAbsorb = Value
        local player = game.Players.LocalPlayer

        if AutoAbsorb then
            if player.leaderstats.Glove.Value:lower() == "rob" then
                task.spawn(function()
                    while AutoAbsorb do
                        local players = game:GetService("Players"):GetPlayers()
                        local target

                        repeat
                            target = players[math.random(1, #players)]
                        until target ~= player and target.Character and target.Character:FindFirstChild("HumanoidRootPart")

                        local targetHRP = target.Character:FindFirstChild("HumanoidRootPart")
                        if targetHRP and player.Character:FindFirstChild("HumanoidRootPart") then
                            player.Character.HumanoidRootPart.CFrame = CFrame.new(targetHRP.Position + Vector3.new(0, 0, 2))
                        end

                        game:GetService("ReplicatedStorage"):FindFirstChild("Ability"):FireServer()
                        task.wait(1)
                    end
                end)
            else
                AutoAbsorb = false
                OrionLib:MakeNotification({
                    Name = "Error",
                    Content = "Equip Rob Glove",
                    Image = "rbxassetid://7743878857",
                    Time = 3
                })
            end
        end
    end
})

Tab:AddToggle({
    Name = "Auto Rob Transformation",
    Default = false,
    Callback = function(Value)
        if Value then
            -- START
            
            getgenv().AutoRobLoop = true
            task.spawn(function()
                while getgenv().AutoRobLoop do
                    -- Check if the ClickDetector exists
                    local robObject = game:GetService("Workspace"):FindFirstChild("Lobby")
                    if robObject then
                        robObject = robObject:FindFirstChild("rob")
                        if robObject and robObject:FindFirstChild("ClickDetector") then
                            fireclickdetector(robObject.ClickDetector)
                        end
                    end
                    
                    task.wait(0.5)
                    
                    -- Fire the remote
                    game:GetService("ReplicatedStorage").rob:FireServer()
                    
                    task.wait(17)
                end
            end)
        else
            -- STOP
            getgenv().AutoRobLoop = false
            
        end
    end
})

local Tab = Window:MakeTab({
    Name = "Tinkerer",
    Icon = "rbxassetid://7734068321",
    PremiumOnly = false
})

Section = Tab:AddSection({
    Name = "1-3 Quest"
})

Tab:AddToggle({
    Name = "Auto Spawn Frenzy Bots",
    Default = false,
    Callback = function(Value)
        if Value then
            -- START
            
            getgenv().AutoFrenzyLoop = true
            task.spawn(function()
                while getgenv().AutoFrenzyLoop do
                    game:GetService("ReplicatedStorage").GeneralAbility:FireServer()
                    task.wait(7)
                end
            end)
        else
            -- STOP
            getgenv().AutoFrenzyLoop = false
            
        end
    end
})

Tab:AddButton({
	Name = "Tinkerer Slap Aura",
	Callback = function()   
if game.Players.LocalPlayer.leaderstats.Glove.Value == "Tinkerer" then
-- Get the ReplicatedStorage and Players service
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local Players = game:GetService("Players")

-- Start the infinite loop to fire for all players
while true do
    -- Loop through each player in the game
    for _, player in pairs(Players:GetPlayers()) do
        -- Ensure the player has a character and it contains the "Left Leg"
        if player.Character and player.Character:FindFirstChild("Left Leg") then
            -- Create the arguments table for the FireServer function
            local args = {
                [1] = player.Character["Left Leg"],
                [2] = false  -- The second argument, set to false as in your original script
            }

            -- Fire the server with the "Left Leg" part as an argument
            ReplicatedStorage.TinkererHit:FireServer(unpack(args))
        end
    end

    -- Wait 1 second before running the loop again (adjust the wait time as needed)
    wait(0.5)
end
else
OrionLib:MakeNotification({Name = "Error",Content = "Equip Tinkerer",Image = "rbxassetid://7743878857",Time = 3})
end
end
})

local Tab = Window:MakeTab({
    Name = "Glovel",
    Icon = "rbxassetid://7734068321",
    PremiumOnly = false
})

Section = Tab:AddSection({
    Name = "1 Quest"
})

Tab:AddButton({
    Name = "Dig 350 Times",
    Callback = function()
        if game.Players.LocalPlayer.leaderstats.Glove.Value == "Glovel" then
            while true do
                game:GetService("ReplicatedStorage").GlovelFunc:InvokeServer()
                wait()
            end
        else
            OrionLib:MakeNotification({
                Name = "Error",
                Content = "Equip Glovel Glove",
                Image = "rbxassetid://7743878857",
                Time = 3
            })
        end
    end
})

Section = Tab:AddSection({
    Name = "2-3 Quest"
})

Tab:AddButton({
    Name = "Glovel Slap Aura (Only Critical Hits)",
    Callback = function()
        while true do
            local players = game:GetService("Players")
            local replicatedStorage = game:GetService("ReplicatedStorage")

            for _, player in ipairs(players:GetPlayers()) do
                if player.Character and player.Character:FindFirstChild("Head") then
                    local args = {
                        player.Character.Head,
                        true
                    }

                    replicatedStorage.GeneralHit:FireServer(unpack(args))
                end
            end

            wait(1)
        end
    end
})

local Tab = Window:MakeTab({
    Name = "Run",
    Icon = "rbxassetid://7734068321",
    PremiumOnly = false
})

Section = Tab:AddSection({
    Name = "1-2 Quests"
})

Tab:AddButton({
    Name = "Teleport To Closest Player",
    Callback = function()
        local LocalPlayer = game:GetService("Players").LocalPlayer
        local localChar = LocalPlayer.Character
        local localHRP = localChar and localChar:FindFirstChild("HumanoidRootPart")
        
        if not localHRP then
            OrionLib:MakeNotification({
                Name = "Error",
                Content = "Your character not found!",
                Image = "rbxassetid://7743878857",
                Time = 2
            })
            return
        end
        
        local closestPlayer = nil
        local closestDistance = math.huge
        local closestPos = nil
        
        for _, player in ipairs(game:GetService("Players"):GetPlayers()) do
            if player ~= LocalPlayer then
                local character = player.Character
                local hrp = character and character:FindFirstChild("HumanoidRootPart")
                if hrp then
                    local distance = (hrp.Position - localHRP.Position).Magnitude
                    if distance < closestDistance then
                        closestDistance = distance
                        closestPlayer = player
                        closestPos = hrp.Position
                    end
                end
            end
        end
        
        if closestPlayer and closestPos then
            localChar.HumanoidRootPart.CFrame = CFrame.new(closestPos)
            OrionLib:MakeNotification({
                Name = "Teleported",
                Content = "Teleported to " .. closestPlayer.Name .. " (" .. math.floor(closestDistance) .. " studs away)",
                Image = "rbxassetid://7734059095",
                Time = 2
            })
        else
            OrionLib:MakeNotification({
                Name = "Error",
                Content = "No other players found!",
                Image = "rbxassetid://7743878857",
                Time = 2
            })
        end
    end
})

-- Textbox for Vector3 position (format: x, y, z)
local positionInput = Tab:AddTextbox({
    Name = "Put Position (x, y, z)",
    Default = "0, 0, 0",
    TextDisappear = false,
    Callback = function(Value)
        local parts = {}
        for part in string.gmatch(Value, "([^,]+)") do
            table.insert(parts, tonumber(part))
        end
        
        if #parts >= 3 and parts[1] and parts[2] and parts[3] then
            positionValue = Vector3.new(parts[1], parts[2], parts[3])
        else
            positionValue = Vector3.new(0, 0, 0)
            positionInput:Set("0, 0, 0")
            OrionLib:MakeNotification({
                Name = "Invalid Input",
                Content = "Please use format: x, y, z",
                Image = "rbxassetid://7743878857",
                Time = 2
            })
        end
    end
})

-- Textbox for seconds delay
local secondsInput = Tab:AddTextbox({
    Name = "Repeat Ability Seconds Delay",
    Default = "7",
    TextDisappear = false,
    Callback = function(Value)
        local num = tonumber(Value)
        if num and num > 0 then
            secondsValue = num
        else
            secondsValue = 7
            secondsInput:Set("7")
        end
    end
})

-- Default values
local positionValue = Vector3.new(0, 0, 0)
local secondsValue = 7
local autoRunEnabled = false
local autoRunConnection = nil

-- Toggle for Auto Use Run Ability
Tab:AddToggle({
    Name = "Auto Use Run Ability",
    Default = false,
    Callback = function(Value)
        autoRunEnabled = Value
        
        if autoRunEnabled then
            if autoRunConnection then
                autoRunConnection:Disconnect()
                autoRunConnection = nil
            end
            
            autoRunConnection = game:GetService("RunService").Stepped:Connect(function()
                if autoRunEnabled then
                    local args = {
                        [1] = positionValue
                    }
                    game:GetService("ReplicatedStorage").GeneralAbility:FireServer(unpack(args))
                    task.wait(secondsValue)
                end
            end)
            
            OrionLib:MakeNotification({
                Name = "Auto Run",
                Content = string.format("Enabled! Position: (%.1f, %.1f, %.1f) Every %.1f seconds", positionValue.X, positionValue.Y, positionValue.Z, secondsValue),
                Image = "rbxassetid://7734059095",
                Time = 3
            })
        else
            if autoRunConnection then
                autoRunConnection:Disconnect()
                autoRunConnection = nil
            end
            
        end
    end
})

local Tab = Window:MakeTab({
    Name = "Frostbite",
    Icon = "rbxassetid://7734068321",
    PremiumOnly = false
})

Section = Tab:AddSection({
    Name = "1-2-3-4 Quests"
})

Tab:AddButton({
    Name = "Frostbite Slap Aura",
    Callback = function()
if game.Players.LocalPlayer.leaderstats.Glove.Value == "Frostbite" then
-- Get the ReplicatedStorage and Players service
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local Players = game:GetService("Players")

-- Start the infinite loop to fire for all players
while true do
    -- Loop through each player in the game
    for _, player in pairs(Players:GetPlayers()) do
        -- Ensure the player has a character and it contains the "Right Arm"
        if player.Character and player.Character:FindFirstChild("Right Arm") then
            -- Create the arguments table for the FireServer function
            local args = {
                [1] = player.Character["Right Arm"]
            }

            -- Fire the server with the "Right Arm" part as an argument
            ReplicatedStorage.GeneralHit:FireServer(unpack(args))
        end
    end

    -- Wait 1 second before running the loop again (adjust the wait time as needed)
    task.wait(1)
end
else
OrionLib:MakeNotification({Name = "Error",Content = "Equip Frostbite",Image = "rbxassetid://7743878857",Time = 3})
end
end
})

local frostbiteEnabled = false
local frostbiteLoop = nil

Tab:AddToggle({
    Name = "Auto Use Frostbite Ability",
    Default = false,
    Callback = function(Value)
        frostbiteEnabled = Value
        
        if Value then
            -- Start the loop
            if frostbiteLoop then
                task.cancel(frostbiteLoop)
                frostbiteLoop = nil
            end
            
            frostbiteLoop = task.spawn(function()
                while frostbiteEnabled do
                    pcall(function()
                        local args = {
                            [1] = 0.012500000651925802
                        }
                        game:GetService("ReplicatedStorage").GeneralAbility:FireServer(unpack(args))
                    end)
                    task.wait(4) -- Wait 4 seconds between fires
                end
            end)
            
            OrionLib:MakeNotification({
                Name = "Frostbite",
                Content = "Enabled! Firing every 4 seconds",
                Image = "rbxassetid://7743878857",
                Time = 2
            })
        else
            -- Stop the loop
            if frostbiteLoop then
                task.cancel(frostbiteLoop)
                frostbiteLoop = nil
            end
            
        end
    end
})

local Tab = Window:MakeTab({
    Name = "Plank",
    Icon = "rbxassetid://7734068321",
    PremiumOnly = false
})

Section = Tab:AddSection({
    Name = "1-2-3 Quests"
})

local AutoPlankToggle = Tab:AddToggle({
    Name = "Auto Use Plank On Players",
    Default = false,
    Callback = function(Value)
        _G.AutoPlank = Value
        
        -- Main loop for auto plank
        while _G.AutoPlank and task.wait(0.1) do
            -- Get all players except yourself
            local players = game:GetService("Players"):GetPlayers()
            local targetPlayers = {}
            
            for _, player in ipairs(players) do
                if player ~= game:GetService("Players").LocalPlayer and player.Character then
                    -- Get the humanoid root part of the target player
                    local hrp = player.Character:FindFirstChild("HumanoidRootPart")
                    if hrp then
                        table.insert(targetPlayers, {
                            player = player,
                            position = hrp.Position
                        })
                    end
                end
            end
            
            -- If there are valid players, pick a random one
            if #targetPlayers > 0 then
                local randomTarget = targetPlayers[math.random(1, #targetPlayers)]
                
                -- Create the args with the target player's position
                local args = {
                    [1] = "OutOfBody",
                    [2] = {
                        ["mousebehavior"] = Enum.MouseBehavior.Default,
                        ["ismoving"] = false,
                        ["dir"] = Vector3.new(
                            randomTarget.position.X,
                            randomTarget.position.Y,
                            randomTarget.position.Z
                        )
                    }
                }
                
                -- Fire the remote
                game:GetService("ReplicatedStorage").Blink:FireServer(unpack(args))
            end
        end
    end
})

local Tab = Window:MakeTab({
    Name = "Hexa",
    Icon = "rbxassetid://7734068321",
    PremiumOnly = false
})

Section = Tab:AddSection({
    Name = "1-2-3-4 Quest"
})

Tab:AddToggle({
    Name = "Auto Use Ability",
    Default = false,
    Callback = function(Value)
        if Value then
            -- START
            if game.Players.LocalPlayer.leaderstats.Glove.Value == "Hexa" and game.Players.LocalPlayer.Character:FindFirstChild("entered") then
                
                getgenv().HexaAbilityLoop = true
                task.spawn(function()
                    while getgenv().HexaAbilityLoop do
                        game:GetService("ReplicatedStorage").GeneralAbility:FireServer()
                        task.wait(10)
                    end
                end)
            else
                OrionLib:MakeNotification({ 
                    Name = "Error", 
                    Content = "Equip Hexa Glove", 
                    Image = "rbxassetid://7743878857", 
                    Time = 3 
                })
                return false -- Turn toggle off
            end
        else
            -- STOP
            getgenv().HexaAbilityLoop = false
            
        end
    end
})

Tab:AddButton({
	Name = "Hexa Slap Aura",
	Callback = function()   
if game.Players.LocalPlayer.leaderstats.Glove.Value == "Hexa" then
-- Get the ReplicatedStorage and Players service
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local Players = game:GetService("Players")

-- Start the infinite loop to fire for all players
while true do
    -- Loop through each player in the game
    for _, player in pairs(Players:GetPlayers()) do
        -- Ensure the player has a character and it contains the "Left Leg"
        if player.Character and player.Character:FindFirstChild("Left Leg") then
            -- Create the arguments table for the FireServer function
            local args = {
                [1] = player.Character["Left Leg"],
                [2] = false  -- The second argument, set to false as in your original script
            }

            -- Fire the server with the "Left Leg" part as an argument
            ReplicatedStorage.GeneralHit:FireServer(unpack(args))
        end
    end

    -- Wait 1 second before running the loop again (adjust the wait time as needed)
    wait(0.7)
end
else
OrionLib:MakeNotification({Name = "Error",Content = "Equip Hexa",Image = "rbxassetid://7743878857",Time = 3})
end
end
})

local Tab = Window:MakeTab({
    Name = "Eggler",
    Icon = "rbxassetid://7734068321",
    PremiumOnly = false
})

Tab:AddButton({
	Name = "Use Eggler (Tongue) Ability",
	Callback = function()   
game:GetService("ReplicatedStorage"):WaitForChild("GeneralAbility"):FireServer()
end
})

Tab:AddButton({
	Name = "Use Eggler (Egg Yourself) Ability",
	Callback = function()   
game:GetService("ReplicatedStorage"):WaitForChild("Events"):WaitForChild("EgglerRAbility"):FireServer()
end
})

Tab:AddButton({
	Name = "Eggler Slap Aura",
	Callback = function()   
if game.Players.LocalPlayer.leaderstats.Glove.Value == "Eggler" then
-- Get the ReplicatedStorage and Players service
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local Players = game:GetService("Players")

-- Start the infinite loop to fire for all players
while true do
    -- Loop through each player in the game
    for _, player in pairs(Players:GetPlayers()) do
        -- Ensure the player has a character and it contains the "Left Leg"
        if player.Character and player.Character:FindFirstChild("Left Leg") then
            -- Create the arguments table for the FireServer function
            local args = {
                [1] = player.Character["Left Leg"],
                [2] = false  -- The second argument, set to false as in your original script
            }

            -- Fire the server with the "Left Leg" part as an argument
            ReplicatedStorage.GeneralHit:FireServer(unpack(args))
        end
    end

    -- Wait 1 second before running the loop again (adjust the wait time as needed)
    wait(0.5)
end
else
OrionLib:MakeNotification({Name = "Error",Content = "Equip Eggler",Image = "rbxassetid://7743878857",Time = 3})
end
end
})

local Tab = Window:MakeTab({
    Name = "Car Keys",
    Icon = "rbxassetid://7734068321",
    PremiumOnly = false
})

Section = Tab:AddSection({
    Name = "1 Quest"
})

local toggleOn = false
local upgradeLoop = nil

local attributeFrames = {
    "UtilityAttributeFrame",
    "ArmorAttributeFrame",
    "ReverseAttributeFrame",
    "SteerAngleAttributeFrame",
    "AccelerationAttributeFrame",
    "MaxSpeedAttributeFrame",
    "KnockbackAttributeFrame"
}

Tab:AddToggle({
    Name = "Auto Upgrade All Stats",
    Default = false,
    Callback = function(Value)
        toggleOn = Value
        if toggleOn then
            -- Start upgrade loop
            upgradeLoop = task.spawn(function()
                while toggleOn do
                    for _, attr in ipairs(attributeFrames) do
                        local args = { attr }
                        game:GetService("ReplicatedStorage"):WaitForChild("CarKeysUpgrade"):FireServer(unpack(args))
                        task.wait(0.1) -- small delay to prevent spamming too fast
                    end
                    task.wait(0.5) -- wait between full upgrade cycles
                end
            end)
        else
            -- Stop loop by breaking the while condition
            toggleOn = false
        end
    end
})

Section = Tab:AddSection({
    Name = "2-3 Quest"
})

Tab:AddButton({
    Name = "Car Keys Slap Aura",
    Callback = function()
if game.Players.LocalPlayer.leaderstats.Glove.Value == "Car Keys" then
-- Get the ReplicatedStorage and Players service
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local Players = game:GetService("Players")

-- Start the infinite loop to fire for all players
while true do
    -- Loop through each player in the game
    for _, player in pairs(Players:GetPlayers()) do
        -- Ensure the player has a character and it contains the "Right Arm"
        if player.Character and player.Character:FindFirstChild("Right Arm") then
            -- Create the arguments table for the FireServer function
            local args = {
                [1] = player.Character["Right Arm"]
            }

            -- Fire the server with the "Right Arm" part as an argument
            ReplicatedStorage.GeneralHit:FireServer(unpack(args))
        end
    end

    -- Wait 1 second before running the loop again (adjust the wait time as needed)
    task.wait(0.5)
end
else
OrionLib:MakeNotification({Name = "Error",Content = "Equip Car Keys",Image = "rbxassetid://7743878857",Time = 3})
end
end
})

local Tab = Window:MakeTab({
    Name = "Ultra Instinct",
    Icon = "rbxassetid://7734068321",
    PremiumOnly = false
})

Section = Tab:AddSection({
    Name = "1-2 Quest"
})

local running = false -- declare global toggle state

Tab:AddToggle({
    Name = "Auto Slap Players",
    Default = false,
    Callback = function(AutoSlapVoodoo)
        running = AutoSlapVoodoo

        if not running then
            return -- stop immediately if toggled off
        end

        -- Run everything in a separate thread
        task.spawn(function()
            local Players = game:GetService("Players")
            local ReplicatedStorage = game:GetService("ReplicatedStorage")
            local LocalPlayer = Players.LocalPlayer

            -- Function to teleport to a random player
            local function teleportToRandomPlayer()
                local playerList = Players:GetPlayers()
                if #playerList > 1 then
                    local randomPlayer = nil
                    repeat
                        randomPlayer = playerList[math.random(1, #playerList)]
                    until randomPlayer ~= LocalPlayer

                    local targetChar = randomPlayer.Character
                    if targetChar and targetChar:FindFirstChild("HumanoidRootPart") then
                        LocalPlayer.Character.HumanoidRootPart.CFrame = CFrame.new(targetChar.HumanoidRootPart.Position + Vector3.new(0, 3, 0))
                    end
                end
            end

            -- Function to fire GeneralHit for all players
            local function fireGeneralHitForAllPlayers()
                for _, player in pairs(Players:GetPlayers()) do
                    if player ~= LocalPlayer and player.Character and player.Character:FindFirstChild("HumanoidRootPart") then
                        ReplicatedStorage:WaitForChild("GeneralHit"):FireServer(player.Character.HumanoidRootPart)
                    end
                end
            end

            -- Loop 750 times or until stopped
            local count = 0
            while running and count < 750 do
                if LocalPlayer.leaderstats.Glove.Value == "Ultra Instinct" then
                    teleportToRandomPlayer()
                    fireGeneralHitForAllPlayers()
                    count += 1
                    task.wait(1)
                else
                    OrionLib:MakeNotification({
                        Name = "Error",
                        Content = "Equip Ultra Instinct Glove",
                        Image = "rbxassetid://7743878857",
                        Time = 3
                    })
                    break
                end
            end
        end)
    end
})

Tab:AddButton({
    Name = "Teleport to Random Player",
    Callback = function()
        local players = {}
        local localPlayer = game.Players.LocalPlayer
        
        -- Get all players except yourself
        for _, player in pairs(game.Players:GetPlayers()) do
            if player ~= localPlayer and player.Character then
                local hrp = player.Character:FindFirstChild("HumanoidRootPart")
                if hrp then
                    table.insert(players, player)
                end
            end
        end
        
        if #players > 0 then
            -- Select random player
            local randomPlayer = players[math.random(1, #players)]
            local targetHRP = randomPlayer.Character.HumanoidRootPart
            
            -- Teleport to the random player
            if localPlayer.Character and localPlayer.Character:FindFirstChild("HumanoidRootPart") then
                localPlayer.Character.HumanoidRootPart.CFrame = targetHRP.CFrame
                
            end
        else

        end
    end
})

Tab:AddToggle({
    Name = "Auto Teleport to Random Players",
    Default = false,
    Callback = function(Value)
        if Value then
            -- START
            
            getgenv().AutoTeleportPlayersLoop = true
            task.spawn(function()
                while getgenv().AutoTeleportPlayersLoop do
                    local localPlayer = game.Players.LocalPlayer
                    local players = {}
                    
                    -- Get all players except yourself
                    for _, player in pairs(game.Players:GetPlayers()) do
                        if player ~= localPlayer and player.Character then
                            local hrp = player.Character:FindFirstChild("HumanoidRootPart")
                            if hrp then
                                table.insert(players, player)
                            end
                        end
                    end
                    
                    if #players > 0 and localPlayer.Character and localPlayer.Character:FindFirstChild("HumanoidRootPart") then
                        -- Select random player
                        local randomPlayer = players[math.random(1, #players)]
                        local targetHRP = randomPlayer.Character.HumanoidRootPart
                        
                        -- Teleport to the random player
                        localPlayer.Character.HumanoidRootPart.CFrame = targetHRP.CFrame
                    end
                    
                    task.wait(0.1) -- Wait 0.1 seconds as requested
                end
            end)
        else
            -- STOP
            getgenv().AutoTeleportPlayersLoop = false
            
        end
    end
})

local Tab = Window:MakeTab({
    Name = "Acrobat",
    Icon = "rbxassetid://7734068321",
    PremiumOnly = false
})

Section = Tab:AddSection({
    Name = "1-2-3 Quest"
})

Tab:AddButton({
    Name = "No Cooldown Abilities Script (NOT AUTO)",
    Callback = function()
local player = game.Players.LocalPlayer
local character = player.Character or player.CharacterAdded:Wait()
local tool = character:FindFirstChildOfClass("Tool") or player.Backpack:FindFirstChildOfClass("Tool")

while character.Humanoid.Health ~= 0 do
local localscript = tool:FindFirstChildOfClass("LocalScript")
local localscriptclone = localscript:Clone()
localscriptclone = localscript:Clone()
localscriptclone:Clone()
localscript:Destroy()
localscriptclone.Parent = tool
wait(0.1)
end
end
})

Tab:AddButton({
    Name = "Acrobat Slap Aura",
    Callback = function()
if game.Players.LocalPlayer.leaderstats.Glove.Value == "Acrobat" then
-- Get the ReplicatedStorage and Players service
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local Players = game:GetService("Players")

-- Start the infinite loop to fire for all players
while true do
    -- Loop through each player in the game
    for _, player in pairs(Players:GetPlayers()) do
        -- Ensure the player has a character and it contains the "Right Arm"
        if player.Character and player.Character:FindFirstChild("Right Arm") then
            -- Create the arguments table for the FireServer function
            local args = {
                [1] = player.Character["Right Arm"]
            }

            -- Fire the server with the "Right Arm" part as an argument
            ReplicatedStorage.AcHit:FireServer(unpack(args))
        end
    end

    -- Wait 1 second before running the loop again (adjust the wait time as needed)
    task.wait(0.5)
end
else
OrionLib:MakeNotification({Name = "Error",Content = "Equip Acrobat",Image = "rbxassetid://7743878857",Time = 3})
end
end
})





end
