-- Skid if u want idc just dont forget about credits or smt
-- Webhook
local WebhookScripterHub = loadstring(game:HttpGet("https://raw.githubusercontent.com/Scripter-Coder/Scripter-Coder/refs/heads/main/Universal/Webhook"))()

-- Load Fluent Library
local Fluent = loadstring(game:HttpGet("https://github.com/dawid-scripts/Fluent/releases/latest/download/main.lua"))()
local SaveManager = loadstring(game:HttpGet("https://raw.githubusercontent.com/dawid-scripts/Fluent/master/Addons/SaveManager.lua"))()
local InterfaceManager = loadstring(game:HttpGet("https://raw.githubusercontent.com/dawid-scripts/Fluent/master/Addons/InterfaceManager.lua"))()

-- Check if game is Slap Battles or related
local validPlaceIds = {
    6403373529, 9015014224, 11520107397, 124596094333302, 7234087065, 9020359053,
    9068206286, 9426795465, 9431156611, 11359765900, 11367956190, 11828384869,
    12432152490, 12712288037, 12845859004, 13833961666, 15228348051, 15507333474,
    16034567693, 17290438723, 18550498098, 18698003301, 74169485398268, 89837553336708,
    102908326578005, 103505724406848, 14422118326, 92516899071319, 115782629143468,
    95706409702231, 118650724506449, 97220865182663, 98726100529621, 136005148166028,
    117232463555132, 129665246576996, 132277598079047, 136690395520488, 86045914443715,
    75803343514634, 80420091630966, 98871361390239, 90553746175288, 106620300132058,
    94481828238038, 122901288403496, 79885102123162, 86643839793301, 113228834069218,
    95356852680586
}

local function isSlapBattlesGame()
    for _, id in pairs(validPlaceIds) do
        if game.PlaceId == id then
            return true
        end
    end
    return false
end

if not isSlapBattlesGame() then
    Fluent:Notify({
        Title = "Error",
        Content = "This script only works in Slap Battles and related games!",
        Duration = 5
    })
    return
end

-- Create Window
local Window = Fluent:CreateWindow({
    Title = "Slap Farm Hub (Scripter Hub)",
    SubTitle = "Slap Battles - Slap Farm Hub",
    TabWidth = 160,
    Size = UDim2.fromOffset(580, 460),
    Acrylic = false,
    Theme = "Dark",
    MinimizeKey = Enum.KeyCode.RightControl
})

Fluent:Notify({
    Title = "From Scripter",
    Content = "Script Loads (Can Take 1-5 Seconds)",
    Duration = 3
})

-- Create Tabs
local Tabs = {
    Welcome = Window:AddTab({ Title = "Welcome", Icon = "home" }),
    Dual = Window:AddTab({ Title = "Dual", Icon = "hand" }),
    Reverse = Window:AddTab({ Title = "Reverse", Icon = "refresh-cw" }),
    Engineer = Window:AddTab({ Title = "Engineer", Icon = "wrench" }),
    ElGato = Window:AddTab({ Title = "el gato", Icon = "cat" }),
    Angler = Window:AddTab({ Title = "Angler", Icon = "fish" }),
    Bounty = Window:AddTab({ Title = "Bounty", Icon = "target" }),
    Debug = Window:AddTab({ Title = "Debug", Icon = "bug" }),
    Settings = Window:AddTab({ Title = "Settings", Icon = "settings" }),
    Other = Window:AddTab({ Title = "Other Settings", Icon = "more-horizontal" })
}

Window:SelectTab(1)

Fluent:Notify({
    Title = "From Scripter",
    Content = "Good Luck!",
    Duration = 3
})

-- ===============================
-- WELCOME TAB
-- ===============================
local player = game.Players.LocalPlayer

Tabs.Welcome:AddParagraph({
    Title = "Hello " .. player.Name .. "!",
    Content = ""
})

local bypassSection = Tabs.Welcome:AddSection("Before u will start using mine script would u like to bypass anticheat?")

Tabs.Welcome:AddButton({
    Title = "Bypass All Anticheat",
    Description = "Bypasses the game's anticheat system",
    Callback = function()
        loadstring(game:HttpGet("https://raw.githubusercontent.com/RobScripter/Slap-Battles/main/BypassAntiCheat.lua"))()
        task.wait(1.5)
        Fluent:Notify({
            Title = "Success",
            Content = "Bypassed All Anticheat",
            Duration = 3
        })
    end
})

-- Slapples Section
local slapplesSection = Tabs.Welcome:AddSection("Slapples")

Tabs.Welcome:AddToggle("AutoCollectSlapples", {
    Title = "Auto Collect Slapples",
    Description = "Automatically collects Slapples and Golden Slapples",
    Default = false,
    Callback = function(Value)
        local BringSlapples = Value
        
        task.spawn(function()
            while BringSlapples do
                local player = game.Players.LocalPlayer
                if not player then task.wait(0.1) continue end
                
                local character = player.Character
                if not character or not character.Parent then 
                    task.wait(0.01) 
                    continue 
                end
                
                if not character:FindFirstChild("entered") then 
                    task.wait(0.01) 
                    continue 
                end
                
                local hrp = character:FindFirstChild("HumanoidRootPart")
                if not hrp then 
                    task.wait(0.01) 
                    continue 
                end
                
                local slapplesFolder = workspace:FindFirstChild("Arena")
                    and workspace.Arena:FindFirstChild("island5")
                    and workspace.Arena.island5:FindFirstChild("Slapples")
                
                if not slapplesFolder then 
                    task.wait(0.01) 
                    continue 
                end
                
                for _, slapple in pairs(slapplesFolder:GetChildren()) do
                    if slapple:IsA("Model") and (slapple.Name == "Slapple" or slapple.Name == "GoldenSlapple") then
                        pcall(function()
                            local glove = slapple:FindFirstChild("Glove")
                            if glove then
                                glove.CFrame = hrp.CFrame
                            else
                                slapple:SetPrimaryPartCFrame(hrp.CFrame)
                            end
                        end)
                    end
                end
                
                task.wait(0.01)
                if Fluent.Unloaded then break end
            end
        end)
    end
})

-- Auto Slapples ServerHop Section
local serverhopSection = Tabs.Welcome:AddSection("Auto Slapples ServerHop")

Tabs.Welcome:AddButton({
    Title = "Auto Serverhop Collect Slapples",
    Description = "Copies the auto serverhop script to clipboard",
    Callback = function()
        local loadstringCode = 'loadstring(game:HttpGet("https://raw.githubusercontent.com/Scripter-Coder/Scripter-Coder/refs/heads/main/Slap%20Battles/AutoFarm%20Slapples"))()'
        setclipboard(loadstringCode)
        Fluent:Notify({
            Title = "Copied Successfully",
            Content = "Script copied. Put it in Auto Execute folder to auto work",
            Duration = 5
        })
    end
})

-- ===============================
-- DUAL GLOVE BOT (WALKING VERSION)
-- ===============================
local Players = game:GetService("Players")
local localPlayer = Players.LocalPlayer
local RunService = game:GetService("RunService")
local VirtualUser = game:GetService("VirtualUser")
local ReplicatedStorage = game:GetService("ReplicatedStorage")

-- Control variables
local dualBotActive = false
local ignoreRocks = false
local ignoreOneshots = false
local ignoreBarzilPortal = false
local autoCollectSlapplesDual = false
local noclipEnabled = true
local walkSpeed = 50
local detectionSpeed = 0.5
local antiAfkEnabled = false
local isUsingDual = false
local dualConnection = nil
local hasTeleportedToArena = false

-- Character references
local character
local humanoid
local humanoidRootPart
local noclipConnection
local antiAfkConnection

-- Safe positions on islands
local safePositions = {
    ["main island"] = Vector3.new(0, 10, 0),
    ["island1"] = Vector3.new(-100, 10, -100),
    ["island2"] = Vector3.new(100, 10, -100),
    ["island3"] = Vector3.new(-100, 10, 100),
    ["island4"] = Vector3.new(100, 10, 100),
    ["island5"] = Vector3.new(0, 10, 200),
    ["Planks"] = Vector3.new(0, 5, 0)
}

-- Anti AFK function
local function setupAntiAfk()
    if antiAfkConnection then
        antiAfkConnection:Disconnect()
        antiAfkConnection = nil
    end
    
    if antiAfkEnabled then
        antiAfkConnection = localPlayer.Idled:Connect(function()
            VirtualUser:CaptureController()
            VirtualUser:ClickButton2(Vector2.new())
        end)
    end
end

-- Enable noclip
local function enableNoclip()
    if noclipConnection then
        noclipConnection:Disconnect()
    end
    
    noclipConnection = RunService.Stepped:Connect(function()
        if not character then return end
        
        for _, part in pairs(character:GetChildren()) do
            if part:IsA("BasePart") then
                part.CanCollide = false
            end
        end
    end)
end

-- Disable noclip
local function disableNoclip()
    if noclipConnection then
        noclipConnection:Disconnect()
        noclipConnection = nil
    end
    
    if character then
        for _, part in pairs(character:GetChildren()) do
            if part:IsA("BasePart") then
                part.CanCollide = true
            end
        end
    end
end

-- Walk to position using Humanoid:MoveTo
local function walkToPosition(targetPosition, stopDistance)
    if not character or not humanoid or not humanoidRootPart then return false end
    
    if noclipEnabled then
        enableNoclip()
    end
    
    humanoid.WalkSpeed = walkSpeed
    humanoid:MoveTo(targetPosition)
    
    local startTime = tick()
    local maxTime = 15
    
    while tick() - startTime < maxTime and dualBotActive do
        if not character or not humanoid or not humanoidRootPart then return false end
        
        local distance = (humanoidRootPart.Position - targetPosition).Magnitude
        
        if distance <= stopDistance then
            humanoid:MoveTo(humanoidRootPart.Position)
            return true
        end
        
        humanoid:MoveTo(targetPosition)
        task.wait(0.1)
        if Fluent.Unloaded then break end
    end
    
    humanoid:MoveTo(humanoidRootPart.Position)
    return false
end

-- Update character references
local function setupCharacter()
    character = localPlayer.Character
    if character then
        humanoid = character:FindFirstChild("Humanoid")
        humanoidRootPart = character:FindFirstChild("HumanoidRootPart")
        if humanoid then
            humanoid.WalkSpeed = walkSpeed
        end
        if noclipEnabled then
            enableNoclip()
        end
    end
end

-- Check if player is in arena
local function isPlayerInArena(plr)
    if not plr.Character then return false end
    return plr.Character:FindFirstChild("entered") ~= nil
end

-- Check if we are in arena
local function amIInArena()
    return isPlayerInArena(localPlayer)
end

-- Walk to Teleport1
local function walkToTeleport1()
    local lobby = workspace:FindFirstChild("Lobby")
    if not lobby then return false end
    
    local teleport1 = lobby:FindFirstChild("Teleport1")
    if not teleport1 or not teleport1:IsA("BasePart") then return false end
    
    local teleportPos = teleport1.Position + Vector3.new(0, 3, 0)
    
    Fluent:Notify({
        Title = "Walking",
        Content = "Walking to Teleport1...",
        Duration = 2
    })
    
    return walkToPosition(teleportPos, 5)
end

-- Check if Dual glove is equipped
local function isDualEquipped()
    local leaderstats = localPlayer:FindFirstChild("leaderstats")
    if leaderstats then
        local glove = leaderstats:FindFirstChild("Glove")
        if glove then
            return glove.Value == "Dual"
        end
    end
    return false
end

-- Function to start auto using Dual tool
local function startAutoUseDual()
    if isUsingDual then return end
    
    isUsingDual = true
    
    dualConnection = RunService.Heartbeat:Connect(function()
        if not dualBotActive then return end
        
        local character = localPlayer.Character
        if not character then return end
        
        local hrp = character:FindFirstChild("HumanoidRootPart")
        if not hrp then return end
        
        if not amIInArena() then return end
        
        local closestPlayer = nil
        local closestDistance = 10
        
        for _, plr in pairs(Players:GetPlayers()) do
            if plr ~= localPlayer and plr.Character then
                local targetHRP = plr.Character:FindFirstChild("HumanoidRootPart")
                if targetHRP and isPlayerInArena(plr) then
                    if ignoreRocks and plr.Character:FindFirstChild("rock") then
                        -- skip
                    else
                        local distance = (hrp.Position - targetHRP.Position).Magnitude
                        if distance < closestDistance then
                            closestDistance = distance
                            closestPlayer = plr
                        end
                    end
                end
            end
        end
        
        if closestPlayer and closestDistance <= 10 then
            local dualTool = character:FindFirstChild("Dual") or localPlayer.Backpack:FindFirstChild("Dual")
            
            if dualTool then
                if dualTool.Parent == localPlayer.Backpack then
                    dualTool.Parent = character
                end
                
                if dualTool:IsA("Tool") then
                    pcall(function()
                        dualTool:Activate()
                    end)
                end
            end
        end
    end)
end

local function stopAutoUseDual()
    isUsingDual = false
    if dualConnection then
        dualConnection:Disconnect()
        dualConnection = nil
    end
end

-- Barzil portal control
local function controlBarzilPortal()
    local brazilFolder = workspace:FindFirstChild("Lobby") and workspace.Lobby:FindFirstChild("brazil")
    if brazilFolder then
        for _, v in pairs(brazilFolder:GetDescendants()) do
            if v:IsA("BasePart") then
                v.CanTouch = not ignoreBarzilPortal
            end
        end
    end
end

-- Get closest player for walking
local function getClosestPlayerForWalking()
    if not character or not humanoidRootPart then return nil, math.huge end
    
    local closestPlayer = nil
    local closestDistance = math.huge
    
    for _, plr in pairs(Players:GetPlayers()) do
        if plr ~= localPlayer and plr.Character then
            local targetHRP = plr.Character:FindFirstChild("HumanoidRootPart")
            if targetHRP and isPlayerInArena(plr) then
                local shouldSkip = false
                
                if ignoreRocks and plr.Character:FindFirstChild("rock") then
                    shouldSkip = true
                end
                
                if not shouldSkip then
                    local distance = (humanoidRootPart.Position - targetHRP.Position).Magnitude
                    if distance < closestDistance then
                        closestDistance = distance
                        closestPlayer = plr
                    end
                end
            end
        end
    end
    
    return closestPlayer, closestDistance
end

-- Auto collect slapples
local function collectSlapplesDual()
    if not character or not humanoidRootPart or not autoCollectSlapplesDual then return end
    
    local island5 = workspace:FindFirstChild("Arena") and workspace.Arena:FindFirstChild("island5")
    if not island5 then return end
    
    local slapplesFolder = island5:FindFirstChild("Slapples")
    if not slapplesFolder then return end
    
    for _, slappleModel in pairs(slapplesFolder:GetChildren()) do
        if slappleModel:IsA("Model") and (slappleModel.Name == "Slapple" or slappleModel.Name == "GoldenSlapple") then
            pcall(function()
                local glove = slappleModel:FindFirstChild("Glove")
                if glove and glove:IsA("BasePart") then
                    glove.CFrame = humanoidRootPart.CFrame
                end
            end)
        end
    end
end

-- Main Dual bot function
local function startDualBot()
    Fluent:Notify({
        Title = "Dual Bot Started",
        Content = "Walking mode enabled!",
        Duration = 3
    })
    
    hasTeleportedToArena = false
    startAutoUseDual()
    
    while dualBotActive do
        if character and humanoid and humanoidRootPart then
            if not isDualEquipped() then
                Fluent:Notify({
                    Title = "Error",
                    Content = "Equip Dual glove!",
                    Duration = 3
                })
                dualBotActive = false
                break
            end
            
            if not amIInArena() then
                hasTeleportedToArena = false
                walkToTeleport1()
                task.wait(2)
                if Fluent.Unloaded then break end
                continue
            else
                if not hasTeleportedToArena then
                    hasTeleportedToArena = true
                end
            end
            
            if ignoreBarzilPortal then
                controlBarzilPortal()
            end
            
            if autoCollectSlapplesDual then
                task.spawn(function()
                    collectSlapplesDual()
                end)
            end
            
            local target, distanceToTarget = getClosestPlayerForWalking()
            
            if target and target.Character then
                local targetHRP = target.Character:FindFirstChild("HumanoidRootPart")
                if targetHRP then
                    local targetPos = targetHRP.Position
                    local currentDistance = (humanoidRootPart.Position - targetPos).Magnitude
                    
                    if currentDistance > 15 then
                        local walkPos = Vector3.new(targetPos.X, targetPos.Y + 3, targetPos.Z)
                        walkToPosition(walkPos, 12)
                    else
                        humanoid:MoveTo(humanoidRootPart.Position)
                        local lookAt = CFrame.new(humanoidRootPart.Position, targetPos)
                        humanoidRootPart.CFrame = lookAt
                    end
                end
            else
                if amIInArena() then
                    local safePos = safePositions["island5"] or safePositions["main island"]
                    if safePos then
                        walkToPosition(safePos, 10)
                    end
                end
            end
        else
            setupCharacter()
        end
        
        task.wait(detectionSpeed)
        if Fluent.Unloaded then break end
    end
    
    stopAutoUseDual()
    disableNoclip()
    if humanoid then
        humanoid.WalkSpeed = 16
    end
end

-- Character respawn handling
localPlayer.CharacterAdded:Connect(function(newChar)
    character = newChar
    hasTeleportedToArena = false
    
    task.wait(1)
    humanoid = character:FindFirstChild("Humanoid")
    humanoidRootPart = character:FindFirstChild("HumanoidRootPart")
    
    if humanoid then
        humanoid.WalkSpeed = walkSpeed
    end
    
    if noclipEnabled then
        enableNoclip()
    end
    
    if dualBotActive then
        task.spawn(function()
            walkToTeleport1()
        end)
    end
end)

setupCharacter()

-- DUAL TAB UI
local dualSection = Tabs.Dual:AddSection("Dual Glove Bot Settings")

Tabs.Dual:AddToggle("EnableDualBot", {
    Title = "Enable Dual Bot",
    Description = "Automatically walks to players and slaps them with Dual glove",
    Default = false,
    Callback = function(Value)
        if Value then
            if not isDualEquipped() then
                Fluent:Notify({
                    Title = "Error",
                    Content = "Equip Dual glove first!",
                    Duration = 3
                })
                return
            end
            dualBotActive = true
            hasTeleportedToArena = false
            task.spawn(startDualBot)
        else
            dualBotActive = false
            stopAutoUseDual()
        end
    end
})

Tabs.Dual:AddToggle("EnableNoclip", {
    Title = "Enable Noclip (Recommended)",
    Description = "Allows walking through walls",
    Default = true,
    Callback = function(Value)
        noclipEnabled = Value
        if Value then
            enableNoclip()
        else
            disableNoclip()
        end
    end
})

Tabs.Dual:AddToggle("AntiAfkDual", {
    Title = "Anti AFK",
    Description = "Prevents being kicked for inactivity",
    Default = false,
    Callback = function(Value)
        antiAfkEnabled = Value
        setupAntiAfk()
    end
})

local filterSection = Tabs.Dual:AddSection("Player Filters")

Tabs.Dual:AddToggle("IgnoreRocks", {
    Title = "Ignore Rocks",
    Description = "Ignores players in rock form",
    Default = false,
    Callback = function(Value)
        ignoreRocks = Value
    end
})

Tabs.Dual:AddToggle("IgnoreBarzilPortal", {
    Title = "Ignore Barzil Portal",
    Description = "Prevents entering the Barzil portal",
    Default = false,
    Callback = function(Value)
        ignoreBarzilPortal = Value
        controlBarzilPortal()
    end
})

Tabs.Dual:AddToggle("AutoCollectSlapplesDual", {
    Title = "Auto Collect Slapples",
    Description = "Automatically collects Slapples while bot is running",
    Default = false,
    Callback = function(Value)
        autoCollectSlapplesDual = Value
    end
})

local botSettingsSection = Tabs.Dual:AddSection("Bot Settings")

Tabs.Dual:AddSlider("WalkSpeed", {
    Title = "Walk Speed",
    Description = "Speed at which the bot walks",
    Min = 1,
    Max = 100,
    Default = 20,
    Rounding = 1,
    Callback = function(Value)
        walkSpeed = Value
        if humanoid then
            humanoid.WalkSpeed = walkSpeed
        end
    end
})

Tabs.Dual:AddSlider("DetectionSpeed", {
    Title = "Detection Speed",
    Description = "How often the bot checks for players",
    Min = 0.01,
    Max = 2,
    Default = 0.5,
    Rounding = 2,
    Callback = function(Value)
        detectionSpeed = Value
    end
})

local statusLabel = Tabs.Dual:AddParagraph({
    Title = "Status",
    Content = "Status: Ready"
})

-- Update status periodically
task.spawn(function()
    while true do
        if dualBotActive then
            if isDualEquipped() then
                if amIInArena() then
                    local target, distance = getClosestPlayerForWalking()
                    if target then
                        statusLabel:SetContent("Status: Active | Target: " .. target.Name .. " (" .. math.floor(distance) .. " studs)")
                    else
                        statusLabel:SetContent("Status: Active | No players in arena")
                    end
                else
                    statusLabel:SetContent("Status: Walking to Teleport1...")
                end
            else
                statusLabel:SetContent("Status: ERROR - Equip Dual glove!")
            end
        else
            statusLabel:SetContent("Status: Ready")
        end
        task.wait(1)
        if Fluent.Unloaded then break end
    end
end)

-- Dual Slap Farm (Teleport Version)
Tabs.Dual:AddToggle("DualSlapFarm", {
    Title = "Dual Slap Farm (Teleport Version)",
    Description = "Teleports and slaps players with Dual glove",
    Default = false,
    Callback = function(State)
        local AutoDualRunning = State
        
        local function teleportToRandomPlayer()
            local playerList = Players:GetPlayers()
            if #playerList > 1 then
                local randomPlayer
                repeat
                    randomPlayer = playerList[math.random(1, #playerList)]
                until randomPlayer ~= localPlayer
                
                local char = randomPlayer.Character
                if char and char:FindFirstChild("HumanoidRootPart") then
                    local localHRP = localPlayer.Character and localPlayer.Character:FindFirstChild("HumanoidRootPart")
                    if localHRP then
                        localHRP.CFrame = char.HumanoidRootPart.CFrame
                    end
                end
            end
        end
        
        local function fireGeneralHitForAllPlayers()
            for _, player in pairs(Players:GetPlayers()) do
                if player.Character then
                    local rightArm = player.Character:FindFirstChild("Right Arm") or player.Character:FindFirstChild("RightHand")
                    if rightArm then
                        pcall(function()
                            ReplicatedStorage:WaitForChild("GeneralHit"):FireServer(rightArm)
                        end)
                    end
                end
            end
        end
        
        local function runAutoDualSlapLoop()
            local leaderstats = localPlayer:FindFirstChild("leaderstats")
            local glove = leaderstats and leaderstats:FindFirstChild("Glove")
            
            if not (glove and glove.Value == "Dual") then
                Fluent:Notify({
                    Title = "Error",
                    Content = "Equip Dual glove.",
                    Duration = 3
                })
                return
            end
            
            while AutoDualRunning do
                task.wait(0.5)
                teleportToRandomPlayer()
                fireGeneralHitForAllPlayers()
                if Fluent.Unloaded then break end
            end
        end
        
        if State then
            task.spawn(runAutoDualSlapLoop)
        end
    end
})

-- ===============================
-- REVERSE TAB
-- ===============================
Tabs.Reverse:AddSection("Slap Farmer")

Tabs.Reverse:AddToggle("AutoSlapReverse", {
    Title = "Auto Slap Reverse With Default Glove",
    Description = "Automatically slaps players who have the Reversed effect",
    Default = false,
    Callback = function(Value)
        local AutoSlapReverse = Value
        
        if Value then
            task.spawn(function()
                local ReverseLocalPlayer = game.Players.LocalPlayer
                local ReverseReplicatedStorage = game:GetService("ReplicatedStorage")
                
                local leaderstats = ReverseLocalPlayer:FindFirstChild("leaderstats")
                local glove = leaderstats and leaderstats:FindFirstChild("Glove")
                
                if not (glove and glove.Value == "Default") then
                    Fluent:Notify({
                        Title = "Error",
                        Content = "Please equip Default glove first!",
                        Duration = 3
                    })
                    return
                end
                
                local function getReversedPlayers()
                    local reversedPlayers = {}
                    
                    for _, player in ipairs(game.Players:GetPlayers()) do
                        if player ~= ReverseLocalPlayer then
                            local playerModel = workspace:FindFirstChild(player.Name)
                            if playerModel then
                                local reversedBool = playerModel:FindFirstChild("Reversed")
                                if reversedBool and reversedBool:IsA("BoolValue") and reversedBool.Value == true then
                                    table.insert(reversedPlayers, player)
                                end
                            end
                        end
                    end
                    
                    return reversedPlayers
                end
                
                local function teleportToRandomReversedPlayer()
                    local reversedPlayers = getReversedPlayers()
                    
                    if #reversedPlayers > 0 then
                        local targetPlayer = reversedPlayers[math.random(1, #reversedPlayers)]
                        
                        if targetPlayer and targetPlayer.Character then
                            local targetHRP = targetPlayer.Character:FindFirstChild("HumanoidRootPart")
                            local localHRP = ReverseLocalPlayer.Character and ReverseLocalPlayer.Character:FindFirstChild("HumanoidRootPart")
                            
                            if targetHRP and localHRP then
                                localHRP.CFrame = targetHRP.CFrame
                                return true
                            end
                        end
                    end
                    return false
                end
                
                local function fireGeneralHitOnPlayer(player)
                    if player and player.Character then
                        local rightArm = player.Character:FindFirstChild("Right Arm") or player.Character:FindFirstChild("RightHand")
                        if rightArm then
                            pcall(function()
                                ReverseReplicatedStorage:WaitForChild("b"):FireServer(rightArm)
                            end)
                        end
                    end
                end
                
                local function fireGeneralHitOnAllReversedPlayers()
                    local reversedPlayers = getReversedPlayers()
                    for _, player in ipairs(reversedPlayers) do
                        fireGeneralHitOnPlayer(player)
                    end
                end
                
                while AutoSlapReverse do
                    local reversedPlayers = getReversedPlayers()
                    
                    if #reversedPlayers > 0 then
                        teleportToRandomReversedPlayer()
                        task.wait(0.05)
                        fireGeneralHitOnAllReversedPlayers()
                    end
                    
                    task.wait(0.05)
                    if Fluent.Unloaded then break end
                end
            end)
            
            Fluent:Notify({
                Title = "Auto Slap Reverse",
                Content = "Targeting players with Reversed!",
                Duration = 3
            })
        end
    end
})

local helperSection = Tabs.Reverse:AddSection("Helper")

Tabs.Reverse:AddToggle("AutoReverseAbility", {
    Title = "Auto Use Reverse's Ability (Each 6.7 Sec)",
    Description = "Automatically uses Reverse's ability",
    Default = false,
    Callback = function(Value)
        local AutoRevAbility = Value
        local abilityRemote = nil
        local loopConnection = nil
        
        if Value then
            pcall(function()
                abilityRemote = game:GetService("ReplicatedStorage"):WaitForChild("ReverseAbility")
            end)
            
            pcall(function()
                if abilityRemote then
                    abilityRemote:FireServer()
                end
            end)
            
            loopConnection = task.spawn(function()
                while AutoRevAbility do
                    task.wait(6.7)
                    pcall(function()
                        if abilityRemote then
                            abilityRemote:FireServer()
                        else
                            abilityRemote = game:GetService("ReplicatedStorage"):FindFirstChild("ReverseAbility")
                            if abilityRemote then
                                abilityRemote:FireServer()
                            end
                        end
                    end)
                    if Fluent.Unloaded then break end
                end
            end)
            
            Fluent:Notify({
                Title = "Auto Reverse Ability",
                Content = "Auto using Reverse ability every 6 seconds!",
                Duration = 3
            })
        else
            if loopConnection then
                task.cancel(loopConnection)
            end
        end
    end
})

-- ===============================
-- ENGINEER TAB
-- ===============================
Tabs.Engineer:AddToggle("EngineerSlapFarm", {
    Title = "Engineer Slap Farm",
    Description = "Teleports to turret for Engineer slap farming",
    Default = false,
    Callback = function(Value)
        local EngineerFarm = Value
        
        if EngineerFarm then
            task.spawn(function()
                while EngineerFarm do
                    local turret = workspace:FindFirstChild("Turret")
                    if turret and turret:FindFirstChild("HingePart") and turret.HingePart:FindFirstChild("Glove") then
                        game.Players.LocalPlayer.Character.HumanoidRootPart.CFrame = turret.HingePart.Glove.CFrame
                    end
                    task.wait()
                    if Fluent.Unloaded then break end
                end
            end)
        end
    end
})

-- ===============================
-- EL GATO TAB
-- ===============================
Tabs.ElGato:AddSection("Slap Farmer")

Tabs.ElGato:AddToggle("AutoSlapElGato", {
    Title = "Auto Slap el gato With Default Glove",
    Description = "Automatically slaps players with el gato glove",
    Default = false,
    Callback = function(Value)
        local AutoSlapElGato = Value
        
        if Value then
            task.spawn(function()
                local EG_LocalPlayer = game.Players.LocalPlayer
                local EG_ReplicatedStorage = game:GetService("ReplicatedStorage")
                
                local leaderstats = EG_LocalPlayer:FindFirstChild("leaderstats")
                local glove = leaderstats and leaderstats:FindFirstChild("Glove")
                
                if not (glove and glove.Value == "el gato") then
                    Fluent:Notify({
                        Title = "Error",
                        Content = "Please equip el gato glove first!",
                        Duration = 3
                    })
                    return
                end
                
                local function getReversedPlayers()
                    local reversedPlayers = {}
                    
                    for _, player in ipairs(game.Players:GetPlayers()) do
                        if player ~= EG_LocalPlayer then
                            local playerModel = workspace:FindFirstChild(player.Name)
                            if playerModel then
                                local reversedBool = playerModel:FindFirstChild("Reversed")
                                if reversedBool and reversedBool:IsA("BoolValue") and reversedBool.Value == true then
                                    table.insert(reversedPlayers, player)
                                end
                            end
                        end
                    end
                    
                    return reversedPlayers
                end
                
                local function teleportToRandomReversedPlayer()
                    local reversedPlayers = getReversedPlayers()
                    
                    if #reversedPlayers > 0 then
                        local targetPlayer = reversedPlayers[math.random(1, #reversedPlayers)]
                        
                        if targetPlayer and targetPlayer.Character then
                            local targetHRP = targetPlayer.Character:FindFirstChild("HumanoidRootPart")
                            local localHRP = EG_LocalPlayer.Character and EG_LocalPlayer.Character:FindFirstChild("HumanoidRootPart")
                            
                            if targetHRP and localHRP then
                                localHRP.CFrame = targetHRP.CFrame
                                return true
                            end
                        end
                    end
                    return false
                end
                
                local function fireGeneralHitOnPlayer(player)
                    if player and player.Character then
                        local rightArm = player.Character:FindFirstChild("Right Arm") or player.Character:FindFirstChild("RightHand")
                        if rightArm then
                            pcall(function()
                                local generalHit = EG_ReplicatedStorage:FindFirstChild("GeneralHit")
                                if generalHit then
                                    generalHit:FireServer(rightArm)
                                end
                            end)
                        end
                    end
                end
                
                local function fireGeneralHitOnAllReversedPlayers()
                    local reversedPlayers = getReversedPlayers()
                    for _, player in ipairs(reversedPlayers) do
                        fireGeneralHitOnPlayer(player)
                    end
                end
                
                while AutoSlapElGato do
                    local reversedPlayers = getReversedPlayers()
                    
                    if #reversedPlayers > 0 then
                        teleportToRandomReversedPlayer()
                        task.wait(0.05)
                        fireGeneralHitOnAllReversedPlayers()
                    end
                    
                    task.wait(0.05)
                    if Fluent.Unloaded then break end
                end
            end)
            
            Fluent:Notify({
                Title = "Auto Slap el gato",
                Content = "Targeting players with Reversed!",
                Duration = 3
            })
        end
    end
})

local elGatoHelperSection = Tabs.ElGato:AddSection("Helper")

Tabs.ElGato:AddToggle("AutoElGatoAbility", {
    Title = "Auto Use el gato's Ability (Each 7.1 Sec)",
    Description = "Automatically uses el gato's ability",
    Default = false,
    Callback = function(Value)
        local AutoEGAbility = Value
        local abilityRemote = nil
        local loopConnection = nil
        
        if Value then
            pcall(function()
                abilityRemote = game:GetService("ReplicatedStorage"):WaitForChild("GeneralAbility")
            end)
            
            pcall(function()
                if abilityRemote then
                    abilityRemote:FireServer()
                end
            end)
            
            loopConnection = task.spawn(function()
                while AutoEGAbility do
                    task.wait(7.1)
                    pcall(function()
                        if abilityRemote then
                            abilityRemote:FireServer()
                        else
                            abilityRemote = game:GetService("ReplicatedStorage"):FindFirstChild("GeneralAbility")
                            if abilityRemote then
                                abilityRemote:FireServer()
                            end
                        end
                    end)
                    if Fluent.Unloaded then break end
                end
            end)
            
            Fluent:Notify({
                Title = "Auto General Ability",
                Content = "Auto using General ability every 7.1 seconds!",
                Duration = 3
            })
        else
            if loopConnection then
                task.cancel(loopConnection)
            end
        end
    end
})

-- ===============================
-- ANGLER TAB
-- ===============================
Tabs.Angler:AddToggle("AutoFish", {
    Title = "Angler Slap Farm (Auto Fishing)",
    Description = "Automatically casts fishing line",
    Default = false,
    Callback = function(Value)
        local ReplicatedStorage = game:GetService("ReplicatedStorage")
        local FishermanEvent = ReplicatedStorage:WaitForChild("Events"):WaitForChild("Fisherman")
        local running = Value
        
        local function autoFish()
            while running do
                task.wait(8)
                local args = { "cast" }
                FishermanEvent:FireServer(unpack(args))
                if Fluent.Unloaded then break end
            end
        end
        
        if Value then
            task.spawn(autoFish)
        end
    end
})

-- ===============================
-- BOUNTY TAB
-- ===============================
Tabs.Bounty:AddToggle("BountySlapFarm", {
    Title = "Bounty Slap Farm",
    Description = "Automatically teleports and slaps players",
    Default = false,
    Callback = function(State)
        local BountySlapRunning = State
        
        local function teleportToRandomPlayer()
            local playerList = Players:GetPlayers()
            if #playerList > 1 then
                local randomPlayer
                repeat
                    randomPlayer = playerList[math.random(1, #playerList)]
                until randomPlayer ~= localPlayer
                
                local char = randomPlayer.Character
                if char and char:FindFirstChild("HumanoidRootPart") then
                    local localHRP = localPlayer.Character and localPlayer.Character:FindFirstChild("HumanoidRootPart")
                    if localHRP then
                        localHRP.CFrame = char.HumanoidRootPart.CFrame
                    end
                end
            end
        end
        
        local function fireGeneralHitForAllPlayers()
            for _, player in pairs(Players:GetPlayers()) do
                if player.Character then
                    local rightArm = player.Character:FindFirstChild("Right Arm") or player.Character:FindFirstChild("RightHand")
                    if rightArm then
                        pcall(function()
                            ReplicatedStorage.GeneralHit:FireServer(rightArm)
                        end)
                    end
                end
            end
        end
        
        local function runAutoSlapBountyLoop()
            if localPlayer.leaderstats.Glove.Value ~= "Bounty" then
                Fluent:Notify({
                    Title = "Error",
                    Content = "Equip Bounty glove.",
                    Duration = 3
                })
                return
            end
            
            while BountySlapRunning do
                wait(0.5)
                teleportToRandomPlayer()
                fireGeneralHitForAllPlayers()
                if Fluent.Unloaded then break end
            end
        end
        
        if State then
            task.spawn(runAutoSlapBountyLoop)
        end
    end
})

Tabs.Bounty:AddButton({
    Title = "Bounty Slap Farm 2 (Slap Aura)",
    Description = "Activates continuous slapping with Killstreak glove",
    Callback = function()
        if game.Players.LocalPlayer.leaderstats.Glove.Value == "Killstreak" then
            task.spawn(function()
                while true do
                    for _, player in pairs(Players:GetPlayers()) do
                        if player.Character and player.Character:FindFirstChild("Right Arm") then
                            local args = { player.Character["Right Arm"] }
                            pcall(function()
                                ReplicatedStorage.GeneralHit:FireServer(unpack(args))
                            end)
                        end
                    end
                    wait(0.5)
                    if Fluent.Unloaded then break end
                end
            end)
        else
            Fluent:Notify({
                Title = "Error",
                Content = "Equip Killstreak",
                Duration = 3
            })
        end
    end
})

-- Load the code from external source (you can change the URL)
loadstring(game:HttpGet("https://raw.githubusercontent.com/Scripter-Coder/Scripter-Coder/refs/heads/main/Slap%20Battles/Auto%20Cards%20Giver"))()

-- ==========================================
-- TOGGLE: Auto Give Yourself Cards
-- ==========================================
Tabs.Bounty:AddToggle("AutoGiveYourselfCards", {
    Title = "Auto Give Yourself Cards",
    Default = false,
    Callback = function(Value)
        if Value then
            if _G.AutoGiveCards then
                _G.AutoGiveCards.Start()
            end
        else
            if _G.AutoGiveCards then
                _G.AutoGiveCards.Stop()
            end
        end
    end
})

-- ==========================================
-- PLATFORM SETTINGS (Define these first)
-- ==========================================
local PLATFORM_POSITION = Vector3.new(-103, 3000000, 144.5)
local PLATFORM_SIZE = Vector3.new(500, 1, 500)

-- ==========================================
-- VARIABLES
-- ==========================================
local safespotPart = nil

-- ==========================================
-- TOGGLE: Create Safespot
-- ==========================================
Tabs.Bounty:AddToggle("CreateSafespot", {
    Title = "Create Safespot",
    Default = false,
    Callback = function(Value)
        if Value then
            -- Remove existing safespot if any
            if safespotPart then
                safespotPart:Destroy()
                safespotPart = nil
            end
            
            -- Create safespot at the platform position
            safespotPart = Instance.new("Part")
            safespotPart.Name = "Safespot"
            safespotPart.Size = PLATFORM_SIZE
            safespotPart.Position = PLATFORM_POSITION
            safespotPart.Anchored = true
            safespotPart.CanCollide = true
            safespotPart.BrickColor = BrickColor.new("Bright green")
            safespotPart.Material = Enum.Material.Neon
            safespotPart.Transparency = 0.3
            safespotPart.Parent = workspace
            
            Fluent:Notify({
                Title = "Safespot",
                Content = "Safespot created at platform position!",
                Duration = 3
            })
        else
            -- Remove safespot
            if safespotPart then
                safespotPart:Destroy()
                safespotPart = nil
                
                Fluent:Notify({
                    Title = "Safespot",
                    Content = "Safespot removed!",
                    Duration = 3
                })
            end
        end
    end
})

-- ==========================================
-- BUTTON: Teleport To Safespot
-- ==========================================
Tabs.Bounty:AddButton({
    Title = "Teleport To Safespot",
    Callback = function()
        if safespotPart then
            local character = game.Players.LocalPlayer.Character
            if character then
                local hrp = character:FindFirstChild("HumanoidRootPart")
                if hrp then
                    hrp.CFrame = CFrame.new(PLATFORM_POSITION + Vector3.new(0, (PLATFORM_SIZE.Y / 2) + 4, 0))
                    
                    Fluent:Notify({
                        Title = "Teleport",
                        Content = "Teleported to safespot!",
                        Duration = 2
                    })
                end
            end
        else
            Fluent:Notify({
                Title = "Error",
                Content = "No safespot created! Turn on 'Create Safespot' first.",
                Duration = 3
            })
        end
    end
})

-- ==========================================
-- CLEANUP ON CHARACTER RESPAWN
-- ==========================================
game.Players.LocalPlayer.CharacterAdded:Connect(function()
    if _G.AutoGiveCards then
        _G.AutoGiveCards.Stop()
    end
end)

-- Cleanup when script stops
game.Players.LocalPlayer.OnTeleport:Connect(function()
    if _G.AutoGiveCards then
        _G.AutoGiveCards.Stop()
    end
end)

-- ===============================
-- DEBUG TAB
-- ===============================
local DebugPlayersList = {}
local DebugSelectedPlayer = nil
local DebugActiveToggles = {}

local function RefreshDebugPlayersList()
    DebugPlayersList = {}
    for _, player in pairs(Players:GetPlayers()) do
        if player ~= localPlayer then
            table.insert(DebugPlayersList, player.Name)
        end
    end
    return DebugPlayersList
end

local function GetPlayerHitbox(playerName)
    local hitboxName = playerName .. "_hitbox"
    local hitbox = workspace:FindFirstChild(hitboxName)
    if hitbox and hitbox:IsA("Part") then
        return hitbox
    end
    return nil
end

local function GetLocalPlayerHitbox()
    local hitboxName = localPlayer.Name .. "_hitbox"
    local hitbox = workspace:FindFirstChild(hitboxName)
    if hitbox and hitbox:IsA("Part") then
        return hitbox
    end
    return nil
end

local function GetAnyHitbox()
    for _, obj in pairs(workspace:GetChildren()) do
        if obj:IsA("Part") and string.match(obj.Name, "_hitbox$") then
            return obj
        end
    end
    return nil
end

local function FlyToTarget(targetHitbox)
    local character = localPlayer.Character
    if not character then return end
    
    local hrp = character:FindFirstChild("HumanoidRootPart")
    if not hrp then return end
    
    for _, obj in pairs(hrp:GetChildren()) do
        if obj:IsA("BodyVelocity") then
            obj:Destroy()
        end
    end
    
    local bodyVelocity = Instance.new("BodyVelocity")
    bodyVelocity.Velocity = Vector3.new(0, 0, 0)
    bodyVelocity.MaxForce = Vector3.new(50000, 50000, 50000)
    bodyVelocity.Parent = hrp
    
    local direction = (targetHitbox.Position - hrp.Position).Unit
    local distance = (targetHitbox.Position - hrp.Position).Magnitude
    local speed = 50 + (distance * 2)
    bodyVelocity.Velocity = direction * speed
    
    local connection
    connection = RunService.Heartbeat:Connect(function()
        local currentDistance = (targetHitbox.Position - hrp.Position).Magnitude
        if currentDistance < 10 then
            bodyVelocity:Destroy()
            connection:Disconnect()
            hrp.CFrame = targetHitbox.CFrame + Vector3.new(0, 0, 0)
        end
        if Fluent.Unloaded then
            bodyVelocity:Destroy()
            if connection then connection:Disconnect() end
        end
    end)
    
    delay(3, function()
        if bodyVelocity and bodyVelocity.Parent then
            bodyVelocity:Destroy()
        end
        if connection then
            connection:Disconnect()
        end
    end)
end

local debugSelectionSection = Tabs.Debug:AddSection("Player Selection")

local debugDropdown = Tabs.Debug:AddDropdown("DebugPlayerDropdown", {
    Title = "Select Player",
    Values = RefreshDebugPlayersList(),
    Default = "",
    Callback = function(Value)
        DebugSelectedPlayer = Value
    end
})

Tabs.Debug:AddButton({
    Title = "Refresh Players List",
    Description = "Updates the player dropdown list",
    Callback = function()
        debugDropdown:SetValues(RefreshDebugPlayersList())
    end
})

Tabs.Debug:AddToggle("AutoSpawnCube", {
    Title = "Auto Spawn Cube",
    Description = "Automatically spawns the debug cube",
    Default = false,
    Callback = function(Value)
        DebugActiveToggles["AutoSpawn"] = Value
        if Value then
            task.spawn(function()
                local DebugEvent = ReplicatedStorage:WaitForChild("Events"):WaitForChild("Debug")
                
                while DebugActiveToggles["AutoSpawn"] do
                    local localHitbox = GetLocalPlayerHitbox()
                    if not localHitbox then
                        DebugEvent:FireServer()
                    end
                    task.wait(0.1)
                    if Fluent.Unloaded then break end
                end
            end)
        end
    end
})

local debugTeleportSection = Tabs.Debug:AddSection("Auto Teleport")

Tabs.Debug:AddToggle("AutoTouchSelectedCube", {
    Title = "Auto Touch Cube's Selected Player",
    Description = "Automatically flies to the selected player's cube",
    Default = false,
    Callback = function(Value)
        DebugActiveToggles["TeleportToSelected"] = Value
        if Value then
            task.spawn(function()
                while DebugActiveToggles["TeleportToSelected"] and DebugSelectedPlayer do
                    local targetHitbox = GetPlayerHitbox(DebugSelectedPlayer)
                    if targetHitbox then
                        FlyToTarget(targetHitbox)
                    end
                    task.wait(0.1)
                    if Fluent.Unloaded then break end
                end
            end)
        end
    end
})

Tabs.Debug:AddToggle("AutoTouchAnyCube", {
    Title = "Auto Touch To Any Cube",
    Description = "Automatically flies to any available cube",
    Default = false,
    Callback = function(Value)
        DebugActiveToggles["TeleportToAny"] = Value
        if Value then
            task.spawn(function()
                while DebugActiveToggles["TeleportToAny"] do
                    local anyHitbox = GetAnyHitbox()
                    if anyHitbox then
                        FlyToTarget(anyHitbox)
                    end
                    task.wait(0.1)
                    if Fluent.Unloaded then break end
                end
            end)
        end
    end
})

-- ===============================
-- SETTINGS TAB (VALUE CHANGER)
-- ===============================
local SlapChanger = nil
local nullshardChanger = nil

local valueSection = Tabs.Settings:AddSection("Value Changer")

Tabs.Settings:AddInput("SlapInput", {
    Title = "How Many Slaps? (Visual)",
    Default = "",
    Placeholder = "Enter number",
    Numeric = true,
    Finished = false,
    Callback = function(Value)
        local slapNumber = tonumber(Value)
        if slapNumber and slapNumber > 0 then
            SlapChanger = slapNumber
            Fluent:Notify({
                Title = "Success",
                Content = "Slap value set to: " .. slapNumber,
                Duration = 2
            })
        else
            Fluent:Notify({
                Title = "Error",
                Content = "Type a correct number (greater than 0)!",
                Duration = 3
            })
        end
    end
})

Tabs.Settings:AddButton({
    Title = "Set Slaps (Visual)",
    Description = "Sets your slap count visually",
    Callback = function()
        if SlapChanger then
            local success, err = pcall(function()
                game.Players.LocalPlayer.leaderstats.Slaps.Value = SlapChanger
            end)
            if success then
                Fluent:Notify({
                    Title = "Success",
                    Content = "Slaps set to: " .. SlapChanger,
                    Duration = 3
                })
            else
                Fluent:Notify({
                    Title = "Error",
                    Content = "Failed to set slaps: " .. tostring(err),
                    Duration = 3
                })
            end
        else
            Fluent:Notify({
                Title = "Error",
                Content = "Please enter a valid number of slaps first!",
                Duration = 3
            })
        end
    end
})

Tabs.Settings:AddInput("NullShardInput", {
    Title = "How Many Null Shards? (Visual)",
    Default = "",
    Placeholder = "Enter number",
    Numeric = true,
    Finished = false,
    Callback = function(Value)
        local nullshardNumber = tonumber(Value)
        if nullshardNumber and nullshardNumber >= 0 then
            nullshardChanger = tostring(nullshardNumber)
            Fluent:Notify({
                Title = "Success",
                Content = "Null Shards value set to: " .. nullshardNumber,
                Duration = 2
            })
        else
            Fluent:Notify({
                Title = "Error",
                Content = "Type a correct number (0 or greater)!",
                Duration = 3
            })
        end
    end
})

Tabs.Settings:AddButton({
    Title = "Set Null Shards (Visual)",
    Description = "Sets your Null Shards count visually",
    Callback = function()
        if nullshardChanger then
            local playerName = game.Players.LocalPlayer.Name
            local ReplicatedStorage = game:GetService("ReplicatedStorage")
            
            local playerData = ReplicatedStorage:FindFirstChild("PlayerData")
            if not playerData then
                Fluent:Notify({
                    Title = "Error",
                    Content = "PlayerData folder not found!",
                    Duration = 3
                })
                return
            end
            
            local playerFolder = playerData:FindFirstChild(playerName)
            if not playerFolder then
                Fluent:Notify({
                    Title = "Error",
                    Content = "Player folder not found!",
                    Duration = 3
                })
                return
            end
            
            local nullShardsValue = playerFolder:FindFirstChild("NullShards")
            if not nullShardsValue then
                Fluent:Notify({
                    Title = "Error",
                    Content = "NullShards value not found!",
                    Duration = 3
                })
                return
            end
            
            if nullShardsValue.ClassName ~= "StringValue" then
                Fluent:Notify({
                    Title = "Error",
                    Content = "NullShards is not a StringValue!",
                    Duration = 3
                })
                return
            end
            
            local success, err = pcall(function()
                local oldValue = nullShardsValue.Value
                nullShardsValue.Value = nullshardChanger
            end)
            
            if success then
                Fluent:Notify({
                    Title = "Success",
                    Content = "Null Shards set to: " .. nullshardChanger,
                    Duration = 3
                })
            else
                Fluent:Notify({
                    Title = "Error",
                    Content = "Failed to set Null Shards: " .. tostring(err),
                    Duration = 3
                })
            end
        else
            Fluent:Notify({
                Title = "Error",
                Content = "Please enter a valid number first!",
                Duration = 3
            })
        end
    end
})

local badgeSection = Tabs.Settings:AddSection("Stuff Giver")

Tabs.Settings:AddButton({
    Title = "Give All Badges (Visual)",
    Description = "Gives you all badges (visual only)",
    Callback = function()
        loadstring(game:HttpGet('https://raw.githubusercontent.com/Pro666Pro/getallbadges_fake/main/main.lua'))()
    end
})

Tabs.Settings:AddButton({
    Title = "Give All Emotes (Free + Fe)",
    Description = "Gives you all emotes",
    Callback = function()
        loadstring(game:HttpGet("https://raw.githubusercontent.com/KietVN02202/KietVN02202/refs/heads/main/FeFreeEmote.lua"))()
    end
})

-- Death Sound Variables
local deathSoundEnabled = false
local currentSoundId = ""
local monitorConnection = nil
local childAddedConnections = {}

local function cleanupConnections()
    if monitorConnection then
        monitorConnection:Disconnect()
        monitorConnection = nil
    end
    
    for _, conn in pairs(childAddedConnections) do
        pcall(function()
            conn:Disconnect()
        end)
    end
    childAddedConnections = {}
end

local function modifyDeathSounds(character)
    if not deathSoundEnabled then return end
    if not character then return end
    
    local torso = character:FindFirstChild("Torso") or character:FindFirstChild("UpperTorso")
    if not torso then return end
    
    for _, sound in pairs(torso:GetChildren()) do
        if sound:IsA("Sound") then
            local soundName = sound.Name:lower()
            if soundName:find("death") or soundName:find("die") then
                pcall(function()
                    if sound.SoundId ~= currentSoundId then
                        sound.SoundId = currentSoundId
                    end
                end)
            end
        end
    end
end

local function setupSoundListener(character)
    if not character then return end
    
    local torso = character:FindFirstChild("Torso") or character:FindFirstChild("UpperTorso")
    if not torso then return end
    
    local connection = torso.ChildAdded:Connect(function(child)
        if child:IsA("Sound") and deathSoundEnabled then
            local soundName = child.Name:lower()
            if soundName:find("death") or soundName:find("die") then
                task.wait(0.1)
                pcall(function()
                    child.SoundId = currentSoundId
                end)
            end
        end
    end)
    
    table.insert(childAddedConnections, connection)
end

local function startMonitoring()
    if monitorConnection then return end
    
    monitorConnection = game:GetService("RunService").Heartbeat:Connect(function()
        if deathSoundEnabled then
            local character = game.Players.LocalPlayer.Character
            if character then
                modifyDeathSounds(character)
            end
        end
    end)
    
    local character = game.Players.LocalPlayer.Character
    if character then
        setupSoundListener(character)
    end
end

local function stopMonitoring()
    cleanupConnections()
end

local function updateAllDeathSounds()
    if not deathSoundEnabled then return end
    
    local character = game.Players.LocalPlayer.Character
    if character then
        local torso = character:FindFirstChild("Torso") or character:FindFirstChild("UpperTorso")
        if torso then
            for _, sound in pairs(torso:GetChildren()) do
                if sound:IsA("Sound") then
                    local soundName = sound.Name:lower()
                    if soundName:find("death") or soundName:find("die") then
                        pcall(function()
                            sound.SoundId = currentSoundId
                        end)
                    end
                end
            end
        end
    end
end

Tabs.Settings:AddParagraph({
    Title = "Custom Sound ID:",
    Content = "Enter your sound ID below (format: rbxassetid://1234567890)"
})

Tabs.Settings:AddInput("SoundIDInput", {
    Title = "Put Custom Sound ID",
    Default = "",
    Placeholder = "rbxassetid://1234567890",
    Callback = function(value)
        if value and value ~= "" then
            if not value:find("rbxassetid://") then
                currentSoundId = "rbxassetid://" .. value
            else
                currentSoundId = value
            end
            
            if deathSoundEnabled then
                updateAllDeathSounds()
            end
            
            Fluent:Notify({
                Title = "Sound ID Updated",
                Content = "New sound ID set",
                Duration = 2
            })
        end
    end
})

Tabs.Settings:AddToggle("AutoApplyDeathSound", {
    Title = "Auto Apply Death Sound",
    Description = "Changes death sounds to your custom sound",
    Default = false,
    Callback = function(Value)
        deathSoundEnabled = Value
        
        if Value then
            startMonitoring()
            updateAllDeathSounds()
            Fluent:Notify({
                Title = "Death Sound Modifier",
                Content = "Enabled!",
                Duration = 3
            })
        else
            stopMonitoring()
        end
    end
})

game.Players.LocalPlayer.CharacterAdded:Connect(function(character)
    task.wait(0.5)
    
    if deathSoundEnabled then
        setupSoundListener(character)
        task.wait(0.2)
        modifyDeathSounds(character)
    end
end)

startMonitoring()

Tabs.Settings:AddToggle("AntiAfkSettings", {
    Title = "Anti Afk",
    Description = "Prevents being kicked for inactivity",
    Default = false,
    Callback = function(Value)
        local VirtualUser = game:GetService('VirtualUser')
        game:GetService('Players').LocalPlayer.Idled:Connect(function()
            VirtualUser:CaptureController()
            VirtualUser:ClickButton2(Vector2.new())
        end)
    end
})

-- Nametag Variables
local nametagText = ""
local autoNametag = false

Tabs.Settings:AddInput("NametagInput", {
    Title = "Type Your Own Any Nametag",
    Default = "",
    Placeholder = "Enter nametag text",
    Callback = function(Value)
        nametagText = Value
    end
})

Tabs.Settings:AddButton({
    Title = "Change Nametag",
    Description = "Changes your nametag text",
    Callback = function()
        if nametagText == "" then
            Fluent:Notify({
                Title = "Error",
                Content = "Please enter text in the textbox first!",
                Duration = 3
            })
            return
        end
        
        local character = player.Character
        if character then
            local nametag = character:FindFirstChild("Head") and character.Head:FindFirstChild("Nametag")
            if nametag then
                local topLabel = nametag:FindFirstChild("Labels") and nametag.Labels:FindFirstChild("TopLabel")
                if topLabel then
                    topLabel.Text = nametagText
                    Fluent:Notify({
                        Title = "Success",
                        Content = "Nametag changed to: " .. nametagText,
                        Duration = 3
                    })
                else
                    Fluent:Notify({
                        Title = "Error",
                        Content = "TopLabel not found!",
                        Duration = 3
                    })
                end
            else
                Fluent:Notify({
                    Title = "Error",
                    Content = "Nametag not found!",
                    Duration = 3
                })
            end
        else
            Fluent:Notify({
                Title = "Error",
                Content = "Character not found!",
                Duration = 3
            })
        end
    end
})

Tabs.Settings:AddToggle("AutoChangeNametag", {
    Title = "Auto Change Nametag",
    Description = "Automatically reapplies your nametag",
    Default = false,
    Callback = function(Value)
        autoNametag = Value
        if Value then
            task.spawn(function()
                while autoNametag do
                    if nametagText ~= "" then
                        local character = player.Character
                        if character then
                            local nametag = character:FindFirstChild("Head") and character.Head:FindFirstChild("Nametag")
                            if nametag then
                                local topLabel = nametag:FindFirstChild("Labels") and nametag.Labels:FindFirstChild("TopLabel")
                                if topLabel then
                                    topLabel.Text = nametagText
                                end
                            end
                        end
                    end
                    task.wait(0.01)
                    if Fluent.Unloaded then break end
                end
            end)
        end
    end
})

player.CharacterAdded:Connect(function(character)
    if autoNametag and nametagText ~= "" then
        wait(2)
        local nametag = character:FindFirstChild("Head") and character.Head:FindFirstChild("Nametag")
        if nametag then
            local topLabel = nametag:FindFirstChild("Labels") and nametag.Labels:FindFirstChild("TopLabel")
            if topLabel then
                topLabel.Text = nametagText
            end
        end
    end
end)

-- ==========================================
-- BACKPACK V2 TOGGLE
-- ==========================================

_G.BackpackV2 = false
_G.BackpackV2Loop = true
_G.ConnectFun = _G.ConnectFun or {}

-- Store original positions
if _G.Backpack == nil then
    _G.Backpack = {
        ["Old"] = {
            ["Hotbar"] = game:GetService("CoreGui").RobloxGui.Backpack.Hotbar.Position,
            ["Inventory"] = game:GetService("CoreGui").RobloxGui.Backpack.Inventory.Position,
            ["Backpack Number"] = {}
        },
        ["New"] = {
            ["Hotbar"] = (game:GetService("CoreGui").RobloxGui.Backpack.Hotbar.Position + UDim2.new(0, 0, 0, -10)),
            ["Inventory"] = (game:GetService("CoreGui").RobloxGui.Backpack.Inventory.Position + UDim2.new(0, 0, 0, -10)),
            ["Backpack Number"] = {}
        }
    }
    wait()
    for i, v in pairs(game:GetService("CoreGui").RobloxGui.Backpack.Hotbar:GetChildren()) do
        if v:IsA("TextButton") then
            _G.Backpack["Old"]["Backpack Number"][v.Name] = v.Position
            _G.Backpack["New"]["Backpack Number"][v.Name] = UDim2.new(0, 3, 0, 3)
        end
    end
end

-- Main loop for backpack styling
spawn(function()
    while _G.BackpackV2Loop do
        if game:GetService("CoreGui").RobloxGui.Backpack:FindFirstChild("Hotbar") then
            game:GetService("CoreGui").RobloxGui.Backpack.Hotbar.Position = (_G.BackpackV2 == true and (_G.Backpack["New"]["Hotbar"]) or (_G.Backpack["Old"]["Hotbar"]))
        end
        if game:GetService("CoreGui").RobloxGui.Backpack:FindFirstChild("Inventory") then
            game:GetService("CoreGui").RobloxGui.Backpack.Inventory.Position = (_G.BackpackV2 == true and (_G.Backpack["New"]["Inventory"]) or (_G.Backpack["Old"]["Inventory"]))
        end
        for i, v in pairs(game:GetService("CoreGui").RobloxGui.Backpack.Hotbar:GetChildren()) do
            if v:IsA("TextButton") and v:FindFirstChild("Number") then
                v.Number.Position = (_G.BackpackV2 == true and (_G.Backpack["New"]["Backpack Number"][v.Name]) or (_G.Backpack["Old"]["Backpack Number"][v.Name]))
            end
        end
        if _G.BackpackV2 == true then
            if game:GetService("CoreGui").RobloxGui.Backpack:FindFirstChild("Inventory").Visible == false then
                if game:GetService("CoreGui").RobloxGui.Backpack:FindFirstChild("Hotbar"):FindFirstChild("UIListLayout") == nil then
                    local UIListLayout = Instance.new("UIListLayout")
                    UIListLayout.HorizontalAlignment = Enum.HorizontalAlignment.Left
                    UIListLayout.Padding = UDim.new(0, 5)
                    UIListLayout.FillDirection = Enum.FillDirection.Horizontal
                    UIListLayout.HorizontalAlignment = Enum.HorizontalAlignment.Center
                    UIListLayout.VerticalAlignment = Enum.VerticalAlignment.Center
                    UIListLayout.Parent = game:GetService("CoreGui").RobloxGui.Backpack.Hotbar
                end
            else
                if game:GetService("CoreGui").RobloxGui.Backpack.Hotbar:FindFirstChild("UIListLayout") then
                    game:GetService("CoreGui").RobloxGui.Backpack.Hotbar:FindFirstChild("UIListLayout"):Destroy()
                end
            end
            for i, v in pairs(game:GetService("CoreGui").RobloxGui.Backpack.Hotbar:GetChildren()) do
                if v:IsA("TextButton") then
                    if v:FindFirstChild("Equipped") then
                        v.Equipped.Visible = false
                    end
                    if v:FindFirstChild("Equipped") and v:FindFirstChild("SelectionObjectClipper") then
                        v.SelectionObjectClipper.Visible = true
                    else
                        v.SelectionObjectClipper.Visible = false
                    end
                    if v:FindFirstChild("UICorner") == nil then
                        local RobloxUi = Instance.new("UICorner", v)
                        RobloxUi.CornerRadius = UDim.new(0, 10)
                    end
                end
            end
            for i, v in pairs(game:GetService("CoreGui").RobloxGui.Backpack.Inventory.ScrollingFrame.UIGridFrame:GetChildren()) do
                if v:IsA("TextButton") then
                    if v:FindFirstChild("Equipped") then
                        v.Equipped.Visible = false
                    end
                    if v:FindFirstChild("Equipped") and v:FindFirstChild("SelectionObjectClipper") then
                        v.SelectionObjectClipper.Visible = true
                    else
                        v.SelectionObjectClipper.Visible = false
                    end
                    if v:FindFirstChild("UICorner") == nil then
                        local RobloxUi = Instance.new("UICorner", v)
                        RobloxUi.CornerRadius = UDim.new(0, 10)
                    end
                end
            end
            for i, v in pairs(game:GetService("CoreGui").RobloxGui.Backpack.Inventory:GetChildren()) do
                if v.Name == "Search" then
                    if v:FindFirstChild("UICorner") == nil then
                        local RobloxUi = Instance.new("UICorner", v)
                        RobloxUi.CornerRadius = UDim.new(0, 10)
                    end
                    if v:FindFirstChild("X") and v["X"]:FindFirstChild("UICorner") == nil then
                        local RobloxUi = Instance.new("UICorner", v["X"])
                        RobloxUi.CornerRadius = UDim.new(0, 10)
                    end
                end
            end
            if game:GetService("CoreGui").RobloxGui.Backpack:FindFirstChild("Inventory") and game:GetService("CoreGui").RobloxGui.Backpack.Inventory:FindFirstChild("UICorner") == nil then
                local RobloxUi = Instance.new("UICorner", game:GetService("CoreGui").RobloxGui.Backpack.Inventory)
                RobloxUi.CornerRadius = UDim.new(0, 10)
            end
        elseif _G.BackpackV2 == false then
            if game:GetService("CoreGui").RobloxGui.Backpack.Hotbar:FindFirstChild("UIListLayout") then
                game:GetService("CoreGui").RobloxGui.Backpack.Hotbar:FindFirstChild("UIListLayout"):Destroy()
            end
            for i, v in pairs(game:GetService("CoreGui").RobloxGui.Backpack.Hotbar:GetChildren()) do
                if v:IsA("TextButton") then
                    if v:FindFirstChild("Equipped") then
                        v.Equipped.Visible = true
                    end
                    if v:FindFirstChild("SelectionObjectClipper") then
                        v.SelectionObjectClipper.Visible = false
                    end
                    if v:FindFirstChild("UICorner") then
                        v:FindFirstChild("UICorner"):Destroy()
                    end
                end
            end
            for i, v in pairs(game:GetService("CoreGui").RobloxGui.Backpack.Inventory.ScrollingFrame.UIGridFrame:GetChildren()) do
                if v:IsA("TextButton") then
                    if v:FindFirstChild("Equipped") then
                        v.Equipped.Visible = true
                    end
                    if v:FindFirstChild("SelectionObjectClipper") then
                        v.SelectionObjectClipper.Visible = false
                    end
                    if v:FindFirstChild("UICorner") then
                        v:FindFirstChild("UICorner"):Destroy()
                    end
                end
            end
            for i, v in pairs(game:GetService("CoreGui").RobloxGui.Backpack.Inventory:GetChildren()) do
                if v.Name == "Search" then
                    if v:FindFirstChild("UICorner") then
                        v:FindFirstChild("UICorner"):Destroy()
                    end
                    if v:FindFirstChild("X") and v["X"]:FindFirstChild("UICorner") then
                        v["X"]:FindFirstChild("UICorner"):Destroy()
                    end
                end
            end
            if game:GetService("CoreGui").RobloxGui.Backpack:FindFirstChild("Inventory") and game:GetService("CoreGui").RobloxGui.Backpack.Inventory:FindFirstChild("UICorner") then
                game:GetService("CoreGui").RobloxGui.Backpack.Inventory:FindFirstChild("UICorner"):Destroy()
            end
        end
        task.wait()
    end
end)

-- Function for backpack slot interactions
local function BackpackV2(v)
    v.MouseEnter:Connect(function()
        if _G.BackpackV2 == true then
            local sound = Instance.new("Sound", workspace)
            sound.SoundId = "rbxassetid://10066942189"
            sound.Volume = 2
            sound.PlaybackSpeed = 1
            sound.PlayOnRemove = true
            sound:Destroy()
            game:GetService("TweenService"):Create(v, TweenInfo.new(0.1, Enum.EasingStyle.Quad, Enum.EasingDirection.Out), {Size = UDim2.new(0, 70, 0, 70)}):Play()
        end
    end)
    v.MouseLeave:Connect(function()
        if _G.BackpackV2 == true then
            local sound = Instance.new("Sound", workspace)
            sound.SoundId = "rbxassetid://10066942189"
            sound.Volume = 2
            sound.PlaybackSpeed = 1
            sound.PlayOnRemove = true
            sound:Destroy()
            game:GetService("TweenService"):Create(v, TweenInfo.new(0.1, Enum.EasingStyle.Quad, Enum.EasingDirection.Out), {Size = UDim2.new(0, 60, 0, 60)}):Play()
        end
    end)
    v.MouseButton1Click:Connect(function()
        if _G.BackpackV2 == true then
            local sound = Instance.new("Sound", workspace)
            sound.SoundId = "rbxassetid://10066936758"
            sound.Volume = 2
            sound.PlaybackSpeed = 1
            sound.PlayOnRemove = true
            sound:Destroy()
            local TweenGui = game:GetService("TweenService"):Create(v, TweenInfo.new(0.2, Enum.EasingStyle.Quad, Enum.EasingDirection.Out), {Size = UDim2.new(0, 70, 0, 70)})
            TweenGui:Play()
            TweenGui.Completed:Connect(function()
                game:GetService("TweenService"):Create(v, TweenInfo.new(0.1, Enum.EasingStyle.Quad, Enum.EasingDirection.Out), {Size = UDim2.new(0, 60, 0, 60)}):Play()
            end)
        end
    end)
end

-- Apply to existing hotbar slots
for i, v in pairs(game:GetService("CoreGui").RobloxGui.Backpack.Hotbar:GetChildren()) do
    if v:IsA("TextButton") then
        BackpackV2(v)
    end
end

-- Apply to new slots when added
table.insert(_G.ConnectFun, game:GetService("CoreGui").RobloxGui.Backpack.Inventory.ScrollingFrame.UIGridFrame.ChildAdded:Connect(function(v)
    if v:IsA("TextButton") then
        BackpackV2(v)
    end
end))

-- ==========================================
-- TOGGLE: Backpack V2
-- ==========================================
Tab:AddToggle("BackpackV2Toggle", {
    Title = "Backpack V2 (Cool Style UI)",
    Default = false,
    Callback = function(Value)
        _G.BackpackV2 = Value
        
        if Value then
            Fluent:Notify({
                Title = "Backpack V2",
                Content = "Backpack V2 Enabled!",
                Duration = 2
            })
        else
            -- do nothing
        end
    end
})

-- Slap Aura Section
local slapAuraSection = Tabs.Settings:AddSection("Slap Aura")

-- Optimized Slap Aura
do
    local slapAuraConnection
    local lastHitTime = 0
    local currentCooldown = 0.5
    local isSlapAuraEnabled = false
    local auraLocalPlayer = Players.LocalPlayer
    
    local gloveHits = {
        ["Default"] = ReplicatedStorage.b,
        ["Dual"] = ReplicatedStorage.GeneralHit,
        ["Diamond"] = ReplicatedStorage.DiamondHit,
        ["ZZZZZZZ"] = ReplicatedStorage.ZZZZZZZHit,
        ["Extended"] = ReplicatedStorage.GeneralHit,
        ["Brick"] = ReplicatedStorage.BrickHit,
        ["Snow"] = ReplicatedStorage.SnowHit,
        ["Pull"] = ReplicatedStorage.PullHit,
        ["Flash"] = ReplicatedStorage.FlashHit,
        ["Reverse"] = ReplicatedStorage.ReverseHit,
        ["Killstreak"] = ReplicatedStorage.KSHit,
        ["Reaper"] = ReplicatedStorage.ReaperHit,
        ["Golden"] = ReplicatedStorage.GoldenHit,
        ["God's Hand"] = ReplicatedStorage.Godshand,
        ["GeneralHit"] = ReplicatedStorage.GeneralHit,
        ["b"] = ReplicatedStorage.b,
        ["HtStun"] = ReplicatedStorage.HtStun
    }
    
    setmetatable(gloveHits, {
        __index = function(t, key)
            for _, child in pairs(ReplicatedStorage:GetChildren()) do
                if child:IsA("RemoteEvent") then
                    local childName = child.Name:lower()
                    local gloveName = tostring(key):lower()
                    
                    if childName == gloveName or
                       childName == gloveName .. "hit" or
                       childName == "ht" .. gloveName then
                        rawset(t, key, child)
                        return child
                    end
                end
            end
            return nil
        end
    })
    
    local function getCurrentGlove()
        local success, result = pcall(function()
            if auraLocalPlayer:FindFirstChild("leaderstats") then
                local gloveValue = auraLocalPlayer.leaderstats:FindFirstChild("Glove")
                if gloveValue then
                    return gloveValue.Value
                end
            end
            return "Default"
        end)
        return success and result or "Default"
    end
    
    local function getGloveRemote(gloveName)
        local remote = gloveHits[gloveName]
        if remote and typeof(remote) == "Instance" and remote:IsA("RemoteEvent") then
            return remote
        end
        return gloveHits[gloveName]
    end
    
    Tabs.Settings:AddSlider("SlapAuraSpeed", {
        Title = "Slap Aura Speed",
        Description = "Delay between slaps (seconds)",
        Min = 0.1,
        Max = 2,
        Default = 0.5,
        Rounding = 1,
        Callback = function(Value)
            currentCooldown = Value
        end
    })
    
    Tabs.Settings:AddToggle("SlapAura", {
        Title = "Slap Aura",
        Description = "Automatically slaps nearby players",
        Default = false,
        Callback = function(enabled)
            if isSlapAuraEnabled == enabled then
                return
            end
            
            isSlapAuraEnabled = enabled
            
            if enabled then
                if slapAuraConnection then
                    slapAuraConnection:Disconnect()
                    slapAuraConnection = nil
                end
                
                local currentGlove = getCurrentGlove()
                local remote = getGloveRemote(currentGlove)
                
                if not remote then
                    Fluent:Notify({
                        Title = "Slap Aura Error",
                        Content = "No remote found for glove: " .. currentGlove,
                        Duration = 3
                    })
                    isSlapAuraEnabled = false
                    return
                end
                
                slapAuraConnection = RunService.Heartbeat:Connect(function()
                    local currentTime = tick()
                    
                    if currentTime - lastHitTime < currentCooldown then
                        return
                    end
                    
                    if not isSlapAuraEnabled or not auraLocalPlayer.Character then
                        return
                    end
                    
                    local localChar = auraLocalPlayer.Character
                    local localRoot = localChar:FindFirstChild("HumanoidRootPart")
                    if not localRoot then
                        return
                    end
                    
                    for _, player in ipairs(Players:GetPlayers()) do
                        if player ~= auraLocalPlayer and player.Character then
                            local char = player.Character
                            local humanoid = char:FindFirstChildOfClass("Humanoid")
                            local torso = char:FindFirstChild("Torso") or char:FindFirstChild("UpperTorso")
                            
                            if torso and humanoid and humanoid.Health > 0 then
                                local playerRoot = char:FindFirstChild("HumanoidRootPart")
                                if playerRoot then
                                    local distance = (playerRoot.Position - localRoot.Position).Magnitude
                                    if distance > 40 then
                                        continue
                                    end
                                end
                                
                                local success = pcall(function()
                                    remote:FireServer(torso)
                                end)
                                
                                if success then
                                    lastHitTime = currentTime
                                    break
                                end
                            end
                        end
                    end
                end)
                
                Fluent:Notify({
                    Title = "Slap Aura Enabled",
                    Content = "Glove: " .. currentGlove,
                    Duration = 3
                })
            else
                if slapAuraConnection then
                    slapAuraConnection:Disconnect()
                    slapAuraConnection = nil
                end
                Fluent:Notify({
                    Title = "Slap Aura Disabled",
                    Content = "",
                    Duration = 2
                })
            end
        end
    })
end

-- Ghost On/Off Section
local ghostSection = Tabs.Settings:AddSection("Ghost On/Off")

Tabs.Settings:AddToggle("GhostInvisibility", {
    Title = "Ghost Invisibility",
    Description = "Toggles Ghost glove invisibility",
    Default = false,
    Callback = function(Value)
        local hasGhost = game.Players.LocalPlayer:FindFirstChild("leaderstats") and
                         game.Players.LocalPlayer.leaderstats:FindFirstChild("Glove") and
                         game.Players.LocalPlayer.leaderstats.Glove.Value == "Ghost"
        
        if Value then
            if hasGhost then
                game:GetService("ReplicatedStorage").Ghostinvisibilityactivated:FireServer()
            else
                Fluent:Notify({
                    Title = "Error",
                    Content = "Equip Ghost Glove",
                    Duration = 3
                })
            end
        elseif not Value and hasGhost then
            game:GetService("ReplicatedStorage").Ghostinvisibilitydeactivated:FireServer()
        end
    end
})

-- Server Settings Section
local serverSection = Tabs.Settings:AddSection("Server Settings")

Tabs.Settings:AddButton({
    Title = "Rejoin",
    Description = "Rejoins the server",
    Callback = function()
        loadstring(game:HttpGet("https://raw.githubusercontent.com/Jelly-plays/Rejoin-script/main/obf_L744By559M18BbuseSG6en8r1zL31daK9060LV7WyvmS4bQp92aONWfwRE36FdcZ.lua.txt"))()
    end
})

Tabs.Settings:AddButton({
    Title = "ServerHop (Regular Server)",
    Description = "Hops to a different server",
    Callback = function()
        local PlaceID = game.PlaceId
        local AllIDs = {}
        local foundAnything = ""
        local actualHour = os.date("!*t").hour
        
        local success, data = pcall(function()
            return game:GetService('HttpService'):JSONDecode(readfile("NotSameServers.json"))
        end)
        if success then
            AllIDs = data
        else
            table.insert(AllIDs, actualHour)
            writefile("NotSameServers.json", game:GetService('HttpService'):JSONEncode(AllIDs))
        end
        
        local function TPReturner()
            local Site
            if foundAnything == "" then
                Site = game.HttpService:JSONDecode(game:HttpGet('https://games.roblox.com/v1/games/' .. PlaceID .. '/servers/Public?sortOrder=Asc&limit=100'))
            else
                Site = game.HttpService:JSONDecode(game:HttpGet('https://games.roblox.com/v1/games/' .. PlaceID .. '/servers/Public?sortOrder=Asc&limit=100&cursor=' .. foundAnything))
            end
            
            if Site.nextPageCursor and Site.nextPageCursor ~= "null" and Site.nextPageCursor ~= nil then
                foundAnything = Site.nextPageCursor
            end
            
            for i, v in pairs(Site.data) do
                local Possible = true
                local ID = tostring(v.id)
                if tonumber(v.maxPlayers) > tonumber(v.playing) then
                    for _, Existing in pairs(AllIDs) do
                        if ID == tostring(Existing) then
                            Possible = false
                            break
                        end
                    end
                    if Possible then
                        table.insert(AllIDs, ID)
                        wait()
                        pcall(function()
                            writefile("NotSameServers.json", game:GetService('HttpService'):JSONEncode(AllIDs))
                            wait()
                            game:GetService("TeleportService"):TeleportToPlaceInstance(PlaceID, ID, game.Players.LocalPlayer)
                        end)
                        wait(4)
                        break
                    end
                end
            end
        end
        
        local function Teleport()
            while wait() do
                pcall(function()
                    TPReturner()
                    if foundAnything ~= "" then
                        TPReturner()
                    end
                end)
                if Fluent.Unloaded then break end
            end
        end
        
        Teleport()
    end
})

Tabs.Settings:AddButton({
    Title = "ServerHop (Small Server)",
    Description = "Hops to a server with few players",
    Callback = function()
        local Http = game:GetService("HttpService")
        local TPS = game:GetService("TeleportService")
        local Api = "https://games.roblox.com/v1/games/"
        local place = game.PlaceId
        local serversUrl = Api .. place .. "/servers/Public?sortOrder=Asc&limit=100"
        
        local function ListServers(cursor)
            local Raw = game:HttpGet(serversUrl .. ((cursor and "&cursor=" .. cursor) or ""))
            return Http:JSONDecode(Raw)
        end
        
        local Server, Next
        repeat
            local Servers = ListServers(Next)
            Server = Servers.data[1]
            Next = Servers.nextPageCursor
            if Fluent.Unloaded then break end
        until Server
        
        TPS:TeleportToPlaceInstance(place, Server.id, game:GetService('Players').LocalPlayer)
    end
})

local kickSection = Tabs.Settings:AddSection("Kick/Reset If Stuck")

Tabs.Settings:AddButton({
    Title = "Kick (Yourself)",
    Description = "Kicks yourself from the game",
    Callback = function()
        game.Players.LocalPlayer:Kick("Kicked By Script")
    end
})

Tabs.Settings:AddButton({
    Title = "Reset Player",
    Description = "Resets your character",
    Callback = function()
        local character = game.Players.LocalPlayer.Character
        if character and character:FindFirstChild("Humanoid") then
            character.Humanoid.Health = 0
        end
    end
})

-- Arena Settings Section
local arenaSection = Tabs.Settings:AddSection("Arena Settings")

local AutoEnterActive = false
local AutoEnterMode = "Arena"

Tabs.Settings:AddDropdown("EnterMode", {
    Title = "Enter",
    Values = {"Arena", "Arena Default"},
    Default = "Arena",
    Callback = function(Value)
        AutoEnterMode = Value
    end
})

Tabs.Settings:AddToggle("AutoEnter", {
    Title = "Auto Enter",
    Description = "Automatically enters the arena",
    Default = false,
    Callback = function(Value)
        AutoEnterActive = Value
        
        task.spawn(function()
            while AutoEnterActive do
                local player = game.Players.LocalPlayer
                if not player then 
                    task.wait(0.1) 
                    continue 
                end
                
                local character = player.Character
                if not character or not character.Parent then 
                    task.wait(0.5) 
                    continue 
                end
                
                local hrp = character:FindFirstChild("HumanoidRootPart")
                if not hrp then 
                    task.wait(0.1) 
                    continue 
                end
                
                local entered = character:FindFirstChild("entered")
                
                if not entered then
                    local teleportFolder = workspace:FindFirstChild("Lobby")
                    if not teleportFolder then
                        task.wait(0.5)
                        continue
                    end
                    
                    local teleportPart = nil
                    
                    if AutoEnterMode == "Arena" then
                        teleportPart = teleportFolder:FindFirstChild("Teleport1")
                    elseif AutoEnterMode == "Arena Default" then
                        teleportPart = teleportFolder:FindFirstChild("Teleport2")
                    end
                    
                    if teleportPart then
                        pcall(function()
                            local touchPart = nil
                            
                            if teleportPart:IsA("BasePart") then
                                touchPart = teleportPart
                            else
                                touchPart = teleportPart:FindFirstChildWhichIsA("BasePart")
                            end
                            
                            if touchPart then
                                hrp.CFrame = touchPart.CFrame + Vector3.new(0, 3, 0)
                                task.wait(0.1)
                                hrp.CFrame = touchPart.CFrame
                                task.wait(0.1)
                                
                                for i = 1, 5 do
                                    hrp.CFrame = touchPart.CFrame
                                    task.wait(0.01)
                                end
                                
                                hrp.CFrame = touchPart.CFrame + touchPart.CFrame.LookVector * 3
                                task.wait(0.1)
                                
                                for i = 1, 3 do
                                    hrp.CFrame = touchPart.CFrame - touchPart.CFrame.LookVector * 2
                                    task.wait(0.05)
                                    hrp.CFrame = touchPart.CFrame + touchPart.CFrame.LookVector * 4
                                    task.wait(0.05)
                                end
                            end
                        end)
                    end
                else
                    task.wait(1)
                end
                
                task.wait(0.5)
                if Fluent.Unloaded then break end
            end
        end)
    end
})

Tabs.Settings:AddButton({
    Title = "Teleport To Arena",
    Description = "Teleports to the arena",
    Callback = function()
        local character = game.Players.LocalPlayer.Character
        if character and character:FindFirstChild("HumanoidRootPart") then
            character.HumanoidRootPart.CFrame = CFrame.new()
        end
    end
})

Tabs.Settings:AddButton({
    Title = "Teleport To Default Arena",
    Description = "Teleports to the default arena",
    Callback = function()
        local character = game.Players.LocalPlayer.Character
        if character and character:FindFirstChild("HumanoidRootPart") then
            character.HumanoidRootPart.CFrame = CFrame.new(122, 360, -3)
        end
    end
})

local destroySection = Tabs.Settings:AddSection("Destroy Script")

Tabs.Settings:AddButton({
    Title = "Destroy Gui",
    Description = "Destroys the GUI",
    Callback = function()
        Fluent:Destroy()
    end
})

-- ===============================
-- OTHER SETTINGS TAB
-- ===============================
local otherSection = Tabs.Other:AddSection("Destroy Script")

Tabs.Other:AddButton({
    Title = "Destroy Gui",
    Description = "Destroys the GUI",
    Callback = function()
        Fluent:Destroy()
    end
})

-- ===============================
-- SAVE MANAGER & INTERFACE MANAGER
-- ===============================
SaveManager:SetLibrary(Fluent)
InterfaceManager:SetLibrary(Fluent)

SaveManager:IgnoreThemeSettings()
SaveManager:SetIgnoreIndexes({})

InterfaceManager:SetFolder("ScripterHub")
SaveManager:SetFolder("ScripterHub/SlapBattles-SlapFarm")

InterfaceManager:BuildInterfaceSection(Tabs.Other)
SaveManager:BuildConfigSection(Tabs.Other)

SaveManager:LoadAutoloadConfig()

Fluent:Notify({
    Title = "Scripter Hub",
    Content = "Slap Farm Hub loaded successfully!",
    Duration = 5
})
