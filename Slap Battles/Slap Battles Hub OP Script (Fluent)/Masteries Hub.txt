--skid if u want idc just dont forget about credits or smt
--Webhook
local WebhookScripterHub = loadstring(game:HttpGet("https://raw.githubusercontent.com/Scripter-Coder/Scripter-Coder/refs/heads/main/Universal/Webhook"))()

--Library
local Fluent = loadstring(game:HttpGet("https://github.com/dawid-scripts/Fluent/releases/latest/download/main.lua"))()

--Startup Script Notification
Fluent:Notify({Title = "From Scripter", Content = "Script Loads (Can Take 1-5 Seconds)", Duration = 1})

--If Slap Battles Then
if game.PlaceId == 6403373529 or game.PlaceId == 9015014224 or game.PlaceId == 11520107397 or game.PlaceId == 124596094333302 or game.PlaceId == 7234087065 or game.PlaceId == 9020359053 or game.PlaceId == 9068206286 or game.PlaceId == 9426795465 or game.PlaceId == 9431156611 or game.PlaceId == 11359765900 or game.PlaceId == 11367956190 or game.PlaceId == 11828384869 or game.PlaceId == 12432152490 or game.PlaceId == 12712288037 or game.PlaceId == 12845859004 or game.PlaceId == 13833961666 or game.PlaceId == 15228348051 or game.PlaceId == 15507333474 or game.PlaceId == 16034567693 or game.PlaceId == 17290438723 or game.PlaceId == 18550498098 or game.PlaceId == 18698003301 or game.PlaceId == 74169485398268 or game.PlaceId == 89837553336708 or game.PlaceId == 102908326578005 or game.PlaceId == 103505724406848 or game.PlaceId == 14422118326 or game.PlaceId == 92516899071319 or game.PlaceId == 115782629143468 or game.PlaceId == 95706409702231 or game.PlaceId == 118650724506449 or game.PlaceId == 97220865182663 or game.PlaceId == 98726100529621 or game.PlaceId == 136005148166028 or game.PlaceId == 117232463555132 or game.PlaceId == 129665246576996 or game.PlaceId == 132277598079047 or game.PlaceId == 136690395520488 or game.PlaceId == 86045914443715 or game.PlaceId == 75803343514634 or game.PlaceId == 80420091630966 or game.PlaceId == 98871361390239 or game.PlaceId == 90553746175288 or game.PlaceId == 106620300132058 or game.PlaceId == 94481828238038 or game.PlaceId == 122901288403496 or game.PlaceId == 79885102123162 or game.PlaceId == 86643839793301 or game.PlaceId == 113228834069218 or game.PlaceId == 95356852680586 then

local Window = Fluent:CreateWindow({Title = "Masteries Hub (Scripter Hub)", SubTitle = "Slap Battles - Mastery Hub", TabWidth = 160, Size = UDim2.fromOffset(580, 460), Acrylic = false, Theme = "Dark", MinimizeKey = Enum.KeyCode.RightControl})

Fluent:Notify({Title = "From Scripter", Content = "GoodLuck!", Duration = 3})

Tab = Window:AddTab({Title = "Welcome", Icon = ""})

local player = game.Players.LocalPlayer
Tab:AddParagraph({Title = "Hello " .. player.Name .. "!"})

Section = Tab:AddSection("Before u will start using mine script would u like to bypass anticheat?")

Tab:AddButton({
	Title = "Bypass All Anticheat",
	Callback = function()
loadstring(game:HttpGet("https://raw.githubusercontent.com/RobScripter/Slap-Battles/main/BypassAntiCheat.lua"))()
task.wait(1.5)
Fluent:Notify({Title = "Success", Content = "Bypassed All Anticheat", Duration = 3})
end
})

Section = Tab:AddSection("Give FE/Visual Free Mastery")

-- Create dropdown for Mastery selection
local masteryNames = {
    "spin", "Stick", "Brick", "Flash", "Spring", "Thanos", "Fort", "Space", "Hive", "Killstreak", 
    "Engineer", "Phantom", "Wormhole", "Stalker", "Cherry", "Booster", "BONK", "Cloud", "Soul", 
    "[REDACTED]", "bus", "Phase", "Bomb", "Bubble", "Shard", "Moon", "Rage", "Chain", "Tycoon", 
    "🗿", "Obby", "Voodoo", "rob", "Tinkerer", "Glovel", "Run", "Plank", "Eggler", "Car Keys", 
    "Ultra Instinct", "Acrobat"
}

-- Variable to store selected mastery
local selectedMastery = masteryNames[1]

-- Check if hookfunction is supported
local hookfunctionSupported = pcall(function()
    return hookfunction
end)

-- Add dropdown for mastery selection
Tab:AddDropdown("Dropdown_1", {
    Title = "Select Glove for Mastery",
    Values = masteryNames,
    Default = selectedMastery,
    Callback = function(Value)
        selectedMastery = Value
        if hookfunctionSupported then
            Fluent:Notify({Title = "Mastery Selected", Content = "Selected: ", Duration = 2})
        end
    end
})

-- Add button to apply mastery
Tab:AddButton({
    Title = "Apply Mastery",
    Callback = function()
        if not hookfunctionSupported then
            Fluent:Notify({Title = "Error", Content = "Your executor doesn't support hookfunction", Duration = 5})
            return
        end
        
        local success, err = pcall(function()
            local mod = require(game.ReplicatedFirst.Dependencies.GloveMasteryClient)
            local old = mod.IsMasteredVersionEnabled
            hookfunction(mod.IsMasteredVersionEnabled, function(self, g)
                return g == selectedMastery or old(self, g)
            end)
        end)
        
        if success then
            Fluent:Notify({Title = "Success", Content = "", Duration = 2})
        else
            Fluent:Notify({Title = "Error", Content = "Failed to apply mastery: ", Duration = 3})
	        Fluent:Notify({Title = "Warning", Content = "Your executor doesn't support hookfunction", Duration = 5})
        end
    end
})

local Tab = Window:AddTab({Title = "spin", Icon = ""})

Section = Tab:AddSection("1 Quest")

Tab:AddButton({
    Title = "AFK (10 Hours)",
    Callback = function()
        if game.Players.LocalPlayer.leaderstats.Glove.Value == "spin" and game.Players.LocalPlayer.Character:FindFirstChild("entered") then
            game.Players.LocalPlayer.Character.HumanoidRootPart.CFrame = CFrame.new(17893, -24, -3542)
        else
            Fluent:Notify({Title = "Error", Content = "Equip Spin Glove/Enter Arena", Duration = 3})
        end
    end
})

AntiAfk = Tab:AddToggle("Toggle_1", {
	Title = "Anti Afk",
	Default = false,
	Callback = function(Value)
	_G.AntiAfk = Value
local VirtualUser = game:GetService('VirtualUser')

game:GetService('Players').LocalPlayer.Idled:Connect(function()
    VirtualUser:CaptureController()
    VirtualUser:ClickButton2(Vector2.new())
end)
	end    
})

local Tab = Window:AddTab({Title = "Stick", Icon = ""})

Section = Tab:AddSection("1 Quest")

Tab:AddButton({
    Title = "Stick Slap Aura",
    Callback = function()
if game.Players.LocalPlayer.leaderstats.Glove.Value == "Stick" then
-- Get the ReplicatedStorage and Players service
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local Players = game:GetService("Players")

-- Start the infinite loop to fire for all players
while true do
    -- Loop through each player in the game
    for _, player in pairs(Players:GetPlayers()) do
        -- Ensure the player has a character and it contains the "Right Arm"
        if player.Character and player.Character:FindFirstChild("Right Arm") then
            -- Create the arguments table for the FireServer function
            local args = {
                [1] = player.Character["Right Arm"]
            }

            -- Fire the server with the "Right Arm" part as an argument
            ReplicatedStorage.GeneralHit:FireServer(unpack(args))
        end
    end

    -- Wait 1 second before running the loop again (adjust the wait time as needed)
    task.wait(0.5)
end
else
Fluent:Notify({Title = "Error", Content = "Equip Stick", Duration = 3})
end
end
})

local Tab = Window:AddTab({Title = "Brick", Icon = ""})

Section = Tab:AddSection("1 Quest")

local brickMasterRunning = false
local brickMasterConnection = nil

Tab:AddToggle("Toggle_2", {
    Title = "Auto Obtain Brick Master Badge",
    Default = false,
    Callback = function(Value)
        if Value then
            -- START
            if game.Players.LocalPlayer.leaderstats.Glove.Value == "Brick" and game.Players.LocalPlayer.Character:FindFirstChild("entered") then
                task.wait(0.5)
                local player = game.Players.LocalPlayer

                if player.Character and player.Character:FindFirstChild("HumanoidRootPart") then
                    task.wait(0.5)
                    player.Character.HumanoidRootPart.CFrame = CFrame.new(17894, -24, -3533)
                end

                task.wait(0.5)
                
                -- Create the loop
                getgenv().BrickMasterLoop = true
                task.spawn(function()
                    while getgenv().BrickMasterLoop do
                        game:GetService("ReplicatedStorage").lbrick:FireServer()
                        local gui = game:GetService("Players").LocalPlayer.PlayerGui.BRICKCOUNT.ImageLabel.TextLabel
                        gui.Text = tonumber(gui.Text) + 1
                        task.wait(1.5)
                    end
                end)
                
            else
                Fluent:Notify({Title = "Error", Content = "Equip Brick Glove/Enter Arena", Duration = 3})
                return false -- Turn toggle off
            end
        else
            -- STOP
            getgenv().BrickMasterLoop = false
            
        end
    end    
})

Section = Tab:AddSection("3 Quest")

Tab:AddToggle("Toggle_3", {
    Title = "Auto Fling Bricks (can lag)",
    Default = false,
    Callback = function(Value)
        if Value then
            -- START
            if game.Players.LocalPlayer.leaderstats.Glove.Value == "Brick" then
                
                getgenv().UnionsFlingLoop = true
                
                -- Store original walk speed
                local originalWalkSpeed = game.Players.LocalPlayer.Character:FindFirstChildWhichIsA("Humanoid").WalkSpeed
                
                task.spawn(function()
                    -- Infinite Yield style fling loop
                    local RunService = game:GetService("RunService")
                    local speaker = game.Players.LocalPlayer
                    local flinging = true
                    
                    -- Noclip for flinging
                    local noclipConnection
                    noclipConnection = RunService.Stepped:Connect(function()
                        if speaker.Character then
                            for _, part in pairs(speaker.Character:GetDescendants()) do
                                if part:IsA("BasePart") and part.CanCollide then
                                    part.CanCollide = false
                                end
                            end
                        end
                    end)
                    
                    -- Death handler
                    local humanoid = speaker.Character:FindFirstChildWhichIsA("Humanoid")
                    if humanoid then
                        humanoid.Died:Connect(function()
                            flinging = false
                            if noclipConnection then
                                noclipConnection:Disconnect()
                            end
                            getgenv().UnionsFlingLoop = false
                        end)
                    end
                    
                    while getgenv().UnionsFlingLoop and flinging do
                        RunService.Heartbeat:Wait()
                        local character = speaker.Character
                        local root = character and character:FindFirstChild("HumanoidRootPart")
                        local vel, movel = nil, 0.1

                        while not (character and character.Parent and root and root.Parent) do
                            RunService.Heartbeat:Wait()
                            character = speaker.Character
                            root = character and character:FindFirstChild("HumanoidRootPart")
                        end

                        vel = root.Velocity
                        root.Velocity = vel * 10000 + Vector3.new(0, 10000, 0)

                        RunService.RenderStepped:Wait()
                        if character and character.Parent and root and root.Parent then
                            root.Velocity = vel
                        end

                        RunService.Stepped:Wait()
                        if character and character.Parent and root and root.Parent then
                            root.Velocity = vel + Vector3.new(0, movel, 0)
                            movel = movel * -1
                        end
                        
                        -- Find and bring Unions with TouchInterest to player while flinging
                        local unions = {}
                        for _, obj in pairs(workspace:GetDescendants()) do
                            if obj:IsA("BasePart") and obj.Name == "Union" then
                                -- Check if this Union has TouchInterest inside
                                local hasTouchInterest = false
                                for _, child in pairs(obj:GetChildren()) do
                                    if child:IsA("TouchTransmitter") or child.Name == "TouchInterest" then
                                        hasTouchInterest = true
                                        break
                                    end
                                end
                                
                                if hasTouchInterest then
                                    table.insert(unions, obj)
                                end
                            end
                        end
                    end
                    
                    -- Cleanup connections when loop ends
                    if noclipConnection then
                        noclipConnection:Disconnect()
                    end
                end)
            else
                Fluent:Notify({Title = "Error", Content = "Equip Brick Glove first!", Duration = 3})
                return false -- Turn toggle off
            end
        else
            -- STOP
            getgenv().UnionsFlingLoop = false
            
            -- Restore original walk speed
            if game.Players.LocalPlayer.Character then
                local humanoid = game.Players.LocalPlayer.Character:FindFirstChildWhichIsA("Humanoid")
                if humanoid then
                    humanoid.WalkSpeed = 16 -- Default walk speed
                end
            end
            
            -- Clean up velocity objects from Unions
            for _, obj in pairs(workspace:GetDescendants()) do
                if obj:IsA("BasePart") and obj.Name == "Union" then
                    local velocity = obj:FindFirstChild("FlingVelocity")
                    if velocity then
                        velocity:Destroy()
                    end
                end
            end
            
        end
    end    
})

local Tab = Window:AddTab({Title = "Flash", Icon = ""})

Section = Tab:AddSection("1-3 Quest")

Tab:AddButton({
    Title = "Auto Use Ability (Instant But Kicks)",
    Callback = function()
        if game.Players.LocalPlayer.leaderstats.Glove.Value == "Flash" then
            for i = 1, 1500 do
                game:GetService("ReplicatedStorage"):WaitForChild("FlashTeleport"):FireServer()
                task.wait()
            end
        else
            Fluent:Notify({Title = "Error", Content = "Equip Flash Glove", Duration = 3})
        end
    end
})

Section = Tab:AddSection("2 Quest")

local running = false

Tab:AddToggle("Toggle_4", {
    Title = "Auto Slap/Use Ability 750 Times",
    Default = false,
    Callback = function(AutoSlapFlash)
        running = AutoSlapFlash
        if not running then return end

        task.spawn(function()
            local Players = game:GetService("Players")
            local ReplicatedStorage = game:GetService("ReplicatedStorage")
            local LocalPlayer = Players.LocalPlayer

            local function teleportToRandomPlayer()
                local playerList = Players:GetPlayers()
                if #playerList > 1 then
                    local randomPlayer
                    repeat
                        randomPlayer = playerList[math.random(1, #playerList)]
                    until randomPlayer ~= LocalPlayer

                    local targetChar = randomPlayer.Character
                    if targetChar and targetChar:FindFirstChild("HumanoidRootPart") then
                        LocalPlayer.Character.HumanoidRootPart.CFrame = CFrame.new(targetChar.HumanoidRootPart.Position + Vector3.new(0, 3, 0))
                    end
                end
            end

            local function fireFlashHitForAllPlayers()
                for _, player in pairs(Players:GetPlayers()) do
                    if player ~= LocalPlayer and player.Character and player.Character:FindFirstChild("HumanoidRootPart") then
                        ReplicatedStorage:WaitForChild("FlashHit"):FireServer(player.Character.HumanoidRootPart)
                    end
                end
            end

            local count = 0
            while running and count < 750 do
                if LocalPlayer.leaderstats.Glove.Value == "Flash" then
                    ReplicatedStorage:WaitForChild("FlashTeleport"):FireServer()
                    teleportToRandomPlayer()
                    fireFlashHitForAllPlayers()
                    count += 1
                    task.wait(2.1)
                else
                    Fluent:Notify({Title = "Error", Content = "Equip Flash Glove", Duration = 3})
                    break
                end
            end
        end)
    end
})

Tab:AddButton({
    Title = "Flash Slap Aura",
    Callback = function()
if game.Players.LocalPlayer.leaderstats.Glove.Value == "Flash" then
-- Get the ReplicatedStorage and Players service
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local Players = game:GetService("Players")

-- Start the infinite loop to fire for all players
while true do
    -- Loop through each player in the game
    for _, player in pairs(Players:GetPlayers()) do
        -- Ensure the player has a character and it contains the "Right Arm"
        if player.Character and player.Character:FindFirstChild("Right Arm") then
            -- Create the arguments table for the FireServer function
            local args = {
                [1] = player.Character["Right Arm"]
            }

            -- Fire the server with the "Right Arm" part as an argument
            ReplicatedStorage.FlashHit:FireServer(unpack(args))
        end
    end

    -- Wait 1 second before running the loop again (adjust the wait time as needed)
    task.wait(0.5)
end
else
Fluent:Notify({Title = "Error", Content = "Equip Flash", Duration = 3})
end
end
})

local Tab = Window:AddTab({Title = "Spring", Icon = ""})

Section = Tab:AddSection("1-3 Quest")

Tab:AddButton({
    Title = "Spring Slap Aura",
    Callback = function()
if game.Players.LocalPlayer.leaderstats.Glove.Value == "Spring" then
-- Get the ReplicatedStorage and Players service
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local Players = game:GetService("Players")

-- Start the infinite loop to fire for all players
while true do
    -- Loop through each player in the game
    for _, player in pairs(Players:GetPlayers()) do
        -- Ensure the player has a character and it contains the "Right Arm"
        if player.Character and player.Character:FindFirstChild("Right Arm") then
            -- Create the arguments table for the FireServer function
            local args = {
                [1] = player.Character["Right Arm"]
            }

            -- Fire the server with the "Right Arm" part as an argument
            ReplicatedStorage.springhit:FireServer(unpack(args))
        end
    end

    -- Wait 1 second before running the loop again (adjust the wait time as needed)
    task.wait(0.5)
end
else
Fluent:Notify({Title = "Error", Content = "Equip Spring", Duration = 3})
end
end
})

local Tab = Window:AddTab({Title = "Thanos", Icon = ""})

Section = Tab:AddSection("1-2-3 Quest")

Tab:AddToggle("Toggle_5", {
    Title = "Auto Slap/Use Ability",
    Default = false,
    Callback = function(AutoSlapThanos)
        running = AutoSlapThanos
        if not running then return end

        task.spawn(function()
            local Players = game:GetService("Players")
            local ReplicatedStorage = game:GetService("ReplicatedStorage")
            local LocalPlayer = Players.LocalPlayer

            local function teleportToRandomPlayer()
                local playerList = Players:GetPlayers()
                if #playerList > 1 then
                    local randomPlayer
                    repeat
                        randomPlayer = playerList[math.random(1, #playerList)]
                    until randomPlayer ~= LocalPlayer

                    local targetChar = randomPlayer.Character
                    if targetChar and targetChar:FindFirstChild("HumanoidRootPart") then
                        LocalPlayer.Character.HumanoidRootPart.CFrame = CFrame.new(targetChar.HumanoidRootPart.Position + Vector3.new(0, 3, 0))
                    end
                end
            end

            local function fireThanosHitForAllPlayers()
                for _, player in pairs(Players:GetPlayers()) do
                    if player ~= LocalPlayer and player.Character and player.Character:FindFirstChild("HumanoidRootPart") then
                        ReplicatedStorage:WaitForChild("GeneralHit"):FireServer(player.Character.HumanoidRootPart)
                    end
                end
            end

            local count = 0
            while running and count < 750 do
                if LocalPlayer.leaderstats.Glove.Value == "Thanos" then
                    local args = {
    [1] = 1
}

game:GetService("ReplicatedStorage"):WaitForChild("GeneralAbility"):FireServer(unpack(args))
task.wait(0.2)
local args = {
    [1] = 2
}

game:GetService("ReplicatedStorage"):WaitForChild("GeneralAbility"):FireServer(unpack(args))
                    teleportToRandomPlayer()
                    fireThanosHitForAllPlayers()
                    count += 1
                    task.wait(2.1)
                else
                    Fluent:Notify({Title = "Error", Content = "Equip Thanos Glove", Duration = 3})
                    break
                end
            end
        end)
    end
})

Tab:AddButton({
	Title = "Use Thanos (1) Ability",
	Callback = function()  
fireclickdetector(workspace.Lobby["Thanos"].ClickDetector)
wait(0.1)
local args = {
    [1] = 1
}

game:GetService("ReplicatedStorage"):WaitForChild("GeneralAbility"):FireServer(unpack(args))
end
})

Tab:AddButton({
	Title = "Use Thanos (2) Ability",
	Callback = function()   
fireclickdetector(workspace.Lobby["Thanos"].ClickDetector)
wait(0.1)
local args = {
    [1] = 2
}

game:GetService("ReplicatedStorage"):WaitForChild("GeneralAbility"):FireServer(unpack(args))
end
})

Tab:AddButton({
    Title = "Thanos Slap Aura",
    Callback = function()
if game.Players.LocalPlayer.leaderstats.Glove.Value == "Thanos" then
-- Get the ReplicatedStorage and Players service
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local Players = game:GetService("Players")

-- Start the infinite loop to fire for all players
while true do
    -- Loop through each player in the game
    for _, player in pairs(Players:GetPlayers()) do
        -- Ensure the player has a character and it contains the "Right Arm"
        if player.Character and player.Character:FindFirstChild("Right Arm") then
            -- Create the arguments table for the FireServer function
            local args = {
                [1] = player.Character["Right Arm"]
            }

            -- Fire the server with the "Right Arm" part as an argument
            ReplicatedStorage.GeneralHit:FireServer(unpack(args))
        end
    end

    -- Wait 1 second before running the loop again (adjust the wait time as needed)
    task.wait(0.5)
end
else
Fluent:Notify({Title = "Error", Content = "Equip Thanos", Duration = 3})
end
end
})

local Tab = Window:AddTab({Title = "Fort", Icon = ""})

Section = Tab:AddSection("1 Quest")

Tab:AddButton({
    Title = "Get Badge + Plank Glove",
    Callback = function()
        local player = game.Players.LocalPlayer
        if player.leaderstats.Glove.Value == "Fort" and game.Players.LocalPlayer.Character:FindFirstChild("entered") then
            local hrp = player.Character.HumanoidRootPart
            local originalPos = hrp.CFrame

            hrp.CFrame = CFrame.new(8, 97, 4)
            wait(0.2)
            hrp.Anchored = true
            wait(0.3)
            game:GetService("ReplicatedStorage").Fortlol:FireServer()
            wait(3.5)
            hrp.Anchored = false
            wait(0.1)
            hrp.CFrame = CFrame.new(8, 106, -6)
            wait(0.5)
            hrp.CFrame = originalPos
        else
            Fluent:Notify({Title = "Error", Content = "Equip Fort Glove/Enter Arena", Duration = 3})
        end
    end
})

Section = Tab:AddSection("2 Quest")

Tab:AddButton({
    Title = "Destroy Wall (Stay Very Close To Wall)",
    Callback = function()
        local player = game.Players.LocalPlayer
        if player.leaderstats.Glove.Value == "Fort" then
            game.ReplicatedStorage.SelfKnockback:FireServer({ Force = 0, Direction = Vector3.new(0, 0.01, 0) })
        else
            Fluent:Notify({Title = "Error", Content = "Equip Fort Glove", Duration = 3})
        end
    end
})

Section = Tab:AddSection("3 Quest")

Tab:AddButton({
    Title = "Build Wall",
    Callback = function()
        local player = game.Players.LocalPlayer
        if player.leaderstats.Glove.Value == "Fort" then
            game:GetService("ReplicatedStorage").Fortlol:FireServer()
        else
            Fluent:Notify({Title = "Error", Content = "Equip Fort Glove", Duration = 3})
        end
    end
})

Tab:AddToggle("Toggle_6", {
    Title = "Auto Build Wall",
    Default = false,
    Callback = function(Value)
        if Value then
            -- START
            local player = game.Players.LocalPlayer
            if player.leaderstats.Glove.Value == "Fort" then
                
                getgenv().BuildWallLoop = true
                task.spawn(function()
                    while getgenv().BuildWallLoop do
                        game:GetService("ReplicatedStorage").Fortlol:FireServer()
                        task.wait(3.5)
                    end
                end)
            else
                Fluent:Notify({Title = "Error", Content = "Equip Fort Glove", Duration = 3})
                return false -- Turn toggle off
            end
        else
            -- STOP
            getgenv().BuildWallLoop = false
            
        end
    end
})

Section = Tab:AddSection("2-3 Quest")

local AutoWall = false
Tab:AddToggle("Toggle_7", {
    Title = "Auto Build And Destroy Wall",
    Default = false,
    Callback = function(Value)
        AutoWall = Value
        local player = game.Players.LocalPlayer

        if AutoWall then
            if player.leaderstats.Glove.Value == "Fort" then
                task.spawn(function()
                    while AutoWall do
                        player.Character.HumanoidRootPart.CFrame = CFrame.new(10019, -42, 9960)
                        wait(1)
                        game:GetService("ReplicatedStorage").Fortlol:FireServer()
                        wait(1)
                        player.Character.HumanoidRootPart.CFrame = CFrame.new(10018, -42, 9951)
                        wait(1)
                        game.ReplicatedStorage.SelfKnockback:FireServer({ Force = 0, Direction = Vector3.new(0, 0.01, 0) })
                        wait(4)

                        if player.leaderstats.Glove.Value ~= "Fort" then
                            AutoWall = false
                            Fluent:Notify({Title = "Error", Content = "Equip Fort Glove", Duration = 3})
                        end
                    end
                end)
            else
                Fluent:Notify({Title = "Error", Content = "Equip Fort Glove", Duration = 3})
            end
        end
    end
})

local Tab = Window:AddTab({Title = "Space", Icon = ""})

Section = Tab:AddSection("1 Quest")

Tab:AddToggle("Toggle_8", {
    Title = "Auto Use Ability",
    Default = false,
    Callback = function(Value)
        if Value then
            -- START
            if game.Players.LocalPlayer.leaderstats.Glove.Value == "Space" and game.Players.LocalPlayer.Character:FindFirstChild("entered") then
                
                getgenv().SpaceAbilityLoop = true
                task.spawn(function()
                    while getgenv().SpaceAbilityLoop do
                        game:GetService("ReplicatedStorage").ZeroGSound:FireServer()
                        task.wait()
                    end
                end)
            else
                Fluent:Notify({Title = "Error", Content = "Equip Space Glove", Duration = 3})
                return false -- Turn toggle off
            end
        else
            -- STOP
            getgenv().SpaceAbilityLoop = false
            
        end
    end
})

Section = Tab:AddSection("2 Quest")

Tab:AddButton({
    Title = "Someone Using bus?",
    Callback = function()
        local found = false

        for _, player in ipairs(game.Players:GetPlayers()) do
            local leaderstats = player:FindFirstChild("leaderstats")
            if leaderstats then
                local glove = leaderstats:FindFirstChild("Glove")
                if glove and glove.Value == "bus" then
                    found = true
                    Fluent:Notify({Title = "Detected!", Content = "", Duration = 3})
                end
            end
        end

        if not found then
            Fluent:Notify({Title = "Error", Content = "No one has equipped the bus glove", Duration = 3})
        end
    end
})

Tab:AddButton({
    Title = "Teleport To bus Player",
    Callback = function()
        local localPlayer = game.Players.LocalPlayer
        local foundPlayer = nil

        for _, player in pairs(game.Players:GetPlayers()) do
            if player ~= localPlayer then
                local stats = player:FindFirstChild("leaderstats")
                if stats and stats:FindFirstChild("Glove") and stats.Glove.Value == "bus" then
                    foundPlayer = player
                    break
                end
            end
        end

        if foundPlayer and foundPlayer.Character and foundPlayer.Character:FindFirstChild("HumanoidRootPart") then
            local hrp = foundPlayer.Character.HumanoidRootPart
            localPlayer.Character:PivotTo(hrp.CFrame + Vector3.new(0, 5, 0)) -- Teleport above

            Fluent:Notify({Title = "Success!", Content = "Teleported To: ", Duration = 3})
        else
            Fluent:Notify({Title = "Error", Content = "bus Player Not Found", Duration = 3})
        end
    end
})

Tab:AddToggle("Toggle_9", {
	Title = "Auto Bring Bus",
	Default = false,
	Callback = function(Value) 
Giftfarm = Value
while Giftfarm do
for i,v in pairs(game.Workspace:GetChildren()) do
                    if v.Name == "BusModel" then
firetouchinterest(game.Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart"), v, 0)
firetouchinterest(game.Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart"), v, 1)
                    end
                end
task.wait()
end
end
})

Section = Tab:AddSection("3 Quest")

Tab:AddButton({
    Title = "Teleport To Cannon Island",
    Callback = function()
        if game.Players.LocalPlayer.leaderstats.Glove.Value == "Space" then
            game.Players.LocalPlayer.Character.HumanoidRootPart.CFrame = CFrame.new(229, 47, 170)
        else
            Fluent:Notify({Title = "Error", Content = "Equip Space Glove", Duration = 3})
        end
    end
})

Tab:AddButton({
    Title = "Teleport To Slapple Island",
    Callback = function()
        if game.Players.LocalPlayer.leaderstats.Glove.Value == "Space" then
            game.Players.LocalPlayer.Character.HumanoidRootPart.CFrame = CFrame.new(-389, 51, -23)
        else
            Fluent:Notify({Title = "Error", Content = "Equip Space Glove", Duration = 3})
        end
    end
})

Section = Tab:AddSection("4 Quest")

local running = false
Tab:AddToggle("Toggle_10", {
    Title = "Auto Slap/Use Ability 500 Times",
    Default = false,
    Callback = function(AutoSlapSpace)
        running = AutoSlapSpace
        if not running then return end

        task.spawn(function()
            local Players = game:GetService("Players")
            local ReplicatedStorage = game:GetService("ReplicatedStorage")
            local LocalPlayer = Players.LocalPlayer

            local function teleportToRandomPlayer()
                local playerList = Players:GetPlayers()
                if #playerList > 1 then
                    local randomPlayer
                    repeat
                        randomPlayer = playerList[math.random(1, #playerList)]
                    until randomPlayer ~= LocalPlayer

                    local targetChar = randomPlayer.Character
                    if targetChar and targetChar:FindFirstChild("HumanoidRootPart") then
                        LocalPlayer.Character.HumanoidRootPart.CFrame = CFrame.new(targetChar.HumanoidRootPart.Position + Vector3.new(0, 3, 0))
                    end
                end
            end

            local function fireSpaceHitForAllPlayers()
                for _, player in pairs(Players:GetPlayers()) do
                    if player ~= LocalPlayer and player.Character and player.Character:FindFirstChild("HumanoidRootPart") then
                        ReplicatedStorage:WaitForChild("HtSpace"):FireServer(player.Character.HumanoidRootPart)
                    end
                end
            end

            while true do
                if LocalPlayer.leaderstats.Glove.Value == "Space" then
                    ReplicatedStorage:WaitForChild("ZeroGSound"):FireServer()
                    teleportToRandomPlayer()
                    fireSpaceHitForAllPlayers()
                    task.wait()
                else
                    Fluent:Notify({Title = "Error", Content = "Equip Space Glove", Duration = 3})
                    break
                end
            end
        end)
    end
})

local Tab = Window:AddTab({Title = "Hive", Icon = ""})

Section = Tab:AddSection("1-4 Quest")

local AutoAbiltySlapHiveRunning = false
Tab:AddToggle("Toggle_11", {
    Title = "Auto Slap/Use Ability",
    Default = false,
    Callback = function(Value)
        AutoAbiltySlapHiveRunning = Value
        local player = game.Players.LocalPlayer

        if AutoAbiltySlapHiveRunning then
            if player.leaderstats.Glove.Value == "Hive" then
                task.spawn(function()
                    while AutoAbiltySlapHiveRunning do
                        local players = game:GetService("Players"):GetPlayers()
                        local targetPlayer
                        repeat
                            targetPlayer = players[math.random(1, #players)]
                        until targetPlayer ~= player and targetPlayer.Character and targetPlayer.Character:FindFirstChild("HumanoidRootPart")

                        local targetHRP = targetPlayer.Character.HumanoidRootPart
                        player.Character.HumanoidRootPart.CFrame = CFrame.new(targetHRP.Position)

                        task.wait(1)
                    end

                    while true do
                        game:GetService("ReplicatedStorage").GeneralAbility:FireServer()
                        task.wait(1)
                        game:GetService("ReplicatedStorage").GeneralAbility:FireServer()
                        task.wait(14)
                    end
                end)
            else
                AutoAbiltySlapHiveRunning = false
                Fluent:Notify({Title = "Error", Content = "Equip Hive Glove", Duration = 3})
            end
        end
    end
})

local Tab = Window:AddTab({Title = "Defense", Icon = ""})

Section = Tab:AddSection("1-4 Quest")

local AutoAbiltySlapDefenseRunning = false
Tab:AddToggle("Toggle_12", {
    Title = "Auto Slap/Use Ability",
    Default = false,
    Callback = function(Value)
        AutoAbiltySlapDefenseRunning = Value
        local player = game.Players.LocalPlayer

        if AutoAbiltySlapDefenseRunning then
            if player.leaderstats.Glove.Value == "Defense" then
                task.spawn(function()
                    while AutoAbiltySlapDefenseRunning do
                        local players = game:GetService("Players"):GetPlayers()
                        local targetPlayer
                        repeat
                            targetPlayer = players[math.random(1, #players)]
                        until targetPlayer ~= player and targetPlayer.Character and targetPlayer.Character:FindFirstChild("HumanoidRootPart")

                        local targetHRP = targetPlayer.Character.HumanoidRootPart
                        player.Character.HumanoidRootPart.CFrame = CFrame.new(targetHRP.Position)

                        task.wait(1)
                    end

                    while true do
                        game:GetService("ReplicatedStorage").Barrier:FireServer()
                        task.wait(25)
                    end
                end)
            else
                AutoAbiltySlapDefenseRunning = false
                Fluent:Notify({Title = "Error", Content = "Equip Defense Glove", Duration = 3})
            end
        end
    end
})

local Tab = Window:AddTab({Title = "Killstreak", Icon = ""})

Section = Tab:AddSection("1-3 Quest")

Tab:AddToggle("Toggle_13", {
    Title = "Auto Slap Players",
    Default = false,
    Callback = function(AutoSlapKillstreak)
        local running = AutoSlapKillstreak

        local function teleportToRandomPlayer()
            local Players = game:GetService("Players")
            local playerList = Players:GetPlayers()
            if #playerList > 1 then
                local randomPlayer
                repeat
                    randomPlayer = playerList[math.random(1, #playerList)]
                until randomPlayer ~= Players.LocalPlayer

                local randomPlayerCharacter = randomPlayer.Character
                if randomPlayerCharacter and randomPlayerCharacter:FindFirstChild("HumanoidRootPart") then
                    local teleportPosition = randomPlayerCharacter.HumanoidRootPart.Position
                    Players.LocalPlayer.Character.HumanoidRootPart.CFrame = CFrame.new(teleportPosition)
                end
            end
        end

        local function fireKSHitForAllPlayers()
            local Players = game:GetService("Players")
            local ReplicatedStorage = game:GetService("ReplicatedStorage")
            for _, player in pairs(Players:GetPlayers()) do
                if player.Character and player.Character:FindFirstChild("HumanoidRootPart") then
                    ReplicatedStorage:WaitForChild("KSHit"):FireServer(player.Character.HumanoidRootPart)
                end
            end
        end

        local function runAutoSlapKillstreakLoop()
            if game.Players.LocalPlayer.leaderstats.Glove.Value == "Killstreak" then
                while running do
                    wait(0.2)
                    teleportToRandomPlayer()
                    fireKSHitForAllPlayers()
                    wait(1)
                end
            else
                Fluent:Notify({Title = "Error", Content = "Equip Killstreak Glove", Duration = 3})
            end
        end

        if AutoSlapKillstreak then
            running = true
            runAutoSlapKillstreakLoop()
        else
            running = false
        end
    end
})

local Tab = Window:AddTab({Title = "Engineer", Icon = ""})

Section = Tab:AddSection("1-3 Quest")

Tab:AddButton({
    Title = "Spawn Sentry",
    Callback = function()
        if game.Players.LocalPlayer.leaderstats.Glove.Value == "Engineer" then
            game:GetService("ReplicatedStorage").Sentry:FireServer()
        else
            Fluent:Notify({Title = "Error", Content = "Equip Engineer Glove", Duration = 3})
        end
    end
})

Tab:AddToggle("Toggle_14", {
    Title = "Auto Spawn Sentry",
    Default = false,
    Callback = function(Value)
        if Value then
            -- START
            if game.Players.LocalPlayer.leaderstats.Glove.Value == "Engineer" then
                
                getgenv().AutoSentryLoop = true
                task.spawn(function()
                    while getgenv().AutoSentryLoop do
                        game:GetService("ReplicatedStorage").Sentry:FireServer()
                        task.wait()
                    end
                end)
            else
                Fluent:Notify({Title = "Error", Content = "Equip Engineer Glove", Duration = 3})
                return false -- Turn toggle off
            end
        else
            -- STOP
            getgenv().AutoSentryLoop = false
            
        end
    end
})

local Tab = Window:AddTab({Title = "Phantom", Icon = ""})

Section = Tab:AddSection("1-2-3 Quest")

Tab:AddButton({
    Title = "Phantom Slap Aura",
    Callback = function()
if game.Players.LocalPlayer.leaderstats.Glove.Value == "Phantom" then
-- Get the ReplicatedStorage and Players service
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local Players = game:GetService("Players")

-- Start the infinite loop to fire for all players
while true do
    -- Loop through each player in the game
    for _, player in pairs(Players:GetPlayers()) do
        -- Ensure the player has a character and it contains the "Right Arm"
        if player.Character and player.Character:FindFirstChild("Right Arm") then
            -- Create the arguments table for the FireServer function
            local args = {
                [1] = player.Character["Right Arm"]
            }

            -- Fire the server with the "Right Arm" part as an argument
            ReplicatedStorage.PhantomHit:FireServer(unpack(args))
        end
    end

    -- Wait 1 second before running the loop again (adjust the wait time as needed)
    task.wait(0.5)
end
else
Fluent:Notify({Title = "Error", Content = "Equip Phantom", Duration = 3})
end
end
})

Tab:AddButton({
	Title = "Use Phantom's Abiity",
	Callback = function()  
local args = {
    [1] = game:GetService("Players").LocalPlayer.Character.Phantom
}

game:GetService("ReplicatedStorage").PhantomDash:InvokeServer(unpack(args))

end
})

local Tab = Window:AddTab({Title = "Wormhole", Icon = ""})

Section = Tab:AddSection("1 Quest")

Tab:AddButton({
    Title = "Wormhole Slap Aura",
    Callback = function()
if game.Players.LocalPlayer.leaderstats.Glove.Value == "Wormhole" then
-- Get the ReplicatedStorage and Players service
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local Players = game:GetService("Players")

-- Start the infinite loop to fire for all players
while true do
    -- Loop through each player in the game
    for _, player in pairs(Players:GetPlayers()) do
        -- Ensure the player has a character and it contains the "Right Arm"
        if player.Character and player.Character:FindFirstChild("Right Arm") then
            -- Create the arguments table for the FireServer function
            local args = {
                [1] = player.Character["Right Arm"]
            }

            -- Fire the server with the "Right Arm" part as an argument
            ReplicatedStorage.WormHit:FireServer(unpack(args))
        end
    end

    -- Wait 1 second before running the loop again (adjust the wait time as needed)
    task.wait(0.5)
end
else
Fluent:Notify({Title = "Error", Content = "Equip Wormhole", Duration = 3})
end
end
})

Section = Tab:AddSection("2 Quest")

Tab:AddToggle("Toggle_15", {
    Title = "Auto Teleport To Wormholes",
    Default = false,
    Callback = function(Value)
        if Value then
            -- START
            if game.Players.LocalPlayer.leaderstats.Glove.Value == "Wormhole" then
                
                getgenv().AutoTeleportLoop = true
                task.spawn(function()
                    while getgenv().AutoTeleportLoop do
                        local args = {
                            [1] = CFrame.new(-15.540994644165039, -4.673734664916992, -7.913032531738281) * CFrame.Angles(0.02266099490225315, -0.13298073410987854, 0.0024420826230198145)
                        }
                        game:GetService("ReplicatedStorage").WormholeTP:FireServer(unpack(args))
                        task.wait()
                    end
                end)
            else
                Fluent:Notify({Title = "Error", Content = "Equip Wormhole/Use In Lobby", Duration = 3})
                return false -- Turn toggle off
            end
        else
            -- STOP
            getgenv().AutoTeleportLoop = false
            
        end
    end
})

Section = Tab:AddSection("3 Quest")

Tab:AddToggle("Toggle_16", {
    Title = "Auto Place Wormholes",
    Default = false,
    Callback = function(Value)
        if Value then
            -- START
            if game.Players.LocalPlayer.leaderstats.Glove.Value == "Wormhole" then
                
                getgenv().AutoWormholeLoop = true
                task.spawn(function()
                    while getgenv().AutoWormholeLoop do
                        game:GetService("ReplicatedStorage").WormholePlace:FireServer()
                        task.wait()
                    end
                end)
            else
                Fluent:Notify({Title = "Error", Content = "Equip Wormhole/Use In Lobby", Duration = 3})
                return false -- Turn toggle off
            end
        else
            -- STOP
            getgenv().AutoWormholeLoop = false
            
        end
    end
})

local Tab = Window:AddTab({Title = "Stalker", Icon = ""})

Section = Tab:AddSection("1-2 Quest")

Tab:AddToggle("Toggle_17", {
    Title = "Auto Slap Players",
    Default = false,
    Callback = function(AutoSlapStalker)
        local running = AutoSlapStalker

        local function teleportToRandomPlayer()
            local Players = game:GetService("Players")
            local playerList = Players:GetPlayers()
            if #playerList > 1 then
                local randomPlayer
                repeat
                    randomPlayer = playerList[math.random(1, #playerList)]
                until randomPlayer ~= Players.LocalPlayer

                local randomPlayerCharacter = randomPlayer.Character
                if randomPlayerCharacter and randomPlayerCharacter:FindFirstChild("HumanoidRootPart") then
                    local teleportPosition = randomPlayerCharacter.HumanoidRootPart.Position
                    Players.LocalPlayer.Character.HumanoidRootPart.CFrame = CFrame.new(teleportPosition)
                end
            end
        end

        local function fireGeneralHitForAllPlayers()
            local Players = game:GetService("Players")
            local ReplicatedStorage = game:GetService("ReplicatedStorage")
            for _, player in pairs(Players:GetPlayers()) do
                if player.Character and player.Character:FindFirstChild("HumanoidRootPart") then
                    ReplicatedStorage:WaitForChild("GeneralHit"):FireServer(player.Character.HumanoidRootPart)
                end
            end
        end

        local function runAutoSlapStalkerLoop()
            if game.Players.LocalPlayer.leaderstats.Glove.Value == "Stalker" then
                while running do
                    wait(0.2)
                    teleportToRandomPlayer()
                    fireGeneralHitForAllPlayers()
                    wait(1)
                end
            else
                Fluent:Notify({Title = "Error", Content = "Equip Stalker Glove", Duration = 3})
            end
        end

        if AutoSlapStalker then
            running = true
            runAutoSlapStalkerLoop()
        else
            running = false
        end
    end
})

local Tab = Window:AddTab({Title = "Cherry", Icon = ""})

Section = Tab:AddSection("1-2-3 Quest")

local running = false -- declare global toggle state

Tab:AddToggle("Toggle_18", {
    Title = "Auto Spawn Cherry Clones",
    Default = false,
    Callback = function(Value)
        if Value then
            -- Start the loop with exact 0.1 second interval
            cherryCloneLoop = true
            
            spawn(function()
                while cherryCloneLoop do
                    local player = game.Players.LocalPlayer
                    
                    -- Check if player has Cherry glove
                    if player.leaderstats and player.leaderstats.Glove and player.leaderstats.Glove.Value == "Cherry" then
                        -- Check if player has entered
                        if player.Character and player.Character:FindFirstChild("entered") then
                            -- Fire the Cherry remote
                            game:GetService("ReplicatedStorage").Events.Cherry:FireServer()
                        else
                            Fluent:Notify({Title = "Not Entered", Content = "You need to enter the arena first!", Duration = 3})
                            wait(1) -- Wait longer when not entered
                        end
                    else
                        Fluent:Notify({Title = "Wrong Glove", Content = "You need Cherry glove equipped!", Duration = 3})
                        wait(1) -- Wait longer when wrong glove
                    end
                    
                    wait(0.1) -- Normal 0.1 second delay
                end
            end)
        else
            -- Stop the loop
            cherryCloneLoop = false
        end
    end
})

local Tab = Window:AddTab({Title = "Booster", Icon = ""})

Section = Tab:AddSection("1 Quest")

local running = false -- declare global toggle state

Tab:AddToggle("Toggle_19", {
    Title = "Ambush Players 750 Times",
    Default = false,
    Callback = function(AutoSlapBooster)
        running = AutoSlapBooster

        if not running then
            return -- stop immediately if toggled off
        end

        -- Run everything in a separate thread
        task.spawn(function()
            local Players = game:GetService("Players")
            local ReplicatedStorage = game:GetService("ReplicatedStorage")
            local LocalPlayer = Players.LocalPlayer

            -- Function to teleport to a random player
            local function teleportToRandomPlayer()
                local playerList = Players:GetPlayers()
                if #playerList > 1 then
                    local randomPlayer = nil
                    repeat
                        randomPlayer = playerList[math.random(1, #playerList)]
                    until randomPlayer ~= LocalPlayer

                    local targetChar = randomPlayer.Character
                    if targetChar and targetChar:FindFirstChild("HumanoidRootPart") then
                        LocalPlayer.Character.HumanoidRootPart.CFrame = CFrame.new(
                            targetChar.HumanoidRootPart.Position + Vector3.new(0, 3, 0)
                        )
                    end
                end
            end

            -- Function to fire GeneralHit for all players
            local function fireGeneralHitForAllPlayers()
                for _, player in pairs(Players:GetPlayers()) do
                    if player ~= LocalPlayer and player.Character and player.Character:FindFirstChild("HumanoidRootPart") then
                        ReplicatedStorage:WaitForChild("GeneralHit"):FireServer(player.Character.HumanoidRootPart)
                    end
                end
            end

            -- Loop 750 times or until stopped
            local count = 0
            while running and count < 750 do
                if LocalPlayer.leaderstats.Glove.Value == "Booster" then
                    teleportToRandomPlayer()
                    fireGeneralHitForAllPlayers()
                    count += 1
                    task.wait(2.1)
                else
                    Fluent:Notify({Title = "Error", Content = "Equip Booster Glove", Duration = 3})
                    break
                end
            end
        end)
    end
})

local Tab = Window:AddTab({Title = "BONK", Icon = ""})

Section = Tab:AddSection("1-2 Quest")

local running = false -- declare global toggle state

Tab:AddToggle("Toggle_20", {
    Title = "Auto Slap Players",
    Default = false,
    Callback = function(AutoSlapBonk)
        running = AutoSlapBonk

        if not running then
            return -- stop immediately if toggled off
        end

        -- Run everything in a separate thread
        task.spawn(function()
            local Players = game:GetService("Players")
            local ReplicatedStorage = game:GetService("ReplicatedStorage")
            local LocalPlayer = Players.LocalPlayer

            -- Function to teleport to a random player
            local function teleportToRandomPlayer()
                local playerList = Players:GetPlayers()
                if #playerList > 1 then
                    local randomPlayer = nil
                    repeat
                        randomPlayer = playerList[math.random(1, #playerList)]
                    until randomPlayer ~= LocalPlayer

                    local targetChar = randomPlayer.Character
                    if targetChar and targetChar:FindFirstChild("HumanoidRootPart") then
                        LocalPlayer.Character.HumanoidRootPart.CFrame = CFrame.new(
                            targetChar.HumanoidRootPart.Position + Vector3.new(0, 3, 0)
                        )
                    end
                end
            end

            -- Function to fire GeneralHit for all players
            local function fireGeneralHitForAllPlayers()
                for _, player in pairs(Players:GetPlayers()) do
                    if player ~= LocalPlayer and player.Character and player.Character:FindFirstChild("HumanoidRootPart") then
                        ReplicatedStorage:WaitForChild("GeneralHit"):FireServer(player.Character.HumanoidRootPart)
                    end
                end
            end

            -- Loop 750 times or until stopped
            local count = 0
            while running and count < 750 do
                if LocalPlayer.leaderstats.Glove.Value == "BONK" then
                    teleportToRandomPlayer()
                    fireGeneralHitForAllPlayers()
                    count += 1
                    task.wait(2.1)
                else
                    Fluent:Notify({Title = "Error", Content = "Equip BONK Glove", Duration = 3})
                    break
                end
            end
        end)
    end
})

Section = Tab:AddSection("3 Quest")

Tab:AddToggle("Toggle_21", {
    Title = "Auto Use BONK's Hammer",
    Default = false,
    Callback = function(Value)
        if Value then
            -- START
            
            getgenv().AutoBonkLoop = true
            task.spawn(function()
                while getgenv().AutoBonkLoop do
                    game:GetService("ReplicatedStorage").BONK:FireServer()
                    task.wait(0.1)
                end
            end)
        else
            -- STOP
            getgenv().AutoBonkLoop = false
            
        end
    end
})

local Tab = Window:AddTab({Title = "Cloud", Icon = ""})

Section = Tab:AddSection("1 Quest")

Tab:AddButton({
    Title = "Cloud Slap Aura",
    Callback = function()
if game.Players.LocalPlayer.leaderstats.Glove.Value == "Cloud" then
-- Get the ReplicatedStorage and Players service
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local Players = game:GetService("Players")

-- Start the infinite loop to fire for all players
while true do
    -- Loop through each player in the game
    for _, player in pairs(Players:GetPlayers()) do
        -- Ensure the player has a character and it contains the "Right Arm"
        if player.Character and player.Character:FindFirstChild("Right Arm") then
            -- Create the arguments table for the FireServer function
            local args = {
                [1] = player.Character["Right Arm"]
            }

            -- Fire the server with the "Right Arm" part as an argument
            ReplicatedStorage.CloudHit:FireServer(unpack(args))
        end
    end

    -- Wait 1 second before running the loop again (adjust the wait time as needed)
    task.wait(0.5)
end
else
Fluent:Notify({Title = "Error", Content = "Equip Cloud", Duration = 3})
end
end
})

Section = Tab:AddSection("2 Quest")

Tab:AddButton({
    Title = "AFK 200 Minutes",
    Callback = function()
        if game.Players.LocalPlayer.leaderstats.Glove.Value == "Cloud" then
            local teleportPosition = Vector3.new(-980, -151, 2)
            local player = game.Players.LocalPlayer

            if player.Character and player.Character:FindFirstChild("HumanoidRootPart") then
                player.Character.HumanoidRootPart.CFrame = CFrame.new(teleportPosition)
            end
        else
            Fluent:Notify({Title = "Error", Content = "Equip Cloud/Use In Arena/Stay In Cloud", Duration = 3})
        end
    end
})

Section = Tab:AddSection("3 Quest")

Tab:AddSlider("Slider_1", {
    Title = "Set Speed Cloud",
    Min = 0.1,
    Max = 1.2,
    Default = 0.5,
    Rounding = 1,
    Callback = function(Value)
        _G.SetSpeedCloud = Value
    end
})

CloudSpeed = Tab:AddToggle("Toggle_22", {
    Title = "Auto Set Cloud Speed",
    Default = false,
    Callback = function(Value)
        _G.CloudSpeed = Value

        if game.Players.LocalPlayer.leaderstats.Glove.Value == "Cloud" then
            while _G.CloudSpeed and game.Players.LocalPlayer.leaderstats.Glove.Value == "Cloud" do
                for i, v in pairs(game.Workspace:GetChildren()) do
                    if v.Name:match(game.Players.LocalPlayer.Name) and v:FindFirstChild("BodyVelocity") then
                        v.BodyVelocity.Velocity = v.BodyVelocity.Velocity * _G.SetSpeedCloud
                    end
                end
                task.wait(0.10)
            end
        elseif _G.CloudSpeed == true then
            Fluent:Notify({Title = "Error", Content = "Equip Cloud", Duration = 5})
            wait(0.05)
            CloudSpeed:SetValue(false)
        end
    end
})

Section = Tab:AddSection("4 Quest")

local AutoPickUpCloudRunning = false

Tab:AddToggle("Toggle_23", {
    Title = "Auto Pick Up People",
    Default = false,
    Callback = function(Value)
        AutoPickUpCloudRunning = Value
        local player = game.Players.LocalPlayer

        if AutoPickUpCloudRunning then
            if player.leaderstats.Glove.Value == "Cloud" then
                task.spawn(function()
                    while AutoPickUpCloudRunning do
                        local players = game:GetService("Players"):GetPlayers()
                        local targetPlayer = nil

                        repeat
                            targetPlayer = players[math.random(1, #players)]
                        until targetPlayer ~= player and targetPlayer.Character and targetPlayer.Character:FindFirstChild("HumanoidRootPart")

                        if targetPlayer and targetPlayer.Character then
                            local targetHRP = targetPlayer.Character.HumanoidRootPart
                            player.Character.HumanoidRootPart.CFrame = CFrame.new(targetHRP.Position)
                        end

                        task.wait(1)
                    end
                end)
            else
                AutoPickUpCloudRunning = false
                Fluent:Notify({Title = "Error", Content = "Equip Cloud Glove", Duration = 3})
            end
        end
    end
})

local Tab = Window:AddTab({Title = "Soul", Icon = ""})

Section = Tab:AddSection("1-2-3-4 Quest")

Tab:AddButton({
    Title = "Soul Slap Aura",
    Callback = function()
if game.Players.LocalPlayer.leaderstats.Glove.Value == "Soul" then
-- Get the ReplicatedStorage and Players service
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local Players = game:GetService("Players")

-- Start the infinite loop to fire for all players
while true do
    -- Loop through each player in the game
    for _, player in pairs(Players:GetPlayers()) do
        -- Ensure the player has a character and it contains the "Right Arm"
        if player.Character and player.Character:FindFirstChild("Right Arm") then
            -- Create the arguments table for the FireServer function
            local args = {
                [1] = player.Character["Right Arm"]
            }

            -- Fire the server with the "Right Arm" part as an argument
            ReplicatedStorage.GeneralHit:FireServer(unpack(args))
        end
    end

    -- Wait 1 second before running the loop again (adjust the wait time as needed)
    task.wait(0.5)
end
else
Fluent:Notify({Title = "Error", Content = "Equip Soul", Duration = 3})
end
end
})

Tab:AddToggle("Toggle_24", {
    Title = "Auto Use Ability",
    Default = false,
    Callback = function(Value)
        if Value then
            -- START
            
            getgenv().AutoSoulLoop = true
            task.spawn(function()
                while getgenv().AutoSoulLoop do
                    -- Check if the ClickDetector exists
                    local SoulObject = game:GetService("Workspace"):FindFirstChild("Lobby")
                    if SoulObject then
                        SoulObject = SoulObject:FindFirstChild("Soul")
                        if SoulObject and SoulObject:FindFirstChild("ClickDetector") then
                            fireclickdetector(SoulObject.ClickDetector)
                        end
                    end
                    
                    task.wait(0.5)
                    
                    -- Fire the remote
                    game:GetService("ReplicatedStorage").Events.soul:FireServer()
                    
                    task.wait(2.8)
                end
            end)
        else
            -- STOP
            getgenv().AutoSoulLoop = false
            
        end
    end
})

local Tab = Window:AddTab({Title = "[REDACTED]", Icon = ""})

Section = Tab:AddSection("1-2-3 Quest")

Tab:AddButton({
    Title = "Spawn Portal",
    Callback = function()
        if game.Players.LocalPlayer.leaderstats.Glove.Value == "[REDACTED]" then
            game:GetService("ReplicatedStorage").Well:FireServer()
        else
            Fluent:Notify({Title = "Error", Content = "Equip Redacted", Duration = 3})
        end
    end
})

Tab:AddToggle("Toggle_25", {
    Title = "Auto Secure Portal",
    Default = false,
    Callback = function(Value)
        if Value then
            -- START
            if game.workspace:FindFirstChild("WellModel") and workspace.WellModel:FindFirstChild("Portal") then
                
                getgenv().SecurePortalLoop = true
                task.spawn(function()
                    while getgenv().SecurePortalLoop do
                        if game.Players.LocalPlayer.Character and game.Players.LocalPlayer.Character:FindFirstChild("HumanoidRootPart") then
                            game.Players.LocalPlayer.Character.HumanoidRootPart.CFrame = workspace.WellModel.Portal.CFrame
                        end
                        task.wait()
                    end
                end)
            else
                Fluent:Notify({Title = "Error", Content = "Portal Not Spawned/Founded", Duration = 3})
                return false -- Turn toggle off
            end
        else
            -- STOP
            getgenv().SecurePortalLoop = false
            
        end
    end
})

local Tab = Window:AddTab({Title = "bus", Icon = ""})

Section = Tab:AddSection("1 Quest")

Tab:AddButton({
    Title = "bus Slap Aura",
    Callback = function()
if game.Players.LocalPlayer.leaderstats.Glove.Value == "bus" then
-- Get the ReplicatedStorage and Players service
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local Players = game:GetService("Players")

-- Start the infinite loop to fire for all players
while true do
    -- Loop through each player in the game
    for _, player in pairs(Players:GetPlayers()) do
        -- Ensure the player has a character and it contains the "Right Arm"
        if player.Character and player.Character:FindFirstChild("Right Arm") then
            -- Create the arguments table for the FireServer function
            local args = {
                [1] = player.Character["Right Arm"]
            }

            -- Fire the server with the "Right Arm" part as an argument
            ReplicatedStorage.hitbus:FireServer(unpack(args))
        end
    end

    -- Wait 1 second before running the loop again (adjust the wait time as needed)
    task.wait(0.5)
end
else
Fluent:Notify({Title = "Error", Content = "Equip bus", Duration = 3})
end
end
})

Section = Tab:AddSection("2-3 Quest")

Tab:AddButton({
    Title = "Spawn Bus (Normal)",
    Callback = function()
        if game.Players.LocalPlayer.leaderstats.Glove.Value == "Bus" then
            game:GetService("ReplicatedStorage").busmoment:FireServer()
        else
            Fluent:Notify({Title = "Error", Content = "Equip Bus", Duration = 3})
        end
    end
})

Tab:AddButton({
    Title = "Spawn Bus (Fast + Kicks)",
    Callback = function()
        while true do
            game:GetService("ReplicatedStorage").busmoment:FireServer()
            wait()
        end
    end
})

local Tab = Window:AddTab({Title = "Phase", Icon = ""})

Section = Tab:AddSection("1-3 Quest")

local AutoAbiltySlapPhaseRunning = false

Tab:AddToggle("Toggle_26", {
    Title = "Auto Ability + Slap People",
    Default = false,
    Callback = function(Value)
        AutoAbiltySlapPhaseRunning = Value
        local player = game.Players.LocalPlayer

        if AutoAbiltySlapPhaseRunning then
            if player.leaderstats.Glove.Value == "Phase" then
                task.spawn(function()
                    while AutoAbiltySlapPhaseRunning do
                        local players = game:GetService("Players"):GetPlayers()
                        local targetPlayer

                        repeat
                            targetPlayer = players[math.random(1, #players)]
                        until targetPlayer ~= player and targetPlayer.Character and targetPlayer.Character:FindFirstChild("HumanoidRootPart")

                        if targetPlayer and targetPlayer.Character then
                            player.Character.HumanoidRootPart.CFrame = CFrame.new(targetPlayer.Character.HumanoidRootPart.Position)
                        end

                        task.wait(1)
                    end
                end)

                task.spawn(function()
                    while true do
                        game:GetService("ReplicatedStorage").PhaseA:FireServer()
                        task.wait(14)
                    end
                end)
            else
                AutoAbiltySlapPhaseRunning = false
                Fluent:Notify({Title = "Error", Content = "Equip Phase Glove", Duration = 3})
            end
        end
    end
})

Section = Tab:AddSection("2 Quest")

Tab:AddButton({
    Title = "Auto Ability + Teleport To All Islands (Takes 112 Seconds)",
    Callback = function()
        local player = game.Players.LocalPlayer

        if player.leaderstats.Glove.Value == "Phase" and player.Character:FindFirstChild("entered") then
            local function phaseTo(position)
                player.Character.HumanoidRootPart.CFrame = CFrame.new(122, 255, 3)
                task.wait(0.1)
                game:GetService("ReplicatedStorage").PhaseA:FireServer()
                task.wait(0.1)
                player.Character.HumanoidRootPart.CFrame = CFrame.new(position)
                task.wait(14)
            end

            phaseTo(Vector3.new(236, -16, -6))
            phaseTo(Vector3.new()) -- likely needs correction
            phaseTo(Vector3.new(4, -5, -223))
            phaseTo(Vector3.new(-216, -5, -6))
            phaseTo(Vector3.new(-401, 51, -16))
            phaseTo(Vector3.new(256, 34, 194))
            phaseTo(Vector3.new(4, -5, 215))
            phaseTo(Vector3.new(-125, -5, 122))
        else
            Fluent:Notify({Title = "Error", Content = "Equip Phase Glove/Use In Arena", Duration = 3})
        end
    end
})

local Tab = Window:AddTab({Title = "Bomb", Icon = ""})

Section = Tab:AddSection("1-2-3 Quest")

Tab:AddButton({
    Title = "Throw Bomb/Explode Bomb",
    Callback = function()
        if game.Players.LocalPlayer.leaderstats.Glove.Value == "Bomb" then
            game:GetService("ReplicatedStorage").BombThrow:FireServer("Ebutton")
        else
            Fluent:Notify({Title = "Error", Content = "Equip Bomb", Duration = 3})
        end
    end
})

Tab:AddToggle("Toggle_27", {
    Title = "Auto Do 1-2-3 Tasks",
    Default = false,
    Callback = function(Value)
        if Value then
            -- START
            if game.Players.LocalPlayer.leaderstats.Glove.Value == "Bomb" then
                
                getgenv().AutoBombTasksLoop = true
                task.spawn(function()
                    while getgenv().AutoBombTasksLoop do
                        local player = game.Players.LocalPlayer

                        -- Check if character exists
                        if not player.Character or not player.Character:FindFirstChild("HumanoidRootPart") then
                            task.wait(1)
                            continue
                        end

                        -- Move to safe area
                        player.Character.HumanoidRootPart.CFrame = CFrame.new(-908, 328, 5)
                        task.wait(1)

                        -- Move to platform
                        player.Character.HumanoidRootPart.CFrame = CFrame.new(10019, -42, 9960)
                        task.wait(1)

                        -- Throw Bomb
                        game:GetService("ReplicatedStorage").BombThrow:FireServer("Ebutton")
                        task.wait(1)

                        -- Wait to teleport to bomb
                        local bombFound = false
                        for i = 1, 30 do -- Timeout after 30 seconds
                            if not getgenv().AutoBombTasksLoop then break end
                            
                            for _, v in pairs(game.Workspace:GetChildren()) do
                                if string.find(v.Name, "_bømb") then
                                    player.Character.HumanoidRootPart.CFrame = v.CFrame
                                    bombFound = true
                                    break
                                end
                            end
                            
                            if bombFound then
                                break
                            else
                                task.wait(1)
                            end
                        end

                        task.wait(9)
                        
                        -- Kill player
                        local humanoid = player.Character:FindFirstChild("Humanoid")
                        if humanoid then
                            humanoid.Health = 0
                        end
                        
                        task.wait(23)
                    end
                end)
            else
                Fluent:Notify({Title = "Error", Content = "Equip Bomb Glove", Duration = 3})
                return false -- Turn toggle off
            end
        else
            -- STOP
            getgenv().AutoBombTasksLoop = false
            
        end
    end
})

local Tab = Window:AddTab({Title = "Bubble", Icon = ""})

Section = Tab:AddSection("1-2-3-4 Quest")

Tab:AddButton({
	Title = "Use Bubble's Ability",
	Callback = function()  
game:GetService("ReplicatedStorage").BubbleThrow:FireServer()
end
})

Tab:AddToggle("Toggle_28", {
    Title = "Auto Use Bubble's Ability",
    Default = false,
    Callback = function(Value)
        if Value then
            -- START
            
            getgenv().AutoBubbleLoop = true
            task.spawn(function()
                while getgenv().AutoBubbleLoop do
                    game:GetService("ReplicatedStorage").BubbleThrow:FireServer()
                    task.wait(10)
                end
            end)
        else
            -- STOP
            getgenv().AutoBubbleLoop = false
            
        end
    end
})

Tab:AddButton({
    Title = "Bubble Slap Aura",
    Callback = function()
if game.Players.LocalPlayer.leaderstats.Glove.Value == "Bubble" then
-- Get the ReplicatedStorage and Players service
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local Players = game:GetService("Players")

-- Start the infinite loop to fire for all players
while true do
    -- Loop through each player in the game
    for _, player in pairs(Players:GetPlayers()) do
        -- Ensure the player has a character and it contains the "Right Arm"
        if player.Character and player.Character:FindFirstChild("Right Arm") then
            -- Create the arguments table for the FireServer function
            local args = {
                [1] = player.Character["Right Arm"]
            }

            -- Fire the server with the "Right Arm" part as an argument
            ReplicatedStorage.BubbleHit:FireServer(unpack(args))
        end
    end

    -- Wait 1 second before running the loop again (adjust the wait time as needed)
    task.wait(0.5)
end
else
Fluent:Notify({Title = "Error", Content = "Equip Bubble", Duration = 3})
end
end
})

local Tab = Window:AddTab({Title = "Shard", Icon = ""})

Section = Tab:AddSection("1-2-3-4-5 Quest")

local running = false -- declare global toggle state

Tab:AddToggle("Toggle_29", {
    Title = "Auto Use Shard's Glove Ability",
    Default = false,
    Callback = function(Value)
        if Value then
            -- Start the loop with exact 0.1 second interval
            shardCloneLoop = true
            
            spawn(function()
                while cherryCloneLoop do
                    local player = game.Players.LocalPlayer
                    
                    -- Check if player has Cherry glove
                    if player.leaderstats and player.leaderstats.Glove and player.leaderstats.Glove.Value == "Shard" then
                        -- Check if player has entered
                        if player.Character and player.Character:FindFirstChild("entered") then
                            -- Fire the Cherry remote
                            game:GetService("ReplicatedStorage").Events.Shards:FireServer()
                        else
                            Fluent:Notify({Title = "Not Entered", Content = "You need to enter the arena first!", Duration = 3})
                            wait(1) -- Wait longer when not entered
                        end
                    else
                        Fluent:Notify({Title = "Wrong Glove", Content = "You need Shard glove equipped!", Duration = 3})
                        wait(1) -- Wait longer when wrong glove
                    end
                    
                    wait(0.1) -- Normal 0.1 second delay
                end
            end)
        else
            -- Stop the loop
            shardCloneLoop = false
        end
    end
})

Tab:AddButton({
    Title = "Shard Slap Aura",
    Callback = function()
        while true do
            local players = game:GetService("Players")
            local replicatedStorage = game:GetService("ReplicatedStorage")

            for _, player in ipairs(players:GetPlayers()) do
                local character = player.Character
                if character then
                    local leftArm = character:FindFirstChild("Left Arm")
                    if leftArm then
                        replicatedStorage.ShardHIT:FireServer(leftArm)
                    end
                end
            end

            wait(0.5)
        end
    end
})

local Tab = Window:AddTab({Title = "Moon", Icon = ""})

Section = Tab:AddSection("1-2 Quest")

Tab:AddToggle("Toggle_30", {
    Title = "Auto Ragdoll + Touch Ground",
    Default = false,
    Callback = function(Value)
        if Value then
            -- Start the loop
            autoRagdollLoop = game:GetService("RunService").Heartbeat:Connect(function()
                -- Check if player has entered
                if game.Players.LocalPlayer.Character and game.Players.LocalPlayer.Character:FindFirstChild("entered") then
                    -- Fire knockback remote
                    game.ReplicatedStorage.SelfKnockback:FireServer({
                        ["Force"] = 0,
                        ["Direction"] = Vector3.new(0, 0.01, 0)
                    })
                    
                    -- Teleport to position
                    if game.Players.LocalPlayer.Character:FindFirstChild("HumanoidRootPart") then
                        game.Players.LocalPlayer.Character.HumanoidRootPart.CFrame = CFrame.new(1, -5, 8)
                    end
                end
            end)
        else
            -- Stop the loop
            if autoRagdollLoop then
                autoRagdollLoop:Disconnect()
                autoRagdollLoop = nil
            end
        end
    end
})

Tab:AddToggle("Toggle_31", {
    Title = "Auto Spam Equip/Unequip Moon Glove",
    Default = false,
    Callback = function(Value)
        if Value then
            -- Start the loop
            moonGloveLoop = game:GetService("RunService").Heartbeat:Connect(function()
                local player = game.Players.LocalPlayer
                local backpack = player:FindFirstChild("Backpack")
                
                if backpack then
                    local moonTool = backpack:FindFirstChild("Moon")
                    
                    if moonTool then
                        -- Check if tool is currently equipped
                        local character = player.Character
                        local currentlyEquipped = character and character:FindFirstChild("Moon")
                        
                        if currentlyEquipped then
                            -- Unequip: move tool back to backpack
                            moonTool.Parent = backpack
                        else
                            -- Equip: move tool to character
                            moonTool.Parent = character
                        end
                    end
                end
            end)
        else
            -- Stop the loop
            if moonGloveLoop then
                moonGloveLoop:Disconnect()
                moonGloveLoop = nil
            end
        end
    end
})

local Tab = Window:AddTab({Title = "Rage", Icon = ""})

Section = Tab:AddSection("1-4 Quest")

Tab:AddButton({
    Title = "Rage Slap Aura",
    Callback = function()
        local ReplicatedStorage = game:GetService("ReplicatedStorage")
        local Players = game:GetService("Players")

        local function fireForAllPlayers()
            for _, player in pairs(Players:GetPlayers()) do
                if player.Character and player.Character:FindFirstChild("Torso") then
                    ReplicatedStorage.GRRRR:FireServer(player.Character.Torso)
                end
            end
        end

        while true do
            fireForAllPlayers()
            wait(0.5)
        end
    end
})

Tab:AddButton({
    Title = "Use Rage Ability",
    Callback = function()
game:GetService("ReplicatedStorage").GRRRRR:FireServer()
    end
})

local Tab = Window:AddTab({Title = "Chain", Icon = ""})

Section = Tab:AddSection("1-2-3 Quest")

Tab:AddToggle("Toggle_32", {
    Title = "Auto Slap Players",
    Default = false,
    Callback = function(AutoSlapChain)
        running = AutoSlapChain
        if not running then return end

        task.spawn(function()
            local Players = game:GetService("Players")
            local ReplicatedStorage = game:GetService("ReplicatedStorage")
            local LocalPlayer = Players.LocalPlayer

            local function teleportToRandomPlayer()
                local playerList = Players:GetPlayers()
                if #playerList > 1 then
                    local randomPlayer
                    repeat
                        randomPlayer = playerList[math.random(1, #playerList)]
                    until randomPlayer ~= LocalPlayer

                    local targetChar = randomPlayer.Character
                    if targetChar and targetChar:FindFirstChild("HumanoidRootPart") then
                        LocalPlayer.Character.HumanoidRootPart.CFrame = CFrame.new(
                            targetChar.HumanoidRootPart.Position + Vector3.new(0, 3, 0)
                        )
                    end
                end
            end

            local function fireGeneralHitForAllPlayers()
                for _, player in pairs(Players:GetPlayers()) do
                    if player ~= LocalPlayer and player.Character and player.Character:FindFirstChild("HumanoidRootPart") then
                        ReplicatedStorage:WaitForChild("GeneralHit"):FireServer(player.Character.HumanoidRootPart)
                    end
                end
            end

            local count = 0
            while running and count < 750 do
                if LocalPlayer.leaderstats.Glove.Value == "Chain" then
                    teleportToRandomPlayer()
                    fireGeneralHitForAllPlayers()
                    count += 1
                    task.wait(2.1)
                else
                    Fluent:Notify({Title = "Error", Content = "Equip Chain Glove", Duration = 3})
                    break
                end
            end
        end)
    end
})

local Tab = Window:AddTab({Title = "Tycoon", Icon = ""})

Section = Tab:AddSection("1-3 Quest")

Tab:AddButton({
    Title = "Place Tycoon To Safe Spot",
    Callback = function()
        if game.Players.LocalPlayer.leaderstats.Glove.Value == "Tycoon" then
            local player = game.Players.LocalPlayer

            -- Step 1: Move to spawn
            player.Character.HumanoidRootPart.CFrame = CFrame.new(0, 0, 0)
            task.wait(1)

            -- Step 2: Move to safe tycoon location
            player.Character.HumanoidRootPart.CFrame = CFrame.new(17884, -24, -3539)
            task.wait(1)

            -- Step 3: Activate ability
            local args = {
                [1] = CFrame.new(17893.552734375, -26.257728576660156, -3535.35888671875, 
                0.00862560048699379, 3.3499091500743816e-08, -0.9999628067016602, 
                8.43312697451637e-10, 1, 3.350761090814558e-08, 
                0.9999628067016602, -1.1323045834288337e-09, 0.00862560048699379)
            }

            game:GetService("ReplicatedStorage"):WaitForChild("GeneralAbility"):FireServer(unpack(args))
            task.wait(1)

            -- Step 4: Teleport back to spawn
            player.Character.HumanoidRootPart.CFrame = CFrame.new(0, 0, 0)
        else
            Fluent:Notify({Title = "Error", Content = "Equip Tycoon Glove/Get Safespot", Duration = 3})
        end
    end
})

local running = false

Tab:AddToggle("Toggle_33", {
    Title = "Auto Slap Players",
    Default = false,
    Callback = function(AutoSlapTycoon)
        running = AutoSlapTycoon
        if not running then return end

        task.spawn(function()
            local Players = game:GetService("Players")
            local ReplicatedStorage = game:GetService("ReplicatedStorage")
            local LocalPlayer = Players.LocalPlayer

            local function teleportToRandomPlayer()
                local playerList = Players:GetPlayers()
                if #playerList > 1 then
                    local randomPlayer
                    repeat
                        randomPlayer = playerList[math.random(1, #playerList)]
                    until randomPlayer ~= LocalPlayer

                    local targetChar = randomPlayer.Character
                    if targetChar and targetChar:FindFirstChild("HumanoidRootPart") then
                        LocalPlayer.Character.HumanoidRootPart.CFrame = CFrame.new(
                            targetChar.HumanoidRootPart.Position + Vector3.new(0, 3, 0)
                        )
                    end
                end
            end

            local function fireGeneralHitForAllPlayers()
                for _, player in pairs(Players:GetPlayers()) do
                    if player ~= LocalPlayer and player.Character and player.Character:FindFirstChild("HumanoidRootPart") then
                        ReplicatedStorage:WaitForChild("GeneralHit"):FireServer(player.Character.HumanoidRootPart)
                    end
                end
            end

            local count = 0
            while running and count < 750 do
                if LocalPlayer.leaderstats.Glove.Value == "Tycoon" then
                    teleportToRandomPlayer()
                    fireGeneralHitForAllPlayers()
                    count += 1
                    task.wait(2.1)
                else
                    Fluent:Notify({Title = "Error", Content = "Equip Tycoon Glove", Duration = 3})
                    break
                end
            end
        end)
    end
})

local Tab = Window:AddTab({Title = "🗿", Icon = ""})

Section = Tab:AddSection("1 Quest")

Tab:AddButton({
    Title = "Spawn 🗿",
    Callback = function()
        if game.Players.LocalPlayer.leaderstats.Glove.Value == "🗿" then
            local args = {
                [1] = CFrame.new(0, 0, 0) * CFrame.Angles(0, 0, 0)
            }
            game:GetService("ReplicatedStorage").GeneralAbility:FireServer(unpack(args))
        else
            Fluent:Notify({Title = "Error", Content = "Equip 🗿", Duration = 3})
        end
    end
})

Section = Tab:AddSection("2-3 Quest")

Tab:AddButton({
    Title = "🗿 Slap Aura",
    Callback = function()
        local ReplicatedStorage = game:GetService("ReplicatedStorage")
        local Players = game:GetService("Players")

        local function fireForAllPlayers()
            for _, player in pairs(Players:GetPlayers()) do
                if player.Character and player.Character:FindFirstChild("Torso") then
                    ReplicatedStorage.GeneralHit:FireServer(player.Character.Torso)
                end
            end
        end

        while true do
            fireForAllPlayers()
            wait(1)
        end
    end
})

local Tab = Window:AddTab({Title = "Obby", Icon = ""})

Section = Tab:AddSection("1 Quest")

Tab:AddButton({
    Title = "Obby Slap Aura",
    Callback = function()
        while true do
            local players = game:GetService("Players")
            local replicatedStorage = game:GetService("ReplicatedStorage")

            for _, player in ipairs(players:GetPlayers()) do
                local character = player.Character
                if character then
                    local leftArm = character:FindFirstChild("Left Arm")
                    if leftArm then
                        replicatedStorage.GeneralHit:FireServer(leftArm)
                    end
                end
            end

            wait(1)
        end
    end
})

local Tab = Window:AddTab({Title = "Voodoo", Icon = ""})

Section = Tab:AddSection("1-2 Quest")

local running = false -- declare global toggle state

Tab:AddToggle("Toggle_34", {
    Title = "Auto Slap Players",
    Default = false,
    Callback = function(AutoSlapVoodoo)
        running = AutoSlapVoodoo
        if not running then return end

        task.spawn(function()
            local Players = game:GetService("Players")
            local ReplicatedStorage = game:GetService("ReplicatedStorage")
            local LocalPlayer = Players.LocalPlayer

            local function teleportToRandomPlayer()
                local playerList = Players:GetPlayers()
                if #playerList > 1 then
                    local randomPlayer = nil
                    repeat
                        randomPlayer = playerList[math.random(1, #playerList)]
                    until randomPlayer ~= LocalPlayer

                    local targetChar = randomPlayer.Character
                    if targetChar and targetChar:FindFirstChild("HumanoidRootPart") then
                        LocalPlayer.Character.HumanoidRootPart.CFrame = CFrame.new(targetChar.HumanoidRootPart.Position + Vector3.new(0, 3, 0))
                    end
                end
            end

            local function fireGeneralHitForAllPlayers()
                for _, player in pairs(Players:GetPlayers()) do
                    if player ~= LocalPlayer and player.Character and player.Character:FindFirstChild("HumanoidRootPart") then
                        ReplicatedStorage:WaitForChild("GeneralHit"):FireServer(player.Character.HumanoidRootPart)
                    end
                end
            end

            local count = 0
            while running and count < 750 do
                if LocalPlayer.leaderstats.Glove.Value == "Voodoo" then
                    teleportToRandomPlayer()
                    fireGeneralHitForAllPlayers()
                    count += 1
                    task.wait(1)
                else
                    Fluent:Notify({Title = "Error", Content = "Equip Voodoo Glove", Duration = 3})
                    break
                end
            end
        end)
    end
})

Section = Tab:AddSection("3 Quest")

local voodooToggle = false
local loopThread = nil

Tab:AddToggle("Toggle_35", {
    Title = "Voodoo Spam Ability Without CD",
    Default = false,
    Callback = function(state)
        voodooToggle = state
        local player = game.Players.LocalPlayer

        if voodooToggle then
            if player.leaderstats and player.leaderstats.Glove.Value == "Voodoo" then
                loopThread = task.spawn(function()
                    while voodooToggle and player.leaderstats.Glove.Value == "Voodoo" do
                        game:GetService("ReplicatedStorage").GeneralAbility:FireServer()
                        task.wait(3.5)
                    end
                end)
            else
                voodooToggle = false
                Fluent:Notify({Title = "Error", Content = "Equip Voodoo Glove", Duration = 3})
            end
        else
            if loopThread then
                print("Voodoo spam stopped")
                loopThread = nil
            end
        end
    end
})

local Tab = Window:AddTab({Title = "rob", Icon = ""})

Section = Tab:AddSection("1 Quest")

Tab:AddButton({
    Title = "Create Null Portal (Need Bob And Rob Gloves)",
    Callback = function()
        game.Players.LocalPlayer.Character.HumanoidRootPart.CFrame = CFrame.new()
        wait(0.5)

        fireclickdetector(game:GetService("Workspace").Lobby.rob.ClickDetector)
        wait(0.5)
        game:GetService("ReplicatedStorage").rob:FireServer()
        wait(0.5)

        fireclickdetector(game:GetService("Workspace").Lobby.bob.ClickDetector)
        wait(0.5)
        game:GetService("ReplicatedStorage").bob:FireServer()

        Fluent:Notify({Title = "Error", Content = "Need Bob/Rob Badges To Own", Duration = 3})
    end
})

Section = Tab:AddSection("2 Quest")

Tab:AddSlider("Slider_2", {
    Title = "WalkSpeed",
    Min = 20,
    Max = 1000,
    Default = 20,
    Rounding = 0,
    Callback = function(Value)
        game.Players.LocalPlayer.Character.Humanoid.WalkSpeed = Value
        Walkspeed = Value
    end
})

Tab:AddInput("Input_1", {
    Title = "WalkSpeed",
    Default = "UserSpeed",
    TextDisappear = false,
    Callback = function(Value)
        game.Players.LocalPlayer.Character.Humanoid.WalkSpeed = Value
        Walkspeed = Value
    end
})

Tab:AddToggle("Toggle_36", {
    Title = "Walkspeed Set Auto",
    Default = false,
    Callback = function(Value)
        KeepWalkspeed = Value

        while KeepWalkspeed do
            if game.Players.LocalPlayer.Character:FindFirstChild("Humanoid") and
               game.Players.LocalPlayer.Character.Humanoid.WalkSpeed ~= Walkspeed then
                game.Players.LocalPlayer.Character.Humanoid.WalkSpeed = Walkspeed
            end
            task.wait()
        end
    end
})

Section = Tab:AddSection("3 Quest")

local AutoAbsorb = false

Tab:AddToggle("Toggle_37", {
    Title = "Auto Absorb People",
    Default = false,
    Callback = function(Value)
        AutoAbsorb = Value
        local player = game.Players.LocalPlayer

        if AutoAbsorb then
            if player.leaderstats.Glove.Value:lower() == "rob" then
                task.spawn(function()
                    while AutoAbsorb do
                        local players = game:GetService("Players"):GetPlayers()
                        local target

                        repeat
                            target = players[math.random(1, #players)]
                        until target ~= player and target.Character and target.Character:FindFirstChild("HumanoidRootPart")

                        local targetHRP = target.Character:FindFirstChild("HumanoidRootPart")
                        if targetHRP and player.Character:FindFirstChild("HumanoidRootPart") then
                            player.Character.HumanoidRootPart.CFrame = CFrame.new(targetHRP.Position + Vector3.new(0, 0, 2))
                        end

                        game:GetService("ReplicatedStorage"):FindFirstChild("Ability"):FireServer()
                        task.wait(1)
                    end
                end)
            else
                AutoAbsorb = false
                Fluent:Notify({Title = "Error", Content = "Equip Rob Glove", Duration = 3})
            end
        end
    end
})

Tab:AddToggle("Toggle_38", {
    Title = "Auto Rob Transformation",
    Default = false,
    Callback = function(Value)
        if Value then
            -- START
            
            getgenv().AutoRobLoop = true
            task.spawn(function()
                while getgenv().AutoRobLoop do
                    -- Check if the ClickDetector exists
                    local robObject = game:GetService("Workspace"):FindFirstChild("Lobby")
                    if robObject then
                        robObject = robObject:FindFirstChild("rob")
                        if robObject and robObject:FindFirstChild("ClickDetector") then
                            fireclickdetector(robObject.ClickDetector)
                        end
                    end
                    
                    task.wait(0.5)
                    
                    -- Fire the remote
                    game:GetService("ReplicatedStorage").rob:FireServer()
                    
                    task.wait(17)
                end
            end)
        else
            -- STOP
            getgenv().AutoRobLoop = false
            
        end
    end
})

local Tab = Window:AddTab({Title = "Tinkerer", Icon = ""})

Section = Tab:AddSection("1-3 Quest")

Tab:AddToggle("Toggle_39", {
    Title = "Auto Spawn Frenzy Bots",
    Default = false,
    Callback = function(Value)
        if Value then
            -- START
            
            getgenv().AutoFrenzyLoop = true
            task.spawn(function()
                while getgenv().AutoFrenzyLoop do
                    game:GetService("ReplicatedStorage").GeneralAbility:FireServer()
                    task.wait(7)
                end
            end)
        else
            -- STOP
            getgenv().AutoFrenzyLoop = false
            
        end
    end
})

Tab:AddButton({
	Title = "Tinkerer Slap Aura",
	Callback = function()   
if game.Players.LocalPlayer.leaderstats.Glove.Value == "Tinkerer" then
-- Get the ReplicatedStorage and Players service
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local Players = game:GetService("Players")

-- Start the infinite loop to fire for all players
while true do
    -- Loop through each player in the game
    for _, player in pairs(Players:GetPlayers()) do
        -- Ensure the player has a character and it contains the "Left Leg"
        if player.Character and player.Character:FindFirstChild("Left Leg") then
            -- Create the arguments table for the FireServer function
            local args = {
                [1] = player.Character["Left Leg"],
                [2] = false  -- The second argument, set to false as in your original script
            }

            -- Fire the server with the "Left Leg" part as an argument
            ReplicatedStorage.TinkererHit:FireServer(unpack(args))
        end
    end

    -- Wait 1 second before running the loop again (adjust the wait time as needed)
    wait(0.5)
end
else
Fluent:Notify({Title = "Error", Content = "Equip Tinkerer", Duration = 3})
end
end
})

local Tab = Window:AddTab({Title = "Glovel", Icon = ""})

Section = Tab:AddSection("1 Quest")

Tab:AddButton({
    Title = "Dig 350 Times",
    Callback = function()
        if game.Players.LocalPlayer.leaderstats.Glove.Value == "Glovel" then
            while true do
                game:GetService("ReplicatedStorage").GlovelFunc:InvokeServer()
                wait()
            end
        else
            Fluent:Notify({Title = "Error", Content = "Equip Glovel Glove", Duration = 3})
        end
    end
})

Section = Tab:AddSection("2-3 Quest")

Tab:AddButton({
    Title = "Glovel Slap Aura (Only Critical Hits)",
    Callback = function()
        while true do
            local players = game:GetService("Players")
            local replicatedStorage = game:GetService("ReplicatedStorage")

            for _, player in ipairs(players:GetPlayers()) do
                if player.Character and player.Character:FindFirstChild("Head") then
                    local args = {
                        player.Character.Head,
                        true
                    }

                    replicatedStorage.GeneralHit:FireServer(unpack(args))
                end
            end

            wait(1)
        end
    end
})

local Tab = Window:AddTab({Title = "Run", Icon = ""})

Section = Tab:AddSection("1-2 Quests")

Tab:AddButton({
    Title = "Teleport To Closest Player",
    Callback = function()
        local LocalPlayer = game:GetService("Players").LocalPlayer
        local localChar = LocalPlayer.Character
        local localHRP = localChar and localChar:FindFirstChild("HumanoidRootPart")
        
        if not localHRP then
            Fluent:Notify({Title = "Error", Content = "Your character not found!", Duration = 2})
            return
        end
        
        local closestPlayer = nil
        local closestDistance = math.huge
        local closestPos = nil
        
        for _, player in ipairs(game:GetService("Players"):GetPlayers()) do
            if player ~= LocalPlayer then
                local character = player.Character
                local hrp = character and character:FindFirstChild("HumanoidRootPart")
                if hrp then
                    local distance = (hrp.Position - localHRP.Position).Magnitude
                    if distance < closestDistance then
                        closestDistance = distance
                        closestPlayer = player
                        closestPos = hrp.Position
                    end
                end
            end
        end
        
        if closestPlayer and closestPos then
            localChar.HumanoidRootPart.CFrame = CFrame.new(closestPos)
            Fluent:Notify({Title = "Teleported", Content = "Teleported to ", Duration = 2})
        else
            Fluent:Notify({Title = "Error", Content = "No other players found!", Duration = 2})
        end
    end
})

-- Textbox for Vector3 position (format: x, y, z)
local positionInput = Tab:AddInput("Input_2", {
    Title = "Put Position (x, y, z)",
    Default = "0, 0, 0",
    TextDisappear = false,
    Callback = function(Value)
        local parts = {}
        for part in string.gmatch(Value, "([^,]+)") do
            table.insert(parts, tonumber(part))
        end
        
        if #parts >= 3 and parts[1] and parts[2] and parts[3] then
            positionValue = Vector3.new(parts[1], parts[2], parts[3])
        else
            positionValue = Vector3.new(0, 0, 0)
            positionInput:SetValue("0, 0, 0")
            Fluent:Notify({Title = "Invalid Input", Content = "Please use format: x, y, z", Duration = 2})
        end
    end
})

-- Textbox for seconds delay
local secondsInput = Tab:AddInput("Input_3", {
    Title = "Repeat Ability Seconds Delay",
    Default = "7",
    TextDisappear = false,
    Callback = function(Value)
        local num = tonumber(Value)
        if num and num > 0 then
            secondsValue = num
        else
            secondsValue = 7
            secondsInput:SetValue("7")
        end
    end
})

-- Default values
local positionValue = Vector3.new(0, 0, 0)
local secondsValue = 7
local autoRunEnabled = false
local autoRunConnection = nil

-- Toggle for Auto Use Run Ability
Tab:AddToggle("Toggle_40", {
    Title = "Auto Use Run Ability",
    Default = false,
    Callback = function(Value)
        autoRunEnabled = Value
        
        if autoRunEnabled then
            if autoRunConnection then
                autoRunConnection:Disconnect()
                autoRunConnection = nil
            end
            
            autoRunConnection = game:GetService("RunService").Stepped:Connect(function()
                if autoRunEnabled then
                    local args = {
                        [1] = positionValue
                    }
                    game:GetService("ReplicatedStorage").GeneralAbility:FireServer(unpack(args))
                    task.wait(secondsValue)
                end
            end)
            
            Fluent:Notify({Title = "Auto Run", Content = "", Duration = 3})
        else
            if autoRunConnection then
                autoRunConnection:Disconnect()
                autoRunConnection = nil
            end
            
            Fluent:Notify({Title = "Auto Run", Content = "Disabled!", Duration = 2})
        end
    end
})

local Tab = Window:AddTab({Title = "Plank", Icon = ""})

Section = Tab:AddSection("1-2-3 Quests")

local AutoPlankToggle = Tab:AddToggle("Toggle_41", {
    Title = "Auto Use Plank On Players",
    Default = false,
    Callback = function(Value)
        _G.AutoPlank = Value
        
        -- Main loop for auto plank
        while _G.AutoPlank and task.wait(0.1) do
            -- Get all players except yourself
            local players = game:GetService("Players"):GetPlayers()
            local targetPlayers = {}
            
            for _, player in ipairs(players) do
                if player ~= game:GetService("Players").LocalPlayer and player.Character then
                    -- Get the humanoid root part of the target player
                    local hrp = player.Character:FindFirstChild("HumanoidRootPart")
                    if hrp then
                        table.insert(targetPlayers, {
                            player = player,
                            position = hrp.Position
                        })
                    end
                end
            end
            
            -- If there are valid players, pick a random one
            if #targetPlayers > 0 then
                local randomTarget = targetPlayers[math.random(1, #targetPlayers)]
                
                -- Create the args with the target player's position
                local args = {
                    [1] = "OutOfBody",
                    [2] = {
                        ["mousebehavior"] = Enum.MouseBehavior.Default,
                        ["ismoving"] = false,
                        ["dir"] = Vector3.new(
                            randomTarget.position.X,
                            randomTarget.position.Y,
                            randomTarget.position.Z
                        )
                    }
                }
                
                -- Fire the remote
                game:GetService("ReplicatedStorage").Blink:FireServer(unpack(args))
            end
        end
    end
})

local Tab = Window:AddTab({Title = "Eggler", Icon = ""})

Tab:AddButton({
	Title = "Use Eggler (Tongue) Ability",
	Callback = function()   
game:GetService("ReplicatedStorage"):WaitForChild("GeneralAbility"):FireServer()
end
})

Tab:AddButton({
	Title = "Use Eggler (Egg Yourself) Ability",
	Callback = function()   
game:GetService("ReplicatedStorage"):WaitForChild("Events"):WaitForChild("EgglerRAbility"):FireServer()
end
})

Tab:AddButton({
	Title = "Eggler Slap Aura",
	Callback = function()   
if game.Players.LocalPlayer.leaderstats.Glove.Value == "Eggler" then
-- Get the ReplicatedStorage and Players service
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local Players = game:GetService("Players")

-- Start the infinite loop to fire for all players
while true do
    -- Loop through each player in the game
    for _, player in pairs(Players:GetPlayers()) do
        -- Ensure the player has a character and it contains the "Left Leg"
        if player.Character and player.Character:FindFirstChild("Left Leg") then
            -- Create the arguments table for the FireServer function
            local args = {
                [1] = player.Character["Left Leg"],
                [2] = false  -- The second argument, set to false as in your original script
            }

            -- Fire the server with the "Left Leg" part as an argument
            ReplicatedStorage.GeneralHit:FireServer(unpack(args))
        end
    end

    -- Wait 1 second before running the loop again (adjust the wait time as needed)
    wait(0.5)
end
else
Fluent:Notify({Title = "Error", Content = "Equip Eggler", Duration = 3})
end
end
})

local Tab = Window:AddTab({Title = "Car Keys", Icon = ""})

Section = Tab:AddSection("1 Quest")

local toggleOn = false
local upgradeLoop = nil

local attributeFrames = {
    "UtilityAttributeFrame",
    "ArmorAttributeFrame",
    "ReverseAttributeFrame",
    "SteerAngleAttributeFrame",
    "AccelerationAttributeFrame",
    "MaxSpeedAttributeFrame",
    "KnockbackAttributeFrame"
}

Tab:AddToggle("Toggle_42", {
    Title = "Auto Upgrade All Stats",
    Default = false,
    Callback = function(Value)
        toggleOn = Value
        if toggleOn then
            -- Start upgrade loop
            upgradeLoop = task.spawn(function()
                while toggleOn do
                    for _, attr in ipairs(attributeFrames) do
                        local args = { attr }
                        game:GetService("ReplicatedStorage"):WaitForChild("CarKeysUpgrade"):FireServer(unpack(args))
                        task.wait(0.1) -- small delay to prevent spamming too fast
                    end
                    task.wait(0.5) -- wait between full upgrade cycles
                end
            end)
        else
            -- Stop loop by breaking the while condition
            toggleOn = false
        end
    end
})

Section = Tab:AddSection("2-3 Quest")

Tab:AddButton({
    Title = "Car Keys Slap Aura",
    Callback = function()
if game.Players.LocalPlayer.leaderstats.Glove.Value == "Car Keys" then
-- Get the ReplicatedStorage and Players service
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local Players = game:GetService("Players")

-- Start the infinite loop to fire for all players
while true do
    -- Loop through each player in the game
    for _, player in pairs(Players:GetPlayers()) do
        -- Ensure the player has a character and it contains the "Right Arm"
        if player.Character and player.Character:FindFirstChild("Right Arm") then
            -- Create the arguments table for the FireServer function
            local args = {
                [1] = player.Character["Right Arm"]
            }

            -- Fire the server with the "Right Arm" part as an argument
            ReplicatedStorage.GeneralHit:FireServer(unpack(args))
        end
    end

    -- Wait 1 second before running the loop again (adjust the wait time as needed)
    task.wait(0.5)
end
else
Fluent:Notify({Title = "Error", Content = "Equip Car Keys", Duration = 3})
end
end
})

local Tab = Window:AddTab({Title = "Ultra Instinct", Icon = ""})

Section = Tab:AddSection("1-2 Quest")

local running = false -- declare global toggle state

Tab:AddToggle("Toggle_43", {
    Title = "Auto Slap Players",
    Default = false,
    Callback = function(AutoSlapVoodoo)
        running = AutoSlapVoodoo

        if not running then
            return -- stop immediately if toggled off
        end

        -- Run everything in a separate thread
        task.spawn(function()
            local Players = game:GetService("Players")
            local ReplicatedStorage = game:GetService("ReplicatedStorage")
            local LocalPlayer = Players.LocalPlayer

            -- Function to teleport to a random player
            local function teleportToRandomPlayer()
                local playerList = Players:GetPlayers()
                if #playerList > 1 then
                    local randomPlayer = nil
                    repeat
                        randomPlayer = playerList[math.random(1, #playerList)]
                    until randomPlayer ~= LocalPlayer

                    local targetChar = randomPlayer.Character
                    if targetChar and targetChar:FindFirstChild("HumanoidRootPart") then
                        LocalPlayer.Character.HumanoidRootPart.CFrame = CFrame.new(targetChar.HumanoidRootPart.Position + Vector3.new(0, 3, 0))
                    end
                end
            end

            -- Function to fire GeneralHit for all players
            local function fireGeneralHitForAllPlayers()
                for _, player in pairs(Players:GetPlayers()) do
                    if player ~= LocalPlayer and player.Character and player.Character:FindFirstChild("HumanoidRootPart") then
                        ReplicatedStorage:WaitForChild("GeneralHit"):FireServer(player.Character.HumanoidRootPart)
                    end
                end
            end

            -- Loop 750 times or until stopped
            local count = 0
            while running and count < 750 do
                if LocalPlayer.leaderstats.Glove.Value == "Ultra Instinct" then
                    teleportToRandomPlayer()
                    fireGeneralHitForAllPlayers()
                    count += 1
                    task.wait(1)
                else
                    Fluent:Notify({Title = "Error", Content = "Equip Ultra Instinct Glove", Duration = 3})
                    break
                end
            end
        end)
    end
})

Tab:AddButton({
    Title = "Teleport to Random Player",
    Callback = function()
        local players = {}
        local localPlayer = game.Players.LocalPlayer
        
        -- Get all players except yourself
        for _, player in pairs(game.Players:GetPlayers()) do
            if player ~= localPlayer and player.Character then
                local hrp = player.Character:FindFirstChild("HumanoidRootPart")
                if hrp then
                    table.insert(players, player)
                end
            end
        end
        
        if #players > 0 then
            -- Select random player
            local randomPlayer = players[math.random(1, #players)]
            local targetHRP = randomPlayer.Character.HumanoidRootPart
            
            -- Teleport to the random player
            if localPlayer.Character and localPlayer.Character:FindFirstChild("HumanoidRootPart") then
                localPlayer.Character.HumanoidRootPart.CFrame = targetHRP.CFrame
                
            end
        else

        end
    end
})

Tab:AddToggle("Toggle_44", {
    Title = "Auto Teleport to Random Players",
    Default = false,
    Callback = function(Value)
        if Value then
            -- START
            
            getgenv().AutoTeleportPlayersLoop = true
            task.spawn(function()
                while getgenv().AutoTeleportPlayersLoop do
                    local localPlayer = game.Players.LocalPlayer
                    local players = {}
                    
                    -- Get all players except yourself
                    for _, player in pairs(game.Players:GetPlayers()) do
                        if player ~= localPlayer and player.Character then
                            local hrp = player.Character:FindFirstChild("HumanoidRootPart")
                            if hrp then
                                table.insert(players, player)
                            end
                        end
                    end
                    
                    if #players > 0 and localPlayer.Character and localPlayer.Character:FindFirstChild("HumanoidRootPart") then
                        -- Select random player
                        local randomPlayer = players[math.random(1, #players)]
                        local targetHRP = randomPlayer.Character.HumanoidRootPart
                        
                        -- Teleport to the random player
                        localPlayer.Character.HumanoidRootPart.CFrame = targetHRP.CFrame
                    end
                    
                    task.wait(0.1) -- Wait 0.1 seconds as requested
                end
            end)
        else
            -- STOP
            getgenv().AutoTeleportPlayersLoop = false
            
        end
    end
})

local Tab = Window:AddTab({Title = "Acrobat", Icon = ""})

Section = Tab:AddSection("1-2-3 Quest")

Tab:AddButton({
    Title = "No Cooldown Abilities Script (NOT AUTO)",
    Callback = function()
local player = game.Players.LocalPlayer
local character = player.Character or player.CharacterAdded:Wait()
local tool = character:FindFirstChildOfClass("Tool") or player.Backpack:FindFirstChildOfClass("Tool")

while character.Humanoid.Health ~= 0 do
local localscript = tool:FindFirstChildOfClass("LocalScript")
local localscriptclone = localscript:Clone()
localscriptclone = localscript:Clone()
localscriptclone:Clone()
localscript:Destroy()
localscriptclone.Parent = tool
wait(0.1)
end
end
})

Tab:AddButton({
    Title = "Acrobat Slap Aura",
    Callback = function()
if game.Players.LocalPlayer.leaderstats.Glove.Value == "Acrobat" then
-- Get the ReplicatedStorage and Players service
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local Players = game:GetService("Players")

-- Start the infinite loop to fire for all players
while true do
    -- Loop through each player in the game
    for _, player in pairs(Players:GetPlayers()) do
        -- Ensure the player has a character and it contains the "Right Arm"
        if player.Character and player.Character:FindFirstChild("Right Arm") then
            -- Create the arguments table for the FireServer function
            local args = {
                [1] = player.Character["Right Arm"]
            }

            -- Fire the server with the "Right Arm" part as an argument
            ReplicatedStorage.AcHit:FireServer(unpack(args))
        end
    end

    -- Wait 1 second before running the loop again (adjust the wait time as needed)
    task.wait(0.5)
end
else
Fluent:Notify({Title = "Error", Content = "Equip Acrobat", Duration = 3})
end
end
})

-- SETTINGS
local Tab = Window:AddTab({Title = "Settings", Icon = ""})

-- Section: Value Changer
Section = Tab:AddSection("Value Changer")

Tab:AddInput("Input_4", {
    Title = "How Many Slaps? (Visual)",
    Default = "",
    TextDisappear = true,
    Callback = function(Value)
        local slapNumber = tonumber(Value)
        if slapNumber and slapNumber > 0 then
            SlapChanger = slapNumber
        else
            Fluent:Notify({Title = "Error", Content = "Type a correct number (greater than 0)!", Duration = 3})
        end
    end
})

Tab:AddButton({
    Title = "Set Slaps (Visual)",
    Callback = function()
        if SlapChanger then
            game.Players.LocalPlayer.leaderstats.Slaps.Value = SlapChanger
        else
            Fluent:Notify({Title = "Error", Content = "Please enter a valid number of slaps!", Duration = 3})
        end
    end
})

Tab:AddInput("Input_5", {
    Title = "How Many Null Shards? (Visual)",
    Default = "",
    TextDisappear = true,
    Callback = function(Value)
        local nullshardNumber = tonumber(Value)
        if nullshardNumber and nullshardNumber >= 0 then
            getgenv().nullshardChanger = tostring(nullshardNumber)  -- Convert to string since it's a StringValue

        else
            Fluent:Notify({Title = "Error", Content = "Type a correct number (0 or greater)!", Duration = 3})
        end
    end
})

Tab:AddButton({
    Title = "Set Null Shards (Visual)",
    Callback = function()
        if getgenv().nullshardChanger then
            local playerName = game.Players.LocalPlayer.Name
            
            -- Wait for ReplicatedStorage to load
            local ReplicatedStorage = game:GetService("ReplicatedStorage")
            
            -- Check if PlayerData folder exists
            local playerData = ReplicatedStorage:WaitForChild("PlayerData", 5)
            if not playerData then

                return
            end
            
            -- Check if player folder exists
            local playerFolder = playerData:WaitForChild(playerName, 5)
            if not playerFolder then

                return
            end
            
            -- Check if NullShards StringValue exists
            local nullShardsValue = playerFolder:WaitForChild("NullShards", 5)
            if not nullShardsValue then

                return
            end
            
            -- Verify it's a StringValue
            if nullShardsValue.ClassName ~= "StringValue" then

                return
            end
            
            -- Set the value
            local oldValue = nullShardsValue.Value
            nullShardsValue.Value = getgenv().nullshardChanger
            
            
            print("[Null Shards] Changed from " .. oldValue .. " to " .. getgenv().nullshardChanger)
            
        else
            Fluent:Notify({Title = "Error", Content = "Please enter a valid number first!", Duration = 3})
        end
    end
})

-- Section: Badge Giver
Section = Tab:AddSection("Badge Giver")

Tab:AddButton({
    Title = "Give All Badges (Visual)",
    Callback = function()
        loadstring(game:HttpGet('https://raw.githubusercontent.com/Pro666Pro/getallbadges_fake/main/main.lua'))()
    end
})

-- Variables
local nametagText = ""
local autoNametag = false
local player = game.Players.LocalPlayer

-- Textbox for nametag
Tab:AddInput("Input_6", {
    Title = "Type Ur Own Any Nametag",
    Default = "",
    TextDisappear = false,
    Callback = function(Value)
        nametagText = Value
    end
})

-- Button to apply nametag once
Tab:AddButton({
    Title = "Change Nametag",
    Callback = function()
        if nametagText == "" then
            Fluent:Notify({Title = "Error", Content = "Please enter text in the textbox first!", Duration = 3})
            return
        end
        
        -- Find and update nametag
        local character = player.Character
        if character then
            local nametag = character:FindFirstChild("Head") and character.Head:FindFirstChild("Nametag")
            if nametag then
                local topLabel = nametag:FindFirstChild("Labels") and nametag.Labels:FindFirstChild("TopLabel")
                if topLabel then
                    topLabel.Text = nametagText
                    Fluent:Notify({Title = "Success", Content = "Nametag changed to: ", Duration = 3})
                else
                    Fluent:Notify({Title = "Error", Content = "TopLabel not found!", Duration = 3})
                end
            else
                Fluent:Notify({Title = "Error", Content = "Nametag not found!", Duration = 3})
            end
        else
            Fluent:Notify({Title = "Error", Content = "Character not found!", Duration = 3})
        end
    end
})

-- Toggle for auto nametag
Tab:AddToggle("Toggle_45", {
    Title = "Auto Change Nametag",
    Default = false,
    Callback = function(Value)
        autoNametag = Value
        if Value then
            -- Start auto nametag loop
            task.spawn(function()
                while autoNametag do
                    if nametagText ~= "" then
                        local character = player.Character
                        if character then
                            local nametag = character:FindFirstChild("Head") and character.Head:FindFirstChild("Nametag")
                            if nametag then
                                local topLabel = nametag:FindFirstChild("Labels") and nametag.Labels:FindFirstChild("TopLabel")
                                if topLabel then
                                    topLabel.Text = nametagText
                                end
                            end
                        end
                    end
                    task.wait(0.01)
                end
            end)
        end
    end
})

-- Auto reapply nametag when character respawns
player.CharacterAdded:Connect(function(character)
    if autoNametag and nametagText ~= "" then
        -- Wait for character to fully load
        wait(2)
        local nametag = character:FindFirstChild("Head") and character.Head:FindFirstChild("Nametag")
        if nametag then
            local topLabel = nametag:FindFirstChild("Labels") and nametag.Labels:FindFirstChild("TopLabel")
            if topLabel then
                topLabel.Text = nametagText
            end
        end
    end
end)

Section = Tab:AddSection("Slap Aura")

-- Optimized Slap Aura with built-in glove list
do
    local RunService = game:GetService("RunService")
    local ReplicatedStorage = game:GetService("ReplicatedStorage")
    local Players = game:GetService("Players")
    
    local slapAuraConnection
    local lastHitTime = 0
    local currentCooldown = 0.5
    local isSlapAuraEnabled = false
    local localPlayer = Players.LocalPlayer
    
    -- Built-in glove hits table (no external loading)
    local gloveHits = {
        -- Standard Gloves
        ["Default"] = ReplicatedStorage.b,
        ["Dual"] = ReplicatedStorage.GeneralHit,
        ["Diamond"] = ReplicatedStorage.DiamondHit,
        ["ZZZZZZZ"] = ReplicatedStorage.ZZZZZZZHit,
        ["Extended"] = ReplicatedStorage.GeneralHit,
        ["Brick"] = ReplicatedStorage.BrickHit,
        ["Snow"] = ReplicatedStorage.SnowHit,
        ["Pull"] = ReplicatedStorage.PullHit,
        ["Flash"] = ReplicatedStorage.FlashHit,
        ["Stilts"] = ReplicatedStorage.GeneralHit,
        ["Spring"] = ReplicatedStorage.springhit,
        ["Pinwheel"] = ReplicatedStorage.GeneralHit,
        ["Swapper"] = ReplicatedStorage.GeneralHit,
        ["Screwdriver"] = ReplicatedStorage.GeneralHit,
        ["Bull"] = ReplicatedStorage.BullHit,
        ["Dice"] = ReplicatedStorage.DiceHit,
        ["Ghost"] = ReplicatedStorage.GhostHit,
        ["Thanos"] = ReplicatedStorage.GeneralHit,
        ["Stun"] = ReplicatedStorage.HtStun,
        ["L.O.L.B.O.M.B"] = ReplicatedStorage.GeneralHit,
        ["Za Hando"] = ReplicatedStorage.zhramt,
        ["Fort"] = ReplicatedStorage.Fort,
        ["Magnet"] = ReplicatedStorage.MagnetHIT,
        ["Pusher"] = ReplicatedStorage.PusherHit,
        ["Anchor"] = ReplicatedStorage.hitAnchor,
        ["Space"] = ReplicatedStorage.HtSpace,
        ["Boomerang"] = ReplicatedStorage.BoomerangH,
        ["Speedrun"] = ReplicatedStorage.Speedrunhit,
        ["Mail"] = ReplicatedStorage.MailHit,
        ["T H I C K"] = ReplicatedStorage.GeneralHit,
        ["Golden"] = ReplicatedStorage.GoldenHit,
        ["Squid"] = ReplicatedStorage.GeneralHit,
        ["MR"] = ReplicatedStorage.MisterHit,
        ["Hive"] = ReplicatedStorage.GeneralHit,
        ["Reaper"] = ReplicatedStorage.ReaperHit,
        ["Baby"] = ReplicatedStorage.GeneralHit,
        ["Replica"] = ReplicatedStorage.ReplicaHit,
        ["Mace"] = ReplicatedStorage.GeneralHit,
        ["Defense"] = ReplicatedStorage.DefenseHit,
        ["Killstreak"] = ReplicatedStorage.KSHit,
        ["Pineslapple"] = ReplicatedStorage.GeneralHit, 
        ["Reverse"] = ReplicatedStorage.ReverseHit,
        ["Shukuchi"] = ReplicatedStorage.ShukuchiHit,
        ["Duelist"] = ReplicatedStorage.DuelistHit,
        ["woah"] = ReplicatedStorage.GeneralHit,
        ["Ice"] = ReplicatedStorage.IceHit,
        ["Gummy"] = ReplicatedStorage.GeneralHit,
        ["Adios"] = ReplicatedStorage.hitAdios,
        ["Blocked"] = ReplicatedStorage.BlockedHit,
        ["Divert"] = ReplicatedStorage.GeneralHit,
        ["Engineer"] = ReplicatedStorage.engiehit,
        ["Rocky"] = ReplicatedStorage.RockyHit,
        ["Coil"] = ReplicatedStorage.GeneralHit,
        ["Conveyor"] = ReplicatedStorage.ConvHit,
        ["Balloony"] = ReplicatedStorage.GeneralHit,
        ["Phantom"] = ReplicatedStorage.PhantomHit,
        ["Leafblower"] = ReplicatedStorage.LeafblowerHit,
        ["el gato"] = ReplicatedStorage.GeneralHit,
        ["Wormhole"] = ReplicatedStorage.WormHit,
        ["Shackle"] = ReplicatedStorage.GeneralHit,
        ["Flick"] = ReplicatedStorage.GeneralHit,
        ["STOP"] = ReplicatedStorage.STOP,
        ["Equalizer"] = ReplicatedStorage.GeneralHit,
        ["Track"] = ReplicatedStorage.GeneralHit,
        ["Stalker"] = ReplicatedStorage.GeneralHit,
        ["Retribution"] = ReplicatedStorage.RetributionHit,
        ["Prop"] = ReplicatedStorage.GeneralHit,
        ["Shield"] = ReplicatedStorage.GeneralHit,
        ["Clover"] = ReplicatedStorage.GeneralHit,
        ["5 More"] = ReplicatedStorage.GeneralHit,
        ["Booster"] = ReplicatedStorage.GeneralHit,
        ["Chainsaw"] = ReplicatedStorage.GeneralHit,
        ["Ping Pong"] = ReplicatedStorage.GeneralHit,
        ["Trifecta"] = ReplicatedStorage.GeneralHit,
        ["Baller"] = ReplicatedStorage.GeneralHit,
        ["Architect"] = ReplicatedStorage.GeneralHit,
        ["Home Run"] = ReplicatedStorage.GeneralHit,
        ["Friction"] = ReplicatedStorage.GeneralHit,
        ["Whirlwind"] = ReplicatedStorage.GeneralHit,
        ["Disc"] = ReplicatedStorage.GeneralHit,
        ["Slicer"] = ReplicatedStorage.GeneralHit,
        ["Excavator"] = ReplicatedStorage.GeneralHit,
        ["Nightmare"] = ReplicatedStorage.nightmarehit,
        ["Pylon"] = ReplicatedStorage.PylonHit,
        ["Thor"] = ReplicatedStorage.ThorHit,
        ["Slime"] = ReplicatedStorage.GeneralHit,
        ["Pocket"] = ReplicatedStorage.GeneralHit,
        ["Grapple"] = ReplicatedStorage.GeneralHit,
        ["Cloud"] = ReplicatedStorage.CloudHit,
        ["Gravity"] = ReplicatedStorage.GeneralHit,
        ["Parry"] = ReplicatedStorage.GeneralHit,
        ["Jebaited"] = ReplicatedStorage.GeneralHit,
        ["Meteor"] = ReplicatedStorage.GeneralHit,
        ["Oven"] = ReplicatedStorage.GeneralHit,
        ["Guardian Angel"] = ReplicatedStorage.GeneralHit,
        ["Sun"] = ReplicatedStorage.GeneralHit,
        ["Ferryman"] = ReplicatedStorage.GeneralHit,
        ["Blackhole"] = ReplicatedStorage.GeneralHit,
        ["Walrus"] = ReplicatedStorage.GeneralHit,
        ["Blink"] = ReplicatedStorage.GeneralHit,
        ["Tableflip"] = ReplicatedStorage.GeneralHit,
        ["Slapstick"] = ReplicatedStorage.GeneralHit,
        ["Beatdown"] = ReplicatedStorage.GeneralHit,
        ["Chicken"] = ReplicatedStorage.GeneralHit,
        ["BONK"] = ReplicatedStorage.GeneralHit,
        ["Grenadier"] = ReplicatedStorage.GeneralHit,
        ["Sbeve"] = ReplicatedStorage.GeneralHit,
        ["Golem"] = ReplicatedStorage.GeneralHit,
        ["Doomsday"] = ReplicatedStorage.GeneralHit,
        ["Grab"] = ReplicatedStorage.GeneralHit,
        ["UFO"] = ReplicatedStorage.GeneralHit,
        ["Demolition"] = ReplicatedStorage.GeneralHit,
        ["Beachball"] = ReplicatedStorage.GeneralHit,
        ["Shotgun"] = ReplicatedStorage.GeneralHit,
        ["64"] = ReplicatedStorage.GeneralHit,
        ["Roguelike"] = ReplicatedStorage.GeneralHit,
        ["Barrel"] = ReplicatedStorage.GeneralHit,
        ["Lawnmower"] = ReplicatedStorage.GeneralHit,
        ["Virus"] = ReplicatedStorage.GeneralHit,
        ["Smiler"] = ReplicatedStorage.GeneralHit,
        ["Butcher"] = ReplicatedStorage.GeneralHit,
        ["Infinity"] = ReplicatedStorage.GeneralHit,
        ["Aggro"] = ReplicatedStorage.GeneralHit,
        ["Soul"] = ReplicatedStorage.GeneralHit,
        ["R/C"] = ReplicatedStorage.GeneralHit,
        ["Mushroom"] = ReplicatedStorage.GeneralHit,
        ["Scuba"] = ReplicatedStorage.GeneralHit,
        ["Pyromania"] = ReplicatedStorage.GeneralHit,
        ["Automaton"] = ReplicatedStorage.GeneralHit,
        ["Command"] = ReplicatedStorage.GeneralHit,
        ["God's Hand"] = ReplicatedStorage.Godshand,
        ["The Flex"] = ReplicatedStorage.FlexHit,
        
        -- Badge Gloves
        ["MEGAROCK"] = ReplicatedStorage.DiamondHit,
        ["Plague"] = ReplicatedStorage.PlagueHit,
        ["Hallow Jack"] = ReplicatedStorage.HallowHIT,
        ["[REDACTED]"] = ReplicatedStorage.ReHit,
        ["bus"] = ReplicatedStorage.hitbus,
        ["Mitten"] = ReplicatedStorage.MittenHit,
        ["Phase"] = ReplicatedStorage.PhaseH,
        ["Warp"] = ReplicatedStorage.WarpHt,
        ["Bomb"] = ReplicatedStorage.BombHit,
        ["Bubble"] = ReplicatedStorage.BubbleHit,
        ["Jet"] = ReplicatedStorage.JetHit,
        ["Shard"] = ReplicatedStorage.ShardHIT,
        ["CULT"] = ReplicatedStorage.CULTHit,
        ["bob"] = ReplicatedStorage.bobhit,
        ["buddies"] = ReplicatedStorage.buddiesHIT,
        ["Moon"] = ReplicatedStorage.CelestialHit,
        ["Jupiter"] = ReplicatedStorage.GeneralHit,
        ["Spy"] = ReplicatedStorage.SpyHit,
        ["Detonator"] = ReplicatedStorage.DetonatorHit,
        ["Rage"] = ReplicatedStorage.GRRRR,
        ["Trap"] = ReplicatedStorage.traphi,
        ["Orbit"] = ReplicatedStorage.Orbihit,
        ["Hybrid"] = ReplicatedStorage.HybridCLAP,
        ["Slapple"] = ReplicatedStorage.SlappleHit,
        ["Disarm"] = ReplicatedStorage.DisarmH,
        ["Dominance"] = ReplicatedStorage.DominanceHit,
        ["Link"] = ReplicatedStorage.LinkHit,
        ["Chain"] = ReplicatedStorage.GeneralHit,
        ["Rattlebones"] = ReplicatedStorage.GeneralHit,
        ["Charge"] = ReplicatedStorage.GeneralHit,
        ["Tycoon"] = ReplicatedStorage.GeneralHit,
        ["Confusion"] = ReplicatedStorage.GeneralHit,
        ["Glitch"] = ReplicatedStorage.GeneralHit,
        ["Snowball"] = ReplicatedStorage.GeneralHit,
        ["Elude"] = ReplicatedStorage.GeneralHit,
        ["RNG"] = ReplicatedStorage.GeneralHit,
        ["fish"] = ReplicatedStorage.GeneralHit,
        ["🗿"] = ReplicatedStorage.GeneralHit,
        ["Obby"] = ReplicatedStorage.GeneralHit,
        ["Voodoo"] = ReplicatedStorage.GeneralHit,
        ["Goofy"] = ReplicatedStorage.GeneralHit,
        ["Leash"] = ReplicatedStorage.GeneralHit,
        ["Flamarang"] = ReplicatedStorage.GeneralHit,
        ["Kinetic"] = ReplicatedStorage.GeneralHit,
        ["Berserk"] = ReplicatedStorage.GeneralHit,
        ["Sparky"] = ReplicatedStorage.GeneralHit,
        ["Boogie"] = ReplicatedStorage.GeneralHit,
        ["Recall"] = ReplicatedStorage.GeneralHit,
        ["Quake"] = ReplicatedStorage.GeneralHit,
        ["Psycho"] = ReplicatedStorage.GeneralHit,
        ["Kraken"] = ReplicatedStorage.GeneralHit,
        ["Counter"] = ReplicatedStorage.GeneralHit,
        ["Hammer"] = ReplicatedStorage.GeneralHit,
        ["rob"] = ReplicatedStorage.robhit,
        ["Rhythm"] = ReplicatedStorage.rhythmhit,
        ["Rojo"] = ReplicatedStorage.RojoHit,
        ["Hitman"] = ReplicatedStorage.HitmanHit,
        ["Retro"] = ReplicatedStorage.RetroHit,
        ["Null"] = ReplicatedStorage.NullHit,
        ["Lure"] = ReplicatedStorage.GeneralHit,
        ["Tinkerer"] = ReplicatedStorage.TinkererHit,
        ["Necromancer"] = ReplicatedStorage.GeneralHit,
        ["Alchemist"] = ReplicatedStorage.GeneralHit,
        ["Druid"] = ReplicatedStorage.GeneralHit,
        ["Jester"] = ReplicatedStorage.GeneralHit,
        ["Scythe"] = ReplicatedStorage.GeneralHit,
        ["Santa"] = ReplicatedStorage.GeneralHit,
        ["Iceskate"] = ReplicatedStorage.GeneralHit,
        ["Blasphemy"] = ReplicatedStorage.GeneralHit,
        ["Pan"] = ReplicatedStorage.GeneralHit,
        ["Admin"] = ReplicatedStorage.GeneralHit,
        ["Joust"] = ReplicatedStorage.GeneralHit,
        ["Firework"] = ReplicatedStorage.GeneralHit,
        ["Run"] = ReplicatedStorage.GeneralHit,
        ["Glovel"] = ReplicatedStorage.GeneralHit,
        ["Divebomb"] = ReplicatedStorage.GeneralHit,
        ["Lamp"] = ReplicatedStorage.GeneralHit,
        ["Knockoff"] = ReplicatedStorage.GeneralHit,
        ["Frostbite"] = ReplicatedStorage.GeneralHit,
        ["Plank"] = ReplicatedStorage.GeneralHit,
        ["Spoonful"] = ReplicatedStorage.GeneralHit,
        ["the schlob"] = ReplicatedStorage.GeneralHit,
        ["Siphon"] = ReplicatedStorage.GeneralHit,
        ["Wrench"] = ReplicatedStorage.GeneralHit,
        ["Hunter"] = ReplicatedStorage.GeneralHit,
        ["Relude"] = ReplicatedStorage.GeneralHit,
        ["Avatar"] = ReplicatedStorage.GeneralHit,
        ["Water"] = ReplicatedStorage.GeneralHit,
        ["Fan"] = ReplicatedStorage.GeneralHit,
        ["Boxer"] = ReplicatedStorage.GeneralHit,
        ["MATERIALIZE"] = ReplicatedStorage.GeneralHit,
        ["Bind"] = ReplicatedStorage.GeneralHit,
        ["Poltergeist"] = ReplicatedStorage.GeneralHit,
        ["Clock"] = ReplicatedStorage.GeneralHit,
        ["Untitled Tag Glove"] = ReplicatedStorage.GeneralHit,
        ["Pillow"] = ReplicatedStorage.GeneralHit,
        ["Angler"] = ReplicatedStorage.GeneralHit,
        ["Jerry"] = ReplicatedStorage.GeneralHit,
        ["Snowroller"] = ReplicatedStorage.GeneralHit,
        ["Draw4"] = ReplicatedStorage.GeneralHit,
        ["Mouse"] = ReplicatedStorage.GeneralHit,
        ["Swordfighter"] = ReplicatedStorage.GeneralHit,
        ["Tank"] = ReplicatedStorage.GeneralHit,
        ["Eggler"] = ReplicatedStorage.GeneralHit,
        ["Slender"] = ReplicatedStorage.GeneralHit,
        ["Swashbuckler"] = ReplicatedStorage.GeneralHit,
        ["Silly"] = ReplicatedStorage.GeneralHit,
        ["Slasher"] = ReplicatedStorage.GeneralHit,
        ["Car Keys"] = ReplicatedStorage.GeneralHit,
        ["Suction"] = ReplicatedStorage.GeneralHit,
        ["Reflect"] = ReplicatedStorage.GeneralHit,
        ["Dave"] = ReplicatedStorage.GeneralHit,
        ["mortis"] = ReplicatedStorage.GeneralHit,
        ["Bounty"] = ReplicatedStorage.GeneralHit,
        ["Debug"] = ReplicatedStorage.GeneralHit,
        ["Paint"] = ReplicatedStorage.GeneralHit,
        ["Plate"] = ReplicatedStorage.GeneralHit,
        ["Lag"] = ReplicatedStorage.GeneralHit,
        ["Harvester"] = ReplicatedStorage.GeneralHit,
        ["Riftshot"] = ReplicatedStorage.GeneralHit,
        ["Conker"] = ReplicatedStorage.GeneralHit,
        ["Wheelchair"] = ReplicatedStorage.GeneralHit,
        ["Shopkeeper"] = ReplicatedStorage.GeneralHit,
        ["Collector"] = ReplicatedStorage.GeneralHit,
        ["Glaxe"] = ReplicatedStorage.GeneralHit, 
        ["Piñata"] = ReplicatedStorage.GeneralHit, 
        ["Penguin"] = ReplicatedStorage.GeneralHit,

        -- P2W Gloves
        ["OVERKILL"] = ReplicatedStorage.Overkillhit,
        ["Spectator"] = ReplicatedStorage.GeneralHit,
        ["CUSTOM"] = ReplicatedStorage.CustomHit,
        ["Ultra Instinct"] = ReplicatedStorage.GeneralHit,
        
        -- Other Gloves
        ["spin"] = ReplicatedStorage.spinhit,
        ["Stick"] = ReplicatedStorage.GeneralHit,
        ["potato"] = ReplicatedStorage.potatohit,
        ["Error"] = ReplicatedStorage.Errorhit,
        ["killerfish"] = ReplicatedStorage.GeneralHit,
        ["Titan"] = ReplicatedStorage.GeneralHit,
        ["Acrobat"] = ReplicatedStorage.AcHit,
        
        -- Fallback for unknown gloves
        ["GeneralHit"] = ReplicatedStorage.GeneralHit,
        ["b"] = ReplicatedStorage.b,
        ["HtStun"] = ReplicatedStorage.HtStun
    }
    
    -- Add metatable for dynamic glove lookups
    setmetatable(gloveHits, {
        __index = function(t, key)
            -- Try to find any remote that matches the glove name
            for _, child in pairs(ReplicatedStorage:GetChildren()) do
                if child:IsA("RemoteEvent") then
                    local childName = child.Name:lower()
                    local gloveName = tostring(key):lower()
                    
                    if childName == gloveName or
                       childName == gloveName .. "hit" or
                       childName == "ht" .. gloveName or
                       childName:find(gloveName, 1, true) then
                        rawset(t, key, child) -- Cache for next time
                        return child
                    end
                end
            end
            
            -- Ultimate fallback - first available remote
            for _, child in pairs(ReplicatedStorage:GetChildren()) do
                if child:IsA("RemoteEvent") then
                    rawset(t, key, child) -- Cache for next time
                    return child
                end
            end
            
            return nil
        end
    })
    
    -- Function to get current glove
    local function getCurrentGlove()
        local success, result = pcall(function()
            if localPlayer:FindFirstChild("leaderstats") then
                local gloveValue = localPlayer.leaderstats:FindFirstChild("Glove")
                if gloveValue then
                    return gloveValue.Value
                end
            end
            return "Default"
        end)
        
        if success then
            return result
        end
        return "Default"
    end
    
    -- Function to get the remote for a glove
    local function getGloveRemote(gloveName)
        -- Try direct lookup from gloveHits
        local remote = gloveHits[gloveName]
        
        if remote and typeof(remote) == "Instance" and remote:IsA("RemoteEvent") then
            return remote
        end
        
        -- If we get here, the metatable will handle it
        return gloveHits[gloveName]
    end
    
    -- Create slider
    local slapAuraSpeed = Tab:AddSlider("Slider_3", {
        Title = "Slap Aura Speed",
        Min = 0.1,
        Max = 2,
        Default = 0.5,
        Rounding = 1,
        Callback = function(Value)
            currentCooldown = Value
        end
    })
    
    -- Create toggle
    Tab:AddToggle("Toggle_46", {
        Title = "Slap Aura",
        Default = false,
        Callback = function(enabled)
            -- Prevent random "Slap Aura Disabled" popups
            if isSlapAuraEnabled == enabled then
                return
            end
            
            isSlapAuraEnabled = enabled
            
            if enabled then
                -- Disconnect existing connection
                if slapAuraConnection then
                    slapAuraConnection:Disconnect()
                    slapAuraConnection = nil
                end
                
                -- Get current glove
                local currentGlove = getCurrentGlove()
                
                -- Get the remote for this glove
                local remote = getGloveRemote(currentGlove)
                
                if not remote then
                    Fluent:Notify({Title = "Slap Aura Error", Content = "No remote found for glove: ", Duration = 3})
                    isSlapAuraEnabled = false
                    return
                end
                
                print("Slap Aura: Enabled with glove '" .. currentGlove .. "' using remote '" .. remote.Name .. "'")
                
                -- Start slap aura
                slapAuraConnection = RunService.Heartbeat:Connect(function()
                    local currentTime = tick()
                    
                    -- Check cooldown
                    if currentTime - lastHitTime < currentCooldown then
                        return
                    end
                    
                    -- Check if still enabled and has character
                    if not isSlapAuraEnabled or not localPlayer.Character then
                        return
                    end
                    
                    -- Get local character position
                    local localChar = localPlayer.Character
                    local localRoot = localChar:FindFirstChild("HumanoidRootPart")
                    if not localRoot then
                        return
                    end
                    
                    -- Find players to slap
                    for _, player in ipairs(Players:GetPlayers()) do
                        if player ~= localPlayer and player.Character then
                            local char = player.Character
                            local humanoid = char:FindFirstChildOfClass("Humanoid")
                            local torso = char:FindFirstChild("Torso") or char:FindFirstChild("UpperTorso")
                            
                            -- Check if valid target
                            if torso and humanoid and humanoid.Health > 0 then
                                -- Optional distance check (40 studs max)
                                local playerRoot = char:FindFirstChild("HumanoidRootPart")
                                if playerRoot then
                                    local distance = (playerRoot.Position - localRoot.Position).Magnitude
                                    if distance > 40 then
                                        continue
                                    end
                                end
                                
                                -- Fire the remote
                                local success, errorMsg = pcall(function()
                                    remote:FireServer(torso)
                                end)
                                
                                if success then
                                    lastHitTime = currentTime
                                    break -- Hit one player per cycle
                                end
                            end
                        end
                    end
                end)
                
                Fluent:Notify({Title = "Slap Aura Enabled", Content = "Glove: ", Duration = 3})
                
            else
                -- Disable
                if slapAuraConnection then
                    slapAuraConnection:Disconnect()
                    slapAuraConnection = nil
                end
                
                -- Only show notification if it was previously enabled
                Fluent:Notify({Title = "Slap Aura Disabled", Content = "", Duration = 2})
            end
        end
    })
end

-- Add section for Ghost On/Off
Section = Tab:AddSection("Ghost On/Off")

Tab:AddToggle("Toggle_47", {
    Title = "Ghost Invisibility",
    Default = false,
    Callback = function(Value)
        local hasGhost = game.Players.LocalPlayer:FindFirstChild("leaderstats") and
                         game.Players.LocalPlayer.leaderstats:FindFirstChild("Glove") and
                         game.Players.LocalPlayer.leaderstats.Glove.Value == "Ghost"

        if Value == true then
            if hasGhost then
                game:GetService("ReplicatedStorage").Ghostinvisibilityactivated:FireServer()
            else
                Fluent:Notify({Title = "Error", Content = "Equip Ghost Glove", Duration = 3})
            end
        elseif Value == false and hasGhost then
            game:GetService("ReplicatedStorage").Ghostinvisibilitydeactivated:FireServer()
        end
    end
})

-- Add section for Server Settings
Section = Tab:AddSection("Server Settings")

-- Rejoin button
Tab:AddButton({
    Title = "Rejoin",
    Callback = function()
        loadstring(game:HttpGet("https://raw.githubusercontent.com/Jelly-plays/Rejoin-script/main/obf_L744By559M18BbuseSG6en8r1zL31daK9060LV7WyvmS4bQp92aONWfwRE36FdcZ.lua.txt"))()
    end
})

-- ServerHop (Regular Server)
Tab:AddButton({
    Title = "ServerHop (Regular Server)",
    Callback = function()
        local PlaceID = game.PlaceId
        local AllIDs = {}
        local foundAnything = ""
        local actualHour = os.date("!*t").hour
        local Deleted = false

        local success, data = pcall(function()
            return game:GetService('HttpService'):JSONDecode(readfile("NotSameServers.json"))
        end)
        if success then
            AllIDs = data
        else
            table.insert(AllIDs, actualHour)
            writefile("NotSameServers.json", game:GetService('HttpService'):JSONEncode(AllIDs))
        end

        local function TPReturner()
            local Site
            if foundAnything == "" then
                Site = game.HttpService:JSONDecode(game:HttpGet('https://games.roblox.com/v1/games/' .. PlaceID .. '/servers/Public?sortOrder=Asc&limit=100'))
            else
                Site = game.HttpService:JSONDecode(game:HttpGet('https://games.roblox.com/v1/games/' .. PlaceID .. '/servers/Public?sortOrder=Asc&limit=100&cursor=' .. foundAnything))
            end

            if Site.nextPageCursor and Site.nextPageCursor ~= "null" and Site.nextPageCursor ~= nil then
                foundAnything = Site.nextPageCursor
            end

            for i, v in pairs(Site.data) do
                local Possible = true
                local ID = tostring(v.id)
                if tonumber(v.maxPlayers) > tonumber(v.playing) then
                    for _, Existing in pairs(AllIDs) do
                        if ID == tostring(Existing) then
                            Possible = false
                            break
                        end
                    end
                    if Possible then
                        table.insert(AllIDs, ID)
                        wait()
                        pcall(function()
                            writefile("NotSameServers.json", game:GetService('HttpService'):JSONEncode(AllIDs))
                            wait()
                            game:GetService("TeleportService"):TeleportToPlaceInstance(PlaceID, ID, game.Players.LocalPlayer)
                        end)
                        wait(4)
                        break
                    end
                end
            end
        end

        local function Teleport()
            while wait() do
                pcall(function()
                    TPReturner()
                    if foundAnything ~= "" then
                        TPReturner()
                    end
                end)
            end
        end

        -- Start teleporting to new servers
        Teleport()
    end
})

-- ServerHop (Small Server)
Tab:AddButton({
    Title = "ServerHop (Small Server)",
    Callback = function()
        local Http = game:GetService("HttpService")
        local TPS = game:GetService("TeleportService")
        local Api = "https://games.roblox.com/v1/games/"
        local place = game.PlaceId
        local serversUrl = Api .. place .. "/servers/Public?sortOrder=Asc&limit=100"

        local function ListServers(cursor)
            local Raw = game:HttpGet(serversUrl .. ((cursor and "&cursor=" .. cursor) or ""))
            return Http:JSONDecode(Raw)
        end

        local Server, Next
        repeat
            local Servers = ListServers(Next)
            Server = Servers.data[1]
            Next = Servers.nextPageCursor
        until Server

        TPS:TeleportToPlaceInstance(place, Server.id, game:GetService('Players').LocalPlayer)
    end
})

Section = Tab:AddSection("Kick/Reset If Stuck")

Tab:AddButton({
    Title = "Kick (Yourself)",
    Callback = function()
        game.Players.LocalPlayer:Kick("Kicked By S̵͚̈c̴͔͗r̸̡̚ḯ̵̻p̴̦̀ț̶̓ĕ̸r")
    end
})

Tab:AddButton({
    Title = "Reset Player",
    Callback = function()
        local character = game.Players.LocalPlayer.Character
        if character and character:FindFirstChild("Humanoid") then
            character.Humanoid.Health = 0
        end
    end
})

Section = Tab:AddSection("Arena Settings")

-- Auto Enter Arena System
local AutoEnterActive = false
local AutoEnterMode = "Arena" -- Default mode

Tab:AddDropdown("Dropdown_2", {
    Title = "Enter",
    Default = "Arena",
    Values = {"Arena", "Arena Default"},
    Callback = function(Value)
        AutoEnterMode = Value
    end
})

Tab:AddToggle("Toggle_48", {
    Title = "Auto Enter",
    Default = false,
    Callback = function(Value)
        AutoEnterActive = Value
        
        if Value then
            
            -- Start auto enter in separate thread
            task.spawn(function()
                while AutoEnterActive do
                    local player = game.Players.LocalPlayer
                    local character = player.Character
                    
                    if character and character:FindFirstChild("HumanoidRootPart") then
                        local entered = character:FindFirstChild("entered")
                        
                        if not entered then
                            -- Get the correct teleport part based on mode
                            local teleportFolder = workspace.Lobby
                            local teleportPart = nil
                            
                            if AutoEnterMode == "Arena" then
                                teleportPart = teleportFolder:FindFirstChild("Teleport1")
                            elseif AutoEnterMode == "Arena Default" then
                                teleportPart = teleportFolder:FindFirstChild("Teleport2")
                            end
                            
                            if teleportPart then
                                -- Find the TouchInterest parent (the actual part)
                                local touchPart = nil
                                
                                -- Look for TouchTransmitter or TouchInterest
                                local touchInterest = teleportPart:FindFirstChildOfClass("TouchTransmitter") or 
                                                     teleportPart:FindFirstChild("TouchInterest")
                                
                                if touchInterest then
                                    touchPart = touchInterest.Parent
                                else
                                    touchPart = teleportPart
                                end
                                
                                if touchPart and touchPart:IsA("BasePart") then
                                    -- Bring teleport part to player instead of player to teleport
                                    local hrp = character:FindFirstChild("HumanoidRootPart")
                                    if hrp then
                                        -- Save original position
                                        local originalPosition = touchPart.Position
                                        local originalAnchored = touchPart.Anchored
                                        
                                        -- Temporarily move teleport to player
                                        touchPart.Anchored = false
                                        touchPart.Position = hrp.Position + Vector3.new(0, 0, 2)
                                        
                                        -- Touch the part
                                        firetouchinterest(hrp, touchPart, 0)
                                        task.wait(0.05)
                                        firetouchinterest(hrp, touchPart, 1)
                                        
                                        -- Return teleport to original position
                                        task.wait(0.1)
                                        touchPart.Position = originalPosition
                                        touchPart.Anchored = originalAnchored
                                        
                                    end
                                end
                            end
                        else
                            -- Already entered, just wait
                        end
                    else
                        -- Character not loaded yet
                        task.wait(1)
                        continue
                    end
                    
                    -- Wait before next attempt
                    task.wait(0.5)
                end
            end)
        else
        end
    end
})

Tab:AddButton({
    Title = "Teleport To Arena",
    Callback = function()
        local character = game.Players.LocalPlayer.Character
        if character and character:FindFirstChild("HumanoidRootPart") then
            character.HumanoidRootPart.CFrame = CFrame.new()
        end
    end
})

Tab:AddButton({
    Title = "Teleport To Default Arena",
    Callback = function()
        local character = game.Players.LocalPlayer.Character
        if character and character:FindFirstChild("HumanoidRootPart") then
            character.HumanoidRootPart.CFrame = CFrame.new(122, 360, -3)
        end
    end
})

Section = Tab:AddSection("Destroy Script")

Tab:AddButton({
    Title = "Destroy Gui",
    Callback = function()
        Fluent:Destroy()
    end
})

end -- FINISH ENITRE SCRIPT
